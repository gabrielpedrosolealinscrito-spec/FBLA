---
phase: 07-visa-concierge
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/engine/visa.test.ts
  - shared/engine/visa.ts
autonomous: true
requirements: [VISA-01, VISA-02, VISA-03]
must_haves:
  truths:
    - "Running npm test executes a RED visa.test.ts that locks selectVisaPathways, gradeD8, gradeExpressEntry, the skeleton fallback, and the non-filtering invariant"
    - "shared/engine/visa.ts exists as a compiling stub so the tests import and run (and fail) rather than error on missing module"
  artifacts:
    - path: "shared/engine/visa.test.ts"
      provides: "RED test suite for the Phase 7 screener engine (VISA-01/02/03)"
      contains: "selectVisaPathways"
    - path: "shared/engine/visa.ts"
      provides: "Compiling stub exporting selectVisaPathways + GradedFit/VisaScreenerResult interfaces"
      exports: ["selectVisaPathways", "GradedFit", "VisaScreenerResult"]
  key_links:
    - from: "shared/engine/visa.test.ts"
      to: "shared/engine/visa.ts"
      via: "import { selectVisaPathways } from './visa.js'"
      pattern: "from './visa.js'"
---

<objective>
Lay the Wave 0 RED test scaffold for the visa screener engine. Write `shared/engine/visa.test.ts` (mirroring `roadmap.test.ts`) that locks every Phase 7 engine behavior before any implementation exists, plus a compiling `shared/engine/visa.ts` stub so the suite imports cleanly and fails RED (not errors on a missing module).

Purpose: Nyquist gate — every later engine/data behavior has a failing test waiting to turn GREEN, so the authored figures and pure logic in Plans 02–03 are verified continuously.
Output: `shared/engine/visa.test.ts` (RED), `shared/engine/visa.ts` (stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visa-concierge/07-RESEARCH.md
@.planning/phases/07-visa-concierge/07-PATTERNS.md
@.planning/phases/07-visa-concierge/07-VALIDATION.md

<interfaces>
<!-- The LOCKED contract this engine fills. From shared/types.ts lines 189–201. -->
From shared/types.ts:
```typescript
export interface VisaPathway {
  destinationCountry: string;
  visaType: string;
  requirements: string[];
  processingTime: string;
  feeRangeUSD: string;
  pros: string[];
  cons: string[];
  documentChecklist: string[];
  officialSources: string[];
}
```

<!-- Local engine interfaces to declare in visa.ts (PATTERNS.md lines 166–178). -->
```typescript
export interface GradedFit {
  grade: 'strong' | 'possible' | 'long-shot';
  gatingFactor: string;
}
export interface VisaScreenerResult {
  pathway: VisaPathway;
  fit: GradedFit;
}
export function selectVisaPathways(profile: Profile, matchedCountry: string): VisaScreenerResult[];
```

<!-- Confirmed Profile education values (shared/quiz-engine/questions.ts lines 260–265): -->
<!-- 'highschool' | 'associates' | 'bachelors' | 'masters' | 'doctorate' | 'trade' -->
<!-- Profile fields the engine reads: citizenship, hasRemote (boolean), income (number USD/yr), age (number), education (string). All confirmed present in shared/types.ts. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED visa.test.ts suite</name>
  <files>shared/engine/visa.test.ts</files>
  <read_first>
    - shared/engine/roadmap.test.ts (exact analog — fixture shape lines 16–33, describe/it structure lines 165–295, assertion style lines 166–178)
    - shared/engine/visa.ts (will not exist yet — the test imports from './visa.js'; that is intentional, the stub in Task 2 makes it compile)
    - shared/types.ts lines 189–201 (VisaPathway contract) + lines 37–53 (Profile fields hasRemote/income/age/education/citizenship)
    - shared/quiz-engine/questions.ts lines 259–266 (exact education string values)
    - .planning/phases/07-visa-concierge/07-PATTERNS.md §"shared/engine/visa.test.ts" (describe-block blueprint, fixture)
    - .planning/phases/07-visa-concierge/07-RESEARCH.md §"Gating-Factor Logic" (the threshold behaviors the tests assert)
  </read_first>
  <action>
    Author `shared/engine/visa.test.ts` mirroring `roadmap.test.ts`. Import `{ selectVisaPathways }` from `./visa.js` and `type { Profile }` from `../types.js` (`.js` extension required). Define a `baseProfile: Profile` fixture with the full Profile shape — set `citizenship: 'US'`, `hasRemote: true`, `income: 110000`, `age: 28`, `education: 'bachelors'` (exact Phase 2 value, NOT 'bachelor'), and fill remaining required Profile fields per the roadmap.test.ts fixture.

    Write these describe/it groups (assertions use toBe / toHaveLength / toMatch — never toBeTruthy where toBe is precise):
    - `describe('selectVisaPathways — flagship model (VISA-01/VISA-02)')`: returns BOTH Portugal D8 AND Canada Express Entry for a US citizen (length 2; assert the two `pathway.destinationCountry` values include 'Portugal' and 'Canada'); `matchedCountry` does NOT filter — passing matchedCountry='UK' still returns both pathways (length 2); returns the generic skeleton (length 1, destinationCountry equals the passed matchedCountry) for an unlisted citizenship like 'XX'.
    - `describe('computeGradedFit — D8 (VISA-01 D-03)')` exercised via selectVisaPathways output for the D8 pathway: Strong fit when hasRemote=true AND income >= the D8 annual USD threshold (grade 'strong'); Possible when hasRemote=true AND income below threshold (grade 'possible'); Long shot when hasRemote=false (grade 'long-shot'). Assert grade values; assert gatingFactor is a non-empty string. Long shot must NOT be the same grade as a rejection — assert grade !== 'strong'.
    - `describe('computeGradedFit — Express Entry (VISA-01 D-03)')`: Strong fit for age<=35 AND post-secondary degree ('bachelors'); Possible for age>35 with a degree; Long shot for age>35 AND no post-secondary ('highschool' or 'trade').
    - `describe('VISA_PATHWAYS data integrity (VISA-03)')`: every returned `pathway.officialSources` is a non-empty array; every returned pathway has a non-empty `feeRangeUSD` and `processingTime`.
    - `describe('offline deterministic')`: two consecutive calls with identical inputs return deeply-equal output (toEqual); selectVisaPathways does not throw for any fixture.

    Encode VISA-01 (screener maps profile→pathways + graded fit), VISA-02 (both required pathways always present), VISA-03 (officialSources non-empty) in the file-header comment. These tests MUST be RED — the stub in Task 2 returns empty/placeholder so assertions fail.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "visa.test" || echo "visa.test.ts not picked up"</automated>
  </verify>
  <acceptance_criteria>
    - `shared/engine/visa.test.ts` contains `import { selectVisaPathways } from './visa.js'`
    - `shared/engine/visa.test.ts` contains `describe('selectVisaPathways` and at least 10 `it(` blocks
    - The fixture uses `education: 'bachelors'` (with trailing s) — `grep -c "education: 'bachelor'," shared/engine/visa.test.ts` returns 0 (no pre-Phase-2 'bachelor' string)
    - `npm test` runs visa.test.ts and reports FAILING assertions (RED), not a module-resolution error
  </acceptance_criteria>
  <done>visa.test.ts exists, is picked up by Vitest, and fails RED with assertion failures (not import errors).</done>
</task>

<task type="auto">
  <name>Task 2: Write compiling visa.ts stub</name>
  <files>shared/engine/visa.ts</files>
  <read_first>
    - shared/engine/roadmap.ts lines 1–30 (file-header + import pattern, .js extensions)
    - shared/types.ts lines 189–201 (VisaPathway) + Profile import
    - .planning/phases/07-visa-concierge/07-PATTERNS.md §"shared/engine/visa.ts" (interface + signature blueprint)
  </read_first>
  <action>
    Create `shared/engine/visa.ts` as a minimal compiling stub. Declare and export the `GradedFit` interface (`grade: 'strong' | 'possible' | 'long-shot'; gatingFactor: string`) and the `VisaScreenerResult` interface (`pathway: VisaPathway; fit: GradedFit`). Import `type { Profile }` and `type { VisaPathway }` from `../types.js`. Export `function selectVisaPathways(profile: Profile, matchedCountry: string): VisaScreenerResult[]` that returns an empty array `[]` (stub — makes tests compile and fail RED, NOT error). Add a file-header comment noting this is a Wave 0 stub, the authored-truth boundary (no LLM-invented visa facts, D-05), and ZERO network calls (offline-mandatory). Do NOT implement real logic here — that is Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `shared/engine/visa.ts` contains `export function selectVisaPathways(`
    - `shared/engine/visa.ts` contains `export interface GradedFit` and `export interface VisaScreenerResult`
    - `npx tsc --noEmit` exits 0 (stub compiles)
    - `npm test` no longer errors on missing './visa.js' module; visa.test.ts assertions are RED
  </acceptance_criteria>
  <done>visa.ts stub compiles under tsc; visa.test.ts imports resolve; suite is RED on assertions only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (this plan) | Test scaffold + stub only — no rendered content, no inputs crossing a boundary, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Authored visa figures presented as current | accept (deferred) | This plan ships no authored figures — only RED tests. The figure-accuracy threat is mitigated in Plan 02 (human-verify checkpoint) and Plan 04 ("data as of" label). Stated here as genuinely out-of-surface for Wave 0. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH "no install step"). No supply-chain surface. |

This plan's surface is genuinely low-risk: pure TypeScript test files, no rendered output, no network, no authored claims. No injection, auth, or network threats exist here.
</threat_model>

<verification>
- `npm test` picks up and runs `shared/engine/visa.test.ts`
- The suite is RED (assertion failures), proving the behaviors are locked before implementation
- `npx tsc --noEmit` exits 0 (stub + test compile)
</verification>

<success_criteria>
- visa.test.ts locks selectVisaPathways, both-pathway flagship behavior, graded-fit (D8 + Express Entry), skeleton fallback, non-filtering invariant, and officialSources integrity
- visa.ts stub compiles and the suite fails RED on assertions, not imports
</success_criteria>

<output>
Create `.planning/phases/07-visa-concierge/07-01-SUMMARY.md` when done.
</output>

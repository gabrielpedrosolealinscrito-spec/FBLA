---
phase: 07-visa-concierge
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - src/screens/Visa.jsx
  - src/screens/PotentialApp.jsx
autonomous: false
requirements: [VISA-02, VISA-03, VISA-04]
must_haves:
  truths:
    - "The Visa screen renders Portugal D8 and Canada Express Entry as a true side-by-side comparison (auto-fit grid, not stacked cards) with a graded fit badge per column"
    - "Each pathway shows its document checklist, fee/timeline, officialSources (as text, never <a>), and a 'data as of' label"
    - "A visible 'not legal advice — consult a licensed immigration attorney' disclaimer banner and a non-functional attorney-referral CTA render on the immigration content"
    - "The Visa screen is reachable in the running app (CTA from city detail / roadmap teaser) and renders offline with no network calls"
    - "An off-script destination (no authored flagship) renders the generic honest skeleton, never a dead-end"
  artifacts:
    - path: "src/screens/Visa.jsx"
      provides: "Premium visa concierge screen — comparison grid, checklists, sources, disclaimer, CTA, skeleton fallback"
      min_lines: 120
    - path: "src/screens/PotentialApp.jsx"
      provides: "showVisa state + render guard + CTA wiring into the Visa screen"
      contains: "Visa"
  key_links:
    - from: "src/screens/Visa.jsx"
      to: "shared/engine/visa.js selectVisaPathways"
      via: "import + call with profile + matchedCountry"
      pattern: "selectVisaPathways"
    - from: "src/screens/PotentialApp.jsx"
      to: "src/screens/Visa.jsx"
      via: "render <Visa profile={profile} ... /> under a showVisa guard, CTA onClick setShowVisa(true)"
      pattern: "showVisa"
---

<objective>
Build `src/screens/Visa.jsx` — the dedicated Premium visa concierge screen rendering the UI-SPEC contract: the disclaimer banner, the side-by-side pathway comparison (auto-fit grid, graded fit badge per column), per-pathway document checklists, sources-as-text + "data as of" stamps, the attorney-referral CTA, and the off-script generic skeleton. Wire it into `PotentialApp.jsx` (mirroring the existing Roadmap `showRoadmap` pattern) so it is reachable in the running app. End with a browser human-verify checkpoint for VISA-02/VISA-04.

Purpose: VISA-02 (side-by-side comparison surface), VISA-03 (rendered checklist + cited figures + data-as-of), VISA-04 (visible UPL disclaimer + attorney CTA on all immigration content).
Output: `src/screens/Visa.jsx` + nav wiring in `PotentialApp.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visa-concierge/07-CONTEXT.md
@.planning/phases/07-visa-concierge/07-UI-SPEC.md
@.planning/phases/07-visa-concierge/07-PATTERNS.md

<interfaces>
<!-- Engine consumed (Plan 03). selectVisaPathways returns VisaScreenerResult[]. -->
```typescript
import { selectVisaPathways } from '../../shared/engine/visa.js';
// each result: { pathway: VisaPathway, fit: { grade: 'strong'|'possible'|'long-shot', gatingFactor: string } }
```

<!-- EXISTING Roadmap wiring to mirror (PotentialApp.jsx) — the reachability pattern: -->
<!-- const [showRoadmap, setShowRoadmap] = useState(false);                       (line 50) -->
<!-- if (step === 2 && showRoadmap) { return <Roadmap row={...} profile={profile} onBack={() => setShowRoadmap(false)} />; }  (lines 153–162) -->
<!-- CTA in city detail: <button onClick={() => setShowRoadmap(true)}>View relocation roadmap</button>  (lines 257–266) -->
<!-- matchedCountry source: selectedCity?.city?.country ?? (results && results[0]?.city?.country). The flagship model only needs citizenship='US' to render both pathways; matchedCountry drives accent emphasis only. -->

<!-- LOCKED mint token system (PotentialApp.jsx css object lines 94–101) — inherit verbatim. -->
<!-- --accent #6EE7B7, --accent2/--warn #FBBF24, --warn-dim rgba(251,191,36,0.08), --text2 #8896AB, --neg #F87171 (do NOT use for Long shot). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Visa.jsx (comparison + checklists + sources + disclaimer + CTA + skeleton)</name>
  <files>src/screens/Visa.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx lines 94–128 (css token object, heading/mono/label primitives, fadeIn, pill), lines 235–288 (city-detail card + auto-fit grid pattern), lines 50/153–162/255–266 (Roadmap state + render guard + CTA — the wiring to mirror)
    - src/screens/Roadmap.jsx lines 160–178 (authored-content .map loop + sources-as-text-never-link discipline)
    - src/screens/Quiz.jsx lines 79–81 (dealbreaker .warn block — disclaimer banner analog)
    - shared/engine/visa.ts (selectVisaPathways signature + VisaScreenerResult shape)
    - .planning/phases/07-visa-concierge/07-UI-SPEC.md §"Net-New Component Inventory" (all 8 components: fit badge, layout, comparison table, checklist, sources/data-as-of, disclaimer, CTA, off-script skeleton), §"Color", §"Copywriting Contract"
    - .planning/phases/07-visa-concierge/07-PATTERNS.md §"src/screens/Visa.jsx" Parts A–E (split analog: styling shell from PotentialApp, content loop + sources from Roadmap, badge/CTA net-new from UI-SPEC)
  </read_first>
  <action>
    Create `src/screens/Visa.jsx` as a function component `Visa({ profile, matchedCountry, onBack })`. Import `selectVisaPathways` from `'../../shared/engine/visa.js'`. Use INLINE style objects only (no className, no tailwind, no CSS literal, no `@media`, no `useEffect` font loading — Phase 2 D-01 lock). Inherit the mint token `css` object from PotentialApp.jsx by spreading it on the root container; reuse `heading`/`mono`/`label`/`fadeIn` primitives. Use the `anim` mount-fade pattern: `const [anim,setAnim]=useState(false); useEffect(()=>{setTimeout(()=>setAnim(true),60)},[])`.

    Compute `const results = selectVisaPathways(profile, matchedCountry)` once. Render, in order:
    - Screen container: `maxWidth:720, margin:"0 auto", padding:"24px 24px 64px"`, spread `...fadeIn`.
    - Heading block: Instrument Serif 32px "Visa Pathways"; subtitle Manrope 14px text2 "Based on your profile. Informational only — not legal advice."; instant sub-note "No extra questions — derived from your citizenship and destination."
    - Disclaimer banner (VISA-04, ALWAYS rendered, before pathway content): background `rgba(251,191,36,0.08)`, border `1px solid rgba(251,191,36,0.15)`, borderRadius 12, padding "12px 16px", marginBottom 28; heading line "⚠ Not legal advice." in #FBBF24 fontWeight 600; body "This is an informational assessment only. Consult a licensed immigration attorney before acting on this information." in text2.
    - Section label "PATHWAY COMPARISON" (11px uppercase 0.1em tracking text2).
    - Comparison grid: `display:"grid", gridTemplateColumns:"repeat(auto-fit, minmax(300px, 1fr))", gap:16, marginBottom:28`. Map each result to a column card (`background:var(--card), borderRadius:16, border:1px solid var(--border), padding:24, display:flex, flexDirection:column, gap:16`). When `matchedCountry === pathway.destinationCountry`, add `borderLeft:"3px solid var(--accent)"` (accent emphasis only — never a filter). Each column: country-name header (Instrument Serif 20px), visa-type pill, the graded FIT BADGE, then labeled sections REQUIREMENTS / PROCESSING TIME (JetBrains Mono 13px) / FEE RANGE (USD) (JetBrains Mono 13px accent2) / PROS / CONS (each `.map` over the string array, Manrope 13px text2), then the sources block.
    - FIT BADGE (net-new, UI-SPEC §1): inline-flex pill, icon + "[Grade label] — [fit.gatingFactor]". Map grade→style: strong → color var(--accent), bg rgba(110,231,183,0.08), border rgba(110,231,183,0.15), icon ✦; possible → color var(--accent2), bg rgba(251,191,36,0.08), border rgba(251,191,36,0.15), icon ◐; long-shot → color var(--text2), bg var(--surface), border var(--border), icon ○. Long shot MUST use --text2, NEVER --neg (#F87171) — UPL: likelihood signal, not rejection.
    - Per-pathway DOCUMENT CHECKLIST section (UI-SPEC §4): a var(--surface) card per pathway; label "DOCUMENT CHECKLIST" + pathway name (Instrument Serif 24px); `pathway.documentChecklist.map` to rows (flex, gap 10, padding "8px 0", borderBottom 1px var(--border), last row no border) with a ✓ icon in var(--accent).
    - SOURCES + DATA AS OF (UI-SPEC §5): label "SOURCES"; `pathway.officialSources.map` rendered as `<p>` / `<span>` text (NEVER `<a href>` — see-not-click, D-10), 11px text3; a "DATA AS OF: [date]" stamp in JetBrains Mono 11px text3 (the date is read from the officialSources data, not invented in JSX).
    - Attorney referral CTA (D-04, UI-SPEC §7): secondary button (background var(--card), border var(--border-active), color var(--text2), NOT --accent), "Connect with a vetted immigration attorney", `onClick={() => {}}` non-functional placeholder.
    - Off-script skeleton (D-06, UI-SPEC §8): when `results.length === 1` and the single pathway is the generic skeleton (destinationCountry equals matchedCountry / visaType starts "Work Visa —"), render the single-card honest skeleton layout — generic checklist headings, NO invented numbers, the verify-at-authority source note, and the "DATA AS OF: ... GENERIC SKELETON — VERIFY LOCALLY" stamp. Disclaimer banner + attorney CTA still render. Never a dead-end.
    - Empty/error states (UI-SPEC copywriting): if `profile` lacks a citizenship/destination, render "Finish your profile to see visa pathways." / "Complete the 'Going Global' step in the quiz to see visa pathways." rather than crashing.
    - Back affordance using the `onBack` prop (mirror Roadmap's onBack).

    Do NOT call any network/fetch/api on this path — fully offline. Do NOT render any source as a clickable link. Do NOT use --neg for Long shot. Do NOT introduce gold (#E2B56B) or a CSS literal / @media — inherit the mint inline-style system.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/Visa.jsx` contains `selectVisaPathways` and `function Visa(` (or `const Visa =`)
    - File renders the string "not legal advice" (case-insensitive) — `grep -ci "not legal advice" src/screens/Visa.jsx` ≥ 1
    - File renders "Connect with a vetted immigration attorney"
    - No clickable links for sources — `grep -c "<a " src/screens/Visa.jsx` returns 0
    - Long shot does not use --neg — `grep -c "long-shot.*--neg\|--neg.*long" src/screens/Visa.jsx` returns 0
    - No network call on the path — `grep -c "fetch(\|api/\|anthropic" src/screens/Visa.jsx` returns 0
    - `npm run build` succeeds (Visa.jsx compiles into the bundle)
  </acceptance_criteria>
  <done>Visa.jsx renders the full comparison surface + disclaimer + CTA + skeleton with inline mint styling, sources as text, offline, and builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Visa screen into PotentialApp navigation</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx lines 7 (Roadmap import), 50 (showRoadmap state), 153–162 (Roadmap render guard), 255–267 (Roadmap CTA button) — mirror this exact pattern for Visa
    - src/screens/Visa.jsx (the component just built — its prop signature profile/matchedCountry/onBack)
    - .planning/phases/07-visa-concierge/07-CONTEXT.md D-07 (reached from the roadmap visa teaser AND from results)
  </read_first>
  <action>
    Mirror the existing Roadmap wiring for Visa. Add `import Visa from './Visa.jsx';` near the Roadmap import. Add `const [showVisa, setShowVisa] = useState(false);` near the showRoadmap state. Add a render guard BEFORE the `step === 2 && !selectedCity` results checks (same precedence position as the showRoadmap guard): `if (step === 2 && showVisa) { const visaRow = selectedCity ?? (results && results[0]); return <Visa profile={profile} matchedCountry={visaRow?.city?.country ?? ''} onBack={() => setShowVisa(false)} />; }`. Add a CTA button in the city-detail view next to the existing "View relocation roadmap" button — `onClick={() => setShowVisa(true)}`, label "View visa pathways" (secondary styling so it does not duplicate the primary roadmap CTA emphasis). Do NOT remove or alter the existing Roadmap wiring. Keep `profile` passed by reference (read-only in Visa).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/PotentialApp.jsx` contains `import Visa from './Visa.jsx'`
    - File contains `showVisa` and `setShowVisa`
    - File contains a render guard returning `<Visa` with `profile={profile}` and an `onBack`
    - A CTA button contains `onClick={() => setShowVisa(true)}`
    - `npm run build` succeeds; `npm test` exits 0 (no engine regression)
  </acceptance_criteria>
  <done>The Visa screen is reachable in the running app via a city-detail CTA, mirroring the Roadmap pattern, with the existing Roadmap wiring intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser human-verify — disclaimer, comparison, sources (VISA-02/VISA-04)</name>
  <action>Pause for the human to open the Visa screen in the browser and confirm the disclaimer banner, side-by-side pathway comparison, sources-as-text, data-as-of stamps, and attorney CTA render correctly per the steps below.</action>
  <what-built>
    The Visa concierge screen, reachable from the city-detail "View visa pathways" CTA, rendering the side-by-side Portugal D8 / Canada Express Entry comparison with graded fit badges, document checklists, sources-as-text, "data as of" stamps, the UPL disclaimer banner, and the attorney-referral CTA — fully offline.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, complete (or seed) a profile to reach the results → city detail, click "View visa pathways".
    2. Confirm the "Not legal advice — consult a licensed immigration attorney" disclaimer banner is visible at the top, before any pathway content (VISA-04).
    3. Confirm Portugal D8 and Canada Express Entry render SIDE-BY-SIDE as two columns (not stacked cards) at demo laptop width (~720px container), each with a graded fit badge, requirements, processing time, fee range, pros/cons (VISA-02).
    4. Confirm each pathway shows a document checklist and that sources render as plain text (NOT clickable links) with a visible "DATA AS OF: [date]" stamp (VISA-03).
    5. Confirm the attorney-referral CTA "Connect with a vetted immigration attorney" is visible (VISA-04).
    6. Confirm the Long shot badge (if shown) is neutral grey, not red.
    7. (Optional) Kill the network / hotspot and reload — the screen still renders instantly (offline-mandatory).
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored data → rendered JSX | Authored strings (requirements, sources) are interpolated into JSX text nodes |
| rendered content → user (UPL) | Immigration content shown to the user must stay informational, not legal advice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering / injection | Authored strings rendered into Visa.jsx | mitigate | Strings are rendered as React text children (auto-escaped) — never via dangerouslySetInnerHTML; sources rendered as `<p>`/`<span>` text, never `<a href>`. No injection path from authored content into the DOM. |
| T-07-03 | Information disclosure (UPL legal-liability) | Immigration content presented as legal advice | mitigate | The "not legal advice — consult a licensed immigration attorney" disclaimer banner renders above all pathway content on every render path (incl. off-script skeleton); attorney CTA always present; Long shot badge uses neutral --text2, not red (VISA-04). |
| T-07-02 | Tampering | Stale figures shown as current | mitigate | "DATA AS OF: [date]" stamp rendered on every pathway/skeleton; figures already human-verified in Plan 02. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase. No supply-chain surface. |

No auth/session/network threats on this surface: the screen makes zero network calls and reads a read-only in-memory Profile. The realistic surface is (a) safe rendering of authored strings and (b) the UPL framing — both mitigated above.
</threat_model>

<verification>
- `npm run build` succeeds (Visa.jsx + wiring compile)
- `npm test` exits 0 (no engine regression)
- Browser human-verify passed: disclaimer + side-by-side comparison + sources-as-text + data-as-of + attorney CTA all visible; offline render confirmed
</verification>

<success_criteria>
- VISA-02: Portugal D8 + Canada Express Entry render side-by-side with per-column graded fit badge
- VISA-03: rendered document checklist + cited sources (as text) + "data as of" label per pathway
- VISA-04: visible "not legal advice" disclaimer + attorney-referral CTA on all immigration content
- Screen reachable in-app, renders offline, off-script destination shows the honest skeleton (never a dead-end)
</success_criteria>

<output>
Create `.planning/phases/07-visa-concierge/07-04-SUMMARY.md` when done.
</output>

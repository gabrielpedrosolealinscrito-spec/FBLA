---
phase: 07-visa-concierge
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - shared/engine/visa.ts
autonomous: true
requirements: [VISA-01]
must_haves:
  truths:
    - "selectVisaPathways(profile, matchedCountry) returns ALL authored flagship pathways for the citizenship (both D8 + Express Entry for US), never filtered by matchedCountry"
    - "An unlisted citizenship returns a single GENERIC_SKELETON result — never a dead-end"
    - "computeGradedFit derives Strong/Possible/Long-shot per pathway from Profile fields against authored thresholds, with a named gating factor — no LLM, fully deterministic"
    - "All Wave 0 visa.test.ts assertions turn GREEN"
  artifacts:
    - path: "shared/engine/visa.ts"
      provides: "Real selectVisaPathways + computeGradedFit (gradeD8, gradeExpressEntry, isPostSecondaryDegree)"
      exports: ["selectVisaPathways", "GradedFit", "VisaScreenerResult"]
      min_lines: 70
  key_links:
    - from: "shared/engine/visa.ts"
      to: "shared/data/visa-pathways.ts"
      via: "import { VISA_PATHWAYS, GENERIC_SKELETON } from '../data/visa-pathways.js'"
      pattern: "from '../data/visa-pathways.js'"
    - from: "selectVisaPathways"
      to: "computeGradedFit"
      via: "pathways.map(p => ({ pathway: p, fit: computeGradedFit(p, profile) }))"
      pattern: "computeGradedFit"
---

<objective>
Implement the real `shared/engine/visa.ts` pure screener: `selectVisaPathways` (flagship model — returns all authored pathways for the citizenship, falls back to GENERIC_SKELETON, never filters by matchedCountry) and `computeGradedFit` with deterministic `gradeD8` / `gradeExpressEntry` / `isPostSecondaryDegree` helpers using authored thresholds. Turn the Wave 0 RED suite GREEN.

Purpose: VISA-01 — the silent eligibility screener that maps the Profile to likely pathways with a graded fit signal (D-01/D-02/D-03), fully offline and deterministic.
Output: `shared/engine/visa.ts` (real implementation replacing the Plan 01 stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visa-concierge/07-RESEARCH.md
@.planning/phases/07-visa-concierge/07-PATTERNS.md

<interfaces>
<!-- Engine interfaces (already in the Plan 01 stub — keep them). -->
```typescript
export interface GradedFit { grade: 'strong' | 'possible' | 'long-shot'; gatingFactor: string; }
export interface VisaScreenerResult { pathway: VisaPathway; fit: GradedFit; }
export function selectVisaPathways(profile: Profile, matchedCountry: string): VisaScreenerResult[];
```

<!-- Data module exports consumed (from Plan 02). -->
```typescript
export const VISA_PATHWAYS: Record<string, VisaPathway[]>;  // { US: [PORTUGAL_D8, CANADA_EXPRESS_ENTRY] }
export const GENERIC_SKELETON: VisaPathway;
export const PORTUGAL_D8: VisaPathway;       // destinationCountry: 'Portugal'
export const CANADA_EXPRESS_ENTRY: VisaPathway; // destinationCountry: 'Canada'
```

<!-- CONFIRMED Profile.education values (questions.ts 260–265): -->
<!-- 'highschool' | 'associates' | 'bachelors' | 'masters' | 'doctorate' | 'trade' -->
<!-- Post-secondary = ['associates','bachelors','masters','doctorate']. NOT 'bachelor'. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement selectVisaPathways + computeGradedFit (turn Wave 0 GREEN)</name>
  <files>shared/engine/visa.ts</files>
  <behavior>
    - selectVisaPathways(US-citizen profile, any matchedCountry) → returns 2 results: D8 + Express Entry, in registry order
    - matchedCountry='UK' (no authored flagship) → still returns both US pathways (NOT filtered)
    - selectVisaPathways(citizenship='XX') → returns 1 result wrapping GENERIC_SKELETON with destinationCountry set to matchedCountry
    - gradeD8: hasRemote=true & income≥threshold → 'strong'; hasRemote=true & income<threshold → 'possible'; hasRemote=false → 'long-shot'
    - gradeExpressEntry: age≤35 & post-secondary → 'strong'; (degree | age≤29) → 'possible'; else → 'long-shot'
    - Two consecutive identical calls → deeply-equal output (pure, deterministic, no network)
  </behavior>
  <read_first>
    - shared/engine/roadmap.ts (entire file — pure-function + ?? GENERIC fallback pattern lines 75–95, JSDoc style, helper-above-export ordering, .js imports)
    - shared/engine/visa.ts (the Plan 01 stub being replaced — keep the exported interfaces)
    - shared/engine/visa.test.ts (the RED assertions this must satisfy — implement to the tests, do not weaken them)
    - shared/data/visa-pathways.ts (Plan 02 output — VISA_PATHWAYS, GENERIC_SKELETON, the two pathway constants and their destinationCountry values)
    - .planning/phases/07-visa-concierge/07-RESEARCH.md §"Gating-Factor Logic (D-03 Graded Fit Thresholds)" lines 505–582 (the exact threshold logic + unit-mismatch note) and §"Pitfall 2" (education strings)
    - .planning/phases/07-visa-concierge/07-PATTERNS.md §"shared/engine/visa.ts" (selectVisaPathways blueprint, isPostSecondaryDegree against confirmed strings lines 155–164)
  </read_first>
  <action>
    Replace the Plan 01 stub body with the real implementation; keep the exported `GradedFit` and `VisaScreenerResult` interfaces. Import `type { Profile }` from `../types.js` and `{ VISA_PATHWAYS, GENERIC_SKELETON } from '../data/visa-pathways.js'` (`.js` extensions). Update the file header to describe the real engine (pure, offline, deterministic; authored thresholds only, no LLM — D-05).

    Author module-level threshold constants (mark `[ASSUMED] — verify at authoring` in comments): `D8_MIN_MONTHLY_EUR = 3680` (4× €920, Jan 2026), `EUR_USD_CONSERVATIVE = 1.10` (conservative — deliberately below spot so a stale favorable rate cannot trigger a false 'strong'), `D8_MIN_ANNUAL_USD = D8_MIN_MONTHLY_EUR * EUR_USD_CONSERVATIVE * 12`.

    Private helper `isPostSecondaryDegree(education: string): boolean` returning `['associates','bachelors','masters','doctorate'].includes(education)` — use these EXACT confirmed Phase 2 strings (NOT 'bachelor'). Private `gradeD8(profile): GradedFit` per RESEARCH: hasRemote && income≥D8_MIN_ANNUAL_USD → strong ('your remote income likely clears the D8 minimum'); hasRemote && below → possible (name the ~$X/yr required); !hasRemote → long-shot ('D8 requires remote income from non-Portuguese clients; local employment does not qualify'). Private `gradeExpressEntry(profile): GradedFit` per RESEARCH heuristic: age≤35 && post-secondary → strong ('Express Entry favors your age and education; language scores will be the swing factor'); (post-secondary || age≤29) → possible (age-aware gating factor text); else → long-shot. Express Entry is a SIMPLIFIED informational heuristic (CLB language not captured) — gating-factor text must say "language scores will be the swing factor", never "you qualify".

    `computeGradedFit(pathway, profile): GradedFit` dispatches on `pathway.destinationCountry`: 'Portugal' → gradeD8; 'Canada' → gradeExpressEntry; else (skeleton) → a neutral 'possible' with a verify-at-authority gating factor.

    Export `selectVisaPathways(profile, matchedCountry)`: `const citizenship = profile.citizenship || 'US'`; `const pathways = VISA_PATHWAYS[citizenship]`. If empty/undefined, return a single result wrapping `{ ...GENERIC_SKELETON, destinationCountry: matchedCountry }` with a 'possible' fit (D-06 honest skeleton). Otherwise `pathways.map(pathway => ({ pathway, fit: computeGradedFit(pathway, profile) }))`. `matchedCountry` is NEVER used to filter — it only flows into the skeleton label. Keep the function synchronous: no async, no fetch, no await (offline-mandatory).

    Use a conservative EUR/USD constant rather than live FX. Do NOT perform FX conversion on rendered fee strings (those are pre-authored in the data module).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `shared/engine/visa.ts` contains `import { VISA_PATHWAYS, GENERIC_SKELETON } from '../data/visa-pathways.js'`
    - File contains `function isPostSecondaryDegree` comparing against `'bachelors'` (not `'bachelor'`) — `grep -c "'bachelor'" shared/engine/visa.ts` returns 0
    - File contains `gradeD8`, `gradeExpressEntry`, `computeGradedFit`
    - `selectVisaPathways` contains no `fetch`, `await`, or `async` — `grep -c "await\|async\|fetch(" shared/engine/visa.ts` returns 0
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 — ALL visa.test.ts assertions GREEN (no test weakened)
  </acceptance_criteria>
  <done>visa.ts implements the real screener + graded fit; the full Wave 0 suite is GREEN; tsc clean; no network calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile (read-only) → screener | Profile fields (citizenship, hasRemote, income, age, education) are read-only inputs; citizenship is used as a registry key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | citizenship string used as VISA_PATHWAYS lookup key | mitigate | Registry lookup is a safe object index with a defined fallback (GENERIC_SKELETON) for any unknown key — no prototype-pollution path (plain object, known keys), no crash on missing key. |
| T-07-05 | Information disclosure (UPL) | Graded-fit gating-factor text reads as a legal determination | mitigate | Gating-factor strings use likelihood language ('likely clears', 'favors', 'will be the swing factor'), never 'you qualify'; Long shot is a likelihood signal (grade !== rejection). The visible disclaimer + CTA are rendered in Plan 04. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase. No supply-chain surface. |

Genuinely low-risk surface: a pure, synchronous, offline function over read-only inputs. No network, no auth, no mutation, no user-controlled code path.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npm test` exits 0 with the full visa.test.ts suite GREEN (flagship both-pathway behavior, non-filtering invariant, D8 + Express Entry grading, skeleton fallback, determinism)
</verification>

<success_criteria>
- VISA-01: silent screener maps Profile → pathways with a graded Strong/Possible/Long-shot fit and named gating factor, fully deterministic and offline
- matchedCountry never filters; unlisted citizenship returns an honest skeleton; all Wave 0 tests GREEN
</success_criteria>

<output>
Create `.planning/phases/07-visa-concierge/07-03-SUMMARY.md` when done.
</output>

---
phase: 07-visa-concierge
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - shared/data/visa-pathways.ts
  - shared/data/visa-pathways.test.ts
autonomous: false
requirements: [VISA-02, VISA-03]
must_haves:
  truths:
    - "PORTUGAL_D8 and CANADA_EXPRESS_ENTRY are fully authored VisaPathway objects with every figure (income threshold, fees, processing time, checklist) cited to AIMA/MNE or IRCC and carrying a 'data as of' date"
    - "GENERIC_SKELETON is a real, honest fallback VisaPathway with no invented numbers — only verify-at-source directives"
    - "VISA_PATHWAYS is a FLAT Record<string, VisaPathway[]> keyed by citizenship ('US': [PORTUGAL_D8, CANADA_EXPRESS_ENTRY])"
    - "A human has verified the shipped figures against AIMA/vistos.mne.gov.pt and IRCC before the phase advances"
  artifacts:
    - path: "shared/data/visa-pathways.ts"
      provides: "Authored VISA_PATHWAYS registry + PORTUGAL_D8 + CANADA_EXPRESS_ENTRY + GENERIC_SKELETON"
      exports: ["VISA_PATHWAYS", "PORTUGAL_D8", "CANADA_EXPRESS_ENTRY", "GENERIC_SKELETON"]
      contains: "Record<string, VisaPathway[]>"
    - path: "shared/data/visa-pathways.test.ts"
      provides: "Direct-import data-integrity tests (officialSources/feeRangeUSD/data-as-of non-empty; no removed CRS job-offer bonus) — green immediately"
      contains: "officialSources"
  key_links:
    - from: "shared/data/visa-pathways.ts"
      to: "shared/types.ts VisaPathway"
      via: "import type { VisaPathway } from '../types.js'"
      pattern: "import type \\{ VisaPathway \\}"
---

<objective>
Author the `shared/data/visa-pathways.ts` data module: two citation-perfect VisaPathway objects (Portugal D8, Canada Express Entry) plus a generic honest skeleton, exported through a FLAT `VISA_PATHWAYS: Record<string, VisaPathway[]>` registry keyed by citizenship. All figures are authored TypeScript constants transcribed from RESEARCH.md — no LLM-invented visa facts (D-05). End with a blocking human-verify checkpoint of the shipped figures against the official authorities.

Purpose: VISA-02 (both required pathways exist with type/requirements/time/fee/pros/cons) and VISA-03 (per-pathway checklist + cited figures + "data as of"). This is the factual spine of the concierge — every number must be defensible in Q&A.
Output: `shared/data/visa-pathways.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visa-concierge/07-CONTEXT.md
@.planning/phases/07-visa-concierge/07-RESEARCH.md
@.planning/phases/07-visa-concierge/07-PATTERNS.md

<interfaces>
<!-- LOCKED contract — populate, do not redesign. shared/types.ts lines 189–201. -->
From shared/types.ts:
```typescript
export interface VisaPathway {
  destinationCountry: string;
  visaType: string;
  requirements: string[];
  processingTime: string;
  feeRangeUSD: string;
  pros: string[];
  cons: string[];
  documentChecklist: string[];
  officialSources: string[];
}
```

<!-- FLAT registry shape (PATTERNS.md lines 58–62) — NOT the nested roadmap-templates.ts shape. -->
```typescript
export const VISA_PATHWAYS: Record<string, VisaPathway[]> = {
  US: [PORTUGAL_D8, CANADA_EXPRESS_ENTRY],
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author visa-pathways.ts (D8 + Express Entry + skeleton + flat registry)</name>
  <files>shared/data/visa-pathways.ts</files>
  <read_first>
    - shared/data/roadmap-templates.ts lines 1–72 (file-header + Sources-block comment pattern, import pattern, exported-constant pattern) and the GENERIC_TEMPLATE fallback near lines 147+
    - shared/data/cities.ts lines 1–25 (simpler flat export shape, the Record pattern to copy)
    - shared/types.ts lines 189–201 (VisaPathway contract — fill exactly, do not redesign)
    - .planning/phases/07-visa-concierge/07-RESEARCH.md §"Authored Figures — Portugal D8", §"Authored Figures — Canada Express Entry", and Pattern 1 (lines 158–290) — the authoritative field values to transcribe
    - .planning/phases/07-visa-concierge/07-PATTERNS.md §"shared/data/visa-pathways.ts" (flat-vs-nested rule, header comment, GENERIC_SKELETON rule)
  </read_first>
  <action>
    Create `shared/data/visa-pathways.ts`. Import `type { VisaPathway } from '../types.js'` (`.js` extension required). Add a file-header comment block (mirror roadmap-templates.ts) naming the module, Phase 7, D-05/VISA-02/VISA-03, the authored-truth boundary (no LLM-invented facts), and a Sources block listing each authority (AIMA / vistos.mne.gov.pt / Portal das Finanças for D8; IRCC fee list / IRCC proof-of-funds / IRCC Express Entry for Canada) with a "Data as of: 2026-06-05" line.

    Export `const PORTUGAL_D8: VisaPathway` transcribing the RESEARCH Pattern-1 values: `destinationCountry: 'Portugal'`, `visaType: 'Portugal D8 — Digital Nomad / Remote Work Visa'`, the requirements[] (income ≥ €3,680/month = 4× €920 min wage Jan 2026, non-PT client income, €30,000 Schengen health insurance, apostilled criminal record < 90 days, 12-month Finanças-registered lease, NIF, bank history), `processingTime` ('4–9 months total...'), `feeRangeUSD` (the authored range string with embedded FX date — '$300–$330 in gov fees (consulate visa €90–110 + AIMA permit €170; EUR/USD 1.164 Jun 2026 — verify at authoring)'), pros[], cons[] (incl. AIMA backlog, €11,040 savings requirement, PT-sourced income excluded), documentChecklist[] (the 12-item D8 list), officialSources[] (AIMA, MNE/vistos, Portal das Finanças + the "Data as of: 2026-06-05 — verify..." line).

    Export `const CANADA_EXPRESS_ENTRY: VisaPathway` transcribing the RESEARCH values: `destinationCountry: 'Canada'`, `visaType: 'Canada Express Entry — Federal Skilled Worker (FSW)'`, requirements[] (CRS competitive ~480–550+, ≥1yr NOC TEER 0/1/2/3 work, CLB 7 language, ECA-assessed credential, settlement funds CAD $15,263 single per 2026 LICO, NO job offer required — removed March 25 2025), `processingTime` ('6–8 months post-ITA; 60-day window...'), `feeRangeUSD` ('~$1,140 government fees (CAD $1,590 = processing $990 + RPRF $600; CAD/USD 0.719 Jun 2026); excludes ECA, language test, biometrics'), pros[], cons[] (remote foreign work = 0 Canadian WE points; ECA cost/time; language tests; liquid settlement funds), documentChecklist[] (the 10-item post-ITA list), officialSources[] (IRCC express-entry, IRCC fee list updated April 30 2026, IRCC proof-of-funds + "Data as of" line). Do NOT mention any job-offer CRS bonus points (removed March 2025).

    Export `const GENERIC_SKELETON: VisaPathway` (D-06 honest fallback, mirror of roadmap-templates GENERIC_TEMPLATE): `destinationCountry: '[Country]'`, `visaType: 'Work Visa — [Country]'`, generic requirement/checklist headings with NO invented numbers, `processingTime: 'Verify at official source'`, `feeRangeUSD: 'Verify at official source'`, officialSources containing only the verify-at-authority directive and 'DATA AS OF: 2026-06 — GENERIC SKELETON — VERIFY LOCALLY'.

    Export the FLAT registry: `const VISA_PATHWAYS: Record<string, VisaPathway[]> = { US: [PORTUGAL_D8, CANADA_EXPRESS_ENTRY] };`. Do NOT use the nested `Record<string, Record<string, ...>>` shape from roadmap-templates.ts. Do NOT add a destination inner key. Do NOT render any source as a clickable link (this is a data module — strings only; the see-not-click rule applies at render in Plan 04).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `shared/data/visa-pathways.ts` contains `export const VISA_PATHWAYS: Record<string, VisaPathway[]>`
    - File contains `export const PORTUGAL_D8`, `export const CANADA_EXPRESS_ENTRY`, `export const GENERIC_SKELETON`
    - `grep -c "job offer.*point\|job-offer.*point" shared/data/visa-pathways.ts` returns 0 (no removed CRS job-offer bonus)
    - Every authored pathway has a non-empty `officialSources` array containing a "data as of" / "Data as of" / "DATA AS OF" string
    - `npx tsc --noEmit` exits 0 (objects satisfy the VisaPathway interface — VISA-02 type-check)
    - NOTE: the engine-based assertions in visa.test.ts stay RED until Plan 03 (selectVisaPathways is still the Plan 01 stub returning []) — do NOT gate Task 1 on the full `npm test` suite. The data-integrity gate for this plan is visa-pathways.test.ts (Task 2 below).
  </acceptance_criteria>
  <done>visa-pathways.ts compiles, exports the flat registry + 3 pathway constants, every figure carries a source + data-as-of label, no job-offer CRS bonus mentioned.</done>
</task>

<task type="auto">
  <name>Task 2: Add direct-import data-integrity tests (visa-pathways.test.ts)</name>
  <files>shared/data/visa-pathways.test.ts</files>
  <read_first>
    - shared/data/visa-pathways.ts (the module just authored — import its named exports directly)
    - shared/engine/roadmap.test.ts lines 1–33, 165–256 (Vitest import/describe/assertion style)
    - shared/types.ts lines 189–201 (VisaPathway contract)
  </read_first>
  <action>
    Create `shared/data/visa-pathways.test.ts` that imports `{ VISA_PATHWAYS, PORTUGAL_D8, CANADA_EXPRESS_ENTRY, GENERIC_SKELETON }` DIRECTLY from `./visa-pathways.js` (`.js` extension) — NOT via the engine. This test asserts on the authored data itself, so it goes GREEN as soon as the data module exists (it does not depend on the Plan 03 engine). Assert: VISA_PATHWAYS.US has length 2; PORTUGAL_D8.destinationCountry === 'Portugal' and CANADA_EXPRESS_ENTRY.destinationCountry === 'Canada'; for each of the two authored pathways — officialSources is a non-empty array, feeRangeUSD is a non-empty string, processingTime is a non-empty string, documentChecklist length > 0, requirements length > 0; at least one officialSources entry matches /data as of/i; the joined authored content does NOT match /job[ -]?offer.*point/i (removed CRS bonus, March 2025); GENERIC_SKELETON.officialSources contains a verify-at-source directive and no invented fee number (feeRangeUSD === 'Verify at official source'). This is the VISA-03 Nyquist gate for this plan. This test file CANNOT live in Plan 01 (the module did not exist yet) — it belongs here.
  </action>
  <verify>
    <automated>npm test -- visa-pathways.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `shared/data/visa-pathways.test.ts` imports from `./visa-pathways.js` (direct data import, not the engine)
    - File asserts officialSources non-empty + a /data as of/i match + no /job[ -]?offer.*point/i match
    - `npm test -- visa-pathways.test.ts` exits 0 (GREEN immediately — data exists, no engine dependency)
  </acceptance_criteria>
  <done>visa-pathways.test.ts passes GREEN against the authored data module, gating VISA-03 (sources + data-as-of + no removed CRS bonus) without depending on the Plan 03 engine.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human-verify authored visa figures against official authorities</name>
  <action>Pause for the human to verify the shipped figures in shared/data/visa-pathways.ts against the official authorities (AIMA / vistos.mne.gov.pt for Portugal D8; IRCC for Canada Express Entry) per the steps below, applying any corrections before the phase advances.</action>
  <what-built>
    `shared/data/visa-pathways.ts` with PORTUGAL_D8 and CANADA_EXPRESS_ENTRY fully authored. All figures are MEDIUM confidence (RESEARCH.md: official .gov sites returned 403; figures cross-verified against 3+ immigration guides). They must be human-checked against the official authorities before content ships and before the phase advances.
  </what-built>
  <how-to-verify>
    1. Portugal D8 income threshold: confirm the 2026 Portuguese minimum wage (€920/month) and the 4× multiplier → €3,680/month at AIMA (aima.gov.pt) and/or vistos.mne.gov.pt. Note if the minimum wage or multiplier has changed.
    2. Portugal D8 fees: confirm consulate visa (€90–110) + AIMA residence permit (~€170) at the AIMA fee schedule and your consulate's page.
    3. Portugal D8 savings + processing time: confirm ≥ €11,040 savings and the 4–9 month end-to-end range.
    4. Canada Express Entry fees: confirm processing $990 CAD + RPRF $600 CAD (effective April 30, 2026) at the IRCC fee list.
    5. Canada proof of funds: confirm CAD $15,263 single applicant (2026 LICO) at canada.ca/express-entry/documents/proof-funds.
    6. Confirm the Express Entry job-offer CRS points are still removed (since March 25, 2025) — the authored content must NOT mention a job-offer bonus.
    7. Re-verify the EUR/USD (1.164) and CAD/USD (0.719) FX rates embedded in the feeRangeUSD strings are still reasonable; if materially stale, update the strings.
    If any figure is wrong, correct it in `shared/data/visa-pathways.ts` and update the source citation.
  </how-to-verify>
  <resume-signal>Type "approved" once figures are verified (or list the corrections to apply).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored data → user (UPL) | Visa figures presented to a user are an informational snapshot, not legal advice — must stay within the UPL boundary |
| authoring time → demo time | Figures are a point-in-time snapshot; FX and minimum-wage thresholds drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | Stale/incorrect visa figures presented as current | mitigate | Every figure carries an officialSources[] citation + "data as of 2026-06-05" string; Task 2 blocking human-verify against AIMA/IRCC before phase advances. |
| T-07-03 | Information disclosure (legal-liability "threat") | Authored figures read as a legal determination (UPL) | mitigate | Data module fields are informational (requirements/typical documents), not advisory; the UPL disclaimer + attorney CTA are rendered by Plan 04 on every immigration surface (VISA-04). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages — no install step in Phase 7 (RESEARCH). No supply-chain surface. |

No network/auth threats on this surface: the module is static authored TypeScript constants, zero network calls.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (all three pathway objects satisfy VisaPathway — VISA-02)
- `npm test` — data-integrity tests (officialSources non-empty, fee/time present) green
- Human-verify checkpoint passed against AIMA + IRCC (VISA-03 figures defensible)
</verification>

<success_criteria>
- VISA-02: both Portugal D8 and Canada Express Entry authored with type, requirements, processing time, fee range, pros/cons
- VISA-03: each pathway has a document checklist + cost/timeline, every figure cited to an official authority + "data as of" date
- FLAT registry shape; GENERIC_SKELETON honest with no invented numbers; figures human-verified
</success_criteria>

<output>
Create `.planning/phases/07-visa-concierge/07-02-SUMMARY.md` when done.
</output>

---
phase: 06-relocation-roadmap
plan: 05
type: execute
wave: 4
depends_on: ["06-03"]
optional: true
blocked_on_external: "scripts/capture-golden-path.ts (Phase 5 D-07) now EXISTS on this branch — remaining gate is demo-persona pinning (06-RESEARCH Open Q2)"
files_modified:
  - api/enrich-core.ts
  - scripts/enrich-roadmap.ts
autonomous: false
requirements: [ROAD-02]
must_haves:
  truths:
    - "An OPTIONAL build-time layer can polish authored roadmap detail prose without ever mutating authored label/sourceUrl/order"
    - "The enrich runs at build time only, behind acceptEnrichment, and never touches the render path"
    - "If the Phase 5 capture script is absent, the phase still ships complete (authored prose renders as-is)"
  artifacts:
    - path: "api/enrich-core.ts"
      provides: "Build-time enrich prompt + extractJSON reuse + acceptEnrichment-gated polish"
      exports: ["enrichRoadmapDetails"]
  key_links:
    - from: "api/enrich-core.ts"
      to: "shared/engine/roadmap.ts acceptEnrichment"
      via: "every enrich result passes through the preservation validator before being kept"
      pattern: "acceptEnrichment"
---

<objective>
OPTIONAL, NON-BLOCKING. Add the build-time prose-enrich layer that polishes the `detail` text of authored roadmap steps and bakes the result into the golden-path cache — so even offline the demo shows the enriched prose. This reuses the Phase 5 proxy/sanitize/extractJSON infrastructure and the Plan-02 `acceptEnrichment` validator; it never runs on the render path.

**This plan is OPTIONAL.** The Phase 5 capture script (`scripts/capture-golden-path.ts`) now EXISTS on this branch, so the remaining gate is demo-persona pinning. ROAD-01/02/03 ship complete WITHOUT this plan — the authored prose renders directly (offline, correct). Execute this plan ONLY after the demo persona is pinned. If the persona is still provisional, STOP at Task 0's gate checkpoint and defer.

Purpose: The D-04 flourish — polished prose, baked offline. Strengthens ROAD-02's "LLM-enriched for prose only" without weakening the authored-truth boundary.
Output: `api/enrich-core.ts` (build-time enrich, validator-gated) + `scripts/enrich-roadmap.ts` (the bake hook into the capture flow).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-relocation-roadmap/06-RESEARCH.md

<interfaces>
<!-- The validator the enrich MUST route every result through (Plan 02) -->
From shared/engine/roadmap.ts:
```typescript
export function acceptEnrichment(
  authored: { label: string; detail: string; sourceUrl?: string }[],
  llm: unknown,
): { detail: string }[];  // throws on count/label/sourceUrl/order mutation
```

<!-- Phase 5 infra to reuse — do NOT rebuild -->
From api/live-core.ts:
```typescript
export function extractJSON(text: string): unknown;   // fenced-JSON extraction; throws on bad JSON
export function sanitizeInput(req): LiveDataRequest;
export function buildPrompt(...): string;
```
</interfaces>

@api/live-core.ts
@api/README.md
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Pre-flight gate — capture script + pinned persona</name>
  <action>Pause and confirm the two prerequisites below before any enrich work. If either is missing, defer this optional plan — the phase already ships complete via Plans 01-04.</action>
  <what-built>Pre-flight gate for the OPTIONAL enrich plan.</what-built>
  <how-to-verify>
    Confirm the prerequisites that make this plan executable:
    1. `ls scripts/capture-golden-path.ts` confirms the Phase 5 D-07 capture script is present (it now is, as of 2026-06-05).
    2. The demo persona (citizenship + #1 US city + international city) is finalized (shared blocker with Phase 5; see 06-RESEARCH Open Q2 — still provisional).
    If the persona is still provisional, this plan cannot run — DEFER it. The phase is already complete via Plans 01-04 (authored prose renders directly, offline).
  </how-to-verify>
  <resume-signal>Type "proceed" only if scripts/capture-golden-path.ts exists AND the persona is pinned. Otherwise type "defer" and close this plan as not-yet-executable.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Build api/enrich-core.ts — validator-gated build-time prose enrich</name>
  <files>api/enrich-core.ts</files>
  <read_first>
    - api/live-core.ts (reuse extractJSON for fenced-JSON parsing + the throw→fallback idiom; buildPrompt for prompt shape; do NOT rebuild these)
    - shared/engine/roadmap.ts (Plan 02 — acceptEnrichment, the preservation gate every result must pass through)
    - shared/data/roadmap-templates.ts (Plan 02/03 — the authored steps whose `detail` prose is polished; `label`/`sourceUrl` are IMMUTABLE)
    - 06-RESEARCH.md §"Optional prose-enrich" + §"Enrich validator" + §"Security Domain"
  </read_first>
  <action>
    Create `api/enrich-core.ts` exporting `enrichRoadmapDetails(authoredSteps, llmRawText)`. Build a strict prompt (reuse the buildPrompt style) instructing the model to rewrite ONLY the `detail` prose of each step, preserving step count, order, and every `label`/`sourceUrl` verbatim, returning a JSON array aligned 1:1 with the input. Parse the model output with the Phase 5 `extractJSON` (import from `./live-core.ts`) — on parse failure it throws, and the caller keeps the authored detail. Route the parsed result through `acceptEnrichment(authoredSteps, parsed)` (import from `../shared/engine/roadmap.js`) — if it throws (count/label/sourceUrl/order mutation, D-05), CATCH and return the authored detail unchanged (never ship the LLM mutation). On success, return the polished `detail` strings. This is build-time only — it never imports into or runs on the render path.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "enrich-core" || echo "OK: enrich-core typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `api/enrich-core.ts` exists; exports `enrichRoadmapDetails`; imports `extractJSON` from `./live-core` and `acceptEnrichment` from `../shared/engine/roadmap.js`.
    - Every enrich result passes through `acceptEnrichment` before being returned (grep: `acceptEnrichment` present).
    - On parse failure OR validator rejection, the function returns the AUTHORED detail (try/catch fallback — never throws up an LLM mutation).
    - The file is build-time only — it is NOT imported by any `src/` render code (grep src/ for enrich-core returns nothing).
    - `npx tsc --noEmit` passes for this file.
  </acceptance_criteria>
  <done>
    A validator-gated build-time enrich exists that can only ever polish prose, never mutate authored truth (ROAD-02 / D-05).
  </done>
</task>

<task type="auto">
  <name>Task 2: Hook the enrich bake into the capture flow (scripts/enrich-roadmap.ts)</name>
  <files>scripts/enrich-roadmap.ts</files>
  <read_first>
    - scripts/capture-golden-path.ts (Phase 5 — the existing capture/bake mechanism this extends; present on this branch as of 2026-06-05)
    - api/enrich-core.ts (Task 1 — enrichRoadmapDetails)
    - data/golden-path/demo-results.json (the cache the polished prose is baked into; keyed by category/city)
    - 06-RESEARCH.md §"Don't Hand-Roll" (reuse the Phase 5 capture script — do NOT build a second capture path)
  </read_first>
  <action>
    Create `scripts/enrich-roadmap.ts` that, at build time, takes the authored roadmap steps for the pinned persona's two pairs, calls `enrichRoadmapDetails` (via the Phase 5 `/api` proxy, reusing its sanitize/cache-fallback), and bakes the polished `detail` strings into the golden-path cache so the offline render shows the enriched version (D-04). Reuse the Phase 5 capture script mechanism — extend it, do NOT build a parallel capture path. If the proxy is unreachable at build time, fall back to the authored prose (the bake is best-effort; the render is unaffected). This is the only online touch in the entire phase, and it is build-time + non-blocking.
  </action>
  <verify>
    <automated>test -f scripts/enrich-roadmap.ts && npx tsc --noEmit 2>&1 | grep -i "enrich-roadmap" || echo "OK: enrich-roadmap present and typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/enrich-roadmap.ts` exists and reuses the Phase 5 capture/proxy infrastructure (no parallel capture path).
    - It calls `enrichRoadmapDetails` and writes polished `detail` into the golden-path cache (build-time only).
    - On proxy-unreachable, it falls back to authored prose (best-effort; never breaks the build or the render).
    - The render path (src/, shared/engine/roadmap.ts) is unchanged — the bake writes a file, never a render dependency.
    - `npm test` and `npm run build` still green.
  </acceptance_criteria>
  <done>
    The optional enrich bake is wired into the capture flow; offline demo shows polished prose; render path untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM enrich output → authored cache (build-time) | LLM-produced prose crosses into baked content; the only mutation surface in the phase. |
| build-time proxy call | The single online touch; never on the render path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | LLM rewrites/invents procedural/legal/visa facts during enrich | mitigate | Every result routed through `acceptEnrichment`; rejection → keep authored detail (D-05/ROAD-02). |
| T-06-10 | DoS / Tampering | Malformed LLM JSON during bake | mitigate | Reuse Phase 5 `extractJSON` (throws → fall back to authored prose); build-time only, never affects render. |
| T-06-SC | Tampering | npm/proxy supply chain during build | accept | No new packages installed (reuses existing @anthropic-ai/sdk + Phase 5 proxy); build-time only, no runtime exposure. |
</threat_model>

<verification>
- Pre-flight gate confirms scripts/capture-golden-path.ts (present) + pinned persona before any work.
- `acceptEnrichment` gates every enrich result; parse/validator failure falls back to authored prose.
- Render path (src/, shared/engine/roadmap.ts) unchanged.
- `npm test` + `npm run build` green.
</verification>

<success_criteria>
- Optional build-time prose-enrich exists, validator-gated, baked offline (D-04 / ROAD-02).
- Phase still ships complete if this plan is deferred.
</success_criteria>

<output>
Create `.planning/phases/06-relocation-roadmap/06-05-SUMMARY.md` when done.
</output>

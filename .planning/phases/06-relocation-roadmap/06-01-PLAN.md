---
phase: 06-relocation-roadmap
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/engine/roadmap.test.ts
autonomous: true
requirements: [ROAD-01, ROAD-02, ROAD-03]
must_haves:
  truths:
    - "A RED test suite exists locking every Phase 6 engine behavior before implementation"
    - "Tests assert the user-facing compiled detail string, never an intermediate value"
    - "Suite covers covered-pair 6 sections, threaded numbers, generic fallback, negative-savings reframe, offline determinism, enrich preservation, and visa UPL"
  artifacts:
    - path: "shared/engine/roadmap.test.ts"
      provides: "Wave 0 RED contract for ROAD-01/02/03 + D-02/D-05/D-07 + VISA-04"
      contains: "describe"
  key_links:
    - from: "shared/engine/roadmap.test.ts"
      to: "shared/engine/roadmap.js"
      via: "import { buildRoadmap, acceptEnrichment } from './roadmap.js'"
      pattern: "from './roadmap.js'"
---

<objective>
Author the Wave 0 RED test suite that locks every Phase 6 engine behavior BEFORE any implementation exists. These tests MUST fail (red) at the end of this plan — `shared/engine/roadmap.ts` does not exist yet. Later waves turn them green.

Purpose: Establish the executable contract per 06-VALIDATION.md so every downstream wave has an automated gate. This is the Nyquist sampling backbone for the phase.
Output: `shared/engine/roadmap.test.ts` — a co-located Vitest suite covering ROAD-01, ROAD-02, ROAD-03, D-02 (negative-savings reframe), D-05 (enrich preserves authored), D-07 (generic fallback), VISA-04 (UPL line).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-relocation-roadmap/06-VALIDATION.md
@.planning/phases/06-relocation-roadmap/06-PATTERNS.md

<interfaces>
<!-- The LOCKED contract the compiled roadmap must match (shared/types.ts 178-187). Do NOT edit types.ts. -->
From shared/types.ts:
```typescript
export interface RoadmapSection {
  id: "timeline" | "financial" | "jobs" | "housing" | "logistics" | "visa";
  title: string;
  steps: { label: string; detail: string; sourceUrl?: string }[];
}
export interface Roadmap { cityName: string; sections: RoadmapSection[]; }
```

From shared/types.ts (MatchResult — the engine output the roadmap threads, D-01):
```typescript
export interface MatchResult {
  city: City;            // city.name, city.country ("US" | "UK"), city.medianRent, city.medianHome
  matchScore: number;
  scoreFactors: { factor: string; contribution: number }[];
  estSalary: number;
  monthlyTakeHome: number;
  expenses: ExpenseBreakdown;
  monthlySavings: number;  // CAN be negative — D-02
}
```

<!-- The functions under test (do NOT implement here — Wave 1 implements). Signatures to import: -->
From shared/engine/roadmap.ts (Wave 1 — does not exist yet, import will fail = RED):
```typescript
export function buildRoadmap(profile: Profile, top: MatchResult): Roadmap;
export function acceptEnrichment(
  authored: { label: string; detail: string; sourceUrl?: string }[],
  llm: unknown,
): { detail: string }[];
```
</interfaces>

<!-- Test-file conventions — see 06-PATTERNS.md §roadmap.test.ts -->
@shared/engine/financial.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author RED test suite locking the buildRoadmap contract</name>
  <files>shared/engine/roadmap.test.ts</files>
  <read_first>
    - shared/engine/roadmap.test.ts (the file being created — does not exist yet)
    - shared/types.ts lines 142-187 (the LOCKED Roadmap/RoadmapSection/MatchResult contract — source of truth)
    - shared/engine/financial.test.ts lines 1-75 (analog: globals:true so NO vitest import; `.js` suffix on relative imports; full typed Profile fixture at lines 50-73; RED-header comment convention)
    - .planning/phases/06-relocation-roadmap/06-VALIDATION.md (the Phase Requirements → Test Map: exact `-t` test names to use)
    - .planning/phases/06-relocation-roadmap/06-PATTERNS.md §"shared/engine/roadmap.test.ts" + §"Assert user-facing output in tests"
  </read_first>
  <action>
    Create `shared/engine/roadmap.test.ts` as a Vitest suite using `describe`/`it`/`expect` WITHOUT a vitest import (vite.config.js has `globals: true`). Import the under-test functions with the `.js` suffix: `import { buildRoadmap, acceptEnrichment } from './roadmap.js';` and `import type { Profile, MatchResult } from '../types.js';`. This import WILL fail to resolve because roadmap.ts does not exist yet — that is the intended RED state.

    Open with a RED-header block comment naming the requirements covered (ROAD-01/02/03, D-02, D-05, D-07, VISA-04) and "RED until shared/engine/roadmap.ts is implemented in Wave 1/2", mirroring financial.test.ts lines 1-3.

    Build typed fixtures (reuse the full-Profile literal shape from financial.test.ts lines 50-73, all required Profile fields populated): (a) a US-citizen Profile (`citizenship: 'US'`, `profession: 'Software Engineer'`, `housing: 'rent'`); (b) a covered MatchResult whose `city.country === 'UK'` (London) with a POSITIVE `monthlySavings` (e.g. 1400); (c) a covered MatchResult whose `city.country === 'US'` with positive savings; (d) a negative-savings MatchResult (`monthlySavings: -500`) for the D-02 test; (e) an uncovered MatchResult whose `city.country` is some non-authored value (e.g. 'Germany') for the D-07 fallback test. Each MatchResult fixture must include a `city` literal with at least `name`, `country`, `medianRent`, `medianHome` populated (use realistic numbers).

    Author the following test cases with EXACTLY these `it(...)` titles so the VALIDATION.md `-t` filters match (the title text must contain the quoted substring):
    - "covered pair" — buildRoadmap on a covered (US→UK) pair returns a Roadmap whose `sections` has length 6 and whose section `id`s equal `['timeline','financial','jobs','housing','logistics','visa']` IN THAT ORDER; every section has at least one step with a non-empty `detail` string.
    - "threads numbers" — for a covered pair, assert the threaded user-facing values appear in the rendered `detail` strings: the profession string appears in the jobs section's detail; a currency-formatted savings figure (e.g. the substring derived from `monthlySavings.toLocaleString()`) appears in the timeline/financial detail. Assert against the compiled `roadmap.sections[..].steps[..].detail` STRING the user sees — NEVER an intermediate `monthsToFund` (per 06-PATTERNS.md "Assert user-facing output").
    - "fallback" — buildRoadmap on an UNCOVERED pair (citizenship×country outside the authored set) still returns 6 sections in order (the GENERIC_TEMPLATE), with no thrown error and non-empty steps.
    - "negative savings" — buildRoadmap with `monthlySavings <= 0` produces a timeline/financial detail containing the honest deficit reframe (assert it contains a word like "deficit" and does NOT contain a fabricated "months" countdown number). Assert the rendered detail string.
    - "offline deterministic" — calling buildRoadmap twice with the same inputs yields deeply-equal output (deterministic), and the test makes no network call (pure function). Assert structural equality of the two results.
    - "enrich preserves authored" — acceptEnrichment ACCEPTS an llm array of identical length whose entries have a `detail` string and matching/absent `label`+`sourceUrl` (returns `[{detail}]` with the polished detail); and REJECTS (throws) when the llm array has a different length, a mutated `label`, or a mutated `sourceUrl`. Use `expect(() => acceptEnrichment(...)).toThrow()` for the rejection cases.
    - "visa UPL" — the compiled roadmap's `visa` section contains the fixed UPL line (assert the detail/step text contains an "informational only" / "not legal advice" style phrase and a "licensed attorney" reference per VISA-04) and does NOT contain personalized legal-advice phrasing.

    Do NOT implement buildRoadmap or acceptEnrichment in this plan. Do NOT create roadmap.ts. Do NOT weaken assertions to make them pass — they MUST be red.
  </action>
  <verify>
    <automated>npx vitest run shared/engine/roadmap.test.ts 2>&1 | grep -qiE "cannot find module|failed to resolve|roadmap.js" && echo "RED (import unresolved, as expected)" || (echo "UNEXPECTED: suite did not fail on missing roadmap.ts" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `shared/engine/roadmap.test.ts` exists and imports `buildRoadmap` + `acceptEnrichment` from `'./roadmap.js'` (with the `.js` suffix).
    - The suite is RED: running `npx vitest run shared/engine/roadmap.test.ts` fails because `./roadmap.js` cannot be resolved (roadmap.ts not yet authored). This is the expected/required outcome.
    - The seven `it(...)` titles each contain the exact substrings: "covered pair", "threads numbers", "fallback", "negative savings", "offline deterministic", "enrich preserves authored", "visa UPL".
    - No vitest import statement is present (globals:true convention).
    - All relative imports use the `.js` suffix.
    - Assertions target the compiled `roadmap.sections[..].steps[..].detail` string, not internal intermediates (grep the file: no assertion on a bare `monthsToFund`).
    - The existing suite is untouched: `npm test 2>&1 | tail -5` still shows the prior 123 tests behaving as before (only the new roadmap suite is red).
  </acceptance_criteria>
  <done>
    The RED contract file is committed; every Phase 6 engine behavior has a failing test with the canonical name from 06-VALIDATION.md; downstream waves now have automated gates.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Test authoring only — no runtime input, no network, no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | RED tests later weakened to pass falsely | mitigate | Acceptance criteria require the suite to be RED at end of plan and assert user-facing output; later waves must make tests pass by implementing, not by editing assertions. The enrich-preservation + UPL tests encode the ROAD-02/VISA-04 boundaries so they cannot silently drift. |
</threat_model>

<verification>
- `npx vitest run shared/engine/roadmap.test.ts` fails on unresolved `./roadmap.js` (RED, expected).
- `npm test` shows only the new roadmap suite red; the existing ~123 tests are unaffected.
- The seven canonical `-t` test names are present.
</verification>

<success_criteria>
- A complete RED test suite locks ROAD-01/02/03 + D-02 + D-05 + D-07 + VISA-04.
- Tests assert the compiled, user-facing `detail` string.
- 06-VALIDATION.md `-t` names are honored verbatim.
</success_criteria>

<output>
Create `.planning/phases/06-relocation-roadmap/06-01-SUMMARY.md` when done.
</output>

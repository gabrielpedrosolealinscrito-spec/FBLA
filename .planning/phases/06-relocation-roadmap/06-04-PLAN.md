---
phase: 06-relocation-roadmap
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/lib/matchEngine.js
  - src/screens/Roadmap.jsx
  - src/screens/PotentialApp.jsx
autonomous: false
requirements: [ROAD-01, ROAD-03]
must_haves:
  truths:
    - "A Plus-tier user can open a relocation roadmap for their top city and read all 6 sections offline"
    - "The roadmap is reachable from the results/city-detail surface"
    - "An Export/Print PDF button produces a clean paginated PDF via window.print()"
    - "Source names render as styled TEXT, never clickable links"
  artifacts:
    - path: "src/lib/matchEngine.js"
      provides: "buildRoadmapForRow adapter — reconstructs a MatchResult from a UI row and calls buildRoadmap"
      exports: ["buildRoadmapForRow"]
    - path: "src/screens/Roadmap.jsx"
      provides: "Roadmap UI surface: renders 6 sections + inline @media print CSS + window.print() export"
      min_lines: 60
  key_links:
    - from: "src/screens/Roadmap.jsx"
      to: "src/lib/matchEngine.js buildRoadmapForRow"
      via: "compiles the roadmap offline from the selected row + profile"
      pattern: "buildRoadmapForRow"
    - from: "src/screens/PotentialApp.jsx"
      to: "src/screens/Roadmap.jsx"
      via: "navigation from results/city-detail to the roadmap surface"
      pattern: "Roadmap"
---

<objective>
Build the user-facing Roadmap surface: a React screen that renders the compiled 6-section roadmap offline, an inline `@media print` stylesheet, and an Export/Print PDF button wired to `window.print()`. Add the adapter that lets the UI compile a roadmap from its flattened result rows, and wire navigation so a user can reach the roadmap for their top city.

Purpose: Deliver the half of the phase that lives only in the UI — ROAD-01 SC1 ("user can view a 6-section roadmap") and ROAD-03 SC3/SC4 ("offline-readable" + "export/print as PDF"). The engine + content (Plans 02-03) are already green; this makes them visible and printable.
Output: `buildRoadmapForRow` in `src/lib/matchEngine.js`; `src/screens/Roadmap.jsx` (render + inline print CSS + window.print()); navigation wiring in `src/screens/PotentialApp.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-relocation-roadmap/06-PATTERNS.md

<interfaces>
<!-- The compiler the UI calls (Plan 02) -->
From shared/engine/roadmap.js:
```typescript
export function buildRoadmap(profile: Profile, top: MatchResult): Roadmap;
// Roadmap = { cityName: string; sections: { id, title, steps: { label, detail, sourceUrl? }[] }[] }
```

<!-- CRITICAL integration detail: the UI does NOT hold a clean MatchResult. -->
<!-- scoreProfile() flattens MatchResult into a row = { ...r.city, matchScore, salary, monthlyTakeHome, monthlySavings, expenses, scoreFactors, color }. -->
<!-- buildRoadmap needs a MatchResult shape: { city: City, monthlySavings, estSalary, monthlyTakeHome, expenses, matchScore, scoreFactors }. -->
<!-- The adapter must RECONSTRUCT that nesting from a flat row before calling buildRoadmap. -->
From src/lib/matchEngine.js (existing scoreProfile flattening — the row shape to adapt FROM):
```javascript
.map((r, i) => ({ ...r.city, matchScore: r.matchScore, salary: r.estSalary,
  monthlyTakeHome: r.monthlyTakeHome, monthlySavings: r.monthlySavings,
  expenses: r.expenses, scoreFactors: r.scoreFactors, color: ... }))
```
</interfaces>

<!-- Screen + inline-CSS + print conventions -->
@src/screens/ResultsMap.jsx
@src/lib/matchEngine.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildRoadmapForRow adapter in src/lib/matchEngine.js</name>
  <files>src/lib/matchEngine.js</files>
  <read_first>
    - src/lib/matchEngine.js (the file being modified — note scoreProfile flattens MatchResult into a row that spreads `...r.city`; the city fields and the flat financial fields coexist at the top level)
    - shared/engine/roadmap.ts (Plan 02 — buildRoadmap signature `(profile, top: MatchResult)`; MatchResult needs nested `.city` + `.monthlySavings`/`.estSalary`/etc.)
    - shared/types.ts lines 98-157 (City + MatchResult shapes — what the adapter must reconstruct)
  </read_first>
  <action>
    Add `export function buildRoadmapForRow(profile, row)` to `src/lib/matchEngine.js`. Import buildRoadmap with the `.js` suffix: `import { buildRoadmap } from '../../shared/engine/roadmap.js';` (alongside the existing rankCities import). The function reconstructs a MatchResult from the flattened UI row (the row is `{ ...city, matchScore, salary, monthlyTakeHome, monthlySavings, expenses, scoreFactors }`): build `const top = { city: row, matchScore: row.matchScore, estSalary: row.salary, monthlyTakeHome: row.monthlyTakeHome, monthlySavings: row.monthlySavings, expenses: row.expenses, scoreFactors: row.scoreFactors };` — note the row already IS the city (it spreads `...r.city`), so `city: row` gives buildRoadmap access to `city.name`, `city.country`, `city.medianRent`, `city.medianHome`. Map `salary` back to `estSalary` (the flatten renamed it). Return `buildRoadmap(profile, top)`. Pure, offline — no network.
  </action>
  <verify>
    <automated>node --input-type=module -e "import { buildRoadmapForRow } from './src/lib/matchEngine.js'; const row={ name:'London, UK', country:'UK', medianRent:2600, medianHome:740000, matchScore:88, salary:95000, monthlyTakeHome:6000, monthlySavings:1400, expenses:{}, scoreFactors:[] }; const p={ profession:'Software Engineer', housing:'rent', citizenship:'US' }; const rm=buildRoadmapForRow(p,row); if(rm.sections.length!==6) { console.error('FAIL sections',rm.sections.length); process.exit(1);} console.log('OK', rm.cityName, rm.sections.map(s=>s.id).join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - `buildRoadmapForRow` is exported from src/lib/matchEngine.js and imports buildRoadmap with the `.js` suffix.
    - Given a flattened London row + US-citizen profile, it returns a Roadmap with `cityName === 'London, UK'` and 6 sections in order timeline,financial,jobs,housing,logistics,visa (the UK covered pair, not the generic fallback).
    - The adapter does NOT recompute financials — it reads the row's `monthlySavings`/`salary` directly.
    - No network call (offline).
    - `npm test 2>&1 | tail -5` still green.
  </acceptance_criteria>
  <done>
    The UI can compile a real roadmap for any selected result row, offline, via one adapter call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build src/screens/Roadmap.jsx — render 6 sections + inline @media print CSS + window.print()</name>
  <files>src/screens/Roadmap.jsx</files>
  <read_first>
    - src/screens/ResultsMap.jsx lines 30-120 (the EXACT convention to mirror: `const CSS = \`...\`` template literal rendered via `<style>{CSS}</style>` inline in the component; default-export function taking `{ results, profile, ... }`; the dark-theme tokens; the `.edit`/`.pill` button styles; the `@media(max-width:560px)` block sits inside CSS)
    - src/lib/matchEngine.js (Task 1 — buildRoadmapForRow)
    - 06-PATTERNS.md §"src/screens/Roadmap.jsx" (print CSS goes INLINE in the screen's `<style>` — NOT a separate roadmap-print.css file; source-as-text never `<a>`)
    - 06-RESEARCH.md §"Print CSS skeleton" + §"Pitfall 4" (hide chrome, break-inside:avoid + page-break-inside:avoid per section, drop dark theme to ink, remove sticky/fixed)
  </read_first>
  <action>
    Create `src/screens/Roadmap.jsx`: a default-export function component `Roadmap({ row, profile, onBack })`. Inside, compile the roadmap once: `const roadmap = buildRoadmapForRow(profile, row);` (import from `../lib/matchEngine.js`). Render `roadmap.cityName` as a heading and map `roadmap.sections` to 6 section blocks; each section renders its `title` and maps `steps` to a label + detail line. Render `step.sourceUrl` (and any source name) as styled TEXT only — NEVER an `<a href>` (D-10 / see-not-click). Add a `[Export PDF]` button (styled like ResultsMap's `.edit`/`.pill`) and a `[Back]` button (calls `onBack`); the export button calls `window.print()`.

    Follow the ResultsMap inline-CSS convention EXACTLY: define `const CSS = \`...\`` and render `<style>{CSS}</style>` at the top of the returned JSX. Reuse the dark-theme tokens/look from ResultsMap. Add an `@media print` block INSIDE that same CSS string (do NOT create a separate `.css` file — 06-PATTERNS divergence flag): hide app chrome and the export/back buttons (give them a `no-print` class), `break-inside: avoid; page-break-inside: avoid;` on each roadmap section, drop the dark theme to `background:#fff;color:#000` for ink, remove any sticky/fixed positioning in print. Give the printable root a class (e.g. `roadmap-print-root`) and each section a class (e.g. `roadmap-section`). Keep the screen reasonable and conventional (placement/styling is planner/UI discretion per locked decision #2 — there is no UI-SPEC).

    The screen MUST make zero network calls — the roadmap is fully offline (ROAD-03).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/Roadmap.jsx` exists, default-exports `Roadmap`, compiles via `buildRoadmapForRow`.
    - Print CSS is INLINE in the component's `<style>{CSS}</style>` (grep: an `@media print` substring inside Roadmap.jsx; NO new `*.css` file created).
    - The `@media print` block hides chrome/buttons (`no-print`), applies `break-inside: avoid` + `page-break-inside: avoid` per section, and drops to white/black ink.
    - The Export button calls `window.print()` (grep: `window.print()` present).
    - `step.sourceUrl`/source name is rendered as text — NO `<a ` / `href=` in Roadmap.jsx (grep returns nothing).
    - No `fetch`/network in Roadmap.jsx (grep returns nothing).
    - `npm run build` succeeds with no errors.
  </acceptance_criteria>
  <done>
    A clean, offline, printable Roadmap screen renders all 6 sections with source-as-text and a working window.print() export.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire navigation from the results/city-detail surface to the Roadmap screen</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx lines 26-48 (the `step`/`selectedCity` navigation state), lines 150-220 (ResultsMap render + CityDetail render under `step === 2 && selectedCity` — the top row `c` is a scored result row)
    - src/screens/Roadmap.jsx (Task 2 — the `Roadmap({ row, profile, onBack })` props)
  </read_first>
  <action>
    Wire the Roadmap surface into `src/screens/PotentialApp.jsx`. Import `Roadmap from './Roadmap.jsx'`. Add a navigation affordance to view the roadmap for the user's TOP city (`results[0]`) and/or for the currently selected city in the city-detail view — add a "View relocation roadmap" button (styled to match existing buttons) in the city-detail surface (the `step === 2 && selectedCity` block) and/or near the top match. Introduce a small piece of state (e.g. `const [showRoadmap, setShowRoadmap] = useState(false)`) and render `<Roadmap row={selectedCity ?? results[0]} profile={profile} onBack={() => setShowRoadmap(false)} />` when `showRoadmap` is true. The selected row already carries the flattened financial fields the adapter needs (it is a scoreProfile row). Keep the existing flows intact (results map, city detail). Do NOT add a tier gate / paywall here — tier gating is Phase 8 (out of scope per 06-CONTEXT Deferred Ideas); this just surfaces the roadmap.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -q "Roadmap" src/screens/PotentialApp.jsx && echo "wired"</automated>
  </verify>
  <acceptance_criteria>
    - `PotentialApp.jsx` imports and renders `Roadmap` with `row` (the selected/top result row) + `profile` + an `onBack` handler.
    - A visible affordance ("View relocation roadmap" or equivalent) reaches the roadmap from results/city-detail.
    - Existing results-map and city-detail flows still render (no regression).
    - No tier gate / paywall added (Phase 8 scope).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
    A user can navigate from results/city-detail to the offline roadmap for their top city.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify offline roadmap render + PDF export</name>
  <action>Pause for the user to manually verify the offline roadmap render and the window.print() PDF export in the browser, per the steps below. This is the manual-only verification from 06-VALIDATION.md.</action>
  <what-built>An offline Roadmap screen rendering the personalized 6-section roadmap for the top city, reachable from results/city-detail, with an Export/Print PDF button wired to window.print() and an inline @media print stylesheet. (ROAD-01 SC1, ROAD-03 SC3/SC4 — the manual-only verification from 06-VALIDATION.md.)</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in the browser.
    2. Complete the quiz (or use the demo path) to reach results; open the top city, then click "View relocation roadmap".
    3. Confirm all 6 sections render with personalized numbers (savings/timeline, profession, rent/buy) and the visa section shows the UPL line — no clickable links anywhere (source names are plain text).
    4. Turn off Wi-Fi / kill the hotspot and reload the roadmap — confirm it still renders fully (offline, ROAD-03).
    5. Click "Export PDF" (window.print()). In the print preview confirm: app chrome/nav and the export/back buttons are HIDDEN; each of the 6 sections does NOT split awkwardly across pages; the output is white/black ink (not the dark theme); the result is a clean, readable PDF.
  </how-to-verify>
  <resume-signal>Type "approved" if the offline render + PDF export look correct, or describe what is broken (chrome leaking, section split, dark theme in PDF, a clickable link, missing numbers).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roadmap render → judges/stage | Source links and offline reliability are competition-rule + demo-safety surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | (competition rule) | Clickable source links on stage | mitigate | Render `sourceUrl`/source name as TEXT, never `<a>` (D-10); acceptance grep gate forbids `<a `/`href=` in Roadmap.jsx. |
| T-06-09 | Denial of Service | Network call on the roadmap render path breaks the offline demo | mitigate | `buildRoadmapForRow` + Roadmap.jsx make zero network calls; offline reload is part of the human-verify checkpoint (ROAD-03 / Pitfall 3). |
</threat_model>

<verification>
- `npm run build` succeeds.
- Roadmap.jsx has inline `@media print` CSS, `window.print()`, no `<a>`/`href`, no `fetch`.
- Human-verify checkpoint confirms offline render + clean PDF export (the manual-only item from 06-VALIDATION.md).
- `npm test` keeps the full suite green.
</verification>

<success_criteria>
- A Plus-tier user can view the 6-section roadmap offline (ROAD-01 SC1, ROAD-03 SC3).
- Export/Print produces a clean PDF (ROAD-03 SC4).
- Sources are text, never links.
</success_criteria>

<output>
Create `.planning/phases/06-relocation-roadmap/06-04-SUMMARY.md` when done.
</output>

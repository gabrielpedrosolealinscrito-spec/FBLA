---
phase: 06-relocation-roadmap
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - shared/data/roadmap-templates.ts
autonomous: true
requirements: [ROAD-01, ROAD-02]
must_haves:
  truths:
    - "A US-citizen's #1 US match renders a full, personalized 6-section domestic roadmap"
    - "A US-citizen → London/UK renders a full 6-section international roadmap with a UK visa pathway (never Portugal D8)"
    - "Every authored figure (salary basis, visa facts, targetFundUSD) is cited to a real source in the file"
    - "The visa section carries the immutable UPL line + a Premium-concierge teaser (VISA-04)"
  artifacts:
    - path: "shared/data/roadmap-templates.ts"
      provides: "ROADMAP_TEMPLATES.US.US (domestic) + ROADMAP_TEMPLATES.US.UK (London intl) authored content"
      contains: "US_TO_UK_TEMPLATE"
  key_links:
    - from: "ROADMAP_TEMPLATES.US.UK"
      to: "GOV.UK visa pathway facts"
      via: "authored, cited visa section (Skilled Worker / Global Talent — NOT Portugal D8)"
      pattern: "gov\\.uk"
---

<objective>
Author the two golden-path roadmap templates — US-citizen → #1 US match (domestic) and US-citizen → London/UK (the international "wow") — as full, sourced, 6-section content appended to the existing `shared/data/roadmap-templates.ts`. This is the content deliverable: the engine compiler already exists (Plan 02); this fills it with real authored knowledge so the rehearsed persona gets a personalized roadmap instead of the generic fallback.

Purpose: Deliver ROAD-01 (personalized 6-section roadmap for the top city) and ROAD-02 (template-first authored truth, every figure sourced, no invented steps) for the two demo pairs. Turns the "covered pair", "threads numbers", and "visa UPL" Wave 0 tests GREEN.
Output: `ROADMAP_TEMPLATES.US.US` and `ROADMAP_TEMPLATES.US.UK` populated in `shared/data/roadmap-templates.ts`, each a full 6-section `RoadmapTemplate` with cited facts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-relocation-roadmap/06-CONTEXT.md
@.planning/phases/06-relocation-roadmap/06-RESEARCH.md

<interfaces>
<!-- The authoring types Plan 02 created in this same file. Append to ROADMAP_TEMPLATES; do not redefine the types. -->
From shared/data/roadmap-templates.ts (Plan 02 — already exists):
```typescript
export interface RoadmapContext {
  cityName: string; profession: string; monthlySavings: number; estSalary: number;
  monthlyTakeHome: number; medianRent: number; medianHome: number;
  housing: 'rent' | 'buy'; monthsToFund: number | null; targetFundUSD: number;
}
export type SectionId = "timeline"|"financial"|"jobs"|"housing"|"logistics"|"visa";
export interface TemplateStep { label: string; detail: (ctx: RoadmapContext) => string; sourceUrl?: string; }
export interface TemplateSection { id: SectionId; title: string; steps: TemplateStep[]; }
export type RoadmapTemplate = TemplateSection[]; // exactly the 6 sections, in order
export const ROADMAP_TEMPLATES: Record<string, Record<string, RoadmapTemplate>> = { US: {} };
export const GENERIC_TEMPLATE: RoadmapTemplate = /* ...exists... */;
export const TARGET_FUND_USD: Record<string, number> = /* { US, UK, default } — refine per-destination here if needed */;
```
</interfaces>

<!-- The destination data the templates thread + the city/country keys -->
@shared/data/cities.ts
@.planning/phases/06-relocation-roadmap/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author ROADMAP_TEMPLATES.US.US — domestic 6-section roadmap</name>
  <files>shared/data/roadmap-templates.ts</files>
  <read_first>
    - shared/data/roadmap-templates.ts (Plan 02 output — append to ROADMAP_TEMPLATES.US; reuse the existing authoring types and GENERIC_TEMPLATE patterns; do NOT redefine types)
    - shared/data/cities.ts lines 1-52 (the `Sources:` comment convention + the US city fields available: medianRent, medianHome, costIndex, topIndustries, jobGrowth, stateTax)
    - 06-CONTEXT.md §"decisions" D-01 (thread real numbers), D-02 (honest reframe), D-08 (visa section depth), and the carried-forward UPL/source-as-text defaults
    - 06-RESEARCH.md §"Pattern 3" (move-fund detail function shape) + §"Assumptions A2/A4/A5"
  </read_first>
  <behavior>
    - ROADMAP_TEMPLATES.US.US compiles (via buildRoadmap) to 6 sections in order with non-empty, personalized detail strings.
    - The jobs section detail names the user's profession; the timeline/financial detail threads monthlySavings + the monthsToFund countdown (or the deficit reframe when monthsToFund is null).
    - The housing section threads medianRent/medianHome by the user's rent/buy preference.
    - The visa section is LIGHT (domestic move) but still carries the immutable UPL line + a one-line Premium teaser.
  </behavior>
  <action>
    Append `const US_DOMESTIC_TEMPLATE: RoadmapTemplate = [...]` and assign `ROADMAP_TEMPLATES.US.US = US_DOMESTIC_TEMPLATE` (or author it inline under the `US` key). Author exactly the 6 TemplateSection entries in order: timeline, financial, jobs, housing, logistics, visa.

    Each step: authored immutable `label`, a `detail: (ctx) => string` that interpolates real `ctx` numbers via `toLocaleString()` for currency (06-RESEARCH "Don't Hand-Roll"), and an optional `sourceUrl` (rendered as TEXT later — D-10). Thread the marquee timeline (D-01): the timeline/financial step returns "Based on your projected $X/mo of savings, your ~$Y move fund is reachable in about N months." when `ctx.monthsToFund !== null`, and the honest deficit reframe (D-02) when it is null — reuse the exact branch logic shape from the generic template so behavior is consistent. Jobs section: name `ctx.profession` and reference the destination's industry/salary context. Housing section: branch on `ctx.housing` ('rent' → use `ctx.medianRent`, 'buy' → use `ctx.medianHome`). Logistics: a real domestic-move checklist (address change, license/registration transfer, utilities, movers) — authored, no invented legal steps. Visa section (domestic, D-08): a short authored note that a US-citizen domestic move needs no visa, then the IMMUTABLE UPL line "This is informational only, not legal advice. Consult a licensed immigration attorney for your situation." and a one-line Premium-concierge teaser.

    Cite every authored figure (salary/industry basis, any cost figure, the US targetFundUSD basis) in the file's `Sources:` header block with a real URL (ROAD-02). Do NOT invent procedural/legal steps. Do NOT edit shared/types.ts. Do NOT render sourceUrl as a link (that is the UI plan's concern; here just store the string).
  </action>
  <verify>
    <automated>npx vitest run shared/engine/roadmap.test.ts -t "covered pair" && npx vitest run shared/engine/roadmap.test.ts -t "threads numbers"</automated>
  </verify>
  <acceptance_criteria>
    - `ROADMAP_TEMPLATES.US.US` is a RoadmapTemplate with exactly 6 TemplateSection entries, ids in order timeline,financial,jobs,housing,logistics,visa.
    - The timeline/financial detail threads `ctx.monthlySavings` and `ctx.monthsToFund` (countdown branch) and falls back to the deficit reframe when null (D-02).
    - The jobs detail references `ctx.profession`; the housing detail branches on `ctx.housing` using medianRent/medianHome.
    - The visa step detail contains the immutable UPL phrasing ("informational only", "not legal advice", "licensed" attorney) + a Premium teaser.
    - Every authored figure has a source URL in the `Sources:` block (grep header for URLs).
    - `npx vitest run shared/engine/roadmap.test.ts -t "covered pair"` and `-t "threads numbers"` pass (the covered-pair fixture in Plan 01 includes a US covered MatchResult).
    - shared/types.ts UNCHANGED.
  </acceptance_criteria>
  <done>
    A US-citizen's #1 US match renders a full personalized domestic roadmap from authored, sourced content.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author ROADMAP_TEMPLATES.US.UK — London/UK international roadmap (UK visa pathway, NOT Portugal D8)</name>
  <files>shared/data/roadmap-templates.ts</files>
  <read_first>
    - shared/data/roadmap-templates.ts (Task 1 output — append the UK template alongside US.US)
    - shared/data/cities.ts lines 821-840 (the London, UK record: country "UK", financialModelId "uk-2026", medianRent/medianHome, and its `Sources:` block style)
    - 06-CONTEXT.md §"decisions" D-06 (author BOTH golden-path pairs), D-08 (visa = summary + UPL + Premium teaser)
    - 06-RESEARCH.md §"Pitfall 1" (Lisbon-vs-London trap) + §"Assumptions A1/A3" (UK pathway is Skilled Worker / Global Talent, GOV.UK-cited)
    - 06-PATTERNS.md §"International pair = US→UK (London), NOT Lisbon/Portugal"
  </read_first>
  <behavior>
    - ROADMAP_TEMPLATES.US.UK compiles to 6 personalized sections in order for a US-citizen → London/UK move.
    - The visa section's headline is a UK work/talent pathway (Skilled Worker or Global Talent), cited to GOV.UK — and contains NO reference to Portugal D8.
    - timeline/financial/jobs/housing/logistics thread the user's real numbers and the London/UK relocation context.
  </behavior>
  <action>
    Append `const US_TO_UK_TEMPLATE: RoadmapTemplate = [...]` and assign `ROADMAP_TEMPLATES.US.UK = US_TO_UK_TEMPLATE`. Same 6-section structure and detail-function discipline as Task 1, but for a US-citizen relocating to London/UK.

    HARD CONSTRAINT (locked decision #1 + 06-RESEARCH Pitfall 1): the `visa` section headline pathway is a UK route — Skilled Worker visa or Global Talent visa — authored from official GOV.UK sources, with 2-3 key facts (rough timeline + fee/salary-threshold range), cited to gov.uk URLs in the `Sources:` block. NEVER author a "Portugal D8" section here; this is a London roadmap. The visa section also carries the IMMUTABLE UPL line (identical text to the other templates) + the Premium-concierge teaser (D-08).

    Thread the international relocation context: timeline/financial uses `ctx.monthlySavings` + `ctx.monthsToFund` with the UK `targetFundUSD` (international relocation cost is higher — confirm/refine `TARGET_FUND_USD.UK` here with a cited basis). Jobs section names `ctx.profession` and the London tech/finance market context. Housing branches on `ctx.housing` using London `medianRent`/`medianHome`. Logistics: a real international-move checklist (passport validity, international shipping, NHS/registration, opening a UK bank account, BRP/biometrics) — authored, cited where it states a procedural fact, no invented legal steps. Do NOT edit shared/types.ts.
  </action>
  <verify>
    <automated>npx vitest run shared/engine/roadmap.test.ts -t "covered pair" && npx vitest run shared/engine/roadmap.test.ts -t "visa UPL" && ! grep -qiE "portugal|d8" shared/data/roadmap-templates.ts && echo "OK: no Portugal D8 in templates"</automated>
  </verify>
  <acceptance_criteria>
    - `ROADMAP_TEMPLATES.US.UK` is a RoadmapTemplate with exactly 6 TemplateSection entries in order.
    - The visa section headline is a UK pathway (Skilled Worker / Global Talent), cited to a `gov.uk` URL in the `Sources:` block.
    - NO occurrence of "Portugal" or "D8" anywhere in `shared/data/roadmap-templates.ts` (grep -i returns nothing).
    - The visa step carries the immutable UPL line + Premium teaser (`-t "visa UPL"` passes against the UK covered fixture).
    - `-t "covered pair"` and `-t "visa UPL"` pass; the full new suite `npx vitest run shared/engine/roadmap.test.ts` is fully GREEN.
    - `npm test 2>&1 | tail -5` shows all prior tests + the roadmap suite green.
    - shared/types.ts UNCHANGED.
  </acceptance_criteria>
  <done>
    A US-citizen → London/UK renders a full international roadmap with a correctly-sourced UK visa pathway; all Wave 0 engine tests are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → judges/Q&A | Visa/legal/procedural facts shown on stage; wrong facts or UPL breach are credibility + liability risks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information disclosure / liability | Visa section drifts into legal advice (UPL) | mitigate | Fixed authored UPL line ("informational only, not legal advice; consult a licensed attorney") in every template's visa section; never personalized legal advice (VISA-04 / D-08); enforced by the "visa UPL" Wave 0 test. |
| T-06-06 | Tampering / Spoofing | Wrong international pathway authored (Portugal D8 on a London roadmap) | mitigate | Hard constraint in action text + a grep gate asserting no "Portugal"/"D8" string; UK pathway cited to GOV.UK (06-RESEARCH Pitfall 1, A1/A3). |
| T-06-07 | Repudiation | Uncited figures on stage cannot be defended in Q&A | mitigate | Every authored figure cited to a real URL in the `Sources:` block (ROAD-02 / cities.ts convention). |
</threat_model>

<verification>
- `npx vitest run shared/engine/roadmap.test.ts` is fully GREEN (all seven Wave 0 cases pass).
- `grep -iE "portugal|d8" shared/data/roadmap-templates.ts` returns nothing.
- Every authored figure is cited in the file header `Sources:` block.
- `npm test` keeps the full suite green.
</verification>

<success_criteria>
- Both golden-path pairs (US→US, US→London/UK) render full personalized 6-section roadmaps (ROAD-01).
- All content is authored, sourced, no invented procedural/legal steps (ROAD-02).
- The London visa section is a UK pathway, never Portugal D8.
- UPL line present in every visa section (VISA-04).
</success_criteria>

<output>
Create `.planning/phases/06-relocation-roadmap/06-03-SUMMARY.md` when done.
</output>

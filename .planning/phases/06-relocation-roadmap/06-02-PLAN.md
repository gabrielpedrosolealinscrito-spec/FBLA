---
phase: 06-relocation-roadmap
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - shared/data/roadmap-templates.ts
  - shared/engine/roadmap.ts
autonomous: true
requirements: [ROAD-01, ROAD-02, ROAD-03]
must_haves:
  truths:
    - "buildRoadmap is a pure, offline, deterministic function of Profile + MatchResult"
    - "An uncovered citizenship×country pair renders the GENERIC_TEMPLATE (6 sections, no invented procedural steps)"
    - "Negative/zero monthlySavings produces an honest deficit reframe, never a faked timeline"
    - "acceptEnrichment rejects any mutation of authored label/sourceUrl/order/count and polishes detail only"
  artifacts:
    - path: "shared/data/roadmap-templates.ts"
      provides: "Authoring types (RoadmapTemplate/TemplateStep/RoadmapContext), GENERIC_TEMPLATE, targetFundUSD by destination, empty ROADMAP_TEMPLATES.US scaffold"
      contains: "GENERIC_TEMPLATE"
    - path: "shared/engine/roadmap.ts"
      provides: "buildRoadmap compiler + buildContext + monthsToFund + acceptEnrichment validator"
      exports: ["buildRoadmap", "acceptEnrichment"]
  key_links:
    - from: "shared/engine/roadmap.ts"
      to: "shared/data/roadmap-templates.ts"
      via: "import ROADMAP_TEMPLATES + GENERIC_TEMPLATE with .js suffix"
      pattern: "from '../data/roadmap-templates.js'"
    - from: "shared/engine/roadmap.ts"
      to: "top.monthlySavings"
      via: "reads MatchResult directly, never recomputes"
      pattern: "top\\.monthlySavings"
---

<objective>
Build the pure offline compiler that turns an authoring template + the user's real engine output into the LOCKED `Roadmap` contract — with the generic fallback, the honest negative-savings reframe, and the build-time enrich-preservation validator. This is the engine spine of the phase; it ships complete with ZERO network calls (ROAD-03 / D-03).

Purpose: Deliver ROAD-01's personalization mechanism, ROAD-03's offline determinism, D-07's never-dead-end fallback, D-02's honesty boundary, and ROAD-02/D-05's authored-truth-preservation validator. This plan turns the "fallback", "negative savings", "offline deterministic", and "enrich preserves authored" Wave 0 tests GREEN. (The "covered pair", "threads numbers", and "visa UPL" tests stay red until Plan 03 authors the real templates.)
Output: `shared/data/roadmap-templates.ts` (authoring types + GENERIC_TEMPLATE + targetFundUSD + empty US template scaffold) and `shared/engine/roadmap.ts` (buildRoadmap + buildContext + monthsToFund + acceptEnrichment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-relocation-roadmap/06-RESEARCH.md
@.planning/phases/06-relocation-roadmap/06-PATTERNS.md

<interfaces>
<!-- LOCKED contract this compiler outputs (shared/types.ts 142-187). Do NOT edit types.ts. -->
From shared/types.ts:
```typescript
export interface RoadmapSection {
  id: "timeline" | "financial" | "jobs" | "housing" | "logistics" | "visa";
  title: string;
  steps: { label: string; detail: string; sourceUrl?: string }[];
}
export interface Roadmap { cityName: string; sections: RoadmapSection[]; }
export interface MatchResult {
  city: City;            // city.name, city.country ("US"|"UK"), city.medianRent, city.medianHome
  matchScore: number; estSalary: number; monthlyTakeHome: number;
  expenses: ExpenseBreakdown; monthlySavings: number;  // CAN be negative (D-02)
}
export interface Profile { profession: string; housing: "rent"|"buy"; citizenship: string; /* ...many more... */ }
```

<!-- Established repo idioms to mirror (06-PATTERNS.md) -->
From shared/engine/index.ts (lookup-with-fallback line 65; conditional optional-field spread line 157; reads MatchResult fields directly, never recomputes):
```typescript
const model = FINANCIAL_MODELS[city.financialModelId] ?? FINANCIAL_MODELS['us'];
...(signal !== null ? { reconfirmSignal: signal } : {})
```
From api/live-core.ts (validateItems — the throw-on-mismatch idiom acceptEnrichment extends).
</interfaces>

@shared/engine/index.ts
@shared/engine/financial.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the authoring types, GENERIC_TEMPLATE, and targetFundUSD in shared/data/roadmap-templates.ts</name>
  <files>shared/data/roadmap-templates.ts</files>
  <read_first>
    - shared/data/roadmap-templates.ts (the file being created — does not exist yet)
    - shared/data/cities.ts lines 1-24 (analog: module-header comment with phase/decision IDs + a `Sources:` block citing every authored figure to a real URL; typed-const-array export)
    - shared/types.ts lines 178-187 (the locked RoadmapSection ids — SectionId derives from here)
    - 06-RESEARCH.md §"Pattern 1: Authoring type ≠ contract type" and §"Assumptions Log A2" (targetFundUSD must be a sourced, conservative, per-destination relocation-cost estimate)
    - 06-PATTERNS.md §"shared/data/roadmap-templates.ts"
  </read_first>
  <behavior>
    - GENERIC_TEMPLATE compiles (via the Plan-02 Task-2 buildRoadmap) to exactly the 6 sections in order: timeline, financial, jobs, housing, logistics, visa.
    - The generic visa section carries the fixed UPL line (informational only / not legal advice / consult a licensed attorney) — no invented procedural/legal steps (D-07 honesty).
    - targetFundUSD is resolvable per destination country and is a cited authored constant.
  </behavior>
  <action>
    Create `shared/data/roadmap-templates.ts` with a `cities.ts`-style module header naming Phase 6 + decision IDs (D-01/D-02/D-05/D-07) and a `Sources:` block. Import types with the `.js` suffix: `import type { Profile, MatchResult, RoadmapSection } from '../types.js';`.

    Define the AUTHORING types (separate from the locked contract — do NOT edit shared/types.ts): `RoadmapContext` (fields: `cityName, profession, monthlySavings (number, can be negative), estSalary, monthlyTakeHome, medianRent, medianHome, housing: 'rent'|'buy', monthsToFund: number | null, targetFundUSD: number`); `SectionId = RoadmapSection['id']` (derives the 6 ids from the locked contract); `TemplateStep` (`label: string` IMMUTABLE, `detail: (ctx: RoadmapContext) => string`, `sourceUrl?: string` IMMUTABLE); `TemplateSection` (`{ id: SectionId; title: string; steps: TemplateStep[] }`); `RoadmapTemplate = TemplateSection[]` (exactly the 6 sections, in order).

    Export `ROADMAP_TEMPLATES: Record<string, Record<string, RoadmapTemplate>>` initialized as `{ US: {} }` (an EMPTY US bucket — Plan 03 appends `US.US` and `US.UK` to this same file). Do NOT author the persona pairs here; this plan only delivers the types, the generic fallback, and the cost constants.

    Export `GENERIC_TEMPLATE: RoadmapTemplate` — a real, honest, generic 6-section skeleton (D-07): each of the 6 sections present in order with 2-4 generic-but-truthful steps whose `detail` functions interpolate `ctx` numbers where sensible (e.g. timeline references monthsToFund/deficit, financial references monthlySavings, jobs references profession, housing references medianRent/medianHome + housing preference). The generic `visa` section contains a single fixed UPL step: a `label` like "Immigration & visa" and a `detail` that returns the immutable VISA-04 line — "This is informational only, not legal advice. Consult a licensed immigration attorney for your situation." plus a one-line Premium-concierge teaser. NO invented country-specific procedural/legal steps in the generic template. The negative-savings reframe (D-02) MUST live inside the generic timeline/financial step function: when `ctx.monthsToFund === null` (savings <= 0) return the honest deficit text (containing the word "deficit", no fabricated months count); otherwise return the threaded countdown text.

    Export `targetFundUSD` as cited authored data driving the move-fund math (Assumptions A2). Make it per-destination-country (a domestic US move and an international UK relocation cost differ): e.g. `export const TARGET_FUND_USD: Record<string, number> = { US: <domestic estimate>, UK: <intl relocation estimate>, default: <conservative fallback> };`. Cite each figure in the `Sources:` block with a real URL (flights + deposit + shipping/buffer basis). Plan 03 may refine the per-destination numbers when authoring content, but a sourced default MUST exist now.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -cE "id: ?['\"](timeline|financial|jobs|housing|logistics|visa)['\"]" shared/data/roadmap-templates.ts | grep -qx 6 && echo "OK: GENERIC_TEMPLATE typechecks with 6 section ids"</automated>
  </verify>
  <acceptance_criteria>
    - `shared/data/roadmap-templates.ts` exists; all relative imports use `.js`.
    - Exports present: `RoadmapContext`, `SectionId`, `TemplateStep`, `TemplateSection`, `RoadmapTemplate`, `ROADMAP_TEMPLATES` (with `US: {}`), `GENERIC_TEMPLATE`, `TARGET_FUND_USD`.
    - `GENERIC_TEMPLATE` has exactly 6 TemplateSection entries with ids in order timeline,financial,jobs,housing,logistics,visa (grep: each id appears once).
    - The generic visa step's detail returns the immutable UPL phrasing ("informational only", "not legal advice", "licensed" attorney).
    - The generic timeline/financial step returns deficit text when monthsToFund is null and a countdown when not (the D-02 branch exists in source).
    - `TARGET_FUND_USD` keys include `US`, `UK`, and `default`; each non-default figure is cited in the `Sources:` block (grep the header for a URL per figure).
    - shared/types.ts is UNCHANGED (`git diff --stat shared/types.ts` empty).
    - After Task 2 lands, `npx vitest run shared/engine/roadmap.test.ts -t "fallback"` passes.
  </acceptance_criteria>
  <done>
    The authoring layer + honest generic fallback + sourced relocation-cost constants exist; the file is structured so Plan 03 only appends the two persona pairs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement buildRoadmap + buildContext + monthsToFund + acceptEnrichment in shared/engine/roadmap.ts</name>
  <files>shared/engine/roadmap.ts</files>
  <read_first>
    - shared/engine/roadmap.ts (the file being created — does not exist yet)
    - shared/engine/index.ts lines 19-25 (`.js`-suffix imports), 36-39 (clamp helper, NaN-safe), 63-110 (buildRawResult: reads MatchResult fields directly, never recomputes; conditional optional-field spread line 157), 65 (lookup-with-fallback `?? default`)
    - shared/data/roadmap-templates.ts (Task 1 output — the authoring types + GENERIC_TEMPLATE + TARGET_FUND_USD this compiler consumes)
    - api/live-core.ts (validateItems — the throw-on-mismatch validator idiom acceptEnrichment extends; see 06-PATTERNS.md §"LLM-output validation")
    - shared/engine/roadmap.test.ts (Plan 01 — the RED contract this implementation must satisfy)
    - 06-RESEARCH.md §"Pattern 3: Move-fund timeline + honest reframe" and §"Enrich validator" code example
  </read_first>
  <behavior>
    - buildRoadmap(profile, top) returns the locked Roadmap with the 6 sections compiled from the resolved template, each step's detail(ctx) evaluated to a string. Pure, offline, deterministic.
    - Uncovered pair → GENERIC_TEMPLATE via `?? GENERIC_TEMPLATE` (D-07).
    - monthsToFund = ceil(targetFundUSD / monthlySavings) when monthlySavings > 0, else null (D-02 — never clamp a negative to 0).
    - acceptEnrichment returns `[{detail}]` for valid polished input and throws on count/label/sourceUrl/order mutation (D-05).
  </behavior>
  <action>
    Create `shared/engine/roadmap.ts`. Imports (all `.js`): `import type { Profile, MatchResult, Roadmap } from '../types.js';` and `import { ROADMAP_TEMPLATES, GENERIC_TEMPLATE, TARGET_FUND_USD, type RoadmapContext } from '../data/roadmap-templates.js';`. Module header naming Phase 6 + ROAD-01/02/03 + the offline/pure invariant (mirror index.ts header style). It MUST NOT import or reference `fetch`/`/api`/`await` — zero network (ROAD-03 / Pitfall 3).

    Implement `buildContext(profile: Profile, top: MatchResult): RoadmapContext`: read MatchResult fields DIRECTLY (`top.monthlySavings`, `top.estSalary`, `top.monthlyTakeHome`, `top.city.name`, `top.city.medianRent`, `top.city.medianHome`) — never recompute financials (D-01 anti-pattern). Select `targetFundUSD = TARGET_FUND_USD[top.city.country] ?? TARGET_FUND_USD.default` (lookup-with-fallback idiom). Compute `monthsToFund = top.monthlySavings > 0 ? Math.ceil(targetFundUSD / top.monthlySavings) : null` (D-02 — null, never clamp). Populate `profession` from `profile.profession`, `housing` from `profile.housing`.

    Implement `buildRoadmap(profile: Profile, top: MatchResult): Roadmap`: resolve `const tmpl = ROADMAP_TEMPLATES[profile.citizenship]?.[top.city.country] ?? GENERIC_TEMPLATE;` (D-07). Build ctx via buildContext. Map the template to the locked contract: `cityName: top.city.name`, `sections: tmpl.map(s => ({ id: s.id, title: s.title, steps: s.steps.map(st => ({ label: st.label, detail: st.detail(ctx), ...(st.sourceUrl ? { sourceUrl: st.sourceUrl } : {}) })) }))` — copy `label`/`sourceUrl` verbatim, evaluate `detail(ctx)` to a string. Use the conditional optional-field spread idiom (index.ts line 157) for sourceUrl. Pure: no mutation of inputs.

    Implement `acceptEnrichment(authored: { label: string; detail: string; sourceUrl?: string }[], llm: unknown): { detail: string }[]` extending the validateItems throw-on-mismatch idiom (D-05, ROAD-02 hard boundary — build-time only, NEVER on the render path): throw if `llm` is not an array or its length !== authored.length (count/order change); for each index, throw if `e.detail` is not a string, throw if `e.label !== undefined && e.label !== authored[i].label` (label mutated), throw if `e.sourceUrl !== undefined && e.sourceUrl !== authored[i].sourceUrl` (sourceUrl mutated); otherwise return `{ detail: e.detail }` (polished prose ONLY). On ANY violation the caller keeps the authored detail — this function never silently ships an LLM mutation.
  </action>
  <verify>
    <automated>npx vitest run shared/engine/roadmap.test.ts -t "fallback" && npx vitest run shared/engine/roadmap.test.ts -t "negative savings" && npx vitest run shared/engine/roadmap.test.ts -t "offline deterministic" && npx vitest run shared/engine/roadmap.test.ts -t "enrich preserves authored"</automated>
  </verify>
  <acceptance_criteria>
    - `shared/engine/roadmap.ts` exists; exports `buildRoadmap` and `acceptEnrichment`; all relative imports use `.js`.
    - NO `fetch`/`/api`/network reference in the file (grep -E "fetch|/api|http" returns nothing in source lines).
    - Reads `top.monthlySavings` directly; no re-derivation of savings/tax/expenses (grep: no computeExpenses/computeTax calls).
    - `buildRoadmap` resolves via `ROADMAP_TEMPLATES[profile.citizenship]?.[top.city.country] ?? GENERIC_TEMPLATE`.
    - These Wave 0 tests PASS: `-t "fallback"`, `-t "negative savings"`, `-t "offline deterministic"`, `-t "enrich preserves authored"`.
    - The "covered pair"/"threads numbers"/"visa UPL" tests remain RED (no authored persona pairs yet — that is Plan 03; they pass only when ROADMAP_TEMPLATES.US.US/.UK exist).
    - `npm test 2>&1 | tail -5` shows the prior 123 tests still green.
    - shared/types.ts UNCHANGED.
  </acceptance_criteria>
  <done>
    The pure offline compiler + honest timeline math + authored-truth-preservation validator are implemented and pass their Wave 0 gates; the only remaining red tests are the ones that require Plan 03's authored content.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time enrich → authored content | An LLM-produced enrich payload crosses into authored roadmap data (build-time only, never render path). `acceptEnrichment` is the gate. |
| render path | `buildRoadmap` takes only local Profile + MatchResult — no untrusted/network input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | LLM rewrites/invents procedural/legal facts during enrich | mitigate | `acceptEnrichment` rejects any change to step count/order, `label`, or `sourceUrl`; caller falls back to authored `detail` (D-05/ROAD-02). Validator is unit-tested by the "enrich preserves authored" Wave 0 case. |
| T-06-03 | Tampering | Engine recomputes financials and drifts from MatchResult | mitigate | `buildContext` reads `top.monthlySavings`/`estSalary` directly; acceptance criteria forbid recompute calls (D-01). |
| T-06-04 | Denial of Service | Network call sneaks onto the render path, breaking offline demo | mitigate | `buildRoadmap` is pure with zero `fetch`/`/api` imports; "offline deterministic" Wave 0 test + grep gate enforce it (ROAD-03 / Pitfall 3). |
</threat_model>

<verification>
- `npx vitest run shared/engine/roadmap.test.ts` shows "fallback", "negative savings", "offline deterministic", "enrich preserves authored" GREEN; the three content-dependent tests still RED.
- `npm test` keeps the existing 123 tests green.
- `git diff --stat shared/types.ts` is empty (contract untouched).
</verification>

<success_criteria>
- Pure, offline, deterministic `buildRoadmap` ships (ROAD-03 / D-03).
- Generic fallback never dead-ends (D-07).
- Negative savings produces an honest reframe (D-02).
- Enrich validator preserves authored truth (ROAD-02 / D-05).
</success_criteria>

<output>
Create `.planning/phases/06-relocation-roadmap/06-02-SUMMARY.md` when done.
</output>

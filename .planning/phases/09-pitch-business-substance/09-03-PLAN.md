---
phase: 09-pitch-business-substance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pitch/financials/model.csv
  - pitch/financials/summary.md
autonomous: true
requirements: [PITCH-05]
must_haves:
  truths:
    - "pitch/financials/model.csv has one row per month for ~24 months and a break-even month is identifiable from the cumulative-net column"
    - "Every input cell traces to a stated, sourced assumption documented in pitch/financials/summary.md — the whole model is re-derivable from first principles in ~60 seconds (D-08)"
    - "The model shows per-run live-AI COGS (~$0.06) and confirms Plus and Premium stay margin-positive (D-11)"
    - "Per-channel CAC and LTV-by-tier are present and use one-time-purchase LTV treatment (not ARPU/churn)"
  artifacts:
    - path: "pitch/financials/model.csv"
      provides: "24-month base-case projection: free users, paid users, per-tier revenue, API COGS, hosting, marketing, net, cumulative net"
      contains: "Month"
    - path: "pitch/financials/summary.md"
      provides: "Assumptions table + markdown summary + break-even callout + LTV-by-tier + per-channel CAC, all sourced"
      contains: "break-even"
  key_links:
    - from: "pitch/financials/summary.md assumptions"
      to: "pitch/financials/model.csv input cells"
      via: "every CSV number traces to a stated assumption"
      pattern: "COGS|CAC|conversion"
    - from: "pitch/financials/model.csv COGS column"
      to: "Anthropic per-run pricing (~$0.06)"
      via: "Haiku + web-search per-run cost"
      pattern: "0\\.06"
---

<objective>
Author the feasibility & financials deliverable (PITCH-05) as `pitch/financials/model.csv` (a 24-month base-case projection) plus `pitch/financials/summary.md` (assumptions, markdown summary, break-even callout). Every number must be re-derivable from first principles in ~60 seconds (D-08) and defensible in Q&A. One end-to-end rubric deliverable.

Purpose: Covers the "Feasibility & Financial Thinking" rubric dimension — startup costs, per-channel CAC, LTV by tier, per-run COGS, break-even month, all bottom-up from stated assumptions (D-08, D-09). This is the most Q&A-stress-tested deliverable; the CSV must show the math line-by-line.
Output: `pitch/financials/model.csv` + `pitch/financials/summary.md` (the financials/ dir currently holds only README.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 24-month base-case financial model (pitch/financials/model.csv)</name>
  <files>pitch/financials/model.csv</files>
  <read_first>
    - pitch/financials/README.md (the financials/ landing note — model.csv lives alongside it)
    - .planning/phases/09-pitch-business-substance/09-RESEARCH.md §PITCH-05 (Anthropic COGS ~$0.06/run, startup costs, per-channel CAC, LTV-by-tier, break-even framing, the exact CSV column spec + assumptions list)
    - .planning/phases/09-pitch-business-substance/09-CONTEXT.md (D-08 one base case / CSV / 24-month / 60-sec re-derivable, D-09 model contents, D-10 conversion shape, D-11 per-run COGS margin protection)
  </read_first>
  <action>
    Create `pitch/financials/model.csv` with one row per month for 24 months using exactly the column spec from RESEARCH §Financial Model Structure:
    Month | Free_Users | Paid_Users | Basic_Rev | Plus_Rev | Premium_Rev | Total_Rev | API_COGS | Hosting | Marketing_Spend | Net_Income | Cumulative_Net

    Drive every column from the stated assumptions (RESEARCH §Financial Model Structure):
    - Free-to-any-paid conversion: 5% (months 1–6), 7% (7–12), 8% (13–24).
    - Basic/Plus/Premium mix: 50% / 35% / 15%. Prices: $0.99 / $9.99 / $29.99.
    - API COGS per run: $0.06 (Haiku + up to 3 web searches — confirms Plus run revenue $3.33 keeps ~97–98% margin; Premium unlimited stays margin-positive at realistic use, D-11).
    - Hosting: $20/mo (Vercel Pro — per RESEARCH Open Question 3, use Pro not Hobby for a commercial model).
    - Blended CAC $10 → Marketing_Spend = new paid users × $10.
    - Monthly free-user growth: 500 (month 1) ramping to ~1,500 (month 6) to ~3,000 (month 12), continuing through month 24.
    - Cumulative_Net accumulates Net_Income; the break-even month is the first month Cumulative_Net turns >= 0.

    Keep arithmetic simple and transparent (judges must follow it in 60s). Do NOT use ARPU/churn/MRR math — this is one-time-purchase revenue (D-04). Do NOT add optimistic/conservative scenarios (D-08 — one base case only). Values may be hardcoded computed numbers (a CSV cannot hold formulas), but each must match the assumption it derives from so summary.md can re-derive it.
  </action>
  <verify>
    <automated>test -f pitch/financials/model.csv && awk -F, 'NR>1{c++} END{print c}' pitch/financials/model.csv  # expect ~24 data rows</automated>
  </verify>
  <acceptance_criteria>
    - pitch/financials/model.csv has a header row matching the RESEARCH column spec and ~24 monthly data rows
    - The Cumulative_Net column crosses from negative to >= 0 at an identifiable break-even month
    - API_COGS in each month equals Paid_Users-derived run count × $0.06 (per-run COGS visible, D-11)
    - No subscription/MRR/churn columns exist — revenue is one-time per-tier (D-04, D-08)
    - `awk -F, 'NR>1' pitch/financials/model.csv | wc -l` is ~24
  </acceptance_criteria>
  <done>24-row base-case CSV built; break-even month identifiable; per-run COGS at $0.06; one-time revenue only.</done>
</task>

<task type="auto">
  <name>Task 2: Author the assumptions + break-even summary (pitch/financials/summary.md)</name>
  <files>pitch/financials/summary.md</files>
  <read_first>
    - pitch/financials/model.csv (the model just built — summary must re-derive its inputs)
    - .planning/phases/09-pitch-business-substance/09-RESEARCH.md §PITCH-05 (startup costs table, per-channel CAC table, LTV-by-tier table, break-even formula, Assumptions Log A1–A7, F2/F3/F7 flags)
    - .planning/phases/09-pitch-business-substance/09-CONTEXT.md (D-08/D-09/D-11; LTV must use one-time-purchase treatment, not ARPU/churn)
  </read_first>
  <action>
    Create `pitch/financials/summary.md` in hybrid format (D-01) — a narrative + claim/number/source tables — that makes the CSV fully re-derivable in 60 seconds:

    - Assumptions table: every model input (conversion 5/7/8%, mix 50/35/15, prices, API COGS $0.06, hosting $20/mo, blended CAC $10, user-growth ramp) with its source/justification. Cite Anthropic pricing (anthropic.com/pricing, [FOUNDER-VERIFY: F3]) for COGS; FirstPageSage/Userpilot for the conversion benchmark; HubSpot/WordStream for CAC anchors.
    - Startup costs table: API ($50–$200), domain ($12–$15), hosting (Vercel Pro $20/mo, vercel.com/pricing), legal/registration ($50–$500 [ASSUMED, founder jurisdiction]), marketing ($0–$500) → total ~$200–$1,500, framed as AI-native near-zero marginal cost.
    - Per-channel CAC table (SEO $5–15, Reddit $0–5, TikTok $5–20, University $10–30, blended ~$8–12) — labeled modeled [ASSUMED] estimates with benchmark anchors (RESEARCH Pitfall 2).
    - LTV-by-tier table using ONE-TIME-PURCHASE treatment (NOT ARPU/churn): Basic ~$1.30, Plus ~$14, Premium ~$45, blended ~$13–15, with the 0.3×/0.4×/0.5× repeat factors stated; the 2–3× lifetime relocation rationale carries [FOUNDER-VERIFY: F7].
    - Per-run margin proof (D-11): Plus run revenue $3.33 vs $0.06 COGS = ~98% gross margin; Premium unlimited stays margin-positive even at extreme run counts (e.g. 100 runs = $6 COGS vs $29.99).
    - Break-even callout: state the formula (startup cost ÷ blended net/paid user ≈ 112 paid users) and point to the CSV month where Cumulative_Net >= 0.
    - Carry the Assumptions Log (A1–A7) and the founder-verify flags (F2 census year, F3 Anthropic pricing, F7 relocation frequency) so nothing unsourced is hidden.

    Numbers/URLs from RESEARCH §PITCH-05 — do not invent.
  </action>
  <verify>
    <automated>test -f pitch/financials/summary.md && grep -ciE "break-?even" pitch/financials/summary.md  # must be >=1</automated>
  </verify>
  <acceptance_criteria>
    - pitch/financials/summary.md contains an assumptions table where every model.csv input maps to a stated, sourced assumption (Anthropic pricing, FirstPageSage/Userpilot, HubSpot/WordStream cited)
    - Startup costs, per-channel CAC, and LTV-by-tier tables are present; LTV uses one-time-purchase (repeat-factor) treatment, NOT ARPU/churn
    - A break-even month is stated and matches the CSV's Cumulative_Net >= 0 crossing; the ~112-paid-user formula is shown
    - The Plus/Premium per-run margin proof is shown ($3.33 vs $0.06 COGS; Premium margin-positive at realistic use — D-11)
    - F3 (Anthropic pricing) and F7 (relocation frequency) flags are visible; `grep -ciE "break-?even" pitch/financials/summary.md` >= 1
  </acceptance_criteria>
  <done>summary.md makes the CSV 60-sec re-derivable; startup/CAC/LTV tables sourced; break-even + margin proof present; F3/F7 flagged.</done>
</task>

</tasks>

<threat_model>
CSV + markdown documents — NO executable attack surface, no auth, no input handling, no deploy. "Threats" are pitch-defensibility risks (RESEARCH §Common Pitfalls), each mitigated by an acceptance criterion:

| Threat ID | Risk | Disposition | Mitigation |
|-----------|------|-------------|------------|
| T-09-03-A | Financial model caves in Q&A ("how did you get that?") | mitigate | Every CSV input maps to a stated, sourced assumption in summary.md; 60-sec re-derivable (D-08) |
| T-09-03-B | Per-run unlimited-COGS margin risk unaddressed (Premium) | mitigate | summary shows $0.06/run COGS and Premium margin-positive at extreme use (D-11) |
| T-09-03-C | Subscription/MRR/churn math slips into financials despite D-04 | mitigate | One-time-purchase LTV treatment only; no MRR/churn columns; acceptance criterion enforces |
| T-09-03-D | Unsourced CAC/conversion scores zero | mitigate | All inputs labeled [ASSUMED] modeled with benchmark anchors; F3/F7 flags carried |
</threat_model>

<verification>
- `awk -F, 'NR>1' pitch/financials/model.csv | wc -l` ~= 24
- `grep -F "0.06" pitch/financials/model.csv` present (per-run COGS)
- `grep -ciE "break-?even" pitch/financials/summary.md` >= 1
- `grep -niE "MRR|churn|/month subscription" pitch/financials/summary.md` returns nothing consumer-scoped (D-04)
- Manual: every CSV column re-derivable from summary.md assumptions in ~60s; F3/F7 flags present
</verification>

<success_criteria>
- pitch/financials/model.csv (24-month base case) + pitch/financials/summary.md authored
- Startup costs, per-channel CAC, LTV-by-tier, per-run COGS, break-even month all present, sourced, re-derivable
- PITCH-05 satisfied per ROADMAP Phase 9 SC4
</success_criteria>

<output>
Create `.planning/phases/09-pitch-business-substance/09-03-SUMMARY.md` when done.
</output>

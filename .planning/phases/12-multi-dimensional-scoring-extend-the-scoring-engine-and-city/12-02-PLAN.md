---
phase: 12-multi-dimensional-scoring-extend-the-scoring-engine-and-city
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/engine/scoring-weights.ts
  - shared/types.ts
autonomous: true
requirements: [MATCH-01, MATCH-03]
must_haves:
  truths:
    - "BASE_SCORE + Σ(global-weighted maxContribution caps) ≤ 94 after adding 5 new category caps (D-05 static budget)"
    - "WEIGHT_MAX_PREF, WEIGHT_MAX_PRAC, NEUTRAL_DEFAULT exported from scoring-weights.ts with values matching personality.ts on reconcile/v1"
    - "MatchResult.scoreFactors carries an optional dataLevel field (D-08 label channel)"
  artifacts:
    - path: "shared/engine/scoring-weights.ts"
      provides: "Recalibrated caps (Option A) + 5 new per-category caps + two-tier weight constants"
      contains: "WEIGHT_MAX_PREF"
    - path: "shared/types.ts"
      provides: "dataLevel? on MatchResult.scoreFactors"
      contains: "dataLevel"
  key_links:
    - from: "shared/engine/scoring-weights.ts"
      to: "personality.ts (reconcile/v1)"
      via: "WEIGHT_MAX_PREF/PRAC + NEUTRAL_DEFAULT value parity"
      pattern: "WEIGHT_MAX_PREF\\s*=\\s*1.8"
---

<objective>
Recalibrate the scoring budget and declare the new constants + the `dataLevel?` contract, WITHOUT touching `scoring.ts` (that is Plan 03, which depends on this). This plan owns `scoring-weights.ts` (config + recalibration) and the `MatchResult` half of the `dataLevel?` type change in `types.ts`.

Purpose: D-05 (the clamp BLOCKER) is satisfied by math, not by running the app — set the caps so `BASE_SCORE + Σ(global × maxContribution) ≤ 94`, absorbing rounding slop across 9 factors. The two-tier weight constants and the `dataLevel?` label channel are the contract Plan 03 implements against.

Output: recalibrated `SCORING_WEIGHTS`, 3 new exported weight constants, updated docstring, and `dataLevel?` on `MatchResult.scoreFactors`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-CONTEXT.md
@.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-RESEARCH.md
@.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-PATTERNS.md

<execution_prerequisite>
**Branch:** Execute on `reconcile/v1` (or descendant). Before editing, READ `shared/quiz-engine/personality.ts` on the target branch and confirm `WEIGHT_FLOOR`, `WEIGHT_MAX_PRAC`, `WEIGHT_MAX_PREF`, `NEUTRAL_DEFAULT` actual values (RESEARCH Assumption A1: verified 0.3 / 1.5 / 1.8 / 0.5 — but re-confirm; if they differ, the exported constants here MUST match personality.ts exactly, since `categoryPersonalWeight` in Plan 03 relies on parity).
</execution_prerequisite>

<interfaces>
Current SCORING_WEIGHTS (shared/engine/scoring-weights.ts L26-61):
  global: { cost: 1.0, career: 1.0, lifestyle: 1.0, safety: 0.8 }
  normalization: { costMaxContribution: 12, careerMaxContribution: 12, lifestyleMaxContribution: 10, safetyMaxContribution: 8 }
  BASE_SCORE = 50  (L84)
Current global-weighted cap sum = 12 + 12 + 10 + 0.8×8 = 40.4 → max raw 90.4.

Current MatchResult.scoreFactors (shared/types.ts L152):
  scoreFactors: { factor: string; contribution: number }[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalibrate caps (Option A) + add 5 new category caps + two-tier weight constants</name>
  <read_first>
    - shared/engine/scoring-weights.ts (the file being modified — `global` block L30-35, `normalization` block L55-60, `PERSONAL_WEIGHT_SCALE` L67, docstring L8-25 + L54, `BASE_SCORE` L84 are the analogs per 12-PATTERNS.md "scoring-weights.ts" section)
    - 12-RESEARCH.md §Pattern 3 Option A (the exact recalibration table)
    - shared/quiz-engine/personality.ts on reconcile/v1 (confirm WEIGHT_MAX_PREF=1.8, WEIGHT_MAX_PRAC=1.5, NEUTRAL_DEFAULT=0.5 — value parity is mandatory)
  </read_first>
  <action>
    Apply RESEARCH §Pattern 3 Option A (proportional renorm to a 44-pt budget):
    - In `global`, add 5 new slugs all at 1.0: `healthcare`, `schools`, `childcare`, `connectivity`, `parks`. Keep existing cost/career/lifestyle 1.0, safety 0.8. Keep `as const`.
    - In `normalization`, rescale the 4 existing caps × 0.7: `costMaxContribution: 8.4`, `careerMaxContribution: 8.4`, `lifestyleMaxContribution: 7.0`, `safetyMaxContribution: 5.6`. Add 5 new caps: `healthcareMaxContribution: 4`, `schoolsMaxContribution: 3`, `childcareMaxContribution: 3`, `connectivityMaxContribution: 3`, `parksMaxContribution: 3`.
    - Verify the budget statically: BASE_SCORE(50) + (1.0×8.4 + 1.0×8.4 + 1.0×7.0 + 0.8×5.6 + 1.0×4 + 1.0×3 + 1.0×3 + 1.0×3 + 1.0×3) = 50 + 44.28 = 94.28 theoretical max raw. With per-factor `Math.round`, the displayed integer max stays comfortably < 99 (Plan 04's rankCities gate proves the runtime bound; this plan proves the static bound).
    - Add 3 named exports mirroring `PERSONAL_WEIGHT_SCALE` (L67): `export const WEIGHT_MAX_PREF = 1.8;`, `export const WEIGHT_MAX_PRAC = 1.5;`, `export const NEUTRAL_DEFAULT = 0.5;` — values MUST match personality.ts on the target branch.
    - Update the docstring (L8-25 and the L54 sum comment): replace the old `50 + 12 + 12 + 10 + 0.8×8 = 90.4` figure with the new `50 + 8.4 + 8.4 + 7.0 + 0.8×5.6 + 4 + 3 + 3 + 3 + 3 = 94.28` budget; note the two-tier weight constants and the D-05 ≤94 target.
    Do NOT edit scoring.ts in this plan — `categoryPersonalWeight` and the factor functions are Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i scoring-weights || echo "scoring-weights compiles clean"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^#' shared/engine/scoring-weights.ts | grep -c 'MaxContribution'` returns 9 (4 rescaled + 5 new).
    - Source assertion: file exports `WEIGHT_MAX_PREF`, `WEIGHT_MAX_PRAC`, `NEUTRAL_DEFAULT` (grep each returns a hit) with values 1.8 / 1.5 / 0.5.
    - Static budget assertion (compute by hand / inline node): `50 + 1.0*8.4 + 1.0*8.4 + 1.0*7.0 + 0.8*5.6 + 1.0*4 + 1.0*3 + 1.0*3 + 1.0*3 + 1.0*3 === 94.28` and `94.28 < 94`? NO — note 94.28 is the real-valued max; the D-05 target is `< 99` on the DISPLAYED rounded score, which Plan 04 verifies. Acceptance here: the real-valued max equals 94.28 (≤ 95, leaving ≥4pt headroom to 99 for rounding slop).
    - `as const` retained on SCORING_WEIGHTS.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>SCORING_WEIGHTS recalibrated to a 44.28-pt budget (max raw 94.28), 5 new caps + 3 weight constants exported, docstring updated, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add dataLevel? to MatchResult.scoreFactors (types.ts — Location 2 of 2)</name>
  <read_first>
    - shared/types.ts L149-157 (MatchResult interface; scoreFactors L152 is the field being extended — analog per 12-PATTERNS.md "shared/types.ts — dataLevel? field in TWO locations")
    - 12-RESEARCH.md §Pattern 4 (the dataLevel literal union)
  </read_first>
  <action>
    Change `MatchResult.scoreFactors` (types.ts L152) from `{ factor: string; contribution: number }[]` to `{ factor: string; contribution: number; dataLevel?: 'city' | 'state' | 'proxy' | 'none' | 'display-only' }[]`. The field is optional → zero fixture ripple under strict. (Note: `'none'` marks a no-cited-data, no-proxy category that is EXCLUDED from scoring — contribution 0 — per the D-07 resolution documented in Plan 03; `'display-only'` is reserved for the D-04 seam.) This is Location 2 of the two-location type change; Location 1 (the `CityScore` interface inside scoring.ts) is done in Plan 03 alongside the `.push` calls so they land atomically. Adding the `MatchResult` side here (a separate file from scoring.ts) keeps this plan parallel with Plan 01.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'types.ts' || echo "types.ts compiles clean"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "dataLevel" shared/types.ts` returns ≥ 1.
    - Behavior assertion: the union literally contains `'city' | 'state' | 'proxy' | 'none' | 'display-only'`.
    - `npx tsc --noEmit` clean; existing MatchResult fixtures compile unchanged (optional field).
  </acceptance_criteria>
  <done>MatchResult.scoreFactors carries optional dataLevel; tsc clean; no fixture ripple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config constant → engine | Cap values and weight ceilings drive every score; a wrong constant silently misweights or reintroduces the clamp BLOCKER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | recalibrated caps | mitigate | static budget assertion (max raw = 94.28 ≤ 95); Plan 04 rankCities gate proves the displayed runtime bound < 99 |
| T-12-04 | Tampering | WEIGHT_MAX_PREF/PRAC parity with personality.ts | mitigate | read_first mandates re-confirming personality.ts values; parity is an acceptance criterion |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test` — existing suite green. Verified: existing tests use range/relative assertions (`toBeGreaterThanOrEqual` / distinct-score checks); NO hardcoded absolute matchScore fixtures exist, so recalibration needs no test edits and no test files are in this plan's `files_modified`. (If the suite surfaces a hardcoded value, that is out of scope here — flag it for the Plan 03/04 test tasks that own the test files; do not edit tests from this plan.)
- Static budget hand-check: max raw = 94.28.
</verification>

<success_criteria>
- 9 maxContribution caps present (4 rescaled, 5 new); global has 9 slugs.
- WEIGHT_MAX_PREF/WEIGHT_MAX_PRAC/NEUTRAL_DEFAULT exported, parity with personality.ts.
- MatchResult.scoreFactors carries dataLevel?.
- Docstring reflects the new 94.28 budget. tsc clean; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-02-SUMMARY.md` when done.
</output>

---
phase: 12-multi-dimensional-scoring-extend-the-scoring-engine-and-city
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/data/cities.ts
autonomous: true
requirements: [MATCH-01, MATCH-03]
must_haves:
  truths:
    - "All 22 cited cities carry their healthcare, FEMA, schools, childcare, demographics, and air-connectivity values from deep-category-data.md"
    - "State-level fields repeat the same state value across same-state cities (TX, NC, FL) — correct by design (D-08)"
    - "The 6 anchor cities (LA, SF, Boston, DC, Houston, Philadelphia) + London are NOT given invented data — their missing fields are simply omitted (D-07)"
    - "San Diego toddler childcare is omitted (NR in source) — D-07 neutral exclusion for that single cell"
  artifacts:
    - path: "shared/data/cities.ts"
      provides: "Populated Phase 11 optional City fields for the 22 cited cities"
      contains: "healthcareIndex"
  key_links:
    - from: "shared/data/cities.ts"
      to: "deep-category-data.md"
      via: "every populated value traces to a cited row"
      pattern: "healthcareIndex|schoolProficiencyPct|childcareInfantAnnual|airportEnplanements"
---

<objective>
Populate the optional Phase 11 City fields in `shared/data/cities.ts` for the 22 cited cities, sourced exactly from `.planning/research/deep-category-data.md`. This is pure data entry — no logic, no type changes (the optional fields already exist in `types.ts` L127-139).

Purpose: The scoring extension (Plan 03) and the clamp gate (Plan 04) consume these values. Without populated data, every new factor falls to neutral exclusion and the multi-dimensional score is inert.

Output: 22 city literals in `cities.ts` extended with cited values. The 6 anchor cities + London are left untouched (no cited data — D-07 neutral exclusion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-CONTEXT.md
@.planning/research/deep-category-data.md

<execution_prerequisite>
**Branch:** Phase 12 must execute on `reconcile/v1` (or a descendant) — the Phase 11 constants (`WEIGHT_FLOOR`, `WEIGHT_MAX_PREF`, `WEIGHT_MAX_PRAC`, `NEUTRAL_DEFAULT` in `personality.ts`/`keys.ts`) and the landed Phase 11 contract live there, NOT on `integrate/quiz-engine`. This data-population plan only touches `cities.ts` (whose optional fields exist on both branches), so it can technically run anywhere, but execute the whole phase on the correct branch to avoid divergence. Confirm `git branch --show-current` before starting.
</execution_prerequisite>

<dataset_note>
`cities.ts` now contains **28 cities** (the original 22 cited + 6 anchors added 2026-06-03: Los Angeles, San Francisco, Boston, Washington DC, Houston, Philadelphia + London UK). `deep-category-data.md` covers ONLY the original 22. The 6 anchors and London have NO cited new-category data. Do NOT invent or estimate values for them — omit the keys entirely (D-07 neutral exclusion handles them at scoring time). Only the 22 cited cities get populated.
</dataset_note>

<interfaces>
City optional fields already declared in `shared/types.ts` L127-139 (do NOT edit types.ts in this plan):
  healthcareIndex?: number
  disasterRiskScore?: number
  disasterRiskRating?: string
  schoolProficiencyPct?: number
  childcareInfantAnnual?: number
  childcareToddlerAnnual?: number
  foreignBornPct?: number
  medianAge?: number
  neverMarriedPct?: number
  parkScoreRank?: number
  parkScore?: number
  faaHubClass?: 'Large' | 'Medium' | 'Small' | 'Nonhub'
  airportEnplanements?: number
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate scored-category fields for the 22 cited cities</name>
  <read_first>
    - shared/data/cities.ts (the file being modified — Austin literal L27-52 is the shape analog per 12-PATTERNS.md "shared/data/cities.ts" section)
    - .planning/research/deep-category-data.md §1 (healthcare), §3 (schools), §4 (childcare), §7 (air connectivity), §6 (parks)
    - shared/types.ts L127-139 (field names + types — do NOT edit)
  </read_first>
  <action>
    For EACH of the 22 cited cities (Austin TX through Brooklyn/NYC NY — the original block, NOT the 6 anchors or London), append the scored-category fields to the city literal after `hasIntlAirport`, copying values verbatim from deep-category-data.md:
    - `healthcareIndex` from §1 Healthcare table (e.g., Austin 64.8, Pittsburgh 71.0, Indianapolis 71.9, Brooklyn/NYC 62.9).
    - `schoolProficiencyPct` from §3 NAEP table (state-level — TX cities all 25, CO 35, NY 31, etc.). Same-state cities repeat the same value by design (D-08).
    - `childcareInfantAnnual` from §4 CCAoA table (state-level — all 3 TX cities 11349, Denver 20978, etc.).
    - `childcareToddlerAnnual` from §4 table where present. **OMIT for San Diego** (source says "NR" — D-07 neutral exclusion for that single cell; infant value 22628 still present).
    - `parkScore` + `parkScoreRank` ONLY for the 7 cities with confirmed numeric ParkScore in §6 (Minneapolis 83.4 rank 3, Seattle 75.4 rank 8, Portland 75.1 rank 9, Chicago 74.3 rank 10). For Denver(#11)/Atlanta(#18)/Austin(#47) the §6 table shows rank but "—" for overall score → set `parkScoreRank` only, OMIT `parkScore` (no defensible numeric value — Open Question 2). The other 15 cities get NEITHER key (parks proxy fallback covers them at scoring time — D-07).
    - `faaHubClass` + `airportEnplanements` from §7 FAA table for all 22 (e.g., Atlanta 50950068 Large, Boise 2369164 Medium, Pittsburgh 4493052 Medium). Use the source enplanement integer exactly.
    Do NOT set any field to `undefined` explicitly — omit the key (12-PATTERNS.md cities.ts rule). Do NOT touch the 6 anchors / London.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i cities.ts || echo "cities.ts compiles clean"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'healthcareIndex' shared/data/cities.ts` returns 22 (exactly the cited cities; anchors/London excluded).
    - Source assertion: `grep -c 'airportEnplanements' shared/data/cities.ts` returns 22.
    - Source assertion: `grep -c 'parkScore:' shared/data/cities.ts` returns 4 (only Minneapolis/Seattle/Portland/Chicago carry a numeric `parkScore`).
    - Behavior assertion: San Diego literal has `childcareInfantAnnual: 22628` but NO `childcareToddlerAnnual` key (NR → omitted).
    - Behavior assertion: all three TX cities (Austin, San Antonio, Dallas) carry `schoolProficiencyPct: 25` and `childcareInfantAnnual: 11349` (state value repeats — D-08).
    - `npx tsc --noEmit` reports no error in cities.ts.
  </acceptance_criteria>
  <done>22 cited cities carry their scored-category fields, values traceable to deep-category-data.md; 6 anchors + London untouched; San Diego toddler omitted; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Populate display-only fields (demographics + FEMA) for the 22 cited cities</name>
  <read_first>
    - shared/data/cities.ts (the file being modified)
    - .planning/research/deep-category-data.md §2 (FEMA), §5 (demographics)
    - 12-CONTEXT.md D-03 (demographics display-only), D-09 (FEMA display-only)
  </read_first>
  <action>
    For EACH of the 22 cited cities, append the display-only fields from deep-category-data.md:
    - `disasterRiskScore` + `disasterRiskRating` from §2 FEMA table (e.g., Chicago 99.97 "Very High", Boise 87.75 "Relatively Moderate", Austin 97.74 "Relatively High").
    - `foreignBornPct`, `medianAge`, `neverMarriedPct` from §5 demographics table (e.g., Miami 56.9 / 38.8 / 41.7; Pittsburgh 8.9 / 34.5 / 52.9; Brooklyn/NYC 35.7 / 36.9 / 46.1).
    These fields are NEVER scored (D-03/D-09) — they exist purely for the collaborator's UI to surface as labeled context. This task only populates them; the guard that they never enter `scoreFactors` is enforced by tests in Plan 03. Do NOT touch the 6 anchors / London.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i cities.ts || echo "cities.ts compiles clean"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'disasterRiskScore' shared/data/cities.ts` returns 22.
    - Source assertion: `grep -c 'foreignBornPct' shared/data/cities.ts` returns 22.
    - Behavior assertion: Miami literal carries `foreignBornPct: 56.9` and `disasterRiskRating: "Very High"`.
    - Behavior assertion: Brooklyn/NYC literal carries `disasterRiskScore: 99.27` (Kings County) and `medianAge: 36.9`.
    - `npx tsc --noEmit` reports no error in cities.ts.
  </acceptance_criteria>
  <done>22 cited cities carry FEMA + demographics display-only fields; anchors/London untouched; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source data → cities.ts literal | Hand-transcribed cited values cross into the typed dataset; transcription error is the only risk surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | hand-transcribed city values | mitigate | grep-count acceptance criteria (22 healthcare, 22 enplanements, 4 parkScore) catch missing/extra entries; tsc enforces field types |
| T-12-02 | Information disclosure | demographics fields (foreignBornPct) | accept | D-03 confines them to display-only; no PII, public ACS aggregates |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this plan (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (optional fields populated correctly under strict).
- `npm test` — existing 123 tests still green (data-only change must not regress the engine; cities without new fields fall to neutral exclusion which existing tests already tolerate).
- grep counts match the acceptance-criteria figures.
</verification>

<success_criteria>
- 22 cited cities carry all applicable scored + display-only fields, values verbatim from deep-category-data.md.
- San Diego toddler omitted (NR); only 4 cities carry numeric parkScore; 3 more carry parkScoreRank only.
- 6 anchors + London carry NO new-category data.
- tsc clean; full suite green.
</success_criteria>

<output>
Create `.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-01-SUMMARY.md` when done.
</output>

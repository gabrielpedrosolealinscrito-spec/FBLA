---
phase: 12-multi-dimensional-scoring-extend-the-scoring-engine-and-city
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - shared/engine/index.test.ts
autonomous: true
requirements: [MATCH-01, MATCH-03]
must_haves:
  truths:
    - "D-05 (BLOCKER): displayed matchScore < 99 for the strongest possible profile (max weights AND max categoryWeights) across ALL cities in CITIES_DATA — asserted on rankCities() output, NOT pre-clamp computeRawScore"
    - "The static budget bound is also asserted: BASE_SCORE + Σ(global × maxContribution) < 95 (theoretical max), proving the rankCities pass is not coincidental"
    - "MATCH-01: rankCities returns one result per city with the new multi-dimensional scores"
  artifacts:
    - path: "shared/engine/index.test.ts"
      provides: "clamp BLOCKER gate + static budget assertion via rankCities()"
      contains: "matchScore"
  key_links:
    - from: "shared/engine/index.test.ts"
      to: "rankCities + CITIES_DATA"
      via: "maxProfile exercising both weights and categoryWeights at max"
      pattern: "rankCities\\(maxProfile\\)"
---

<objective>
Add the authoritative clamp BLOCKER gate (D-05) to `shared/engine/index.test.ts`: a test that runs `rankCities(maxProfile)` — where `maxProfile` maxes BOTH `weights` AND `categoryWeights` — across ALL cities and asserts every DISPLAYED `matchScore < 99`. Pair it with a static budget assertion so the runtime pass is provably not coincidental.

Purpose: This is THE gate for the Phase 3 clamp BLOCKER carried into Phase 12. It MUST assert the user-facing post-clamp score (the memory `test-assert-user-facing-output`: a pre-clamp assertion gave false comfort while the displayed score was broken). It lives in `index.test.ts` because `clamp()` runs in `buildRawResult` inside `index.ts`, not in `scoring.ts`. It runs last (Wave 3) because it only passes once data (Plan 01) + recalibration (Plan 02) + the 5 factors (Plan 03) all exist.

Output: the clamp gate test + a static budget assertion in `index.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-CONTEXT.md
@.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-RESEARCH.md
@shared/engine/index.ts

<execution_prerequisite>
**Branch:** Execute on `reconcile/v1` (or descendant), AFTER Plans 01-03 are merged. The gate depends on populated data, recalibrated caps, and the 5 factor contributions all being present — running it earlier produces a false red.
</execution_prerequisite>

<dataset_note>
`CITIES_DATA` has **28 cities** (22 cited + 6 anchors + London). The clamp gate MUST iterate the actual `CITIES_DATA` (use `results.length` / `CITIES_DATA.length`), NOT a hardcoded 22. The binding city for D-05 is a fully-cited city (max factorScores); the 7 missing-data cities score on midpoints (lower), so they are not the constraint — but the test asserts across all of them regardless.
</dataset_note>

<interfaces>
From index.ts: `rankCities(profile: Profile): { results: MatchResult[]; reconfirmSignal? }` — clamp at L99 `clamp(Math.round(rawScore), 0, 99)`.
Existing analog (index.test.ts L53-60): the `clamps all matchScores to the 0–99 range` test using `swEngineerProfile`.
maxProfile fixture (12-RESEARCH.md §Code Examples — transcribe): spread an existing valid profile, then set
  weights: { cost: 4, career: 4, lifestyle: 4, safety: 4 }
  categoryWeights: { healthcare: 1.8, schools: 1.8, childcare: 1.8, connectivity: 1.8, parks: 1.8 }
  lifestyleTags: ['outdoors','nightlife','arts','walkable','diversity','family','startup']
Static budget (from scoring-weights.ts Plan 02): BASE_SCORE(50) + 1.0*8.4 + 1.0*8.4 + 1.0*7.0 + 0.8*5.6 + 1.0*4 + 1.0*3 + 1.0*3 + 1.0*3 + 1.0*3 = 94.28.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the clamp BLOCKER gate + static budget assertion to index.test.ts</name>
  <read_first>
    - shared/engine/index.test.ts (the file being modified — swEngineerProfile fixture L24+, clamp+range block L53-60 analog)
    - shared/engine/index.ts (rankCities + clamp at L99)
    - shared/engine/scoring-weights.ts (Plan 02 — the cap values for the static budget assertion)
    - 12-RESEARCH.md §Code Examples "Strongest-profile clamp test" + §Validation Architecture "Critical Test: Clamp BLOCKER"
    - 12-VALIDATION.md (D-05 row — must live in index.test.ts, asserted on rankCities output)
  </read_first>
  <action>
    Add two tests to index.test.ts (do NOT modify the existing L53-60 clamp-range test — keep it as a guard):
    1. **Clamp BLOCKER gate (D-05):** define `maxProfile` by spreading an existing valid profile and overriding `weights` (all 4) and `categoryWeights` (all 5 at 1.8 = WEIGHT_MAX_PREF) and maximal `lifestyleTags` per the interfaces block. Call `rankCities(maxProfile)`. Assert `results.length === CITIES_DATA.length` (import CITIES_DATA), and `results.forEach(r => { expect(r.matchScore).toBeLessThan(99); expect(r.matchScore).toBeGreaterThanOrEqual(0); })`. Use `toBeLessThan(99)` (strict, per D-05), asserting on the DISPLAYED `matchScore` from rankCities — NOT computeRawScore.
    2. **Static budget assertion:** import `SCORING_WEIGHTS`, `BASE_SCORE` from scoring-weights.js; compute `BASE_SCORE + Σ(global[slug] * normalization[slug+'MaxContribution'])` over all 9 slugs and assert it is `< 95` (the theoretical real-valued max ≈ 94.28). This proves the rankCities pass reflects a genuine budget bound, not a coincidence of which cities have data. (No real city maxes all 9 factorScores, so this static check is the complement to the runtime gate.)
  </action>
  <verify>
    <automated>npm test -- shared/engine/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test command green: the clamp gate asserts `matchScore < 99` for every city in CITIES_DATA (28) via `rankCities(maxProfile)`.
    - Source assertion: the test calls `rankCities(maxProfile)` and references `CITIES_DATA.length` — NOT a hardcoded 22, NOT `computeRawScore`.
    - Behavior assertion: the static budget test asserts `BASE_SCORE + Σ(global × maxContribution) < 95`.
    - The pre-existing L53-60 clamp-range test still present and green.
  </acceptance_criteria>
  <done>index.test.ts carries the D-05 clamp gate (displayed matchScore < 99 across all cities via rankCities) + the static budget assertion; both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → UI badge | the displayed matchScore is what the user/judge sees; a desync between the clamped badge and the contribution bars is the BLOCKER this gate prevents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | clamp firing on strong profiles (badge/bar desync) | mitigate | rankCities-based gate asserts displayed matchScore < 99 across all cities; static budget assertion proves the bound is structural, not coincidental |
| T-12-10 | Repudiation | pre-clamp assertion giving false comfort | mitigate | gate explicitly asserts on rankCities() output, never computeRawScore — directly closes the `test-assert-user-facing-output` failure mode |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npm test -- shared/engine/index.test.ts` green (clamp gate + static budget + existing tests).
- `npm test` — full suite green (the phase gate before /gsd:verify-work).
</verification>

<success_criteria>
- D-05 clamp gate asserts displayed matchScore < 99 for the strongest profile across ALL CITIES_DATA via rankCities().
- Static budget assertion < 95 proves the structural bound.
- Pre-existing clamp-range test retained. Full suite green.
</success_criteria>

<output>
Create `.planning/phases/12-multi-dimensional-scoring-extend-the-scoring-engine-and-city/12-04-SUMMARY.md` when done.
</output>

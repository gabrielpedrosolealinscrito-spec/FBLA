---
phase: 11-deep-profile-expand-the-quiz-with-sourced-life-area-categori
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/types.ts
  - shared/quiz-engine/personality.test.ts
  - shared/quiz-engine/category-modules.test.ts
  - shared/quiz-engine/keys.ts
autonomous: true
requirements: [MATCH-05, QUIZ-06, QUIZ-07, QUIZ-08, QUIZ-09]
must_haves:
  truths:
    - "The extended Profile (with categoryWeights + weightExplanations + module fields) and extended City compile under tsconfig strict:true with zero changes to existing fixtures"
    - "The Phase 3 engine suite (shared/engine/) stays fully green after the Profile/City extension — the additive-contract guard"
    - "WeightExplanation interface exists in shared/types.ts before any synthesizer references it"
    - "Phase 11 RED test scaffolds exist, are file-scoped, and import NO Phase-2 module that is absent on disk (no import of tension.js)"
    - "Shared answer-key string constants (tradeoff IDs, module-selection flags, AND module-importance keys) exist in one place so every scored category has a weight-input path and plans 02/03 bind to identical strings"
  artifacts:
    - path: "shared/types.ts"
      provides: "Additive Profile + City extension and the WeightExplanation interface"
      contains: "export interface WeightExplanation"
    - path: "shared/quiz-engine/keys.ts"
      provides: "Single source of truth for cross-plan answer-key strings + scored-category slug list"
      contains: "ALL_SCORED_CATEGORIES"
    - path: "shared/quiz-engine/personality.test.ts"
      provides: "RED scaffold for detectPersonalityTension + synthesizeCategoryWeights + neutral-skip (QUIZ-06, QUIZ-09)"
    - path: "shared/quiz-engine/category-modules.test.ts"
      provides: "RED scaffold for module showIf + module-presence (QUIZ-07, QUIZ-08)"
  key_links:
    - from: "shared/quiz-engine/personality.test.ts"
      to: "shared/quiz-engine/keys.ts"
      via: "import of ALL_SCORED_CATEGORIES + answer-key constants"
      pattern: "ALL_SCORED_CATEGORIES"
    - from: "shared/types.ts WeightExplanation"
      to: "shared/quiz-engine/personality.ts (plan 02)"
      via: "synthesizeCategoryWeights return type"
      pattern: "WeightExplanation"
---

<objective>
Lay the additive contract foundation for Phase 11 and create the RED test scaffolds the
implementation plans will turn green. This plan owns the `shared/types.ts` extension (the
Phase 12 scoring seam, MATCH-05), a new `keys.ts` constants module that pins the exact
answer-key strings and scored-category slugs so plans 02 and 03 build against identical
identifiers, and the two file-scoped RED test files.

Purpose: Unblock parallel implementation (plans 02 + 03) with a stable, compiling contract.
The additive-only extension keeps in-flight Phase 2 and landed Phase 3 working untouched.
Output: Extended Profile + City + WeightExplanation in types.ts; keys.ts; two RED test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-CONTEXT.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-PATTERNS.md

<interfaces>
<!-- Current contract the executor extends. Read live before editing. -->

shared/types.ts — current Profile interface ends at line 60 (closing `}` after tradeoffTolerance).
City interface ends at line 90 (closing `}` after `climate`). All Phase 11 additions go BEFORE
those closing braces, ALL OPTIONAL (tsconfig strict:true; scoring.test.ts declares a full typed
`const testProfile: Profile = {...}` literal that must compile unchanged).

shared/quiz-engine/questions.ts — already on disk. QuestionDef shape (LOAD-BEARING — note `kicker` is REQUIRED):
```
export interface QuestionDef {
  id: string; type: QuestionType; kicker: string; prompt: string;
  subtext?: string; required?: boolean; autoAdvance?: boolean;
  options?: QuestionOption[]; min?: number; max?: number; step?: number;
  minLabel?: string; maxLabel?: string; maxSelect?: number;
  groupHeader?: GroupHeader; showIf?: (answers: Record<string, unknown>) => boolean;
}
export type Answers = Record<string, unknown>;
```

shared/quiz-engine/resolver.ts — already on disk. `getVisibleQuestions(answers, all)` and
`clearHiddenAnswers(answers, all)` exist and work TODAY (Phase 11 tests MAY call them).

shared/quiz-engine/tension.ts — IMPL ABSENT (only tension.test.ts exists). Phase 11 must NOT
import `./tension.js` from any file — that import would fail-collect. detectPersonalityTension
is a self-contained sibling with its own result type.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared/types.ts additively (WeightExplanation + Profile + City fields)</name>
  <read_first>
    - shared/types.ts (current Profile lines 17-60, City lines 63-90, extension-announcement comment lines 8-11)
    - shared/engine/scoring.test.ts (the `const testProfile: Profile = {...}` fixture at lines 16-38 — must compile unchanged)
    - .planning/phases/11-.../11-PATTERNS.md ("shared/types.ts (MODIFIED — additive contract extension)" section, exact field list)
  </read_first>
  <action>
    Add a `WeightExplanation` interface BEFORE the `Profile` interface with fields: `category: string`,
    `inferredWeight: number` (raw value in WEIGHT_FLOOR..WEIGHT_MAX range, NOT pre-normalized),
    `floor?: number` (present for practical tier only), `explanation: string`. Add a one-line comment
    that it mirrors Phase 3's scoreFactors {factor, contribution}[] explainability pattern (D-08).

    Add to `Profile` (before its closing brace, all OPTIONAL): `categoryWeights?: Record<string, number>`,
    `weightExplanations?: WeightExplanation[]`, `hasChronicCondition?: boolean`, `needsSpecialistAccess?: boolean`,
    `hasDependentsInSchool?: boolean`, `kidsAges?: string[]`, `disasterRiskConcern?: string`,
    `primaryHazardConcern?: string`, `internationalTraveler?: boolean`, `outdoorsFrequency?: string`,
    `demographicsMatters?: boolean`, `personalityTradeoffs?: { scenario: string; choice: string }[]`,
    `personalityFlavor?: Record<string, string>`. Add an announcement comment block (mirror lines 8-11)
    stating: Phase 11 additive extension; categoryWeights/weightExplanations are the MATCH-05 seam; do NOT
    remove or rename `weights?` (scoring.ts line ~38 reads profile.weights.cost; Phase 2 synthesizer emits it);
    D-02 (replace vs layer) stays OPEN until Phase 2 integration.

    Add to `City` (before its closing brace, all OPTIONAL — Phase 12 populates data): `healthcareIndex?: number`,
    `disasterRiskScore?: number`, `disasterRiskRating?: string`, `schoolProficiencyPct?: number`,
    `childcareInfantAnnual?: number`, `childcareToddlerAnnual?: number`, `foreignBornPct?: number`
    (comment: D-14 neutral factual stat, NOT a fit/diversity score), `medianAge?: number`,
    `neverMarriedPct?: number`, `parkScoreRank?: number`, `parkScore?: number`,
    `faaHubClass?: 'Large' | 'Medium' | 'Small' | 'Nonhub'`, `airportEnplanements?: number`.
    Do NOT add any required field. Do NOT touch `weights?`, `importanceRank`, or any existing field.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run shared/engine/ --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface WeightExplanation" shared/types.ts` returns exactly one match positioned BEFORE `export interface Profile`
    - `grep -cE "categoryWeights\?:|weightExplanations\?:|primaryHazardConcern\?:" shared/types.ts` returns 3 (all optional)
    - `grep -cE "healthcareIndex\?:|disasterRiskScore\?:|faaHubClass\?:" shared/types.ts` returns 3
    - `grep -c "weights?: { cost: number; career: number; lifestyle: number; safety: number }" shared/types.ts` still returns 1 (untouched)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (additive contract compiles under strict)
    - `npx vitest run shared/engine/` reports all suites passing (no NaN regression; additive-contract guard)
  </acceptance_criteria>
  <done>WeightExplanation + all new optional Profile/City fields present; tsc clean; Phase 3 engine suite green; `weights?` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared/quiz-engine/keys.ts — cross-plan answer-key + category-slug constants</name>
  <read_first>
    - shared/quiz-engine/questions.ts (existing answer-key naming convention — e.g. 'tradeoff_*', 'hasPartner'; QuestionDef/Answers types)
    - .planning/phases/11-.../11-RESEARCH.md (Pattern 1 tradeoff IDs, "Tier membership" exported-constant section, Data-Gated Module Set table for buildable categories)
    - .planning/phases/11-.../11-PATTERNS.md (SP-1 .js import extension, SP-3 const-export pattern)
  </read_first>
  <action>
    Create `shared/quiz-engine/keys.ts` as the single source of truth for the strings that plans 02 and 03
    must agree on (prevents the scavenger-hunt anti-pattern). Use the boxed header comment (SP-2) and `.js`
    import extensions if importing types (SP-1).

    Export const `ALL_SCORED_CATEGORIES: string[]` listing exactly the buildable Phase 11 categories per the
    data-gated set (D-14): `"healthcare"`, `"climateRisk"`, `"schools"`, `"childcare"`, `"demographics"`,
    `"parks"`, `"connectivity"`. (Tier-3 political/social are OMITTED per D-15 — do not include.)

    Export the five core tradeoff answer-key IDs as named consts (e.g. `TRADEOFF_COST_VS_LIFESTYLE = 'tradeoff_cost_vs_lifestyle'`,
    `TRADEOFF_SAFETY_VS_CAREER = 'tradeoff_safety_vs_career'`, `TRADEOFF_HEALTHCARE = 'tradeoff_healthcare'`,
    `TRADEOFF_FAMILY = 'tradeoff_family_vs_mobility'`, `TRADEOFF_CONNECTIVITY = 'tradeoff_connectivity_vs_cost'`)
    and export the module-selection flag-key prefix `MODULE_SELECTED_PREFIX = 'moduleSelected_'`
    (so `moduleSelected_healthcare` etc. are derivable). ALSO export a `MODULE_IMPORTANCE_PREFIX = 'importance_'` prefix:
    every scored category gets a weight-input via `importance_{category}` (e.g. `importance_climateRisk`, `importance_parks`,
    `importance_demographics`). This is LOAD-BEARING: categories without a personality tradeoff (climateRisk, parks, demographics)
    have NO other weight signal, so their module importance question IS the weight input. synthesizeCategoryWeights (plan 02) reads
    both the tradeoff keys AND these importance keys. Without this prefix those categories would be permanently stuck at NEUTRAL_DEFAULT.
    Also export `FAMILY_CATEGORIES = ['schools','childcare']` so plan 02 can map the single `tradeoff_family_vs_mobility` answer onto
    both family categories explicitly (no invented mapping).

    Add a comment noting these strings are the contract surface shared by personality.ts (plan 02) and
    category-modules.ts (plan 03); changing a string here is a contract change.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const ALL_SCORED_CATEGORIES" shared/quiz-engine/keys.ts` returns 1
    - `grep -oE '"(healthcare|climateRisk|schools|childcare|demographics|parks|connectivity)"' shared/quiz-engine/keys.ts | sort -u | wc -l` returns 7
    - `grep -cE "(political|social|dating|valuesFit)" shared/quiz-engine/keys.ts` returns 0 (Tier-3 omitted, D-15)
    - `grep -c "MODULE_SELECTED_PREFIX" shared/quiz-engine/keys.ts` returns at least 1
    - `grep -cE "MODULE_IMPORTANCE_PREFIX|FAMILY_CATEGORIES" shared/quiz-engine/keys.ts` returns 2 (every scored category has a weight-input path)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>keys.ts exists exporting ALL_SCORED_CATEGORIES (7 buildable slugs, no Tier-3), the 5 tradeoff key consts, MODULE_SELECTED_PREFIX, MODULE_IMPORTANCE_PREFIX, and FAMILY_CATEGORIES; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Create the two file-scoped RED test scaffolds (no tension.js import)</name>
  <read_first>
    - shared/quiz-engine/tension.test.ts (staggered-green header convention, signal-or-null assertions, empty-answers guard test — lines 1-67)
    - shared/engine/scoring.test.ts (fixture-object test pattern)
    - .planning/phases/11-.../11-PATTERNS.md (the "personality.test.ts" and "category-modules.test.ts" sections — exact assertion shapes incl. the neutral-skip loop and the categoryWeights/weightExplanations-ship-together test)
    - shared/quiz-engine/keys.ts (created in Task 2 — import ALL_SCORED_CATEGORIES + tradeoff key consts)
  </read_first>
  <action>
    Create `shared/quiz-engine/personality.test.ts` and `shared/quiz-engine/category-modules.test.ts`.
    Both get a staggered-green header (mirror tension.test.ts lines 1-13) stating: this file top-level imports
    `./personality.js` / `./category-modules.js` which are created in Wave 2 (plans 02/03); it IMPORT-ERRORS until
    then; Wave 1 gate commands EXCLUDE these files; they go green in their own plan's gate. CRITICAL: neither file
    may import `./tension.js` (absent on disk — would fail-collect). Import shared constants from `./keys.js`.

    personality.test.ts (covers QUIZ-06, QUIZ-09): import `detectPersonalityTension`, `synthesizeCategoryWeights`
    from `./personality.js`; `ALL_SCORED_CATEGORIES` + tradeoff key consts from `./keys.js`. Write tests:
    (a) detectPersonalityTension returns non-null when ≥2 "balanced" answers present; (b) returned result has
    `afterId` (string) and `question` (object with string `id`); (c) returns null when no ambiguity; (d) returns
    null for `{}` empty answers (no crash); (e) synthesizeCategoryWeights emits BOTH `categoryWeights` and
    `weightExplanations` (array) together; (f) NEUTRAL-SKIP: for `synthesizeCategoryWeights({})`, loop over
    ALL_SCORED_CATEGORIES asserting each `categoryWeights[cat]` is a defined finite number (never undefined/NaN);
    (g) a healthcare-favoring answer set raises the healthcare weight above NEUTRAL_DEFAULT and adds a matching
    weightExplanations entry; (h) FORCING TEST for a tradeoff-less category — a high `importance_climateRisk` (or `importance_parks`)
    answer raises that category's weight ABOVE NEUTRAL_DEFAULT and emits a matching weightExplanations entry. This proves every scored
    category has a real weight-input path, not just healthcare/connectivity (which have tradeoffs). The assertion is strictly ">" NEUTRAL_DEFAULT and must NOT be weakened
    to "≥" or dropped — if it goes RED, the synthesizer increment/step (plan 02) is wrong, not the test (guards the MEMORY.md false-comfort failure).

    category-modules.test.ts (covers QUIZ-07, QUIZ-08): import `CATEGORY_MODULE_QUESTIONS` from `./category-modules.js`;
    tradeoff key consts from `./keys.js`. Write tests that exercise the module showIf predicates DIRECTLY against the
    exported array (do not call getVisibleQuestions): (a) the healthcare module question's `showIf` returns true when
    `tradeoff_healthcare === 'healthcare_critical'`; (b) returns false when `tradeoff_healthcare === 'healthcare_low'`;
    (c) returns true when the corresponding `moduleSelected_*` flag is true; (d) every entry in CATEGORY_MODULE_QUESTIONS
    has a non-empty `kicker` string (strict-mode contract guard); (e) every entry has `required: false` (skip → neutral default).
  </action>
  <verify>
    <automated>test -f shared/quiz-engine/personality.test.ts && test -f shared/quiz-engine/category-modules.test.ts && ! grep -q "tension.js" shared/quiz-engine/personality.test.ts shared/quiz-engine/category-modules.test.ts && echo SCAFFOLDS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist with the staggered-green header (grep `STAGGERED-GREEN` in each returns ≥1)
    - `grep -c "tension.js" shared/quiz-engine/personality.test.ts shared/quiz-engine/category-modules.test.ts` returns 0 in both (decoupled from absent Phase 2 impl)
    - personality.test.ts references `ALL_SCORED_CATEGORIES`, `detectPersonalityTension`, `synthesizeCategoryWeights`, and asserts `isNaN(...) === false` (or `Number.isFinite`) in the neutral-skip loop
    - personality.test.ts includes the forcing test: an `importance_climateRisk` (or `importance_parks`) answer raises that tradeoff-less category above NEUTRAL_DEFAULT (grep finds `importance_` in the test)
    - category-modules.test.ts references `CATEGORY_MODULE_QUESTIONS` and tests a `.showIf!({...})` predicate directly
    - `npx vitest run shared/engine/ --reporter=dot` still green (Wave 1 gate EXCLUDES the new RED files; they import-error by design until Wave 2)
  </acceptance_criteria>
  <done>Both RED scaffolds exist, import from keys.js (not tension.js), encode the QUIZ-06/07/08/09 assertions, and are correctly excluded from the Wave 1 green gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quiz answers → contract types | Answer values are untrusted strings (`Answers = Record<string, unknown>`); the type contract must tolerate absent/unknown keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | New optional Profile/City fields | mitigate | All new fields OPTIONAL; no consumer in this plan dereferences them required-style; Phase 12 reads with `?? default` |
| T-11-02 | Denial of Service | Existing engine suite regression from contract change | mitigate | Additive-only extension; Task 1 verify runs full `shared/engine/` suite as the guard; `weights?` untouched |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH "No New Packages"); no install task exists |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0 (extended contract compiles under strict).
- `npx vitest run shared/engine/ --reporter=verbose` all green (additive-contract guard).
- The two new RED test files exist, are file-scoped, and contain no `tension.js` import.
- Wave 1 gate command (file-scoped, EXCLUDES the RED files):
  `npx vitest run shared/engine/`
- NEVER run `npx vitest run shared/quiz-engine/` in this wave — tension.test.ts import-errors until Phase 2 Plan 04 lands.
</verification>

<success_criteria>
- types.ts carries WeightExplanation + all additive Profile/City optional fields; `weights?` intact.
- keys.ts pins ALL_SCORED_CATEGORIES (7 buildable, no Tier-3) + tradeoff + module-flag strings.
- Two RED scaffolds encode QUIZ-06/07/08/09 + MATCH-05 type-check, decoupled from absent tension.js.
- Phase 3 engine suite remains green.
</success_criteria>

<output>
Create `.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-01-SUMMARY.md` when done.
</output>

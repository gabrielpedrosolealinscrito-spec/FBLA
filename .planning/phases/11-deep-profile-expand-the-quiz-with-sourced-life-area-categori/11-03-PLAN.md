---
phase: 11-deep-profile-expand-the-quiz-with-sourced-life-area-categori
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - shared/quiz-engine/category-modules.ts
autonomous: true
requirements: [QUIZ-07, QUIZ-08, QUIZ-09]
must_haves:
  truths:
    - "Each buildable life-area module has QuestionDef(s) gated by a showIf that returns true when the personality result recommends it OR the user opted in (D-11 guided-modular)"
    - "Module depth is mixed by category: rich sub-questions for healthcare and family/schools; light (importance + one qualifier) for parks and connectivity (D-12)"
    - "EVERY scored category module carries an importance question keyed `importance_{category}` — the weight signal plan 02 reads; for climateRisk/parks/demographics it is the only signal"
    - "Only data-gated categories are built; Tier-3 (political/values, social/dating) appear nowhere (D-14/D-15)"
    - "Every module question is required: false so a skipped module never breaks synthesis (D-13)"
    - "Every module question carries a non-empty kicker (strict-mode QuestionDef contract)"
    - "Module showIf predicates read ONLY answer keys owned by earlier (personality) questions — trigger-before-injection invariant"
  artifacts:
    - path: "shared/quiz-engine/category-modules.ts"
      provides: "CATEGORY_MODULE_QUESTIONS QuestionDef[] for the data-gated life-area categories"
      exports: ["CATEGORY_MODULE_QUESTIONS"]
      min_lines: 80
  key_links:
    - from: "shared/quiz-engine/category-modules.ts"
      to: "shared/quiz-engine/keys.ts"
      via: "import TRADEOFF_* + MODULE_SELECTED_PREFIX for showIf predicates"
      pattern: "TRADEOFF_HEALTHCARE|MODULE_SELECTED_PREFIX"
---

<objective>
Build the deep-dive category modules: the QuestionDef arrays that capture the data-backed
life-area preferences (healthcare, climate/disaster risk, schools/childcare, demographics,
parks/outdoors, connectivity), each surfaced by a guided-modular showIf and depth-mixed per D-12.

Purpose: Capture the net-new scored-category preferences that Phase 12 will score against city data.
Output: `shared/quiz-engine/category-modules.ts` — turns plan 01's RED category-modules.test.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-CONTEXT.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-RESEARCH.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-PATTERNS.md
@.planning/research/deep-category-data.md

<interfaces>
<!-- Contracts from plan 01. Use directly. -->

shared/quiz-engine/keys.ts (plan 01): TRADEOFF_HEALTHCARE, TRADEOFF_FAMILY, TRADEOFF_CONNECTIVITY, ...,
MODULE_SELECTED_PREFIX = 'moduleSelected_', MODULE_IMPORTANCE_PREFIX = 'importance_', FAMILY_CATEGORIES, ALL_SCORED_CATEGORIES.
EVERY scored category MUST get an importance question keyed `${MODULE_IMPORTANCE_PREFIX}${category}` — this is the weight signal that
plan 02 reads. For tradeoff-less categories (climateRisk, parks, demographics) it is the ONLY weight signal.
shared/quiz-engine/questions.ts: QuestionDef { id; type; kicker(REQUIRED); prompt; options?; showIf?; required?; ... }.

Module showIf analog (resolver.test.ts pattern + RESEARCH Code Examples):
```
{
  id: "healthcare_chronic_condition",
  type: "single_select",
  kicker: "HEALTHCARE",
  showIf: (a) => a[TRADEOFF_HEALTHCARE] === "healthcare_critical" || a[`${MODULE_SELECTED_PREFIX}healthcare`] === true,
  required: false,
}
```
LOAD-BEARING: showIf must read keys owned by personality.ts questions (earlier in ALL_QUESTIONS).

Data-gated buildable set (deep-category-data.md): healthcare (rich), climateRisk (light, capture primaryHazardConcern),
schools+childcare (rich, "state average" caveat), demographics (light, factual opt-in only), parks (light, falls back to
nearMountains/nearCoast for 15/22 cities), connectivity (light). Tier-3 OMITTED.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rich modules — healthcare + family/schools/childcare</name>
  <read_first>
    - shared/quiz-engine/questions.ts (QuestionDef shape; REQUIRED kicker; single_select/multi_select option shape)
    - shared/quiz-engine/keys.ts (TRADEOFF_HEALTHCARE, TRADEOFF_FAMILY, MODULE_SELECTED_PREFIX)
    - shared/quiz-engine/category-modules.test.ts (plan 01 RED — the exact showIf assertions)
    - .planning/research/deep-category-data.md (healthcare, school, childcare sections — what data exists to score against)
    - .planning/phases/11-.../11-RESEARCH.md (Pattern showIf, D-12 module depth, captured-field list e.g. hasChronicCondition, needsSpecialistAccess, hasDependentsInSchool, kidsAges)
  </read_first>
  <behavior>
    - healthcare_chronic_condition.showIf({tradeoff_healthcare:'healthcare_critical'}) === true; ({tradeoff_healthcare:'healthcare_low'}) === false; ({'moduleSelected_healthcare':true}) === true.
    - Healthcare and family modules each contribute ≥2 sub-questions (rich, D-12) writing the captured Profile fields (hasChronicCondition, needsSpecialistAccess, hasDependentsInSchool, kidsAges).
    - Healthcare carries an `importance_healthcare` question; the family module carries an `importance_schools` AND an `importance_childcare` question (FAMILY_CATEGORIES) — the weight signals plan 02 reads.
    - Every question required:false, kicker non-empty.
  </behavior>
  <action>
    Create `shared/quiz-engine/category-modules.ts` with the SP-2 boxed header (cite D-11/D-12/D-14) and SP-1 `.js` imports.
    Import QuestionDef/QuestionOption from `./questions.js` and the TRADEOFF_* consts + MODULE_SELECTED_PREFIX from `./keys.js`.
    Start the exported `CATEGORY_MODULE_QUESTIONS: QuestionDef[]` array (Task 2 appends the light modules).

    Add RICH healthcare module questions (D-12): at least `healthcare_chronic_condition` (single_select → maps to
    hasChronicCondition/needsSpecialistAccess) and one specialist-access qualifier. Each `kicker: 'HEALTHCARE'`, `required: false`,
    `showIf: (a) => a[TRADEOFF_HEALTHCARE] === 'healthcare_critical' || a[`${MODULE_SELECTED_PREFIX}healthcare`] === true`.

    Add an `importance_healthcare` single_select (importance scale, e.g. very/somewhat/not) so healthcare always has a weight signal even
    when the tradeoff was skipped, kicker 'HEALTHCARE', required:false, same showIf as the healthcare module.

    Add RICH family/schools/childcare module questions (D-12): a dependents-in-school question (→ hasDependentsInSchool),
    a kids-ages capture (multi_select or single_select → kidsAges), AND two importance questions `importance_schools` and `importance_childcare`
    (FAMILY_CATEGORIES — both are scored categories, each needs its own weight signal), gated on `a[TRADEOFF_FAMILY] === 'family_critical' || a[`${MODULE_SELECTED_PREFIX}schools`] === true`.
    Add a subtext caveat that school/childcare data is "state average, not city-specific" (deep-category-data.md caveat 1).
    Question wording is Claude's discretion (D-12); keep prompts plain and the option values machine-stable.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const CATEGORY_MODULE_QUESTIONS" shared/quiz-engine/category-modules.ts` returns 1
    - `grep -c "TRADEOFF_HEALTHCARE" shared/quiz-engine/category-modules.ts` ≥ 1 and `grep -c "MODULE_SELECTED_PREFIX" ...` ≥ 1
    - Healthcare + family modules contribute ≥4 QuestionDef entries total, all `required: false`, all with a `kicker:`
    - `grep -cE "importance_healthcare|importance_schools|importance_childcare" shared/quiz-engine/category-modules.ts` returns 3 (each scored category has its weight-signal question)
    - A "state average" caveat string appears in a schools/childcare subtext
    - `grep -cE "political|valuesFit|dating|social_score" shared/quiz-engine/category-modules.ts` returns 0 (Tier-3 omitted)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Healthcare + family/schools rich modules present, guided by showIf on personality keys, required:false, kicker on each; Tier-3 absent; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Light modules — climateRisk, demographics, parks, connectivity</name>
  <read_first>
    - shared/quiz-engine/category-modules.ts (Task 1 output — append to CATEGORY_MODULE_QUESTIONS)
    - shared/quiz-engine/keys.ts (TRADEOFF_CONNECTIVITY, MODULE_SELECTED_PREFIX)
    - shared/quiz-engine/category-modules.test.ts (plan 01 RED — remaining assertions: required:false, kicker present)
    - .planning/research/deep-category-data.md (climate/FEMA per-hazard note, demographics neutral-framing, ParkScore 7/22 gap, FAA connectivity)
    - .planning/phases/11-.../11-RESEARCH.md (Pitfall 3 capture primaryHazardConcern; D-14 demographics neutral framing; captured fields disasterRiskConcern, primaryHazardConcern, internationalTraveler, outdoorsFrequency, demographicsMatters)
  </read_first>
  <behavior>
    - Each light module is importance + one qualifier (D-12), required:false, kicker non-empty, showIf gated on personality key OR module-selected flag.
    - The importance question of each light module is keyed `importance_{category}` (importance_climateRisk, importance_parks, importance_demographics, importance_connectivity) — for the tradeoff-less ones this is the ONLY weight signal plan 02 has.
    - climateRisk captures primaryHazardConcern (hurricane|wildfire|flood|heat|tornado|any) for Phase 12 per-hazard mapping.
    - demographics question is a factual opt-in (demographicsMatters), framed as a neutral statistic — NOT a "people like you" score (D-14).
    - All CATEGORY_MODULE_QUESTIONS entries (rich + light) satisfy required:false and kicker present.
  </behavior>
  <action>
    Append LIGHT modules to `CATEGORY_MODULE_QUESTIONS` (D-12 — each = one `importance_{category}` question + at most one qualifier).
    CRITICAL: climateRisk, parks, and demographics have NO personality tradeoff, so their `importance_{category}` question is their ONLY
    weight signal (plan 02 reads it). Every light module MUST include its importance question:
    - climateRisk: a primary-hazard-concern single_select capturing `primaryHazardConcern` (values hurricane/wildfire/flood/heat/tornado/any),
      plus optional importance; kicker 'CLIMATE & RISK'. Add a comment that Phase 12 maps primaryHazardConcern to FEMA per-hazard NRI sub-scores
      (Pitfall 3 — the composite barely discriminates).
    - demographics: a single factual opt-in (`demographicsMatters`) phrased neutrally ("Would you like to see the share of residents born outside
      the US for each city?"); kicker 'DEMOGRAPHICS'. Add a comment enforcing D-14 neutral framing — never a "people like you" / fit score.
    - parks/outdoors: an outdoors-frequency qualifier (`outdoorsFrequency`: daily/weekend/occasional); kicker 'OUTDOORS'. Add a comment that
      Phase 12 falls back to nearMountains/nearCoast for the 15/22 cities without ParkScore.
    - connectivity: an international-traveler qualifier (`internationalTraveler`) gated on `a[TRADEOFF_CONNECTIVITY] === 'connectivity_critical' || a[`${MODULE_SELECTED_PREFIX}connectivity`] === true`; kicker 'CONNECTIVITY'.
    Every appended question `required: false`. Do NOT add any Tier-3 (political/values, social/dating) question.
  </action>
  <verify>
    <automated>npx vitest run shared/quiz-engine/category-modules.test.ts --reporter=verbose && npx tsc --noEmit -p tsconfig.json && npx vitest run shared/engine/ --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/quiz-engine/category-modules.test.ts` reports ALL tests passing (RED → GREEN for QUIZ-07, QUIZ-08)
    - The "every module question required:false" and "every module question has kicker" tests pass
    - `grep -cE "importance_climateRisk|importance_parks|importance_demographics|importance_connectivity" shared/quiz-engine/category-modules.ts` returns 4 (every light scored category has a weight-signal question)
    - `grep -c "primaryHazardConcern" shared/quiz-engine/category-modules.ts` ≥ 1 and a comment names FEMA per-hazard sub-scores
    - A comment enforces D-14 neutral demographic framing (no "people like you")
    - `grep -cE "political|valuesFit|dating" shared/quiz-engine/category-modules.ts` returns 0
    - `npx vitest run shared/engine/` still green
  </acceptance_criteria>
  <done>All four light modules appended; category-modules.test.ts fully green; per-hazard + ParkScore-fallback + neutral-framing comments present; Tier-3 absent; engine suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior answers → module showIf predicates | showIf receives untrusted Answers; a broken/throwing predicate must not crash the quiz (resolver already try/catches, but predicates must be defensive) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | Module showIf reading personality answer keys | mitigate | Predicates use strict `=== 'value'` / `=== true` comparisons that tolerate any/undefined input and never throw; resolver also try/catches showIf |
| T-11-07 | Information Disclosure | foreignBornPct / demographics framing | mitigate | demographics question is factual opt-in, neutrally worded; D-14 enforced via comment + neutral option labels; never a "people like you" score |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install task |
</threat_model>

<verification>
- `npx vitest run shared/quiz-engine/category-modules.test.ts --reporter=verbose` — all green.
- `npx tsc --noEmit -p tsconfig.json` — exits 0.
- `npx vitest run shared/engine/` — Phase 3 suite still green.
- Wave 2 plan-scoped gate (do NOT run whole quiz-engine dir): `npx vitest run shared/quiz-engine/category-modules.test.ts shared/engine/`
</verification>

<success_criteria>
- category-modules.ts ships rich healthcare + family modules and light climate/demographics/parks/connectivity modules.
- Each gated by guided-modular showIf on personality keys; depth mixed per D-12; required:false; kicker present.
- Tier-3 absent; per-hazard + ParkScore-fallback + neutral-framing documented for Phase 12.
- category-modules.test.ts green; engine suite green.
</success_criteria>

<output>
Create `.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-03-SUMMARY.md` when done.
</output>

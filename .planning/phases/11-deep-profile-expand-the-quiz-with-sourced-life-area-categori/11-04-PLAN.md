---
phase: 11-deep-profile-expand-the-quiz-with-sourced-life-area-categori
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - .planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-UI-SPEC.md
  - shared/quiz-engine/questions.ts
autonomous: true
requirements: [QUIZ-06, QUIZ-07, QUIZ-08, QUIZ-09, MATCH-05]
user_setup: []
must_haves:
  truths:
    - "A written UI spec document exists describing how the collaborator renders the engine's QuestionDef[] and captures personality/category data in the EXISTING gold-cinematic visual language (D-03)"
    - "The spec documents the contract the collaborator's UI consumes (D-04 — shared/quiz-engine is the UI-agnostic source of truth; the UI is a consumer that binds to it): getVisibleQuestions(answers) → render QuestionDef[] → synthesizeProfile(answers) + synthesizeCategoryWeights(answers); detectTension + detectPersonalityTension"
    - "The spec specifies the explainability surface: weightExplanations rendered as 'you chose X, so X ×N' (D-08), never a black-box personality type"
    - "Personality + module questions are registered into ALL_QUESTIONS append-only (personality gate FIRST, then modules) so getVisibleQuestions surfaces them — without touching any other Phase 2 logic"
    - "The spec names the Phase 12 clamp/BASE_SCORE recalibration as a HARD requirement (the Phase 3 BLOCKER), the D-02 open reconciliation, and the data caveats (state-average, FEMA per-hazard, ParkScore gap, neutral demographics)"
    - "Live tension injection is documented as a contract item Phase 2 Plan 04 activates in resolver.ts — NOT implemented here (D-01)"
  artifacts:
    - path: ".planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-UI-SPEC.md"
      provides: "Collaborator-facing quiz UI spec in the established visual language + Phase 12 handoff requirements"
      min_lines: 120
    - path: "shared/quiz-engine/questions.ts"
      provides: "Append-only registration of PERSONALITY_QUESTIONS, TRAIT_QUESTIONS, CATEGORY_MODULE_QUESTIONS into ALL_QUESTIONS"
      contains: "PERSONALITY_QUESTIONS"
  key_links:
    - from: "shared/quiz-engine/questions.ts ALL_QUESTIONS"
      to: "shared/quiz-engine/personality.ts + category-modules.ts"
      via: "spread append (personality FIRST, then modules)"
      pattern: "PERSONALITY_QUESTIONS"
    - from: "11-UI-SPEC.md"
      to: "origin/main src/screens/Quiz.jsx + Landing.jsx"
      via: "documented visual-language reference (gold-cinematic tokens, split-rail, reconcile card)"
      pattern: "Quiz.jsx"
---

<objective>
Produce the D-03 deliverable: a written UI spec for the collaborator who rebuilds the whole app
UI, describing how to render the config-driven engine in the established gold-cinematic visual
language; and perform the single append-only registration of the new questions into ALL_QUESTIONS
so the engine actually surfaces them. Carry forward the Phase 12 hard requirements (clamp
recalibration, D-02 reconciliation, data caveats) so they are not lost.

Purpose: Phase 11 ships logic + contract + spec, not production UI. This plan is the contract
documentation + the one necessary additive wiring touch.
Output: 11-UI-SPEC.md; ALL_QUESTIONS registration in questions.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-CONTEXT.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-RESEARCH.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-PATTERNS.md
@.planning/research/deep-category-data.md

<interfaces>
<!-- The contract surface the spec documents and the registration wires. -->

shared/quiz-engine/questions.ts: ALL_QUESTIONS is a flat QuestionDef[] literal (no spreads yet).
Registration appends Phase 11 arrays. INVARIANT: personality gate questions MUST come before
category-module questions (module showIf reads personality answer keys — trigger-before-injection).

shared/quiz-engine/personality.ts (plan 02): PERSONALITY_QUESTIONS, TRAIT_QUESTIONS,
detectPersonalityTension, synthesizeCategoryWeights, PRACTICAL_CATEGORIES, PREFERENCE_CATEGORIES.
shared/quiz-engine/category-modules.ts (plan 03): CATEGORY_MODULE_QUESTIONS.
shared/quiz-engine/resolver.ts: getVisibleQuestions / clearHiddenAnswers (live). Tension-injection
seam is COMMENTED and owned by Phase 2 Plan 04 — do NOT uncomment it here.
shared/quiz-engine/synthesizer.ts: synthesizeProfile (Phase 2). Phase 11 does NOT edit it; the spec
documents that the collaborator's flow calls synthesizeCategoryWeights alongside it (D-02 open: whether
results merge into one Profile build or layer is decided at Phase 2 integration).

Visual language reference (origin/main ONLY — NOT this branch; the executor must read via git show):
  git show origin/main:src/screens/Quiz.jsx
  git show origin/main:src/screens/Landing.jsx
Tokens: --accent #e2b56b, dark #070a11, ivory #f3ede1; Instrument Serif headers / JetBrains Mono values /
Manrope body; split-rail step layout; Pill/Slider/Toggle/tile primitives; "We noticed something" reconcile
card; "done" summary grid. NOTE the current Quiz.jsx is PROVISIONAL/hardcoded (STEP_META switch, inline
CONFLICT_PAIRS) — it will be REBUILT to consume the engine; the spec describes the rebuild, not a bolt-on.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append-only registration of Phase 11 questions into ALL_QUESTIONS</name>
  <read_first>
    - shared/quiz-engine/questions.ts (current flat ALL_QUESTIONS literal, end of array ~line 442; import block lines 14-18)
    - shared/quiz-engine/personality.ts (exported PERSONALITY_QUESTIONS, TRAIT_QUESTIONS)
    - shared/quiz-engine/category-modules.ts (exported CATEGORY_MODULE_QUESTIONS)
    - .planning/phases/11-.../11-PATTERNS.md ("Array registration pattern" — personality FIRST, then modules; Pitfall 4 ordering)
  </read_first>
  <action>
    In shared/quiz-engine/questions.ts add imports (SP-1 `.js` extensions) for PERSONALITY_QUESTIONS, TRAIT_QUESTIONS from
    `./personality.js` and CATEGORY_MODULE_QUESTIONS from `./category-modules.js`. At the END of the existing ALL_QUESTIONS array
    literal, append by spread in this exact order: PERSONALITY_QUESTIONS, then TRAIT_QUESTIONS, then CATEGORY_MODULE_QUESTIONS.
    Add a comment marking the Phase 11 block and stating the LOAD-BEARING ordering invariant (personality gate before modules so module
    showIf reads already-answered personality keys — Pitfall 4). This is the ONLY edit to a Phase-2-owned file in Phase 11 and it is
    APPEND-ONLY: do NOT modify, reorder, or delete any existing Phase 2 question, and do NOT touch resolver.ts / synthesizer.ts / the
    commented tension seam.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run shared/quiz-engine/personality.test.ts shared/quiz-engine/category-modules.test.ts shared/engine/ --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - grep shows the spread of PERSONALITY_QUESTIONS at a LOWER line number than CATEGORY_MODULE_QUESTIONS in questions.ts (ordering invariant)
    - The existing Phase 2 questions are unchanged: `grep -c "id: 'moveTimeline'" shared/quiz-engine/questions.ts` returns 1 and `grep -c "id: 'profession'"` returns 1
    - `git diff --name-only` for this task touches only shared/quiz-engine/questions.ts (resolver.ts and synthesizer.ts untouched)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - personality.test.ts + category-modules.test.ts + shared/engine/ all green
  </acceptance_criteria>
  <done>ALL_QUESTIONS appends the three Phase 11 arrays (personality before modules); no other Phase 2 file or question touched; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Write 11-UI-SPEC.md — contract render spec in the established visual language + Phase 12 handoff</name>
  <read_first>
    - shared/quiz-engine/personality.ts (PERSONALITY_QUESTIONS ids, TRAIT_QUESTIONS, detectPersonalityTension result shape, synthesizeCategoryWeights output, tier constants)
    - shared/quiz-engine/category-modules.ts (CATEGORY_MODULE_QUESTIONS, showIf keys)
    - shared/types.ts (WeightExplanation + new Profile fields — the data the UI renders)
    - .planning/phases/11-.../11-RESEARCH.md (Architecture diagram, Pitfall 6 clamp/BASE_SCORE, Data-Gated Module Set caveats, Open Question 1 D-02)
    - origin/main visual reference: inside the task run `git show origin/main:src/screens/Quiz.jsx` and `git show origin/main:src/screens/Landing.jsx` (these live on origin/main, NOT this branch; do NOT merge)
  </read_first>
  <action>
    Create the UI spec markdown at the path in files_modified. Required sections:

    (1) Purpose and boundary — Phase 11 ships logic+contract+spec; the collaborator rebuilds the whole app UI (D-04). The current
    origin/main Quiz.jsx is provisional/hardcoded (STEP_META switch, inline CONFLICT_PAIRS) and gets REBUILT to consume the engine
    (D-04/D-05) — this spec describes that rebuild, NOT a bolt-on to the hardcoded architecture.

    (2) The contract to consume — document the exact API: getVisibleQuestions(answers, ALL_QUESTIONS) returns the ordered QuestionDef[]
    to render (by `type`: single_select/multi_select/slider/free_text/boolean, using kicker, prompt, subtext, options, groupHeader);
    persist answers keyed by QuestionDef.id; on completion call synthesizeProfile(answers) AND synthesizeCategoryWeights(answers) to
    populate the extended Profile (categoryWeights + weightExplanations + the module fields). Document detectTension(answers) (Phase 2)
    and detectPersonalityTension(answers) (Phase 11) as signal-or-null follow-up detectors.

    (3) Visual language to render in — reference the gold-cinematic tokens (--accent #e2b56b, #070a11, #f3ede1), Instrument Serif /
    JetBrains Mono / Manrope, split-rail step layout, Pill/Slider/Toggle/tile primitives, the "We noticed something" reconcile card,
    the "done" summary grid (sourced from origin/main Quiz.jsx / Landing.jsx). Map each engine concept to a visual treatment: tradeoff
    scenario → choice tiles; trait statement → agree/disagree pill row; module question → the same step layout with its kicker; the
    personality result → a "what we noticed about you" summary.

    (4) Reconcile moment (engine-driven, not hardcoded) — the "We noticed something" card keeps its visual treatment but is driven by
    the engine's tension output (detectTension + detectPersonalityTension), NOT the hardcoded inline CONFLICT_PAIRS. Live injection into
    the question flow is activated by Phase 2 Plan 04 in resolver.ts (the commented splice seam) — the collaborator renders whatever
    getVisibleQuestions returns; they do not hand-wire conflicts.

    (5) Explainability surface (D-08) — render profile.weightExplanations[] as plain "you chose {category}-focused options {tally}×, so
    {category} ×{weight}" lines next to results; NEVER present a black-box personality type. This is the trust anchor / stage moment.

    (6) Guided-modular flow (D-11/D-12/D-13) — the personality result recommends which modules to surface; the user can add others via a
    moduleSelected_{category} toggle; skipped modules fall back to a neutral default weight (scoring never breaks). Note the mixed depth
    (rich healthcare/family, light parks/connectivity).

    (7) Data caveats to surface (deep-category-data.md) — schools/childcare are "state average, not city-specific"; demographics
    foreignBornPct is a neutral factual statistic, NEVER a "people like you" score (D-14); ParkScore covers only 15/22 cities so parks
    falls back to nearMountains/nearCoast; climate uses primaryHazardConcern (FEMA per-hazard, not the composite).

    (8) Phase 12 HARD requirements (carry forward, do not lose) — (a) Clamp/BASE_SCORE recalibration BLOCKER (Pitfall 6): adding the new
    scored categories to scoring-weights.ts without recalibrating maxContribution caps + BASE_SCORE will make clamp(rawScore,0,99) fire on
    strong profiles and desync the badge from the contribution bars — the exact Phase 3 BLOCKER. Phase 12 MUST verify
    BASE_SCORE + sum(all caps) < 99 and assert the user-facing displayed score (not a pre-clamp invariant). (b) D-02 OPEN: decide whether
    the personality quiz replaces or layers over Phase 2's importanceRank/weights — resolve at Phase 2 integration before Phase 2 is marked
    complete. (c) Two-tier floor/swing values (WEIGHT_FLOOR etc.) are provisional — Phase 12 tunes them in scoring-weights.ts. (d) parks
    fallback + FEMA per-hazard sourcing are Phase 12 items.

    Keep it collaborator-readable: a developer who has not read the engine source can build the UI from this doc.
  </action>
  <verify>
    <automated>SPEC=.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-UI-SPEC.md; test -f "$SPEC" && grep -q getVisibleQuestions "$SPEC" && grep -qiE "BASE_SCORE|clamp" "$SPEC" && grep -q weightExplanations "$SPEC" && echo SPEC_OK</automated>
  </verify>
  <acceptance_criteria>
    - 11-UI-SPEC.md exists with at least 120 lines (`wc -l` >= 120)
    - Documents the contract API: grep finds getVisibleQuestions, synthesizeProfile, synthesizeCategoryWeights, detectPersonalityTension
    - Documents the explainability surface: grep finds weightExplanations and "you chose ... so ... x" language (no black-box type)
    - References the visual language: grep finds Quiz.jsx and the accent token #e2b56b
    - Carries the Phase 12 BLOCKER: grep finds BASE_SCORE and clamp and D-02
    - Carries data caveats: grep finds "state average", primaryHazardConcern, and "people like you" (the D-14 prohibition wording)
    - States live tension injection is Phase 2 Plan 04's resolver.ts responsibility, not implemented in Phase 11
  </acceptance_criteria>
  <done>11-UI-SPEC.md is a complete, collaborator-readable contract+render spec in the established visual language, carrying the Phase 12 hard requirements and data caveats; all grep gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented contract -> collaborator UI | The spec is the only handshake; an under-specified contract leads the collaborator to reverse-engineer (black-box) rather than render the explainability data |
| answers -> ALL_QUESTIONS registration | Registration only orders config; the resolver already treats answer values as untrusted (try/catch on showIf) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering | ALL_QUESTIONS ordering (modules before personality) | mitigate | Append-only spread with personality FIRST; ordering invariant asserted via line-number grep gate (Pitfall 4) |
| T-11-09 | Information Disclosure | foreignBornPct framed as "people like you" in the UI | mitigate | Spec mandates neutral factual framing (D-14); grep gate requires the "people like you" prohibition wording present in the spec |
| T-11-10 | Repudiation | Phase 12 clamp BLOCKER lost between phases | mitigate | Spec carries it as a named HARD requirement; grep gate enforces BASE_SCORE/clamp/D-02 presence |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install task |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0 after registration.
- `npx vitest run shared/quiz-engine/personality.test.ts shared/quiz-engine/category-modules.test.ts shared/engine/ --reporter=verbose` all green.
- 11-UI-SPEC.md exists and passes all grep gates (contract API, explainability, visual language, Phase 12 BLOCKER, data caveats).
- Phase gate (file-scoped — never run the whole quiz-engine dir while tension.test.ts import-errors):
  `npx vitest run shared/quiz-engine/personality.test.ts shared/quiz-engine/category-modules.test.ts shared/engine/`
</verification>

<success_criteria>
- ALL_QUESTIONS registers the three Phase 11 arrays append-only, personality before modules, no other Phase 2 file touched.
- 11-UI-SPEC.md fully documents the config-driven render contract in the established visual language and carries every Phase 12 hard requirement + data caveat.
- All Phase 11 tests green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-04-SUMMARY.md` when done.
</output>

---
phase: 11-deep-profile-expand-the-quiz-with-sourced-life-area-categori
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - shared/quiz-engine/personality.ts
autonomous: true
requirements: [QUIZ-06, QUIZ-09, MATCH-05]
must_haves:
  truths:
    - "synthesizeCategoryWeights always returns a defined finite number for every category in ALL_SCORED_CATEGORIES — never undefined or NaN (D-13 neutral-skip)"
    - "Every scored category has a real weight-input path: tradeoff-bearing categories via their tradeoff key, tradeoff-less ones (climateRisk, parks, demographics) via their module-importance key, family→{schools,childcare} mapped explicitly (D-06 — category weights are INFERRED from the upfront personality/values gate, never set by explicit sliders or ranking)"
    - "synthesizeCategoryWeights emits categoryWeights and weightExplanations together from a single call (D-08 — they never ship apart)"
    - "Every emitted weight is explainable: each non-neutral category has a matching weightExplanations entry with human-readable text and the raw inferred weight"
    - "Practical categories (healthcare, cost, safety) carry a weight floor; preference categories swing freely above zero (D-09 two-tier)"
    - "detectPersonalityTension returns a signal-or-null and never throws on empty/unknown answers (mirrors dealbreakers.checkReconfirm discipline; D-10)"
    - "Trait statements are captured as a non-weight-bearing flavor layer that does NOT feed categoryWeights (D-07 hybrid)"
    - "Weights are emitted as RAW inferred values in the floor..max range, NOT pre-normalized (Phase 12 normalizes)"
  artifacts:
    - path: "shared/quiz-engine/personality.ts"
      provides: "Tradeoff + trait QuestionDefs, detectPersonalityTension, synthesizeCategoryWeights, exported tier constants"
      exports: ["PERSONALITY_QUESTIONS", "TRAIT_QUESTIONS", "detectPersonalityTension", "synthesizeCategoryWeights", "PRACTICAL_CATEGORIES", "PREFERENCE_CATEGORIES", "WEIGHT_FLOOR", "NEUTRAL_DEFAULT"]
      min_lines: 120
  key_links:
    - from: "shared/quiz-engine/personality.ts"
      to: "shared/quiz-engine/keys.ts"
      via: "import ALL_SCORED_CATEGORIES + tradeoff key consts"
      pattern: "ALL_SCORED_CATEGORIES"
    - from: "shared/quiz-engine/personality.ts synthesizeCategoryWeights"
      to: "shared/types.ts WeightExplanation"
      via: "return type"
      pattern: "WeightExplanation"
---

<objective>
Build the personality/values gate: the upfront tradeoff scenarios that anchor category weights,
the trait-statement flavor layer, the adaptive follow-up detector, and the explainable weight
synthesizer with the two-tier floor. This is the defining mechanism of Phase 11 (D-06..D-10).

Purpose: Infer how much each life-area category matters to the user, explainably, so Phase 12
can score cities against a weighted, Q&A-defensible profile.
Output: `shared/quiz-engine/personality.ts` — turns plan 01's RED personality.test.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-CONTEXT.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-RESEARCH.md
@.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-PATTERNS.md

<interfaces>
<!-- Contracts from plan 01 + landed Phase 3 analogs. Use directly — no exploration. -->

shared/quiz-engine/keys.ts (plan 01): ALL_SCORED_CATEGORIES, TRADEOFF_* key consts, MODULE_SELECTED_PREFIX.
shared/types.ts (plan 01): WeightExplanation { category, inferredWeight, floor?, explanation }.
shared/quiz-engine/questions.ts: QuestionDef (kicker REQUIRED), QuestionOption, Answers = Record<string, unknown>.

Analog — dealbreakers.checkReconfirm (signal-or-null, guard discipline):
```
export function checkReconfirm(...): ReconfirmSignal | null {
  if (penalizedRanking.length === 0 || rawRanking.length === 0) return null;
  ...
  return null; // or the signal
}
```
Analog — scoring.computeRawScore (contribution-array → scalar; mirror for tally → weight + explanation):
```
const scoreFactors: { factor: string; contribution: number }[] = [];
scoreFactors.push({ factor: 'Cost', contribution: costContrib });
const rawScore = BASE_SCORE + scoreFactors.reduce((s, f) => s + f.contribution, 0);
```
Analog — scoring-weights.ts const-export (single tunable config object; mirror for tier constants).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Personality + trait QuestionDefs and exported tier constants</name>
  <read_first>
    - shared/quiz-engine/questions.ts (QuestionDef shape with REQUIRED `kicker`; existing single_select option shape)
    - shared/quiz-engine/keys.ts (TRADEOFF_* consts, ALL_SCORED_CATEGORIES)
    - shared/engine/scoring-weights.ts (const-export config pattern to mirror for WEIGHT_FLOOR etc.)
    - .planning/phases/11-.../11-RESEARCH.md (Pattern 1 tradeoff content, Pattern 5 trait statements, the exported tier-constant block)
    - .planning/phases/11-.../11-PATTERNS.md (SP-1 .js imports, SP-2 boxed header, SP-3 const-export)
  </read_first>
  <behavior>
    - PERSONALITY_QUESTIONS is a QuestionDef[] of 5 core tradeoff scenarios whose ids equal the keys.ts TRADEOFF_* consts; every entry has a non-empty `kicker`.
    - TRAIT_QUESTIONS is a QuestionDef[] of 3-5 agree/disagree-style statements, each `required: false`, capturing flavor only.
    - PRACTICAL_CATEGORIES / PREFERENCE_CATEGORIES are the Phase-12 floor/swing REGISTRY over ALL factors (legacy + new), DISTINCT from ALL_SCORED_CATEGORIES (new-only, what synthesizeCategoryWeights iterates). PRACTICAL = {"healthcare","safety"} (cost kept OUT of categoryWeights per the double-count note, pending D-02). PREFERENCE enumerates every remaining factor in the Set literal INCLUDING the literal "career" plus the 6 new preference categories — do not write "remaining" in prose.
    - WEIGHT_FLOOR, WEIGHT_MAX_PRAC, WEIGHT_MAX_PREF, NEUTRAL_DEFAULT exported numeric consts.
  </behavior>
  <action>
    Create `shared/quiz-engine/personality.ts` with the SP-2 boxed header (cite D-06..D-10) and SP-1 `.js` imports.
    Import TRADEOFF key consts + ALL_SCORED_CATEGORIES from `./keys.js`, QuestionDef/QuestionOption/Answers from `./questions.js`,
    WeightExplanation type from `../types.js`.

    Define and export `PERSONALITY_QUESTIONS: QuestionDef[]` — the 5 core `single_select` tradeoff scenarios from RESEARCH
    Pattern 1, using the exact ids from keys.ts (tradeoff_cost_vs_lifestyle, tradeoff_safety_vs_career, tradeoff_healthcare,
    tradeoff_family_vs_mobility, tradeoff_connectivity_vs_cost). Each MUST have a `kicker` (e.g. 'WHAT MATTERS') and a "balanced"/"depends"
    option where applicable (the ambiguity signal detectPersonalityTension keys on). Wording is Claude's discretion (D-07) — keep it
    plain-language and tradeoff-shaped.

    Define and export `TRAIT_QUESTIONS: QuestionDef[]` — 3-5 `single_select` agree/disagree statements (Pattern 5), each `required: false`,
    each with a `kicker` (e.g. 'A LITTLE ABOUT YOU'). These are NON-WEIGHT-BEARING flavor (D-07) — add a comment saying they feed
    personalityFlavor only and never increment category tallies.

    Export the tier constants: `PRACTICAL_CATEGORIES = new Set(["healthcare","safety"])`, `PREFERENCE_CATEGORIES = new Set(["lifestyle","career","parks","connectivity","demographics","climateRisk","schools","childcare"])` (enumerate every member — `career` is EXPLICITLY in the literal, provisionally PREFERENCE per constraint #5), `WEIGHT_FLOOR = 0.3`, `WEIGHT_MAX_PRAC = 1.5`, `WEIGHT_MAX_PREF = 1.8`, `NEUTRAL_DEFAULT = 0.5`. Add a PLANNER-NOTE comment block:
    (a) these two Sets are the Phase-12 floor/swing registry over ALL factors (legacy + new), distinct from ALL_SCORED_CATEGORIES which is
    new-only and is what synthesizeCategoryWeights iterates — an executor must NOT trim the Sets to match ALL_SCORED_CATEGORIES;
    (b) tier membership of the original four factors is PROVISIONAL pending D-02; `cost` is kept OUT of categoryWeights to avoid
    double-counting profile.weights.cost; `career` is provisionally PREFERENCE (do NOT resolve D-02). Group all magic numbers in these
    consts (SP-3) — none inlined in function bodies.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "export const (PERSONALITY_QUESTIONS|TRAIT_QUESTIONS|PRACTICAL_CATEGORIES|PREFERENCE_CATEGORIES|WEIGHT_FLOOR|NEUTRAL_DEFAULT)" shared/quiz-engine/personality.ts` returns 6
    - Every QuestionDef in both arrays has a `kicker:` (grep the file: count of `kicker:` ≥ count of `id:` within the question arrays)
    - `grep -c "tension.js" shared/quiz-engine/personality.ts` returns 0
    - `grep -A2 "PREFERENCE_CATEGORIES = new Set" shared/quiz-engine/personality.ts` contains the literal `"career"` (explicitly assigned, constraint #5)
    - A comment explicitly states `cost` is excluded from categoryWeights pending D-02, `career` is provisionally PREFERENCE, and the Sets are the all-factor registry distinct from ALL_SCORED_CATEGORIES
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>personality.ts exports the 5 tradeoff QuestionDefs (ids match keys.ts), trait statements, and all tier constants; kicker present on every QuestionDef; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: detectPersonalityTension + synthesizeCategoryWeights (explainable, floored, neutral-safe)</name>
  <read_first>
    - shared/quiz-engine/personality.ts (Task 1 output — constants and PERSONALITY_QUESTIONS ids)
    - shared/quiz-engine/keys.ts (MODULE_IMPORTANCE_PREFIX, FAMILY_CATEGORIES, ALL_SCORED_CATEGORIES, tradeoff key consts)
    - shared/quiz-engine/personality.test.ts (plan 01 RED — the exact assertions this must satisfy)
    - shared/engine/dealbreakers.ts (checkReconfirm signal-or-null + guard discipline, ~lines 282-304)
    - shared/engine/scoring.ts (computeRawScore contribution-array→scalar pattern, ~lines 134-168)
    - .planning/phases/11-.../11-RESEARCH.md (Pattern 2 synthesizeCategoryWeights, Pattern 3 detectPersonalityTension, Pitfall 5 no-pre-normalize)
  </read_first>
  <behavior>
    - detectPersonalityTension({}) === null; detectPersonalityTension with ≥2 "balanced" answers returns { afterId: string, question: QuestionDef-with-string-id }; unambiguous answers return null.
    - synthesizeCategoryWeights({}) returns { categoryWeights, weightExplanations } where every ALL_SCORED_CATEGORIES key maps to a finite number === NEUTRAL_DEFAULT and weightExplanations is [].
    - A healthcare-critical answer raises categoryWeights.healthcare above NEUTRAL_DEFAULT and pushes a matching WeightExplanation; practical categories never fall below WEIGHT_FLOOR.
    - A high `importance_climateRisk` (or `importance_parks`, `importance_demographics`) answer raises that tradeoff-less category above NEUTRAL_DEFAULT — every scored category has a real weight-input path, not just the 4 with tradeoffs.
    - The single `tradeoff_family_vs_mobility === 'family_critical'` answer raises BOTH schools and childcare (via FAMILY_CATEGORIES), not just one.
    - Output weights are raw (floor..max), never normalized to [0,1].
  </behavior>
  <action>
    In `shared/quiz-engine/personality.ts` add `export interface PersonalityTensionResult { afterId: string; question: QuestionDef }`
    and `export function detectPersonalityTension(answers: Answers): PersonalityTensionResult | null`. Mirror checkReconfirm guard
    discipline: `if (Object.keys(answers).length === 0) return null;` first. Fire the follow-up when the user chose "balanced"/"depends"
    on ≥2 core tradeoffs (count matching values across the tradeoff keys). When firing, return `afterId` = the last core tradeoff id
    (TRADEOFF_CONNECTIVITY) and a `question` (single_select with `kicker`, string `id` e.g. 'personality_tiebreaker', `required: true`,
    3 options on financial-security / life-quality / career trajectory). Otherwise return null. Never throw on unknown keys.

    Add `export function synthesizeCategoryWeights(answers: Answers): { categoryWeights: Record<string, number>; weightExplanations: WeightExplanation[] }`.
    Build a per-category `tallies: Record<string, number>` by reading BOTH (i) the tradeoff answer keys AND (ii) the per-category
    module-importance keys `${MODULE_IMPORTANCE_PREFIX}${cat}` for every cat in ALL_SCORED_CATEGORIES (guard with `?? 0`; unknown keys never
    throw). LOAD-BEARING: climateRisk, parks, and demographics have NO tradeoff — their importance key is their ONLY weight signal, so a
    synthesizer that reads tradeoff keys only would strand them at NEUTRAL_DEFAULT forever (the gap the forcing test guards). Map the single
    `tradeoff_family_vs_mobility` answer onto BOTH categories in FAMILY_CATEGORIES (schools + childcare) explicitly — do not invent a mapping.
    A high-importance answer (and a favoring tradeoff) increments the category tally.
    TEST-INTEGRITY (load-bearing, prevents the MEMORY.md "false comfort" regression): the importance→tally increment and the step
    constant MUST be chosen so a SINGLE high-importance answer pushes the category weight strictly ABOVE NEUTRAL_DEFAULT (with the
    provisional step=0.3 and NEUTRAL_DEFAULT=0.5, a single +1 increment yields 0.3 < 0.5 and FAILS — so a high-importance answer must
    increment by ≥2, OR raise the step, so the signal registers). If the forcing test is RED, fix the increment/step in the synthesizer —
    NEVER weaken the assertion to go green. The assertion direction (raises ABOVE NEUTRAL_DEFAULT) is the contract.
    Then iterate `ALL_SCORED_CATEGORIES`: for each, `isPractical = PRACTICAL_CATEGORIES.has(cat)`; `maxW = isPractical ? WEIGHT_MAX_PRAC : WEIGHT_MAX_PREF`;
    `floor = isPractical ? WEIGHT_FLOOR : 0`. If tally === 0 → assign NEUTRAL_DEFAULT (D-13, never undefined/NaN) and push no explanation;
    else `rawWeight = Math.min(maxW, floor + tally * step)` (step is Claude's discretion, e.g. 0.3) and push a WeightExplanation
    { category, inferredWeight: rawWeight, floor: isPractical ? floor : undefined, explanation: human-readable string naming the tally and weight }.
    Emit BOTH fields from this one function (D-08). Do NOT pre-normalize (Pitfall 5) — add a comment that Phase 12 normalizes in its rankToWeight successor.
    Also capture trait answers into a returned/sibling shape only if needed for the contract; trait answers must NOT increment tallies.
  </action>
  <verify>
    <automated>npx vitest run shared/quiz-engine/personality.test.ts --reporter=verbose && npx tsc --noEmit -p tsconfig.json && npx vitest run shared/engine/ --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/quiz-engine/personality.test.ts` reports ALL tests passing (RED → GREEN for QUIZ-06, QUIZ-09)
    - The neutral-skip test passes: `synthesizeCategoryWeights({})` yields a finite number for every ALL_SCORED_CATEGORIES key (no undefined/NaN)
    - The forcing test passes: a high `importance_climateRisk`/`importance_parks` answer raises that tradeoff-less category above NEUTRAL_DEFAULT with a matching WeightExplanation
    - `grep -cE "MODULE_IMPORTANCE_PREFIX|FAMILY_CATEGORIES" shared/quiz-engine/personality.ts` returns at least 2 (synthesizer reads importance keys + maps family→{schools,childcare})
    - The ship-together test passes: categoryWeights and weightExplanations both returned from one call
    - `detectPersonalityTension({})` returns null; `grep -c "Object.keys(answers).length === 0" shared/quiz-engine/personality.ts` ≥ 1 (empty guard present)
    - `grep -cE "/ *PERSONAL_WEIGHT_SCALE|normaliz" shared/quiz-engine/personality.ts` shows no pre-normalization division in synthesizeCategoryWeights (raw values emitted)
    - `npx vitest run shared/engine/` still green (no Phase 3 regression)
  </acceptance_criteria>
  <done>detectPersonalityTension + synthesizeCategoryWeights implemented; personality.test.ts fully green; weights raw + floored + neutral-safe + explainable; engine suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quiz answers → synthesizeCategoryWeights / detectPersonalityTension | All answer values are untrusted; unknown/absent/malformed keys must not crash or produce out-of-range weights |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | synthesizeCategoryWeights with sparse/unknown answer keys | mitigate | `tallies[cat] ?? 0`; unknown keys → NEUTRAL_DEFAULT; output clamped to floor..max via Math.min; never throws (covered by neutral-skip + empty-answers tests) |
| T-11-04 | Denial of Service | detectPersonalityTension on empty/garbage answers | mitigate | Empty-answers guard returns null first; value reads are `=== "balanced"` comparisons that tolerate any type; empty-answers test enforces |
| T-11-05 | Tampering | Out-of-range inferred weight reaching Phase 12 | mitigate | Two-tier clamp (floor..WEIGHT_MAX); raw values documented; Phase 12 normalizes within range |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install task |
</threat_model>

<verification>
- `npx vitest run shared/quiz-engine/personality.test.ts --reporter=verbose` — all green.
- `npx tsc --noEmit -p tsconfig.json` — exits 0.
- `npx vitest run shared/engine/ --reporter=verbose` — Phase 3 suite still green.
- Wave 2 plan-scoped gate (do NOT run the whole quiz-engine dir — tension.test.ts import-errors):
  `npx vitest run shared/quiz-engine/personality.test.ts shared/engine/`
</verification>

<success_criteria>
- personality.ts ships tradeoff + trait QuestionDefs, tier constants, detectPersonalityTension, synthesizeCategoryWeights.
- Weights are explainable (D-08), floored (D-09), neutral-safe (D-13), raw (not pre-normalized), and never crash on bad input.
- personality.test.ts green; engine suite green.
</success_criteria>

<output>
Create `.planning/phases/11-deep-profile-expand-the-quiz-with-sourced-life-area-categori/11-02-SUMMARY.md` when done.
</output>

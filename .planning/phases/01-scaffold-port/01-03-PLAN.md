---
phase: 01-scaffold-port
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - vercel.json
  - .gitignore
  - .planning/STATE.md
  - package.json
autonomous: false
requirements:
  - FOUND-02

must_haves:
  truths:
    - "vercel.json exists and configures Vite build output directory"
    - "App deploys to Vercel and returns HTTP 200 at the live URL"
    - "api/health returns { ok: true, phase: 1 } at /api/health on the live URL"
    - "Every push to main triggers an auto-deploy"
    - "ANTHROPIC_API_KEY is NOT present in any committed file"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel build configuration"
      contains: "outputDirectory"
    - path: ".gitignore"
      provides: ".vercel entry confirmed present"
  key_links:
    - from: "Vercel project"
      to: "main branch"
      via: "Git integration auto-deploy"
    - from: "api/health.ts"
      to: "/api/health route"
      via: "Vercel auto-detects api/ directory as serverless functions (no runtime declaration needed)"

user_setup:
  - service: vercel
    why: "Deploy the Vite app and prove the api/ function wiring works on a live URL"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Not needed in Phase 1 — optionally add placeholder 'placeholder_phase5' in Vercel Dashboard -> Project -> Settings -> Environment Variables to prove the .env.example pattern"
    dashboard_config:
      - task: "Connect GitHub repo"
        location: "vercel.com -> Add New Project -> Import Git Repository -> select gabrielpedrosolealinscrito-spec/FBLA"
      - task: "Verify build command: vite build and output directory: dist"
        location: "Vercel project settings -> Build & Development Settings"
---

<objective>
Configure Vercel deployment (vercel.json + project link), deploy the Vite app + api/health stub to a live URL, and verify auto-deploy on push to main.

Purpose: Complete the walking skeleton end-to-end proof — app runs locally AND is live at a real URL that updates on every push.
Output: vercel.json, Vercel project linked to main, live URL confirmed, /api/health proven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-port/01-02-SUMMARY.md
@.planning/phases/01-scaffold-port/01-CONTEXT.md
@STRUCTURE.md
@api/health.ts
</context>

<interfaces>
<!-- api/health.ts (from Plan 01 Task 2) exports: -->
export default function handler(req: VercelRequest, res: VercelResponse): void
  -> responds: res.status(200).json({ ok: true, phase: 1 })

<!-- Vercel routing convention: api/health.ts is auto-detected and served at /api/health -->
<!-- No custom routing config needed — Vercel detects api/ directory as serverless functions by convention -->
<!-- Do NOT declare a "functions" block with a runtime version — this triggers a Vercel schema error -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write vercel.json, update .gitignore, install Vercel CLI</name>
  <files>vercel.json, .gitignore, package.json</files>

  <read_first>
    - api/health.ts (the stub endpoint being deployed — confirm it exists and uses the Vercel handler signature)
    - api/README.md (Vercel serverless function rules)
    - STRUCTURE.md (deploy section: src/ -> static build, api/ -> serverless functions)
    - .planning/phases/01-scaffold-port/01-CONTEXT.md (Deployment decision: Vercel, api/ convention)
    - .gitignore (confirm .vercel is already present from Plan 01 Task 1 Step 9)
  </read_first>

  <action>
    Step 1 — Write vercel.json at repo root:
    The file must contain only buildCommand, outputDirectory, and framework. Do NOT add a "functions"
    block with a runtime version — Vercel auto-detects api/*.ts files as serverless Node functions
    and a bare runtime declaration causes a "Function Runtimes must have a valid version" error.

    vercel.json contents (three fields only):
      buildCommand: "vite build"
      outputDirectory: "dist"
      framework: "vite"

    Vercel will automatically serve api/health.ts at /api/health via its file-system routing convention.
    No additional configuration is required.

    Step 2 — Confirm .vercel is in .gitignore:
    Run: grep -c "\.vercel" .gitignore
    If it returns 0, append .vercel to .gitignore. If >= 1, already present from Plan 01 Task 1 Step 9.

    Step 3 — Install Vercel CLI as a dev dependency:
    Run: npm install --save-dev vercel
    This enables vercel dev (serves both Vite frontend + api/ functions locally) and vercel --prod
    (for first deploy from CLI).

    Step 4 — Add dev:full script to package.json:
    In the scripts object, add: "dev:full": "vercel dev"
    This is the local full-stack command — it serves api/health.ts at localhost:3000/api/health.
    The existing "dev": "vite" remains for frontend-only work (does NOT serve api/ functions).

    Step 5 — Re-confirm TypeScript still compiles cleanly:
    Run: npx tsc --noEmit
    Expected: exits 0. If it fails, investigate and fix api/health.ts only (do not modify shared/types.ts).
  </action>

  <verify>
    <automated>cd /Users/leal/FBLA/FBLA && test -f vercel.json && grep -c "outputDirectory" vercel.json && grep -c "\.vercel" .gitignore && npx tsc --noEmit && echo "TASK1_OK"</automated>
    Must print TASK1_OK. All conditions must pass: vercel.json exists, has outputDirectory, .gitignore has .vercel, tsc passes.
  </verify>

  <acceptance_criteria>
    - vercel.json exists with buildCommand "vite build", outputDirectory "dist", framework "vite"
    - vercel.json does NOT contain a "functions" block (Vercel auto-detects api/*.ts)
    - .gitignore includes .vercel
    - npm install --save-dev vercel completed (vercel in devDependencies)
    - package.json has "dev:full": "vercel dev" script
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>vercel.json configured (no runtime block); .gitignore updated; Vercel CLI installed; tsc clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Link Vercel project and trigger first deploy</name>

  <read_first>
    - vercel.json (just created — confirms build settings)
    - .env.example (documents ANTHROPIC_API_KEY — confirm it's committed before deploy)
  </read_first>

  <what-to-do>
    This step requires your Vercel account. Claude cannot authenticate on your behalf.

    Option A — Vercel CLI (recommended):
      1. Run: npx vercel login   (opens browser for Vercel auth)
      2. Run: npx vercel link    (links this directory to a Vercel project; creates .vercel/ locally — gitignored)
      3. Run: npx vercel --prod  (first production deploy from CLI)
      4. Vercel will print the live URL (e.g. https://fbla-xyz.vercel.app). Copy it.
      5. In Vercel Dashboard -> Project -> Settings -> Git, confirm the repo is connected to main
         and "Auto-Deploy" is enabled for the main branch.

    Option B — Vercel Dashboard (if CLI auth is inconvenient):
      1. Go to vercel.com -> Add New Project
      2. Import the GitHub repo (gabrielpedrosolealinscrito-spec/FBLA or your fork)
      3. Framework preset: Vite (Vercel should auto-detect from vite.config.js)
      4. Build command: vite build
      5. Output directory: dist
      6. Click Deploy. Vercel will print the live URL.
      7. After deploy, confirm Project Settings -> Git shows pushes to main trigger auto-deploy.

    After deploy (either option), test:
      curl https://<your-vercel-url>.vercel.app           -> should return HTML containing "Potential"
      curl https://<your-vercel-url>.vercel.app/api/health -> should return {"ok":true,"phase":1}

    If /api/health returns 404: Vercel may need a moment to propagate the function. Wait 30 seconds and retry.
    If /api/health returns a runtime error: the TypeScript compilation may have failed on Vercel — check
    the build log in the Vercel Dashboard -> Deployments -> Functions tab.

    Optional (good demo hygiene): add ANTHROPIC_API_KEY as a placeholder env var now:
      Vercel Dashboard -> Project -> Settings -> Environment Variables
      Name: ANTHROPIC_API_KEY, Value: placeholder_phase5, Environment: Production
      This proves the .env.example pattern works and shows future env var hygiene to any reviewer.
  </what-to-do>

  <resume-signal>
    Paste the live Vercel URL and confirm:
    1. / returns the app (dark background, "potential" heading visible)
    2. /api/health returns {"ok":true,"phase":1}
    3. Auto-deploy is configured on main

    Example: "approved — https://fbla-abc123.vercel.app — health ok"
    If the deploy failed, paste the error from the Vercel build log and Claude will diagnose.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit vercel config and record live URL in STATE.md</name>
  <files>.planning/STATE.md</files>

  <read_first>
    - .planning/STATE.md (current state — will be updated with live URL and phase completion)
    - vercel.json (confirm on disk before committing)
  </read_first>

  <action>
    Step 1 — Commit the Vercel config and CLI setup:
    git add vercel.json .gitignore package.json package-lock.json
    git commit -m "feat(01-scaffold-port): add vercel.json, deploy config, Vercel CLI"

    Step 2 — Update .planning/STATE.md:
    Change "Current Phase" to: 01-scaffold-port
    Change "Status" to: In Progress (Plan 03 complete)
    Under Session Continuity add:
      Live URL: <the URL confirmed in Task 2 checkpoint>
      Walking Skeleton: proven end-to-end (npm run dev local + Vercel deploy + /api/health confirmed)

    Step 3 — Commit STATE.md update:
    git add .planning/STATE.md
    git commit -m "docs(state): record Phase 1 walking skeleton live URL"

    Step 4 — Push to main:
    git push origin main

    Step 5 — Verify auto-deploy triggered:
    Run: npx vercel ls
    Should show a new deployment in the list within 1-2 minutes of the push.
    Alternatively: check Vercel Dashboard -> Deployments.
  </action>

  <verify>
    <automated>cd /Users/leal/FBLA/FBLA && git log --oneline -5 | grep -ci "vercel"</automated>
    Returns >= 1 (vercel commit present in recent log). The -i flag makes it case-insensitive.
  </verify>

  <acceptance_criteria>
    - vercel.json committed to main
    - .planning/STATE.md contains the live Vercel URL
    - git push to main triggered an auto-deploy on Vercel (visible in Deployments list)
    - /api/health on live URL returns {"ok":true,"phase":1}
    - No API key in any committed file (grep -r "sk-ant" . --include="*.ts" --include="*.js" --include="*.json" returns 0 results)
  </acceptance_criteria>

  <done>Walking skeleton complete: npm run dev works locally, Vercel deploys live on push to main, /api/health proves serverless function wiring end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> Vercel | Live URL is publicly accessible; Phase 1 has no secrets in the deployed code |
| client -> /api/health | Stub endpoint, returns only static JSON; no user input accepted, no secrets returned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Vercel env vars | mitigate | ANTHROPIC_API_KEY not present in Phase 1 codebase; .env.example committed as documentation; real key deferred to Phase 5 |
| T-03-02 | Elevation of Privilege | /api/health endpoint | accept | Returns only { ok: true, phase: 1 }; no user input processed; no secrets returned; no state mutation |
| T-03-03 | Information Disclosure | .vercel/ local metadata | mitigate | .gitignore entry for .vercel confirmed in Task 1 |
| T-03-SC | Tampering | npm install vercel (CLI) | accept | Vercel CLI is the canonical deployment tool for Vercel projects; >2M weekly downloads; official package from vercel.com |
</threat_model>

<verification>
After Plan 03 completes:
- vercel.json in repo, committed to main, no "functions" runtime block
- Live URL returns app HTML at /
- /api/health returns {"ok":true,"phase":1}
- git push to main auto-deploys
- .planning/STATE.md records the live URL
- No API key (sk-ant-*) in any committed file
</verification>

<success_criteria>
- App is live at a Vercel URL (auto-deploys on push to main)
- /api/health on live URL confirms serverless function wiring works
- Walking skeleton proven: local dev + live Vercel deploy + api stub + no secrets committed
- Phase 1 ROADMAP success criteria all satisfied:
  1. git log shows single coherent history with potential_v2.jsx (before deletion), README, PDF, .planning/
  2. npm run dev starts with no errors; ported prototype renders in browser
  3. TS/JSX decision committed in tsconfig.json "notes"; shared/engine/ and api/ exist with .ts extensions
  4. Dark-theme visual identity (Instrument Serif, Manrope, JetBrains Mono) renders without regression
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-port/01-03-SUMMARY.md` when done.
</output>

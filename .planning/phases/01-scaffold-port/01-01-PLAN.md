---
phase: 01-scaffold-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - index.html
  - src/main.jsx
  - src/App.jsx
  - src/index.css
  - tsconfig.json
  - .gitignore
  - .env.example
  - shared/types.ts
  - api/health.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "git log shows a single coherent history containing potential_v2.jsx, README, the FBLA PDF, and .planning/"
    - "npm run dev starts the Vite dev server with no errors"
    - "vite build completes with exit code 0"
    - "shared/types.ts compiles via tsc --noEmit with no errors"
    - "shared/types.ts is importable from api/ TypeScript file"
    - ".env is gitignored; .env.example is committed"
    - "tsconfig.json contains a comment recording the TS(shared+api)/JSX(src) language split decision"
  artifacts:
    - path: "package.json"
      provides: "Vite + React + @vitejs/plugin-react dependencies"
      contains: "vite"
    - path: "vite.config.js"
      provides: "Vite build config with react plugin"
      contains: "plugin-react"
    - path: "src/main.jsx"
      provides: "React root mount"
    - path: "shared/types.ts"
      provides: "TypeScript contract — compiles cleanly"
    - path: "api/health.ts"
      provides: "Vercel serverless stub that imports Tier from shared/types.ts"
    - path: ".env.example"
      provides: "Documents future env vars; .env is gitignored"
  key_links:
    - from: "src/main.jsx"
      to: "src/App.jsx"
      via: "ReactDOM.createRoot import"
    - from: "api/health.ts"
      to: "shared/types.ts"
      via: "import type { Tier }"
---

<objective>
Scaffold the Vite + React project into the existing repo, verify the reconciled git state (FOUND-01), and prove shared/types.ts compiles + is importable from api/. This delivers the build foundation every later plan needs.

Purpose: Establish the toolchain so npm run dev works and the TypeScript contract is verified clean.
Output: package.json, vite.config.js, index.html, src/main.jsx, src/App.jsx, src/index.css, tsconfig.json, .gitignore update, .env.example, api/health.ts, and a compile-clean shared/types.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-port/01-CONTEXT.md
@STRUCTURE.md
@shared/types.ts
</context>

<interfaces>
<!-- shared/types.ts exports (already on disk — executor must NOT rewrite, only confirm it compiles) -->
Profile, City, ExpenseBreakdown, MatchResult, LiveCategory, LiveDataRequest,
LiveDataResponse, RoadmapSection, Roadmap, VisaPathway, Tier — all exported from shared/types.ts.

Key fields needed in Phase 1 compile proof:
  import type { Tier } from '../shared/types'  // used in api/health.ts for compile test
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify reconciled git state (FOUND-01) and scaffold Vite project</name>
  <files>package.json, vite.config.js, index.html, src/main.jsx, src/App.jsx, src/index.css, tsconfig.json, .gitignore, .env.example</files>

  <read_first>
    - potential_v2.jsx (at repo root — confirm it exists on disk, visual source of truth)
    - src/README.md (folder ownership rules)
    - api/README.md (rules: secrets in env, return shared types)
    - shared/types.ts (the contract — do NOT modify it in this task)
    - STRUCTURE.md (repo layout)
  </read_first>

  <action>
    Step 1 — Verify reconciled state (FOUND-01, per 01-CONTEXT.md Decision: "already done manually"):
    Run: git log --oneline --all
    Confirm the log shows potential_v2.jsx, README.md, "Entrepreneurship Pitch Competition.pdf" (or similar PDF name), and .planning/ folder all present on main. If any artifact is missing, STOP and report — do not proceed. If all present, this task continues.

    Step 2 — Scaffold Vite into the existing repo root (do NOT run npm create vite which creates a subdirectory):
    Run: npm init -y
    Then install: npm install react@19 react-dom@19
    Then install dev: npm install --save-dev vite@8 @vitejs/plugin-react@6

    Step 3 — Write vite.config.js at repo root:
    Import defineConfig from 'vite' and react from '@vitejs/plugin-react'.
    Export defineConfig with plugins: [react()].
    Set server.port to 5173.

    Step 4 — Write index.html at repo root:
    Standard HTML5 boilerplate. In <head>: title "Potential", link Google Fonts for Instrument Serif, Manrope (weights 300;400;500;600;700;800), and JetBrains Mono (weights 400;500;600) with display=swap. In <body>: div#root and script type="module" src="/src/main.jsx".
    Move Google Fonts load here (out of the component useEffect — see potential_v2.jsx line 100 — this prevents the flash on load and improves offline resilience).

    Step 5 — Write src/main.jsx:
    Import React, ReactDOM from react/react-dom/client.
    Import App from './App.jsx'.
    Import './index.css'.
    ReactDOM.createRoot(document.getElementById('root')).render(<React.StrictMode><App /></React.StrictMode>).

    Step 6 — Write src/App.jsx (minimal placeholder — the real port happens in Plan 02):
    Import React. Export default App that returns a single div with inline style background "#08090C" and color "#EEF2F7", containing an h1 "Potential — loading..." This confirms the React tree mounts; Plan 02 replaces it.

    Step 7 — Write src/index.css (minimal):
    Only: * { box-sizing: border-box; margin: 0; padding: 0; } body { background: #08090C; } — no Tailwind yet (per 01-CONTEXT.md Decision: port inline styles as-is for Phase 1; Tailwind is deferred).

    Step 8 — Write tsconfig.json at repo root:
    Add a multi-line comment at the top of the file before the JSON object, recording the language split decision (this is the STACK decision artifact for ROADMAP success criterion #3):
      TypeScript is used for shared/ and api/ (typed contract + serverless functions).
      Plain JSX (.jsx) is used for src/ (React components; prototype-origin; not type-checked by tsc).
      Rationale: port speed + prototype fidelity in src/; strict types where contracts cross the network boundary.
    Use compilerOptions: target "ES2022", module "ESNext", moduleResolution "bundler", strict true, skipLibCheck true, noEmit true.
    Include: ["shared/**/*.ts", "api/**/*.ts"] — do NOT include src/ (src is .jsx).

    NOTE: tsconfig.json is standard JSON and does not support // comments. Write the language-split decision as a top-level "notes" key:
    {
      "notes": "TS for shared/ + api/ (typed contract); plain JSX for src/ (prototype port, fast iteration). See .planning/phases/01-scaffold-port/01-CONTEXT.md.",
      "compilerOptions": { ... },
      "include": [...]
    }

    Step 9 — Update .gitignore:
    Ensure these entries are present (append if missing, do not remove existing): node_modules/, dist/, .env, .env.local, .env.*.local, .vercel

    Step 10 — Write .env.example at repo root:
    Line 1: # Environment variables for Potential
    Line 2: # ANTHROPIC_API_KEY is required starting Phase 5 (proxy + live AI).
    Line 3: # Not needed for Phase 1-4.
    Line 4: ANTHROPIC_API_KEY=your_key_here

    Step 11 — Update package.json scripts:
    Add: "dev": "vite", "build": "vite build", "preview": "vite preview"
    Add "type": "module" at the top level of package.json if not already present (npm init -y does NOT add it; required for vite.config.js ESM import syntax).
    Also confirm shared/engine/ and api/ directories exist with correct file extensions (.ts):
    Run: ls shared/engine/ api/  (they exist from the initial scaffold; report if missing)
  </action>

  <verify>
    <automated>cd /Users/leal/FBLA/FBLA && npx vite build && echo "BUILD_OK"</automated>
    vite build exits 0 and prints BUILD_OK. This is the deterministic gate — no backgrounded server process needed.
    Additionally run: git log --oneline --all | grep -c "potential_v2" (returns >= 1).
  </verify>

  <acceptance_criteria>
    - git log --oneline --all shows a single history with potential_v2.jsx, README, FBLA PDF, and .planning/ traceable on main
    - npx vite build exits 0 (BUILD_OK)
    - node_modules/, dist/, .env, and .vercel are listed in .gitignore
    - .env.example exists at repo root with ANTHROPIC_API_KEY placeholder
    - tsconfig.json has a "notes" key documenting the TS(shared+api)/JSX(src) split decision
    - shared/engine/ and api/ directories exist (ls confirms)
  </acceptance_criteria>

  <done>Vite build passes; git history verified; .gitignore and .env.example in place; STACK decision recorded in tsconfig.json "notes".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove shared/types.ts compiles clean and is importable from api/</name>
  <files>api/health.ts, tsconfig.json</files>

  <read_first>
    - shared/types.ts (the full contract — must compile as-is, do NOT modify)
    - api/README.md (file extension rules: .ts; return shapes from shared/types.ts)
    - .planning/phases/01-scaffold-port/01-CONTEXT.md (Decision: TypeScript for shared/ and api/)
  </read_first>

  <behavior>
    - Compile check: tsc --noEmit exits 0 with no errors
    - Import check: api/health.ts imports Tier from '../shared/types' without error
    - The health stub file itself has no runtime logic — just a type import and a Vercel handler signature
  </behavior>

  <action>
    Step 1 — Write api/health.ts:
    File contents (prose description — do not inline fenced code in action):
    Import type Tier from '../shared/types'. Import type VercelRequest and VercelResponse from '@vercel/node'.
    Export a default function named handler that takes req: VercelRequest and res: VercelResponse and returns void.
    Inside the function: declare a const tier typed as Tier with value 'free' (proves the type import works;
    the variable is used only as a type annotation, not sent to the client).
    Call res.status(200).json with an object containing ok: true and phase: 1.
    This file proves: (a) api/ TypeScript compiles under the tsconfig, (b) shared/types.ts Tier is importable from api/.

    Step 2 — Install Vercel types for the handler signature:
    Run: npm install --save-dev @vercel/node

    Step 3 — Run the compile check:
    Run: npx tsc --noEmit
    Expected: exits 0, no errors. If errors appear in shared/types.ts, report them verbatim — do NOT modify shared/types.ts (it is the shared contract). If errors are only in api/health.ts, fix api/health.ts only.
  </action>

  <verify>
    <automated>cd /Users/leal/FBLA/FBLA && npx tsc --noEmit && echo "COMPILE_OK"</automated>
    Must print COMPILE_OK with exit code 0.
  </verify>

  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with output "COMPILE_OK"
    - api/health.ts exists and imports Tier from '../shared/types'
    - shared/types.ts is NOT modified (git diff shows no changes to it)
  </acceptance_criteria>

  <done>shared/types.ts compiles clean; api/health.ts proves importability; TS(shared+api)/JSX(src) language split is committed in tsconfig.json "notes" (ROADMAP success criterion #3 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost dev server | Vite serves on localhost only — no external surface in Phase 1 |
| .env file | Contains future secrets; must be gitignored before first commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file | mitigate | .gitignore entry for .env and .env.local enforced in Task 1 Step 9; .env.example committed instead |
| T-01-02 | Tampering | shared/types.ts (shared contract) | accept | No runtime exposure in Phase 1; contract is dev-time only; file ownership documented in STRUCTURE.md |
| T-01-SC | Tampering | npm installs (vite, react, @vitejs/plugin-react, @vercel/node) | accept | All are canonical packages; vite >25M weekly downloads, react >25M, @vercel/node is the official Vercel runtime types package; no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
After Plan 01 completes:
- git log --oneline --all confirms single coherent history
- npx vite build exits 0
- npx tsc --noEmit exits 0
- .env and .vercel are gitignored; .env.example is committed
- api/health.ts exists and imports Tier from shared/types.ts
- tsconfig.json "notes" key records the TS/JSX language split
</verification>

<success_criteria>
- vite build passes (Vite 8, React 19)
- shared/types.ts compiles with zero TypeScript errors
- api/health.ts imports Tier from shared/types.ts successfully
- .gitignore covers .env, .env.local, .vercel, node_modules, dist
- git history shows potential_v2.jsx, README, PDF, .planning/ on main
- STACK decision documented in tsconfig.json
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-port/01-01-SUMMARY.md` when done.
</output>

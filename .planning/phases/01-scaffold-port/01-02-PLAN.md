---
phase: 01-scaffold-port
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.jsx
  - src/screens/PotentialApp.jsx
  - shared/data/constants.js
autonomous: false
requirements:
  - FOUND-02

must_haves:
  truths:
    - "npm run dev renders the ported prototype with no visual regression vs potential_v2.jsx"
    - "Dark theme (bg #08090C, accent #6EE7B7, Instrument Serif / Manrope / JetBrains Mono) renders correctly"
    - "Landing, quiz (all 5 steps), results list, and city detail screens all function"
    - "AI sections show a placeholder/coming-soon state — no fetch to api.anthropic.com"
    - "No console errors on any screen"
    - "Root potential_v2.jsx is deleted after visual parity is confirmed"
  artifacts:
    - path: "src/screens/PotentialApp.jsx"
      provides: "Ported prototype as main React component"
      min_lines: 750
    - path: "shared/data/constants.js"
      provides: "Extracted PROFESSION_CATEGORIES, BASE_SALARIES, LIFESTYLE_TAGS, DEAL_BREAKERS"
    - path: "src/App.jsx"
      provides: "Wires PotentialApp as the app root"
  key_links:
    - from: "src/App.jsx"
      to: "src/screens/PotentialApp.jsx"
      via: "default import"
    - from: "src/screens/PotentialApp.jsx"
      to: "shared/data/constants.js"
      via: "import { PROFESSION_CATEGORIES, BASE_SALARIES, LIFESTYLE_TAGS, DEAL_BREAKERS }"
---

<objective>
Port potential_v2.jsx into src/screens/PotentialApp.jsx, extract safe constants into shared/data/constants.js, stub the broken AI fetch sections to show a "coming soon" placeholder, then delete root potential_v2.jsx after visual parity is confirmed by the developer.

Purpose: Deliver FOUND-02 — the app builds, runs, and renders the prototype visually with no regression.
Output: PotentialApp.jsx in src/screens/, shared/data/constants.js, updated App.jsx, deleted potential_v2.jsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-port/01-01-SUMMARY.md
@.planning/phases/01-scaffold-port/01-CONTEXT.md
@STRUCTURE.md
@potential_v2.jsx
@shared/types.ts
</context>

<interfaces>
<!-- From potential_v2.jsx — what gets extracted vs stays inline -->

SAFE TO EXTRACT to shared/data/constants.js (pure data, no city shape dependency):
  export const PROFESSION_CATEGORIES = { ... }   // line 23-31
  export const BASE_SALARIES = { ... }            // line 33-41
  export const LIFESTYLE_TAGS = [ ... ]           // line 43-58
  export const DEAL_BREAKERS = [ ... ]            // line 60-65

KEEP INLINE in PotentialApp.jsx (prototype fields don't match City interface; Phase 1 is a port not a migration):
  CITIES_DATA — keep as-is in PotentialApp.jsx. It carries fields (color, pop, climate, stateTax)
  that the financial functions depend on (getTakeHome uses city.stateTax). Aligning to the
  shared/types.ts City interface is Phase 3-4 work (FIN-01, FIN-02). Do NOT modify CITIES_DATA shape.

FINANCIAL FUNCTIONS — keep inline in PotentialApp.jsx (getSalary, getTakeHome, getExpenses, getSavings,
  getMatchScore). These functions reference city.stateTax from CITIES_DATA. Extracting to engine/ is
  Phase 3 (MATCH-01, FIN-01).

AI FETCH — stub fetchCityAI. Do NOT call api.anthropic.com. See action step 2d.

CSS/STYLE VARIABLES — keep all inline styles exactly as-is in PotentialApp.jsx. No Tailwind, no redesign.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract safe constants and create PotentialApp.jsx</name>
  <files>shared/data/constants.js, src/screens/PotentialApp.jsx, src/App.jsx</files>

  <read_first>
    - potential_v2.jsx (full file — this is the visual source of truth; reproduce faithfully)
    - src/README.md (component rules: import types from shared/types.ts, never call api.anthropic.com)
    - shared/README.md (data/ is backend-owned; constants.js is plain JS, not TS)
    - .planning/phases/01-scaffold-port/01-CONTEXT.md (Porting decision, structure conformance)
  </read_first>

  <action>
    Step 1 — Create shared/data/constants.js:
    Export PROFESSION_CATEGORIES (the object from potential_v2.jsx lines 23-31).
    Export BASE_SALARIES (the object from lines 33-41).
    Export LIFESTYLE_TAGS (the array from lines 43-58).
    Export DEAL_BREAKERS (the array from lines 60-65).
    This is a plain .js file (not .ts) — shared/data is backend-owned per STRUCTURE.md and these constants
    are also needed by the engine in later phases. No TypeScript annotations needed here yet.

    Step 2 — Create src/screens/PotentialApp.jsx:
    Copy the entire body of potential_v2.jsx into this file with these specific changes:

    2a. Replace the four inline constant definitions (PROFESSION_CATEGORIES, BASE_SALARIES, LIFESTYLE_TAGS,
        DEAL_BREAKERS) at the top of the file with:
          import { PROFESSION_CATEGORIES, BASE_SALARIES, LIFESTYLE_TAGS, DEAL_BREAKERS } from '../../shared/data/constants.js';
        Keep CITIES_DATA defined inline in this file (do NOT extract or reshape it).

    2b. Keep all React imports and hooks (useState, useEffect, useCallback, useRef) unchanged.

    2c. Keep CITIES_DATA, fmt, fmtFull, and all scoring/financial functions (getSalary, getTakeHome,
        getExpenses, getSavings, getMatchScore) exactly as they are. Do NOT change their logic.

    2d. Replace the fetchCityAI function body (lines 189-231 in original) with a stub that shows a
        placeholder instead of calling api.anthropic.com. The stub must:
          - Accept the same signature: async (city, category)
          - NOT call fetch() to any URL
          - Set cityAIData for the key to the string "coming_soon"
          - Log a console.info message: "[Phase 1] Live AI data is coming in Phase 5"
        This preserves all the Section/AIList/ItemCard rendering logic; the AIList component already
        handles string data (potential_v2.jsx line 628: typeof data === "string" renders it as text).
        The placeholder message visible to the user should be friendly: update the AIList rendering
        branch at line 628 to detect the string "coming_soon" and render:
          a paragraph element with color "var(--text3)", fontSize 14, lineHeight 1.7 containing the text:
          "Live AI data arrives in Phase 5. This section will show real job listings,
           apartments, and a personalized day-in-the-life narrative."

    2e. Remove the Google Fonts useEffect (fonts load from index.html now — Plan 01 Task 1 Step 4
        moved them there). Remove only the useEffect that appends the Google Fonts link tag
        (lines 98-104 in original). Keep all other useEffect hooks (the anim timeout at line 103 is a
        separate setTimeout inside the same useEffect — remove the whole useEffect block since
        the link append is the only side effect; the anim timeout can be moved to its own useEffect).

    2f. Keep every JSX section unchanged: Landing (step 0), Profile quiz (step 1, all 5 sub-steps),
        Results list (step 2), City Detail (step 2 + selectedCity). Reproduce the visual identity
        pixel-faithfully: every color variable, every inline style object, every heading/mono/pill
        style helper, every animation (fadeIn, spin keyframes).

    2g. Keep the export default function name as Potential (same as original) for now — App.jsx will
        import it.

    Step 3 — Update src/App.jsx:
    Replace the placeholder content from Plan 01 with an import of Potential from './screens/PotentialApp.jsx'
    and export a default function App that returns a Potential element.
    Keep the React import.

    Step 4 — Ensure shared/data/ directory exists:
    Run: mkdir -p shared/data
    (shared/README.md says data/ is under shared/ — create if not present.)
  </action>

  <verify>
    <automated>cd /Users/leal/FBLA/FBLA && npx vite build && echo "BUILD_OK" && grep -c "api.anthropic.com" src/screens/PotentialApp.jsx || echo "CLEAN_NO_ANTHROPIC"</automated>
    vite build exits 0 (BUILD_OK). The grep returns 0 matches — "CLEAN_NO_ANTHROPIC" confirms no Anthropic fetch in the ported file.
    Use vite build (deterministic) rather than a backgrounded dev server for automated verification.
    Visual rendering is confirmed in Task 2 (human checkpoint).
  </verify>

  <acceptance_criteria>
    - shared/data/constants.js exists and exports PROFESSION_CATEGORIES, BASE_SALARIES, LIFESTYLE_TAGS, DEAL_BREAKERS
    - src/screens/PotentialApp.jsx exists (minimum 750 lines; port is faithful)
    - src/App.jsx imports and renders PotentialApp
    - CITIES_DATA is defined inline in PotentialApp.jsx (NOT extracted)
    - grep for "api.anthropic.com" in src/screens/PotentialApp.jsx returns 0 matches
    - fetchCityAI sets data to "coming_soon" string; console.info logged
    - Google Fonts useEffect removed from PotentialApp.jsx (fonts load from index.html)
    - npx vite build exits 0
  </acceptance_criteria>

  <done>PotentialApp.jsx renders all screens; AI sections show placeholder; no fetch to Anthropic; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual parity checkpoint — confirm no regression vs prototype</name>

  <read_first>
    - potential_v2.jsx (at repo root — this is the reference you are comparing against; it still exists here)
    - src/screens/PotentialApp.jsx (the ported version)
  </read_first>

  <what-built>
    Task 1 ported the 809-line prototype into src/screens/PotentialApp.jsx and extracted constants into
    shared/data/constants.js. The AI sections show a "coming soon" placeholder instead of calling
    api.anthropic.com. All other screens (landing, quiz, results, city detail) should render identically
    to the original.
  </what-built>

  <how-to-verify>
    1. Run: npm run dev  — open http://localhost:5173 in browser.
    2. Compare against potential_v2.jsx (open it in your editor side-by-side if helpful):
       a. Landing screen: dark background (#08090C), gradient "potential" heading, "Start Exploring" button.
       b. Quiz step 1: profession picker pills render in all 7 categories; remote toggle works.
       c. Quiz step 2: income/savings/debt sliders render; housing rent/buy toggle works.
       d. Quiz step 3: age slider, education pills, current city input, pet toggle work.
       e. Quiz step 4: lifestyle tag grid renders (14 tags); select at least 1 to proceed.
       f. Quiz step 5: importance rank list renders; deal-breaker pills render.
       g. Submit quiz ("Show Me My Potential"): results list renders with all 12 cities, match scores,
          salary/mo savings columns.
       h. Sort buttons work (Best match, Most savings, Top salary, Lowest cost).
       i. Click a city: city detail renders with emoji, financial overview, expense breakdown.
       j. Expand any AI section (Jobs, Housing, Day in Life, etc.): see the "Live AI data arrives in
          Phase 5" placeholder message — NOT a spinner, NOT an error.
    3. Open browser DevTools -> Console: confirm zero errors (warnings OK).
    4. Open DevTools -> Network: confirm zero requests to api.anthropic.com.
    5. Visual identity check: Instrument Serif heading font on h1/h2, Manrope body, JetBrains Mono
       on numbers/money values. Accent color #6EE7B7 on active pills, savings totals, section highlights.
  </how-to-verify>

  <resume-signal>
    If all screens match the prototype and DevTools shows zero errors and zero Anthropic calls,
    type "approved" to continue to the delete step.
    If there is a regression, describe which screen and what looks wrong — Claude will fix it before deletion.
    DO NOT delete potential_v2.jsx until you type "approved" here.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Delete root potential_v2.jsx (post-approval)</name>
  <files>potential_v2.jsx</files>

  <read_first>
    - potential_v2.jsx (confirm it still exists at repo root before deleting)
    - src/screens/PotentialApp.jsx (confirm the port is in place)
  </read_first>

  <action>
    Step 1 — Remove the file from git tracking and disk:
    Run: git rm potential_v2.jsx

    Step 2 — Stage only the specific files from this phase and commit:
    Run: git add src/screens/PotentialApp.jsx src/App.jsx shared/data/constants.js
    Note: git rm already staged the deletion of potential_v2.jsx; no need to re-add it.
    Run: git commit -m "feat(01-scaffold-port): port prototype into src/screens, stub AI sections, delete root jsx"

    Step 3 — Confirm the app still builds after deletion:
    Run: npx vite build
    Expected: exits 0. The root potential_v2.jsx is not imported by anything in src/ (PotentialApp.jsx
    is the canonical version now), so deletion must not break the build.
  </action>

  <verify>
    <automated>cd /Users/leal/FBLA/FBLA && test ! -f potential_v2.jsx && echo "DELETED_OK" && npx vite build && echo "BUILD_STILL_OK"</automated>
    Must print DELETED_OK and BUILD_STILL_OK.
  </verify>

  <acceptance_criteria>
    - potential_v2.jsx no longer exists at repo root
    - git log shows the deletion commit
    - npx vite build still exits 0 after deletion
    - STRUCTURE.md note "potential_v2.jsx — prototype to port into src/, then delete" is now satisfied
  </acceptance_criteria>

  <done>Prototype ported, visual parity confirmed by developer, root file deleted, build still passes. FOUND-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> api.anthropic.com | Broken fetch from prototype — stubbed out entirely in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | fetchCityAI stub | mitigate | fetch to api.anthropic.com removed; replaced with stub that sets local state; verified by grep gate (0 matches) in Task 1 verify + Network tab check in checkpoint |
| T-02-02 | Tampering | shared/data/constants.js | accept | Pure static data, no secrets; file contains no PII or sensitive material |
| T-02-SC | Tampering | No new npm packages in this plan | accept | No new installs; no slopcheck needed |
</threat_model>

<verification>
After Plan 02 completes:
- npx vite build exits 0
- No fetch to api.anthropic.com in src/screens/PotentialApp.jsx (grep confirms 0 matches)
- AI sections show friendly placeholder message (visual parity confirmed by developer)
- potential_v2.jsx deleted from repo root
- git log shows the port + deletion commit
</verification>

<success_criteria>
- src/screens/PotentialApp.jsx renders identically to potential_v2.jsx on all screens
- AI sections display "Live AI data arrives in Phase 5" placeholder (no fetch, no spinner, no error)
- Developer confirmed visual parity via checkpoint
- Root potential_v2.jsx is deleted; build still passes
- Zero console errors on any screen
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-port/01-02-SUMMARY.md` when done.
</output>

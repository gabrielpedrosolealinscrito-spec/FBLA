---
phase: 01-scaffold-port
plan: 01
subsystem: infra
tags: [vite, react, typescript, vercel, build-tooling]

requires: []
provides:
  - Vite 8 + React 19 build toolchain (npm run dev/build/preview)
  - index.html with Google Fonts (Instrument Serif, Manrope, JetBrains Mono)
  - src/main.jsx React root mount, src/App.jsx placeholder, src/index.css dark base
  - tsconfig.json with TS(shared+api)/JSX(src) language split documented in "notes" key
  - .gitignore covering node_modules, dist, .env, .env.local, .vercel
  - .env.example documenting future ANTHROPIC_API_KEY
  - api/health.ts: Vercel serverless stub proving Tier is importable from api/
  - npx vite build exits 0; npx tsc --noEmit exits 0
affects: [01-02, 01-03, all subsequent phases]

tech-stack:
  added:
    - vite@8.0.14
    - react@19.2.6
    - react-dom@19.2.6
    - "@vitejs/plugin-react@6.0.2"
    - "@vercel/node@5.8.8 (types for Vercel serverless functions)"
  patterns:
    - TS for shared/ + api/ (typed contract + serverless functions)
    - Plain JSX (.jsx) for src/ (prototype port, fast iteration)
    - tsconfig includes only shared/**/*.ts and api/**/*.ts — not src/
    - Google Fonts loaded in index.html (not via useEffect) to prevent FOUT

key-files:
  created:
    - package.json
    - vite.config.js
    - index.html
    - src/main.jsx
    - src/App.jsx
    - src/index.css
    - tsconfig.json
    - .gitignore
    - .env.example
    - api/health.ts
  modified:
    - package-lock.json (generated by npm install)

key-decisions:
  - "TS(shared+api)/JSX(src) language split: typed contract at network boundary, fast JSX in prototype-origin UI layer"
  - "Google Fonts loaded in index.html head, not component useEffect, to eliminate FOUT on load"
  - "package.json type=module for ESM-native vite.config.js import syntax"
  - "@vercel/node installed for type-safe Vercel serverless handler signatures"

patterns-established:
  - "Vite entrypoint: index.html at repo root, src/main.jsx as module entry"
  - "Serverless function pattern: api/*.ts exports default handler(req, res) using VercelRequest/VercelResponse"
  - "Shared type import pattern: import type { Tier } from '../shared/types'"

requirements-completed: [FOUND-01, FOUND-02]

duration: 5min
completed: 2026-05-30
---

# Phase 01 Plan 01: Scaffold & Port — Vite Toolchain Summary

**Vite 8 + React 19 build toolchain scaffolded into existing repo; shared/types.ts compile-proven importable from api/ TypeScript with zero errors**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-30T22:36:12Z
- **Completed:** 2026-05-30T22:41:00Z
- **Tasks:** 2
- **Files modified:** 11 created, 1 updated (package.json)

## Accomplishments
- Verified FOUND-01 reconciled git state (potential_v2.jsx, README, FBLA PDF, .planning/ all present on main)
- Scaffolded Vite 8 + React 19 into the existing repo root without creating a subdirectory
- Established TS(shared+api)/JSX(src) language split, documented in tsconfig.json "notes" key (ROADMAP success criterion #3)
- Proved shared/types.ts compiles cleanly and Tier type is importable from api/health.ts
- Both gating commands pass: `npx vite build` (BUILD_OK) and `npx tsc --noEmit` (COMPILE_OK)

## Task Commits

Each task was committed atomically:

1. **Task 1: Verify reconciled git state and scaffold Vite project** - `3c31b43` (chore)
2. **Task 2: Prove shared/types.ts compiles clean and is importable from api/** - `471438b` (feat)

## Files Created/Modified
- `package.json` - type=module, dev/build/preview scripts, react@19, vite@8, @vercel/node
- `vite.config.js` - Vite config with @vitejs/plugin-react, server.port 5173
- `index.html` - HTML5 boilerplate with Google Fonts preloaded (eliminates FOUT)
- `src/main.jsx` - React root mount via ReactDOM.createRoot
- `src/App.jsx` - Minimal placeholder (dark background, "Potential — loading..."); Plan 02 replaces with ported prototype
- `src/index.css` - Box-sizing reset + dark base (#08090C)
- `tsconfig.json` - ES2022/ESNext/bundler, strict, noEmit; includes shared/**/*.ts + api/**/*.ts only; "notes" key records language split decision
- `.gitignore` - node_modules, dist, .env, .env.local, .env.*.local, .vercel
- `.env.example` - Documents future ANTHROPIC_API_KEY (needed Phase 5+)
- `api/health.ts` - Vercel serverless stub: imports Tier from ../shared/types, returns { ok: true, phase: 1 }

## Decisions Made
- Moved Google Fonts load to index.html `<head>` (vs. component useEffect in original prototype) to prevent flash of unstyled text on page load
- Used `"type": "module"` in package.json (npm init -y defaults to "commonjs"; ESM required for vite.config.js import syntax)
- @vercel/node installed as devDependency for VercelRequest/VercelResponse types without runtime cost

## Deviations from Plan

None - plan executed exactly as written. Both npm version targets (vite@8, @vitejs/plugin-react@6) resolved without issue.

## Known Stubs

| File | Description | Resolves in |
|------|-------------|-------------|
| `src/App.jsx` | Placeholder div ("Potential — loading...") — no actual prototype UI | Plan 02 (port potential_v2.jsx) |
| `api/health.ts` | Stub Vercel function returning { ok: true, phase: 1 } — no real logic | Phase 5 (Anthropic proxy) |

Both stubs are intentional and plan-specified. They do not prevent this plan's goal (build toolchain proven).

## Issues Encountered

None. @vercel/node install surfaced 9 audit vulnerabilities in its indirect dependencies (3 moderate, 6 high) — these are in Vercel's types package tree and do not affect our production build. Deferred to routine dependency maintenance.

## User Setup Required

None — no external service configuration required for Plan 01.

## Next Phase Readiness
- Build toolchain is ready: Plan 02 can immediately begin porting potential_v2.jsx into src/screens/
- TypeScript contract is verified: Plan 03 (Vercel deploy) can proceed in parallel
- .env.example documents the future ANTHROPIC_API_KEY placeholder; no action needed until Phase 5

---
*Phase: 01-scaffold-port*
*Completed: 2026-05-30*

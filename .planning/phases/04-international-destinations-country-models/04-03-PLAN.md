---
phase: 04-international-destinations-country-models
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/screens/results/CityDetail.jsx
  - src/screens/results/InfoTooltip.jsx
autonomous: true
requirements: [FIN-02]
user_setup: []

must_haves:
  truths:
    - "International city financial figures render dual-currency: local primary, USD in parentheses (D-03)"
    - "A visible 'data as of [date]' stamp (rendered from the fx.ts DATA_AS_OF constant) and 'FX rate as of [date]' label appear on international financial content (SC#4, D-04)"
    - "Uncommon country-specific concepts have a tappable 'i' affordance revealing a plain-language explanation + source (D-10)"
    - "US cities render exactly as before (USD only, no FX, no stamps) — no regression"
  artifacts:
    - path: "src/screens/results/InfoTooltip.jsx"
      provides: "Reusable tappable 'i' info affordance (explanation + source citation)"
      contains: "InfoTooltip"
    - path: "src/screens/results/CityDetail.jsx"
      provides: "Dual-currency rendering + dated stamps + 'i' tooltips for intl cities"
      contains: "FX rate as of"
  key_links:
    - from: "src/screens/results/CityDetail.jsx"
      to: "shared/engine/fx.ts"
      via: "import FX_RATES/FX_AS_OF/DATA_AS_OF/currencyForCountry for USD->local display conversion + dated stamps"
      pattern: "fx|FX_RATES|DATA_AS_OF|currencyForCountry"
---

<objective>
Make international city detail render financially-honest and sourced: every intl monetary figure shows local currency primary with USD in parentheses (D-03), visible "data as of [date]" + "FX rate as of [date]" stamps (D-04, SC#4), and tappable "i" info affordances on uncommon country-specific concepts (NHR/IFICI, solidarity surcharge, National Insurance, provincial tax, the dated stamp) carrying a plain-language explanation + source (D-10). US cities are untouched.

Purpose: Satisfies FIN-02's display half + SC#4 ("data as of [date]" visible on intl financial content) and the "everything sourced" standard — the "i" tooltips are where intl citations live on screen.

Output: InfoTooltip.jsx (reusable affordance) + intl-aware rendering in CityDetail.jsx.

**UI-SPEC is intentionally absent for this phase (operator chose --skip-ui).** Per the project's division of labor, detailed visual/interaction polish of this surface is the friend's separate frontend pass. Plan to a FUNCTIONAL level: what must render and where (dual-currency figures, dated stamps, "i" affordances) per D-03/D-04/D-10. Do NOT over-specify pixel/visual design — match the existing CityDetail dark-theme inline-style conventions and leave refinement to the frontend pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-international-destinations-country-models/04-RESEARCH.md
@.planning/phases/04-international-destinations-country-models/04-CONTEXT.md
@.planning/phases/04-international-destinations-country-models/04-01-SUMMARY.md

<interfaces>
From shared/engine/fx.ts (created in 04-01 — import these):
  FX_RATES: Record<string,number> (currency -> USD multiplier);
  FX_AS_OF: string (e.g. "2026-06-01");
  DATA_AS_OF: string (e.g. "2026-06-01") — the salary/rent/tax sourcing date; the SC#4 "data as of" stamp MUST read this constant, never an inline literal;
  currencyForCountry(country): "EUR"|"GBP"|"CAD"|"USD";
  toUSD(amountLocal, currency): number.
  For DISPLAY you need USD->local: localAmount = usdAmount / FX_RATES[currency].

From src/screens/results/CityDetail.jsx (the surface to extend):
  - MatchResult numeric fields (estSalary, monthlyTakeHome, monthlySavings, expenses.*) are USD-canonical (per 04-01). For an intl city, convert each USD figure BACK to local for the primary display, show USD in parentheses.
  - Financial summary tile grid (lines ~182-208) renders estSalary/monthlyTakeHome/monthlySavings/medianRent via fmtFull.
  - ExpenseBreakdown component (lines ~93-142) renders itemized USD rows with $.
  - c.country (City.country) is the intl discriminator: c.country !== 'US' -> dual-currency + stamps + tooltips.
  - Existing helpers imported from ResultsView.jsx: css, heading, mono, fmtFull.
</interfaces>

<currency_note>
city.medianRent/medianHome and all MatchResult figures are stored USD-canonical (locked in 04-01). So for display: local = USD / FX_RATES[currency]. e.g. London estSalary $74,085 / 1.347 = £55,000 -> render "£55,000 ($74,085)". US cities: country === 'US' -> render USD only, no conversion, no stamp (no regression). Do NOT re-derive figures from raw salaries; convert the canonical USD MatchResult values only.
</currency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable InfoTooltip "i" affordance (D-10)</name>
  <read_first>
    - src/screens/results/CityDetail.jsx (existing inline-style + Section component conventions, lines 11-89)
    - src/screens/results/ResultsView.jsx (exported helpers css/heading/mono/fmtFull; dark-theme CSS-var conventions e.g. var(--card), var(--text2))
    - .planning/phases/04-international-destinations-country-models/04-CONTEXT.md (D-10 spec lines 46-47, 107)
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (section 5 "i" tooltips lines 37-38; D-09 NHR/IFICI mentioned-not-computed line 134)
  </read_first>
  <action>
    Create src/screens/results/InfoTooltip.jsx exporting a reusable InfoTooltip component: a small tappable "i" affordance (button) that toggles a popover/inline panel showing { label, explanation, source } — a short plain-language explanation plus a source citation (text or link). Match the existing CityDetail dark-theme inline-style conventions (var(--card)/var(--text2)/var(--border)); keep it functional, not pixel-polished (friend's frontend pass refines). Click/tap toggles open (no hover-only — touch-friendly per D-10). This is the single reusable affordance used for NHR/IFICI, solidarity surcharge, National Insurance, provincial tax, and the "data as of" stamp. No network/fetch.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/screens/results/InfoTooltip.jsx','utf8'); if(!/InfoTooltip/.test(s)||!/explanation/.test(s)||!/source/.test(s)) process.exit(1); console.log('InfoTooltip shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/results/InfoTooltip.jsx exists, exports InfoTooltip, accepts explanation + source content, toggles on click.
    - grep finds no fetch//api in InfoTooltip.jsx (offline).
    - Uses existing dark-theme CSS-var conventions (var(--...)).
  </acceptance_criteria>
  <done>Reusable tappable "i" affordance with explanation + source, click-toggle, offline.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-currency rendering + dated stamps + tooltips on intl CityDetail (D-03/D-04/D-10)</name>
  <read_first>
    - src/screens/results/CityDetail.jsx (full file — financial tile grid 182-208, ExpenseBreakdown 93-142, main component 152-end)
    - src/screens/results/InfoTooltip.jsx (from Task 1)
    - shared/engine/fx.ts (FX_RATES, FX_AS_OF, currencyForCountry — for USD->local display conversion)
    - .planning/phases/04-international-destinations-country-models/04-CONTEXT.md (D-03 dual currency, D-04 dated FX stamp, D-09 NHR mentioned-not-computed, D-10 concepts list)
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (country concepts for tooltips: PT NHR/IFICI lines 131-134, DE solidarity surcharge line 139, UK National Insurance line 152, CA provincial tax line 145; "data as of" SC#4)
  </read_first>
  <action>
    Extend CityDetail.jsx so that for international cities (c.country !== 'US'): (1) Render every monetary figure dual-currency — local primary, USD in parentheses — by converting the USD-canonical MatchResult/City values to local via FX_RATES[currencyForCountry(c.country)] (local = usd / rate). Apply to the financial summary tiles (estSalary, monthlyTakeHome, monthlySavings, medianRent/medianHome) and the ExpenseBreakdown rows + total. Use the correct currency symbol per country (EUR euro, GBP pound, CAD C$). (2) Import `DATA_AS_OF` (and `FX_AS_OF`) from `shared/engine/fx.ts` and render a visible "data as of {DATA_AS_OF}" stamp AND an "FX rate as of {FX_AS_OF}" label on the intl financial content (SC#4, D-04) — both dates MUST come from the fx.ts constants, NOT inline literals, so the stamp can never drift from FX_AS_OF (plan-checker warning). Wrap the FX label in an InfoTooltip explaining the hardcoded-offline-rate rationale + ECB source. (3) Add InfoTooltip "i" affordances next to the country-specific tax concepts surfaced for that country: UK -> National Insurance; Portugal -> NHR/IFICI (mentioned-not-computed per D-09 — note the standard-regime take-home is shown, IFICI is an upside, not baked in); Germany -> solidarity surcharge; Canada -> provincial tax — each with plain-language text + source. US cities: country === 'US' branch renders EXACTLY as today (USD only, no conversion, no stamp, no tooltip) — verify no regression. Keep figures derived from the canonical USD values only (do not re-run salary math in the view).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/screens/results/CityDetail.jsx','utf8'); const ok=/currencyForCountry|FX_RATES/.test(s)&&/FX_AS_OF/.test(s)&&/DATA_AS_OF/.test(s)&&/data as of/i.test(s)&&/InfoTooltip/.test(s)&&/country/.test(s); if(!ok)process.exit(1); console.log('CityDetail intl wiring present')"</automated>
  </verify>
  <acceptance_criteria>
    - CityDetail.jsx imports from shared/engine/fx.ts and renders dual-currency (local primary, USD parens) for intl cities (D-03).
    - Visible "data as of {DATA_AS_OF}" stamp AND "FX rate as of {FX_AS_OF}" label render on intl financial content (SC#4, D-04).
    - `grep -n "DATA_AS_OF" src/screens/results/CityDetail.jsx` matches (stamp reads the fx.ts constant, not an inline date literal).
    - At least the UK/National-Insurance InfoTooltip is wired for London; PT/DE/CA concept tooltips wired (active once Plan 04 cities exist).
    - US-city branch unchanged: a US MatchResult renders USD-only, no stamp, no tooltip (no regression) — confirm by reading the country==='US' path.
    - npx vitest run (full suite) still green (no engine import breakage).
  </acceptance_criteria>
  <done>Intl CityDetail shows dual-currency figures + dated stamps + sourced "i" tooltips; US cities unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine (USD-canonical MatchResult) -> display layer | Display reads typed numbers + static FX table; no user input crosses here; offline-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Information disclosure | Intl figures shown without source / wrong-currency mislabel misleads the user/judge | mitigate | Dual-currency labeled local+USD (D-03); dated "data as of"/"FX as of" stamps (D-04, SC#4); "i" tooltips carry source citations (D-10, V5) |
| T-4-05 | Tampering | New network call sneaking into the display layer (breaks offline-on-battery) | mitigate | FX hardcoded from fx.ts (no fetch); grep gate confirms no /api/fetch added (V6) |
| T-4-SC | Tampering | npm/pip package installs | N/A | No new packages — pure React view + existing fx.ts import |
</threat_model>

<verification>
- V5 (sourcing + dated stamp): dual-currency render (local primary, USD parens); "data as of"/"FX rate as of" visible; "i" tooltips carry source.
- V6 (offline): no fetch//api introduced in CityDetail.jsx / InfoTooltip.jsx.
- No regression: US-city render path unchanged; full vitest suite green.
- MANUAL (per VALIDATION.md): open an intl city detail, confirm local-primary/USD-paren figures, dated stamps, tappable "i" affordances with source text.
</verification>

<success_criteria>
- Dual-currency intl figures (local primary, USD parens) (FIN-02, D-03).
- Visible "data as of [date]" + "FX rate as of [date]" on intl content (SC#4, D-04).
- Tappable "i" affordances with explanation + source for uncommon concepts (D-10).
- US cities render exactly as before (no regression).
</success_criteria>

<output>
Create .planning/phases/04-international-destinations-country-models/04-03-SUMMARY.md when done. Record: the USD->local display conversion helper, which country concepts got tooltips, and a note that final visual polish is the friend's frontend pass (UI-SPEC intentionally absent).
</output>

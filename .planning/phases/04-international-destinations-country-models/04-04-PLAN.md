---
phase: 04-international-destinations-country-models
plan: 04
type: tdd
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - shared/engine/financial.ts
  - shared/engine/financial.test.ts
  - shared/data/cities.ts
  - shared/engine/index.test.ts
autonomous: true
requirements: [MATCH-02, FIN-02]
user_setup: []

must_haves:
  truths:
    - "Lisbon, Berlin, and Toronto each appear in rankCities() output alongside US cities and London"
    - "Each uses its own country-correct model (pt-irs-2026 / de-2026 / ca-on-2026), never US tax math"
    - "Each non-remote intl salary comes from a sourced local dataset, not BASE_SALARIES×costIndex"
    - "Each country worked example lands within its documented tolerance band (PT ±5%, DE/CA ±5-7%)"
    - "Portugal uses the STANDARD ~34% IRS regime; NHR/IFICI is NOT computed into take-home (D-09)"
    - "With all 4 intl cities present at openness=0, all 4 are still present with score > 0 (full V3/V4)"
  artifacts:
    - path: "shared/engine/financial.ts"
      provides: "pt-irs-2026, de-2026, ca-on-2026 models + local-salary datasets registered in FINANCIAL_MODELS"
      contains: "pt-irs-2026"
    - path: "shared/data/cities.ts"
      provides: "Lisbon, Berlin, Toronto records appended to CITIES_DATA"
      contains: "Lisbon, Portugal"
  key_links:
    - from: "shared/data/cities.ts intl records"
      to: "FINANCIAL_MODELS[financialModelId]"
      via: "financialModelId dispatch (pt-irs-2026/de-2026/ca-on-2026)"
      pattern: "pt-irs-2026|de-2026|ca-on-2026"
---

<objective>
Add the remaining three slices — Portugal/Lisbon, Germany/Berlin, Canada/Toronto — each as a sourced city record + a registered country-correct financial model + a worked-example test fixture, riding the seam Plan 01 proved (registry dispatch, salary branch, USD canonicalization, openness multiplier). Then complete the full 4-city V3/V4 assertions now that all four intl cities exist.

Purpose: Closes MATCH-02 (all four golden-path cities ranked) and FIN-02 (each with a country-correct, sourced model). Germany and Canada carry the most verification risk (RESEARCH Gaps #5/#6) — fixtures use wider tolerance bands accordingly.

Output: pt-irs-2026 + de-2026 + ca-on-2026 models + local-salary datasets in financial.ts, Lisbon/Berlin/Toronto records in cities.ts, V1 fixtures per country, and the full 4-city V3 (all present at openness=0) + V4 (all four ranked) wave-completion assertions.

**Sequenced to Wave 3:** 04-04 and 04-02 both edit shared/engine/index.test.ts, and 04-04 Task 2 asserts the openness mechanism built in 04-02 — so 04-04 depends on both 04-01 and 04-02 and runs after Wave 2.

**UI-SPEC intentionally absent (operator chose --skip-ui).** Engine + data plan; the intl display surface (Plan 03) already renders any intl city generically via currencyForCountry, so these three cities inherit dual-currency display once their records exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-international-destinations-country-models/04-RESEARCH.md
@.planning/phases/04-international-destinations-country-models/04-CONTEXT.md
@.planning/phases/04-international-destinations-country-models/04-VALIDATION.md
@.planning/phases/04-international-destinations-country-models/04-01-SUMMARY.md

<interfaces>
From shared/engine/financial.ts (established in 04-01):
  FinancialModel interface now has computeSalary(profile, city); UK_FINANCIAL_MODEL ('uk-2026') is the template to mirror for PT/DE/CA.
  FINANCIAL_MODELS holds us + uk-2026; append pt-irs-2026, de-2026, ca-on-2026.
  Each country model: computeTax (local currency) + computeSalary (local dataset, local currency) + computeExpenses (delegate to computeUSExpenses — expenses are USD-canonical from cities.ts).
  index.ts converts local->USD via fx.ts; these models return LOCAL figures (same contract as uk-2026).

From shared/data/cities.ts: append Lisbon/Berlin/Toronto records (country, non-us financialModelId, all Phase-3 City fields per Gap #8). Records are in RESEARCH (Lisbon lines 47-62, Berlin 68-82, Toronto 88-101).
</interfaces>

<currency_canonical_reminder>
Same as 04-01: models compute salary/tax/net in LOCAL currency; index.ts canonicalizes to USD via fx.ts; cities.ts medianRent/medianHome stored USD-canonical. V1 fixtures may assert BOTH the model's local take-home (vs the RESEARCH worked example) AND the user-facing USD monthlySavings/estSalary through rankCities (per MEMORY: assert what the user sees). NHR/IFICI never baked into the PT take-home (D-09).
</currency_canonical_reminder>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pt-irs-2026 + de-2026 + ca-on-2026 models + local salaries (V1 + V2)</name>
  <read_first>
    - shared/engine/financial.ts (UK_FINANCIAL_MODEL + UK_LOCAL_SALARIES from 04-01 as the template; FinancialModel interface; FINANCIAL_MODELS registry)
    - shared/engine/financial.test.ts (uk-2026 V1/V2 blocks from 04-01 as the fixture template)
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (pt-irs-2026 lines 128-134: SS 11%, IRS brackets, dedução específica greater of €4,462 or actual SS, worked example €45,000 -> net €29,638/yr = €2,470/mo standard regime, effective 34.1%; de-2026 lines 136-141: Grundfreibetrag €12,348, progressive 14%->42%->45%, SS ~20.6%, worked example €65,000 -> net €3,300-3,400/mo; ca-on-2026 lines 143-147: federal brackets, Ontario provincial+surtax+health premium, CPP/CPP2/EI, worked example C$95,000 -> net C$5,763/mo)
    - .planning/phases/04-international-destinations-country-models/04-VALIDATION.md (V1 tolerance: PT ±5%, DE/CA ±5-7%; V2 no-US-math)
  </read_first>
  <behavior>
    - V1 PT (RED first): pt-irs-2026.computeTax(45000, 0) yields a STANDARD-regime burden such that net ~= €29,638/yr (within ±5%); reached via FINANCIAL_MODELS['pt-irs-2026']. NHR/IFICI NOT applied (standard ~34% only — D-09).
    - V1 DE: de-2026.computeTax(65000, 0) yields net ~= €39,600-40,800/yr (€3,300-3,400/mo) within ±7%.
    - V1 CA: ca-on-2026.computeTax(95000, 0) yields net ~= C$69,160/yr (C$5,763/mo) within ±7%.
    - V2 no-US-math (each): the model's computeTax does not call computeFederalTax/computeUSTax; differs materially from computeUSTax for the same gross.
    - V2 local-salary (each): computeSalary returns a sourced local figure (PT SWE ~€42k, DE SWE ~€65k, CA SWE ~C$95k), NOT BASE_SALARIES×costIndex.
  </behavior>
  <action>
    First write failing V1/V2 fixtures in financial.test.ts (one describe block per country, mirroring the uk-2026 blocks). Then implement, mirroring UK_FINANCIAL_MODEL: (1) PORTUGAL pt-irs-2026 — computeTax = SS 11% (deducted) + IRS progressive brackets on taxable income after the dedução específica (greater of €4,462 or actual SS = €4,950 at €45k); STANDARD regime only, NO NHR/IFICI (D-09). computeSalary from PT_LOCAL_SALARIES (sourced, e.g. SWE €42,000 cite nextleveljobs.eu). (2) GERMANY de-2026 — computeTax = employee SS ~20.6% (with the documented caps) + progressive income tax (Grundfreibetrag €12,348; a piecewise-linear approximation of the polynomial is acceptable if it lands within the ±7% band per RESEARCH line 141); Soli NOT applied to the worked example (surface via tooltip only). computeSalary from DE_LOCAL_SALARIES (SWE ~€65,000). (3) CANADA ca-on-2026 — computeTax = federal brackets + Ontario provincial (+surtax + health premium per RESEARCH; verify exact 2026 figures, Gap #6) + CPP/CPP2 + EI. computeSalary from CA_LOCAL_SALARIES (SWE ~C$95,000). Each model returns LOCAL-currency figures (index.ts handles USD conversion). Register all three in FINANCIAL_MODELS. Every bracket/rate/salary carries an inline source comment (Phase 3 D-12, V5). Document each chosen tolerance band in the fixture.
  </action>
  <verify>
    <automated>npx vitest run shared/engine/financial</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run shared/engine/financial exits 0 with PT/DE/CA V1+V2 blocks passing within documented bands.
    - FINANCIAL_MODELS has pt-irs-2026, de-2026, ca-on-2026 defined.
    - No country model body references computeFederalTax/computeUSTax (grep the model bodies).
    - PT model does NOT apply a 20% flat IFICI rate (standard regime only — D-09); grep/read confirms no IFICI math in computeTax.
    - PT_LOCAL_SALARIES / DE_LOCAL_SALARIES / CA_LOCAL_SALARIES exist with inline source citations.
    - shared/types.ts unmodified (git diff --quiet shared/types.ts).
  </acceptance_criteria>
  <done>Three country models registered + sourced local salaries; V1 (banded) + V2 (no-US-math) GREEN; PT standard-regime only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append Lisbon/Berlin/Toronto records + full 4-city V3/V4 integrity</name>
  <read_first>
    - shared/data/cities.ts (London record from 04-01 as the intl-record template; header intl-sourcing note; Phase-3 field set)
    - shared/engine/index.test.ts (London integration block from 04-01; openness V3 mechanism block from 04-02; swEngineerProfile fixture)
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (Lisbon lines 47-62, Berlin 68-82, Toronto 88-101 records + sourced fields; Gap #8 Phase-3 City fields to populate)
    - .planning/phases/04-international-destinations-country-models/04-CONTEXT.md (D-05 never strand; result-set size invariant)
  </read_first>
  <behavior>
    - V4 (RED first): rankCities output contains all four — "Lisbon, Portugal", "Berlin, Germany", "Toronto, Canada", "London, UK" — alongside US cities.
    - V3 full: at opennessToAbroad=0, ALL FOUR intl cities are still present (results.length === CITIES_DATA.length) AND each has matchScore > 0 (demoted, never stranded — D-05/D-01).
    - V3 monotonic: each of the four intl cities' matchScore is non-decreasing as openness rises 0 -> max.
    - USD-canonical sanity: each intl estSalary is in a plausible USD band (local-salary × FX), not a US BASE×costIndex scale and not raw local.
    - Invariant: every matchScore in [0,99] integer, no NaN in any financial field with all four intl cities present; clamp/sanitizeProfile hold.
  </behavior>
  <action>
    First write failing tests in index.test.ts (a `rankCities — all 4 intl cities (V3 full + V4)` describe block) per behavior. Then append the Lisbon (financialModelId pt-irs-2026), Berlin (de-2026), and Toronto (ca-on-2026) records to CITIES_DATA, each with country set, all sourced scoring fields from RESEARCH, and all Phase-3 City fields populated (summerHighF/winterLowF/nearMountains/nearCoast/hasIntlAirport/pop/climate/stateTax=0 — Gap #8; source any shown on screen). medianRent/medianHome stored USD-canonical (matching the cities.ts header note from 04-01). Run the full suite to confirm the openness multiplier (Plan 02) and USD canonicalization (Plan 01) carry these three cities for free.
  </action>
  <verify>
    <automated>npx vitest run shared/engine</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run shared/engine exits 0 (full engine suite green; no Phase 3 / Plan 01 / Plan 02 regression).
    - rankCities output contains Lisbon, Portugal / Berlin, Germany / Toronto, Canada / London, UK.
    - At openness=0: all four intl cities present with matchScore > 0; results.length === CITIES_DATA.length.
    - Each intl city's matchScore monotonic non-decreasing with openness.
    - cities.ts gained exactly 3 new records (Lisbon/Berlin/Toronto) vs post-04-01 state.
  </acceptance_criteria>
  <done>All four golden-path intl cities rank, demote-not-strand at openness=0; full V3/V4 GREEN; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static intl data (3 city records, 3 local-salary datasets, 3 tax models) -> engine math | Author-controlled constants; risk is garbage/NaN propagation + stale/incorrect tax figures (esp. DE polynomial approx, CA surtax — Gaps #5/#6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-06 | Information disclosure | DE/CA tax approximations materially wrong, mislead user/judge | mitigate | Worked-example fixtures with documented tolerance bands (DE/CA ±7%) lock the math to cited authorities (V1); inline source comments (V5); [VERIFY] figures flagged in SUMMARY for pre-pitch confirmation |
| T-4-07 | Information disclosure | PT take-home inflated by silently applying NHR/IFICI 20% flat | mitigate | D-09 enforced: standard ~34% regime only in computeTax; IFICI surfaced via tooltip (Plan 03), never in math; acceptance criterion greps for no IFICI math |
| T-4-01 | Tampering/DoS | NaN from intl costIndex/salary into financial math with 3 more cities | mitigate | costIndex guard + clamp/sanitizeProfile asserted to hold with all 4 intl cities (Task 2 no-NaN, score in [0,99]) |
| T-4-SC | Tampering | npm/pip package installs | N/A | No new packages — data + pure functions |
</threat_model>

<verification>
- V1 (per-country, banded): PT ±5%, DE ±7%, CA ±7% worked examples green via FINANCIAL_MODELS dispatch.
- V2 (no-US-math): each foreign model avoids computeUSTax/computeFederalTax; local salary from sourced dataset.
- V3 full: all 4 intl present at openness=0 with score>0; monotonic.
- V4: all four cities ranked; clamp/sanitize hold; no NaN.
- V5: every figure sourced inline.
- V6: no fetch//api in financial.ts (offline).
- No regression: full npx vitest run green.
</verification>

<success_criteria>
- Lisbon, Berlin, Toronto, London all appear in ranked results (SC#1, MATCH-02).
- Each uses a country-correct model, plausible take-home (SC#2, FIN-02).
- Non-remote intl salaries sourced local; PT standard-regime only (D-01/D-09).
- All four demote-not-strand at openness=0 (D-05/D-01).
- Every figure sourced/citable (SC#3, V5).
</success_criteria>

<output>
Create .planning/phases/04-international-destinations-country-models/04-04-SUMMARY.md when done. Record: chosen tolerance bands per country, the DE income-tax approximation method, any [VERIFY] figures still pending pre-pitch (DE net, CA surtax/health premium, costIndex/medianHome per Gaps #1/#2/#5/#6), and confirmation PT take-home excludes IFICI.
</output>

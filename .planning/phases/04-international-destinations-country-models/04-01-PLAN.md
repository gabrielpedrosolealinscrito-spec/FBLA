---
phase: 04-international-destinations-country-models
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shared/engine/fx.ts
  - shared/engine/financial.ts
  - shared/engine/financial.test.ts
  - shared/engine/index.ts
  - shared/engine/index.test.ts
  - shared/data/cities.ts
autonomous: true
requirements: [MATCH-02, FIN-02]
user_setup: []

must_haves:
  truths:
    - "London, UK appears in rankCities() output alongside US cities"
    - "London's take-home is computed by the uk-2026 model (UK bands + NI), NOT the US federal/FICA model"
    - "A non-remote mover to London gets a sourced UK local salary, NOT BASE_SALARIES×costIndex"
    - "A remote (hasRemote=true) mover keeps profile.income in London"
    - "D-02: remote workers (hasRemote=true) keep profile.income regardless of city; local-salary data applies only to the non-remote path"
    - "D-08: the uk-2026 country model matches the US model depth (progressive national brackets + primary social contribution), registered via the FinancialModel interface + FINANCIAL_MODELS registry, no US-spine rewrite"
    - "London's MatchResult numeric fields (estSalary, monthlyTakeHome, monthlySavings) are canonical USD so MATCH-04 sort works across the mixed US+intl list"
  artifacts:
    - path: "shared/engine/fx.ts"
      provides: "Hardcoded dated FX table + toUSD helper; single source of truth for engine + display"
      contains: "FX_RATES"
    - path: "shared/engine/financial.ts"
      provides: "computeSalary added to FinancialModel interface; uk-2026 model registered in FINANCIAL_MODELS; UK_LOCAL_SALARIES sourced dataset"
      contains: "uk-2026"
    - path: "shared/data/cities.ts"
      provides: "London, UK record appended to CITIES_DATA"
      contains: "London, UK"
  key_links:
    - from: "shared/engine/index.ts buildRawResult"
      to: "FINANCIAL_MODELS[city.financialModelId].computeSalary"
      via: "model dispatch on non-remote path"
      pattern: "model\\.computeSalary"
    - from: "shared/engine/financial.ts uk-2026 model"
      to: "shared/engine/fx.ts toUSD"
      via: "convert local net to USD before returning USD-canonical figures"
      pattern: "toUSD|FX_RATES"
---

<objective>
Wire London end-to-end as the walking proof for the whole phase: append the London city record, register the `uk-2026` financial model (UK income-tax bands + National Insurance), make the `FinancialModel` interface own salary so non-remote foreign salaries come from a sourced local dataset (D-01), and lock the USD-canonical conversion so the mixed US+intl ranked list sorts correctly.

Purpose: This proves the four reusable seams the other three slices ride on — registry dispatch, the salary branch, the local->USD currency canonicalization, and intl city presence in `rankCities`. Slices 2-4 (Plan 04) are then just data + model + fixture additions.

Output: `fx.ts` (dated FX table), extended `FinancialModel` interface + `uk-2026` model + UK local-salary dataset in `financial.ts`, London record in `cities.ts`, and RED->GREEN engine tests (V1 UK fixture, V2 no-US-math invariant, V4 London-present integrity).

**UI-SPEC is intentionally absent for this phase (operator chose --skip-ui).** This plan touches the engine only; the display surface is Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-international-destinations-country-models/04-RESEARCH.md
@.planning/phases/04-international-destinations-country-models/04-CONTEXT.md
@.planning/phases/04-international-destinations-country-models/04-VALIDATION.md

<interfaces>
<!-- Verified from codebase. Executor uses these directly — no exploration needed. -->

From shared/engine/financial.ts (the interface to extend — D-01 option A):
```typescript
export interface FinancialModel {
  id: string;
  computeTax(grossIncome: number, stateRate: number): number;
  computeExpenses(profile: Profile, city: City): ExpenseBreakdown;
  // ADD: computeSalary(profile: Profile, city: City): number;  (US delegates to today's computeSalary; country models return sourced local median)
}
export const FINANCIAL_MODELS: Record<string, FinancialModel> = { us: US_FINANCIAL_MODEL };
export function computeSalary(profile: Profile, city: City): number; // US: BASE_SALARIES[profession] × costIndex/100
export function computeUSExpenses(profile: Profile, city: City): ExpenseBreakdown; // intl models reuse this for cost-indexed expenses (rent comes from city.medianRent which is already USD)
```

From shared/engine/index.ts buildRawResult (the salary seam at line ~67):
```typescript
const salaryBase = profile.hasRemote ? profile.income : computeSalary(profile, city);
// After this plan: const salaryBase = profile.hasRemote ? profile.income : model.computeSalary(profile, city);
```

From shared/types.ts (FROZEN — do NOT edit this file in this plan):
```typescript
interface City { country: string; costIndex: number; medianRent: number; medianHome: number; stateTax: number; financialModelId: string; summerHighF; winterLowF; nearMountains; nearCoast; hasIntlAirport; pop; climate; /* + others */ }
interface MatchResult { estSalary: number; monthlyTakeHome: number; monthlySavings: number; /* all USD-canonical */ }
```
</interfaces>

<currency_canonical_decision>
CRITICAL (verified against src/screens/results/ResultsView.jsx sortResults): `sortResults` compares `r.monthlySavings`, `r.estSalary` DIRECTLY across the combined US+intl list (MATCH-04, already shipped). And `computeExpenses` reads `city.medianRent` which `cities.ts` stores in **USD**. Therefore `MatchResult` numeric fields MUST be canonical USD. The model computes salary->tax->net in LOCAL currency, then converts net (and gross) to USD via the dated FX table INSIDE the engine before they enter `MatchResult`. The "convert at display only" line in RESEARCH §4 is incomplete for engine math — display (Plan 03) converts USD back to local for presentation. Assert the USER-FACING USD `monthlySavings`/`estSalary` in fixtures, not just the internal local take-home (per MEMORY: assert what the user sees).
</currency_canonical_decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dated FX table (fx.ts) — Wave 0 + D-04</name>
  <read_first>
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (FX rates line 6: EUR/USD=1.1646, CAD/USD=0.7242, GBP/USD=1.347, ECB 1 June 2026)
    - shared/engine/financial.ts (header sourcing-comment convention to mirror)
  </read_first>
  <action>
    Create `shared/engine/fx.ts` exporting `FX_RATES` (a const map keyed by currency code -> USD multiplier: EUR 1.1646, GBP 1.347, CAD 0.7242, USD 1) with the ECB source URL and "as of 2026-06-01" in the rate-as-of comment, an exported `FX_AS_OF` date-string constant, an exported `DATA_AS_OF` date-string constant set to `"2026-06-01"` (match the research compile date / `FX_AS_OF`) documenting the sourcing date of the salary/rent/tax datasets — this is the SINGLE dedicated constant the SC#4 "data as of [date]" display stamp must read from, so the stamp can never drift from an inline literal (plan-checker warning), a `currencyForCountry(country: string): "EUR"|"GBP"|"CAD"|"USD"` helper (Portugal/Germany->EUR, UK->GBP, Canada->CAD, else USD — derives currency from country so NO shared/types.ts change per D-03), and a `toUSD(amountLocal: number, currency: string): number` helper. Header comment must cite the ECB source and carry the dated stamp (D-04). This file is the single source of truth imported by both the engine (this plan) and the display (Plan 03). NO network/fetch — hardcoded only (D-04, V6).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "fx.ts" ; test $? -ne 0 && echo "fx.ts compiles clean"</automated>
  </verify>
  <acceptance_criteria>
    - `shared/engine/fx.ts` exists, exports `FX_RATES`, `FX_AS_OF`, `DATA_AS_OF`, `currencyForCountry`, `toUSD`.
    - `grep -n "export const DATA_AS_OF" shared/engine/fx.ts` matches (dedicated SC#4 data-as-of constant present, value `"2026-06-01"`).
    - `currencyForCountry("UK")` returns "GBP"; `currencyForCountry("Portugal")` returns "EUR"; `currencyForCountry("US")` returns "USD".
    - `! grep -n "fetch\|/api\|import.*http" shared/engine/fx.ts` (offline-only, V6).
    - Header carries ECB source URL + "as of 2026-06-01" (D-04 dated stamp source).
  </acceptance_criteria>
  <done>fx.ts compiles, FX rates hardcoded + dated + sourced, DATA_AS_OF constant exported for the SC#4 stamp, currency derived from country with USD fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests + uk-2026 model + UK local salaries + interface extension</name>
  <read_first>
    - shared/engine/financial.ts (FinancialModel interface line 138, US model line 148, FINANCIAL_MODELS line 160, computeSalary line 91, computeUSExpenses line 109)
    - shared/engine/financial.test.ts (existing test shape; extend, do not rewrite)
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (`uk-2026` model lines 149-153: PA £12,570; bands 20%/40%/45%; employee NI 8%/2%; worked example £60,000 -> net £45,357/yr = £3,780/mo; UK SWE salary £45k-80k)
    - .planning/phases/04-international-destinations-country-models/04-VALIDATION.md (V1 tolerance UK ±3%; V2 no-US-math invariant)
  </read_first>
  <behavior>
    - V1 (RED first): `FINANCIAL_MODELS['uk-2026'].computeTax(60000, 0)` ~= £14,643 annual (income tax £11,432 + employee NI £3,211), within ±3% of the worked example; monthly local net (60000 - tax)/12 ~= £3,780.
    - V1 dispatch: a London profile is reached via `FINANCIAL_MODELS['uk-2026']`, NOT the US fallback.
    - V2 no-US-math: `uk-2026.computeTax` does NOT call `computeFederalTax`/`computeUSTax` (assert the UK tax for £60k differs materially from `computeUSTax(60000,0)`).
    - V2 local-salary: `uk-2026.computeSalary(nonRemoteProfile, londonCity)` returns a sourced UK figure (e.g. £55k-range for Software Engineer), NOT `BASE_SALARIES['Software Engineer'] × costIndex/100`.
    - V2 remote: a `hasRemote=true` profile keeps `profile.income` (the model's computeSalary is not consulted on the remote path — that branch is in index.ts).
  </behavior>
  <action>
    First write the failing tests in `shared/engine/financial.test.ts` (a `uk-2026 model (V1)` describe block + a `no-US-math invariant (V2)` describe block) per the behavior list. Then implement to GREEN: (1) extend the `FinancialModel` interface with `computeSalary(profile: Profile, city: City): number` and add it to `US_FINANCIAL_MODEL` delegating to the existing `computeSalary` (`financial.ts` interface-only change — do NOT touch shared/types.ts). (2) Add `UK_LOCAL_SALARIES` — a sourced map keyed by profession -> annual GBP local median (cite theemployerofrecord/nextleveljobs; Software Engineer ~= £55,000, with a documented fallback for professions not listed), per Claude's-Discretion storage shape (D-01). (3) Implement `UK_FINANCIAL_MODEL` (`id: 'uk-2026'`): `computeTax` = UK bands (PA £12,570; 20% £12,571-50,270; 40% £50,271-125,140; 45% >£125,140) + employee NI (8% £12,570-50,270, 2% above) computed in GBP; `computeSalary` = `UK_LOCAL_SALARIES[profession] ?? fallback` returned in GBP (the index.ts USD conversion happens in Task 3); `computeExpenses` = delegate to `computeUSExpenses` (rent/expenses already USD-canonical in cities.ts). Register `'uk-2026': UK_FINANCIAL_MODEL` in `FINANCIAL_MODELS`. NHR/IFICI is Portugal-only and not in scope here; do NOT add special regimes. Each sourced figure carries an inline source comment (Phase 3 D-12, V5).
  </action>
  <verify>
    <automated>npx vitest run shared/engine/financial</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine/financial` exits 0 with the new V1/V2 UK describe blocks present and passing.
    - `FINANCIAL_MODELS['uk-2026']` is defined; its `computeTax(60000,0)` is within ±3% of £14,643.
    - `grep -n "computeFederalTax\|computeUSTax" shared/engine/financial.ts` shows NO reference inside the UK model body.
    - `UK_LOCAL_SALARIES` exists with inline source citations.
    - shared/types.ts is unmodified (`git diff --quiet shared/types.ts`).
  </acceptance_criteria>
  <done>uk-2026 registered, interface owns salary, UK local salaries sourced, V1+V2 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire salary dispatch + USD canonicalization + London record + V4 integrity</name>
  <read_first>
    - shared/engine/index.ts (buildRawResult lines 62-93: salaryBase line 67, model dispatch line 64, gross/tax/take-home math)
    - shared/engine/index.test.ts (swEngineerProfile fixture lines 23-45; existing rankCities describe blocks; extend, do not rewrite)
    - shared/data/cities.ts (header sourcing convention lines 1-21; an existing record e.g. Austin lines 26-51 for the full field set)
    - shared/engine/fx.ts (toUSD, currencyForCountry from Task 1)
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (London record lines 107-120: costIndex 165, medianRent 3186 USD, all sourced fields; Phase-3 City fields per Gap #8 — populate summerHighF/winterLowF/nearMountains(false)/nearCoast(true, Thames estuary ~judgment)/hasIntlAirport(true)/pop "9.0M metro"/climate "Temperate maritime"/stateTax 0)
  </read_first>
  <behavior>
    - V4 (RED first): `rankCities(londonNonRemoteProfile).results` contains a city named "London, UK".
    - USD-canonical: the London `MatchResult.estSalary` and `monthlyTakeHome` are in USD (e.g. estSalary ~= £55k × 1.347 ~= $74k, NOT £55k and NOT a US-NY-salary scale) — assert estSalary > 60000 and < 90000 (USD band proving local-salary × FX, not US BASE×costIndex which would be 110000×1.65=181500).
    - Invariant: `rankCities(profile).results.length === CITIES_DATA.length` (D-01 never strand, with London now present).
    - clamp/sanitize hold: every matchScore in [0,99] integer, no NaN in estSalary/monthlyTakeHome/monthlySavings with London in the set.
    - Remote keeps USD income: a `hasRemote=true` profile in London has `estSalary === profile.income (+partner)`, no FX applied (already USD).
  </behavior>
  <action>
    First write failing tests in `shared/engine/index.test.ts` (a `rankCities — London integration (V4)` describe block) per behavior. Then implement to GREEN: (1) In `buildRawResult`, change the non-remote salary line to `model.computeSalary(profile, city)` (D-01 dispatch). (2) Add USD canonicalization: for non-US cities, the model returns local-currency salary and `computeTax` returns local-currency tax, so convert the local net AND the local gross to USD via `toUSD(amount, currencyForCountry(city.country))` before they enter the `MatchResult` (so `estSalary`, `monthlyTakeHome`, `monthlySavings` are all USD-canonical and comparable with US cities and with the already-USD `expenses`). Remote path keeps `profile.income` (already USD) — no FX. Keep the existing US path numerically identical (US currency is USD, conversion is identity). (3) Append the London record to `CITIES_DATA` with `financialModelId: "uk-2026"`, `country: "UK"`, all sourced scoring fields from RESEARCH and all Phase-3 City fields populated (Gap #8). Add a `cities.ts` header note documenting the intl-records sourcing + that medianRent/medianHome are stored USD-canonical. Do NOT add the openness multiplier here (Plan 02 owns index.ts openness) — but note for the executor: this plan and Plan 02 both touch index.ts, so Plan 02 is Wave 2 (sequential after this).
  </action>
  <verify>
    <automated>npx vitest run shared/engine</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine` exits 0 (full engine suite green, including pre-existing Phase 3 tests — no regression).
    - rankCities output contains a city with name "London, UK".
    - London non-remote `estSalary` in (60000, 90000) USD (proves local-salary×FX, not US BASE×costIndex=181500).
    - `results.length === CITIES_DATA.length` with London present.
    - London record appended to CITIES_DATA (one new record vs pre-plan).
    - Austin reference test (estSalary ~= $113,300) still passes (no US-path regression).
  </acceptance_criteria>
  <done>London ranks with a USD-canonical country-correct net via uk-2026; full engine suite green; no US regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 quiz -> engine (`Profile`) | Already sanitized by `sanitizeProfile`/`clamp` at index.ts entry; typed, no free-text into math |
| Static intl data (`cities.ts`, `UK_LOCAL_SALARIES`, `FX_RATES`) -> engine math | Author-controlled constants; risk is garbage/NaN propagation and stale-but-authoritative figures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering/DoS | NaN/garbage from intl costIndex/salary/FX into financial math | mitigate | Existing `costIndex>0` guard in `computeUSExpenses` (reused by uk-2026) + `clamp`/`sanitizeProfile` at index.ts entry must still hold with London present — asserted in Task 3 (no NaN, score in [0,99]) |
| T-4-02 | Repudiation/Info | Stale/incorrect UK tax or FX figures presented as authoritative | mitigate | Sourced/cited standard (inline source comments, Phase 3 D-12, V5) + dated FX stamp (D-04) + dedicated `DATA_AS_OF` constant for the salary/rent/tax sourcing date; worked-example fixture (±3%) locks the math against the cited authority |
| T-4-SC | Tampering | npm/pip package installs | N/A | No new packages this phase — vitest already present, FX hardcoded, no install task |
</threat_model>

<verification>
- V1 (UK fixture, ±3%): `npx vitest run shared/engine/financial` — uk-2026 worked example green.
- V2 (no-US-math invariant): foreign model never calls computeUSTax/computeFederalTax; non-remote London salary from UK_LOCAL_SALARIES not BASE×costIndex; remote keeps profile.income.
- V4 (ranking integrity): London present, result-set size == CITIES_DATA.length, clamp/sanitize hold.
- V6 (offline): `! grep -rn "fetch(\|/api" shared/engine/fx.ts shared/engine/financial.ts`.
- No regression: full `npx vitest run shared/engine` green (Phase 3 US tests untouched).
</verification>

<success_criteria>
- London, UK appears in rankCities() output (SC#1, MATCH-02).
- London take-home computed by uk-2026 (UK bands + NI), not US model (SC#2, FIN-02).
- Non-remote London salary is sourced UK local (D-01); remote keeps income (D-02).
- MatchResult numeric fields USD-canonical so MATCH-04 sort works across mixed list.
- Every uk-2026 + London figure carries an inline source (SC#3, V5).
</success_criteria>

<output>
Create `.planning/phases/04-international-destinations-country-models/04-01-SUMMARY.md` when done. Record: the salary-interface choice (option A confirmed), the USD-canonical conversion point in buildRawResult, the chosen V1 tolerance band for UK, and any [VERIFY] figures still pending pre-pitch.
</output>

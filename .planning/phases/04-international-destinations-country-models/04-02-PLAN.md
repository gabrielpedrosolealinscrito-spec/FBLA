---
phase: 04-international-destinations-country-models
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - shared/engine/index.ts
  - shared/engine/scoring-weights.ts
  - shared/engine/index.test.ts
autonomous: true
requirements: [MATCH-02]
user_setup: []

must_haves:
  truths:
    - "opennessToAbroad is normalized to a 0-1 factor regardless of input scale (0-100 OR 1-5)"
    - "International cities are SOFT-demoted (score multiplied down) at low openness, never filtered out"
    - "At openness=0 every international city is still present with score > 0 (never strand — D-01/D-05)"
    - "At max openness international cities receive full weight (no demotion)"
    - "A given intl city's score is monotonic non-decreasing as openness rises"
    - "The misleading 'country filtering' comment at index.ts:109 is removed/corrected"
    - "D-07: citizenship/visa-feasibility deferred to Phase 7; ranking uses opennessToAbroad only (no citizenship logic in the matching/financial layer)"
  artifacts:
    - path: "shared/engine/scoring-weights.ts"
      provides: "Openness multiplier coefficients (tunable, centralized per Phase 3 D-03)"
      contains: "OPENNESS"
    - path: "shared/engine/index.ts"
      provides: "normalizeOpenness + soft multiplier applied to intl city scores in buildRawResult"
      contains: "normalizeOpenness"
  key_links:
    - from: "shared/engine/index.ts buildRawResult"
      to: "scoring-weights.ts OPENNESS coefficients"
      via: "soft multiplier on intl-city rawScore before clamp"
      pattern: "OPENNESS|normalizeOpenness"
---

<objective>
Activate `opennessToAbroad` (currently a no-op at index.ts:109) as a SOFT MULTIPLIER on international cities' scores: low openness heavily demotes intl cities but never removes them; high openness gives full weight. Normalize the input defensively so it works whether Phase 2 ships a 0-100 slider or a 1-5 button scale (D-06). Coefficients live in centralized tunable config (Phase 3 D-03).

Purpose: This is the MATCH-02 demotion-but-never-strand mechanism (D-05) and one of the two headline assertions. It upholds Phase 3 D-01 (never strand the user) and mirrors the dealbreaker = penalty-not-delete philosophy.

Output: `normalizeOpenness` + soft-multiplier wiring in `buildRawResult`, `OPENNESS` coefficients in `scoring-weights.ts`, corrected docstring, and RED->GREEN tests (V3 multiplier + scale-defensive normalizer).

**Sequenced after Plan 01:** this plan and Plan 01 both edit `shared/engine/index.ts` + `index.test.ts`, so this is Wave 2 (runs after 04-01 lands). UI-SPEC intentionally absent (engine-only plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-international-destinations-country-models/04-RESEARCH.md
@.planning/phases/04-international-destinations-country-models/04-CONTEXT.md
@.planning/phases/04-international-destinations-country-models/04-01-SUMMARY.md

<interfaces>
<!-- Verified from codebase. -->

From shared/engine/index.ts buildRawResult (lines 80-92 — where rawScore is produced and clamped):
```typescript
const { rawScore, scoreFactors } = scoreCity(profile, city);
const matchScore = clamp(Math.round(rawScore), 0, 99);
// Openness multiplier applies HERE: for non-US cities, scale rawScore by the normalized openness factor BEFORE clamp.
// Apply in buildRawResult so BOTH passes (raw + penalized) and checkReconfirm inherit it consistently.
```

From shared/engine/index.ts:108-111 (the misleading comment to rewrite — D-05 overrides it):
```
* D-01: NEVER filter cities out — the result set is always CITIES_DATA.length.
* opennessToAbroad === 0 is US-only; ... (Phase 4 will add country filtering before this function if needed).
```

From shared/types.ts (FROZEN — do NOT edit): `opennessToAbroad: number; // QUIZ-02: 0–100 slider` (Phase 2 may change to 1–5 — coordinate, do not assume range).

From shared/engine/scoring-weights.ts: `SCORING_WEIGHTS` const object (add OPENNESS section here, D-03 — no inline magic numbers).
</interfaces>

<normalizer_contract>
`normalizeOpenness(raw: number): number` must return a 0-1 factor and be SCALE-DEFENSIVE (D-06). Do NOT hardcode `/100`. Suggested approach: detect/handle both ranges — values in [0,5] treated as a 1-5 (or 0-5) scale, values >5 treated as a 0-100 scale; clamp output to [0,1]; NaN/undefined -> a safe default (e.g. treat missing as mid or full openness — document the choice). Assert it produces a sane 0-1 for BOTH a 0-100 input and a 1-5 input (V3).
</normalizer_contract>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests + normalizeOpenness + OPENNESS config</name>
  <read_first>
    - shared/engine/index.ts (buildRawResult 62-93, rankCities docstring 96-111)
    - shared/engine/index.test.ts (swEngineerProfile fixture; existing describe blocks; London integration from 04-01)
    - shared/engine/scoring-weights.ts (SCORING_WEIGHTS structure 26-61; D-03 centralization rule)
    - .planning/phases/04-international-destinations-country-models/04-RESEARCH.md (§3 Openness multiplier lines 27-31; Validation V3 lines 182-186)
    - .planning/phases/04-international-destinations-country-models/04-CONTEXT.md (D-05 soft multiplier never filter; D-06 defensive normalizer)
  </read_first>
  <behavior>
    - normalizeOpenness scale-defensive: normalizeOpenness(0) == 0; normalizeOpenness(100) close to 1; normalizeOpenness(1) low (~0); normalizeOpenness(5) close to 1; output always in [0,1]; NaN/undefined -> documented safe default.
    - Monotonic: normalizeOpenness is non-decreasing across its valid input range.
  </behavior>
  <action>
    First write failing unit tests in `shared/engine/index.test.ts` (a `normalizeOpenness (V3 scale-defensive)` describe block) per behavior. Then: (1) add an `OPENNESS` section to `SCORING_WEIGHTS` in `scoring-weights.ts` with tunable coefficients — at minimum a `minMultiplier` (the demotion floor applied to intl scores at openness=0, e.g. 0.35 so intl cities stay visible but clearly demoted) and an optional `maxMultiplier` (1.0, or slightly >1 for a slight boost at full openness per D-05). Document each with a comment (D-03, no inline magic numbers). (2) Implement and export `normalizeOpenness(raw: number): number` in `index.ts` per the normalizer_contract (scale-defensive, no hardcoded /100, clamp [0,1], safe NaN default). Do NOT wire it into buildRawResult yet — that is Task 2.
  </action>
  <verify>
    <automated>npx vitest run shared/engine/index</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine/index` exits 0 with the new normalizeOpenness V3 block passing.
    - `normalizeOpenness` is exported from index.ts; handles 0, 1, 5, 100 and NaN per behavior.
    - `SCORING_WEIGHTS.OPENNESS` (or equivalent) exists in scoring-weights.ts with documented coefficients — `grep -n "OPENNESS" shared/engine/scoring-weights.ts` non-empty.
    - No openness magic numbers inlined in index.ts (`grep -n "0.35\|0\\.[0-9]" shared/engine/index.ts` shows multiplier values come from config, not literals in buildRawResult).
  </acceptance_criteria>
  <done>normalizeOpenness scale-defensive + OPENNESS coefficients centralized; V3 normalizer GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply soft multiplier to intl scores + rewrite filtering comment + V3 mechanism test</name>
  <read_first>
    - shared/engine/index.ts (buildRawResult 62-93 — rawScore/clamp lines 80-82; rankCities docstring 96-111)
    - shared/engine/index.test.ts (London integration block from 04-01; swEngineerProfile fixture)
    - .planning/phases/04-international-destinations-country-models/04-CONTEXT.md (D-05: soft multiplier, NEVER filter; invariant result-set size == CITIES_DATA.length)
  </read_first>
  <behavior>
    - Mechanism on London (the one intl city present until Plan 04 lands): rankCities with openness=0 -> London still present AND London.matchScore > 0 (demoted, not stranded).
    - Full weight: rankCities with openness=max (100 or 5) -> London.matchScore >= London.matchScore at openness=0 (no penalty at full openness).
    - Monotonic: London.matchScore is non-decreasing across openness 0 -> mid -> max.
    - US cities unaffected: a US city's matchScore is identical regardless of openness (multiplier applies to intl only).
    - Invariant preserved: results.length === CITIES_DATA.length at every openness level (D-01); matchScores in [0,99] integer; no NaN.
  </behavior>
  <action>
    First write failing tests in `shared/engine/index.test.ts` (an `openness soft multiplier (V3 mechanism)` describe block) per behavior, using London as the intl probe. Then implement: in `buildRawResult`, for non-US cities (`city.country !== 'US'`), scale `rawScore` by a multiplier interpolated between `OPENNESS.minMultiplier` and `OPENNESS.maxMultiplier` using `normalizeOpenness(profile.opennessToAbroad)` BEFORE `clamp(Math.round(...), 0, 99)`. Apply it inside buildRawResult so both the raw and penalized passes AND checkReconfirm inherit it consistently. US cities are untouched (identity). Then rewrite the misleading `rankCities` docstring at lines ~108-111: remove the "Phase 4 will add country filtering" sentence; state that openness is a SOFT multiplier on international city scores, never a filter, and the result set stays CITIES_DATA.length (D-05). NOTE for executor: the full V3 "all 4 intl cities present at openness=0" assertion can only be completed once Plan 04 adds Lisbon/Berlin/Toronto — this plan proves the mechanism on London; Plan 04's wave-completion check covers all four.
  </action>
  <verify>
    <automated>npx vitest run shared/engine</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine` exits 0 (full engine suite, no Phase 3 regression).
    - At openness=0, London is present in results AND has matchScore > 0.
    - London matchScore at openness=max >= matchScore at openness=0; monotonic across levels.
    - A US city's matchScore is unchanged across openness values.
    - `results.length === CITIES_DATA.length` at openness 0 and max.
    - `grep -n "country filtering" shared/engine/index.ts` returns nothing (misleading comment removed).
  </acceptance_criteria>
  <done>Soft openness multiplier live on intl scores, never filters; misleading comment corrected; V3 mechanism GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 quiz -> engine (`opennessToAbroad`) | Untyped-range scalar (0-100 today, maybe 1-5 later); normalizer must defend against both + NaN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-03 | Tampering/DoS | Garbage/out-of-range openness (NaN, negative, scale mismatch) amplifying or zeroing intl scores | mitigate | `normalizeOpenness` clamps to [0,1] + safe NaN default; multiplier floor (`minMultiplier`) guarantees intl score > 0 at openness=0 (never strand) — asserted in Task 2 |
| T-4-SC | Tampering | npm/pip package installs | N/A | No new packages — pure config + engine logic |
</threat_model>

<verification>
- V3 normalizer: scale-defensive (0-100 AND 1-5), output [0,1], NaN-safe.
- V3 mechanism: soft demote-not-strand on London; monotonic; US cities unaffected; result-set size invariant.
- No regression: full `npx vitest run shared/engine` green.
- Filtering comment removed (D-05 correction).
</verification>

<success_criteria>
- opennessToAbroad activated as a soft multiplier on intl scores (MATCH-02, D-05).
- Never filters/strands — result set stays CITIES_DATA.length at any openness (D-01).
- Normalizer scale-defensive for 0-100 and 1-5 (D-06).
- Coefficients centralized in scoring-weights.ts (D-03).
</success_criteria>

<output>
Create `.planning/phases/04-international-destinations-country-models/04-02-SUMMARY.md` when done. Record: chosen minMultiplier/maxMultiplier values, the normalizer's scale-detection heuristic, and confirmation the full 4-city V3 assertion is deferred to Plan 04 wave-completion.
</output>

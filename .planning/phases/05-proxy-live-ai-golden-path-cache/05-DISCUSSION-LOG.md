# Phase 5: Proxy, Live AI & Golden-Path Cache - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-03
**Phase:** 5-proxy-live-ai-golden-path-cache
**Areas discussed:** Fallback layering, Live-load trigger & UX, Golden-path cache scope, Category scope, Cache tell, Listing links

---

## Pre-discussion: branch reconciliation

Discovered the working branch (`integrate/quiz-engine`) was missing the Phase-3/4 backend (no intl cities, no tested `shared/engine/`). Phase 5 depends on Phase 4. Found the frontend↔backend merge already done on `reconcile/v1` (123 tests pass, build green). **User chose: promote `reconcile/v1` to trunk.** Discussion + CONTEXT.md done on `reconcile/v1` via its worktree; physical trunk consolidation deferred to a separate pass (touches live Vercel deploy on `main`). Fixed a latent trap: `node_modules` symlink in the worktree wasn't matched by the `node_modules/` ignore pattern — added bare `node_modules` to `.gitignore`.

---

## Fallback layering

| Option | Description | Selected |
|--------|-------------|----------|
| Both layers | Client-bundled cache (instant render if proxy unreachable, SC4) + proxy-embedded same JSON (SC5). Single source file. ~7s client timeout, fallback on any failure. | ✓ |
| Client-only fallback | Only the client bundles the cache; proxy stays thin. Simpler, still satisfies all 5 criteria. | |

**User's choice:** Both layers.
**Notes:** Single `data/golden-path/demo-results.json` imported both sides.

---

## Live-load trigger & UX

| Option | Description | Selected |
|--------|-------------|----------|
| Explicit "go live" button | Presenter clicks "Pull live data"; judges watch listings stream in. Parallel fan-out, per-category skeletons. | ✓ |
| Auto-fetch on city open | Seamless/product-realistic, but less dramatic and risks a spinner stall on stage. | |

**User's choice:** Explicit button.

---

## Golden-path cache scope

| Option | Description | Selected |
|--------|-------------|----------|
| Demo persona + 2 cities | Rehearsed profile × (#1 US match + 1 intl, Lisbon assumed). Generated by a capture script calling the real proxy. | ✓ |
| 1 city only | Just the #1-match city. Less to maintain, no on-stage pivot backup. | |
| Persona + 3 cities | Top US + 2 intl, max flexibility, more to keep fresh. | |

**User's choice:** Demo persona + 2 cities.
**Notes:** Generation via capture script (snapshot real proxy output), not hand-authored.

---

## Category scope

| Option | Description | Selected |
|--------|-------------|----------|
| The 3 required | jobs, housing (rent/buy by preference), dayinlife. Demo-tight, satisfies LIVE-01/02/03. | ✓ |
| All 7 categories | + nightlife, outdoors, food. Richer panel, more to build/cache/de-risk. | |

**User's choice:** The 3 required. Defer nightlife/outdoors/food.

---

## Cache tell (on-stage)

| Option | Description | Selected |
|--------|-------------|----------|
| Seamless / invisible | Cached renders identically to live, no badge. `fromCache` internal-only. Matches SC4. | ✓ |
| Subtle honest indicator | Small "cached · as of date" tag. More Q&A-defensible but shows the seam. | |

**User's choice:** Seamless / invisible.

---

## Listing links

| Option | Description | Selected |
|--------|-------------|----------|
| Show source name, no live link | Title + salary/rent + source name as styled text, not an anchor. Honors see-not-click rule. | ✓ |
| Render clickable links | Real anchors; violates competition rule, misclick risk. | |

**User's choice:** Show source name, no live link.

---

## Claude's Discretion

- Anthropic model choice for live calls (cost/latency; Haiku default pending web_search support confirmation).
- web_search config, per-category prompt design, LLM-output validation schema.
- Exact client timeout (~7s starting budget, tune to real hotspot latency).
- Skeleton/loading visual treatment (reuse existing dark-theme components).

## Deferred Ideas

- nightlife / outdoors / food live categories → later polish phase.
- Trunk consolidation (collapse reconcile/v1 → main, remove redundant worktrees, triage loose untracked files, reconcile Vercel auto-deploy) → separate git pass.
- Tier-gating the live layer → Phase 8 (TIER work).

## Open dependency

- The rehearsed demo persona must be finalized before the capture script can snapshot the cache.

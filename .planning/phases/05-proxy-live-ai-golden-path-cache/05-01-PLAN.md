---
phase: 05-proxy-live-ai-golden-path-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - data/golden-path/demo-results.json
  - data/golden-path/demo-profile.json
  - tests/live-validation.test.ts
  - tests/live-fallback.test.ts
  - tests/live-proxy.test.ts
autonomous: true
requirements: [FOUND-04, LIVE-01, LIVE-02, LIVE-03, LIVE-04]
user_setup: []

must_haves:
  truths:
    - "The golden-path cache file exists with valid-shaped content for the 2 demo cities (keyed by the full city.name strings 'Austin, TX' and 'Lisbon, Portugal') across all 3 shipped categories"
    - "The three Wave 0 test files exist and run (red until Plan 02/03 ship the modules they import)"
    - "TypeScript in api/ can statically import the golden-path JSON without a compile error"
  artifacts:
    - path: "data/golden-path/demo-results.json"
      provides: "Bundled offline fallback — single source for client and proxy (D-01)"
      contains: "jobs"
    - path: "data/golden-path/demo-profile.json"
      provides: "Pinned demo persona the capture script reads (D-06/D-07 open dependency)"
    - path: "tests/live-validation.test.ts"
      provides: "RED test for validateItems (LIVE-01/02/03)"
    - path: "tests/live-fallback.test.ts"
      provides: "RED test for fetchCategoryLive fallback (FOUND-04/LIVE-04)"
    - path: "tests/live-proxy.test.ts"
      provides: "RED test for proxy handler fromCache fallback (LIVE-04)"
    - path: "tsconfig.json"
      provides: "resolveJsonModule:true so api/*.ts can import the cache JSON"
      contains: "resolveJsonModule"
  key_links:
    - from: "tests/live-validation.test.ts"
      to: "api/live-core.ts"
      via: "import { validateItems }"
      pattern: "from ['\"].*live-core"
    - from: "tests/live-proxy.test.ts"
      to: "api/live.ts"
      via: "import handler with mocked @anthropic-ai/sdk"
      pattern: "live"
---

<objective>
Stand up the Phase 5 foundation: add the Anthropic SDK dependency, enable JSON imports in the api/ TypeScript compile, create the golden-path cache directory with a valid-shaped stub for both demo cities, pin the demo persona, and author the three Wave 0 test files (RED) that define the module contracts Plans 02 and 03 implement against.

Purpose: This is the MVP "first failing test" slice. The test files lock the exported signatures of `validateItems` (api/live-core.ts) and `fetchCategoryLive` (src/lib/fetchLive.js) BEFORE those modules exist, so downstream plans implement against a fixed contract instead of exploring. The stub cache makes the offline-fallback path renderable and gives every fallback test a real fixture to assert against.
Output: package.json (+@anthropic-ai/sdk), tsconfig.json (+resolveJsonModule), data/golden-path/{demo-results.json, demo-profile.json}, three test files.
</objective>

## Phase Goal

> **MVP user-story discrepancy (surfaced, not fabricated):** MVP_MODE is on, which calls for a `**As a** … **I want to** … **so that** …` user story sourced from the ROADMAP `**Goal:**` line. The Phase 5 ROADMAP goal — "the live AI data layer works via a backend proxy that never exposes the API key, and the bundled golden-path cache has been tested as the actual offline fallback" — is **infrastructural** and is not in user-story form. Per the MVP rules the planner must NOT invent a story for it. Recommendation: if a strict user-story header is required, run `/gsd mvp-phase 5` to author one. The closest user-facing framing (informational only, not a substitute): *As a Plus-tier user, I want to pull real current jobs/housing/day-in-the-life for a city and still see content instantly when the network drops, so the result feels live and never breaks on stage.* The plans below remain vertical slices (proxy→validate→render→fallback), satisfying the spirit of MVP_MODE.


<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-VALIDATION.md

<interfaces>
<!-- The live contract from shared/types.ts — implement against it, do not redesign. -->

From shared/types.ts:
```typescript
export type LiveCategory =
  | "jobs" | "housing_rent" | "housing_buy"
  | "nightlife" | "outdoors" | "food" | "dayinlife";

export interface LiveDataRequest { category: LiveCategory; cityName: string; profession?: string; age?: number; }

export interface LiveDataResponse<T = unknown> {
  category: LiveCategory;
  cityName: string;
  fromCache: boolean;   // true when served from the offline golden-path cache (LIVE-04)
  items: T;             // array of listings, or a narrative string for "dayinlife"
}
```

Item shapes (from 05-RESEARCH.md Pattern 4 — golden-path cards omit image AND url; D-10 renders `source` as text):
- JobListing: { title, company, salary?, desc?, source }   — all string; salary/desc optional
- HousingListing: { neighborhood? | address? | name?, price, beds, sqft?, feature?, source }
- dayinlife: a single narrative string (NOT an array)

<!-- CITY-KEY CONTRACT (load-bearing): the golden-path cache is keyed by the FULL runtime city.name string, NOT a bare token. Confirmed in shared/data/cities.ts: `name: "Austin, TX"` and `name: "Lisbon, Portugal"`. The proxy (api/live.ts) and client (src/lib/fetchLive.js) both do goldenPath[category][city.name], so the JSON top-level city keys MUST be "Austin, TX" / "Lisbon, Portugal". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SDK dependency + enable JSON imports in api/ TypeScript</name>
  <read_first>
    - package.json (current deps: react, react-dom only; scripts incl. `test`, `dev:full`)
    - tsconfig.json (moduleResolution "bundler", no resolveJsonModule, include shared+api)
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md (Pitfall 5; Package Legitimacy Audit — @anthropic-ai/sdk is Approved)
  </read_first>
  <action>
    Run `npm install @anthropic-ai/sdk` to add it as a production dependency (research confirms ~0.100.x; it is Approved in the Package Legitimacy Audit via npm registry + official Anthropic docs, so NO blocking install checkpoint is required). Then add `"resolveJsonModule": true` to `compilerOptions` in tsconfig.json so `api/live.ts` can statically `import goldenPath from '../data/golden-path/demo-results.json'` under `moduleResolution: "bundler"` (per Pitfall 5). Do not change other tsconfig keys. Do not add the SDK to client (src/) code — it is api/-only per the Architectural Responsibility Map.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['@anthropic-ai/sdk']) process.exit(1)" && node -e "const t=require('./tsconfig.json'); if(t.compilerOptions.resolveJsonModule!==true) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` contains `@anthropic-ai/sdk`
    - tsconfig.json compilerOptions contains `"resolveJsonModule": true`
    - The SDK does not appear in any src/ import (grep src/ for `@anthropic-ai/sdk` returns nothing)
  </acceptance_criteria>
  <done>SDK installed as a prod dep; resolveJsonModule enabled; client bundle untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create golden-path cache dir, valid-shaped stub, and pinned demo persona</name>
  <read_first>
    - shared/types.ts (LiveDataResponse / LiveCategory contract — the `interfaces` block above)
    - shared/data/cities.ts (CONFIRM the city.name strings: `name: "Austin, TX"`, `name: "Lisbon, Portugal"` — these are the EXACT golden-path top-level keys)
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md (Golden-Path Cache Design — file structure; Open Question 1 demo persona; A3 Austin+Lisbon assumption)
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-CONTEXT.md (D-06 demo persona × 2 cities; D-09 ship 3 categories)
  </read_first>
  <action>
    Create directory `data/golden-path/`. Write `data/golden-path/demo-results.json` as a VALID-SHAPED stub (NOT `{}` — an empty object renders blank offline and defeats FOUND-04/SC4 and leaves fallback tests asserting nothing). Top-level keys: `jobs`, `housing_rent`, `housing_buy`, `dayinlife` (the 3 shipped categories per D-09 — housing ships BOTH rent and buy keys so the offline fallback is correct regardless of `demo-profile.json` `housing` preference; omit nightlife/outdoors/food — deferred). Under each, the city keys MUST be the EXACT runtime `city.name` full strings — `"Austin, TX"` and `"Lisbon, Portugal"` (NOT the bare tokens "Austin"/"Lisbon"). This is load-bearing: at runtime the proxy and client look up `goldenPath[category][city.name]`, where `city.name` is the full "City, ST"/"City, Country" string from shared/data/cities.ts (confirmed: `name: "Austin, TX"`, `name: "Lisbon, Portugal"`). Bare-token keys would resolve to `undefined → []` on every offline fallback, voiding SC4/SC5/LIVE-04 — the phase's core deliverable. (D-06; "Austin, TX" is the assumed #1 US match per RESEARCH A3 — flag in SUMMARY that the real #1 is pinned later.) `jobs`/`housing_rent`/`housing_buy` values are arrays of 3-4 placeholder objects each matching the item shapes (JobListing has title/company/salary/desc/source; HousingListing has neighborhood/price/beds/sqft/feature/source — `housing_buy` prices reflect purchase, `housing_rent` reflect monthly rent); `dayinlife` values are a single narrative string per city. Include NO `image` and NO `url` fields anywhere (Pitfall 4 — images break D-08 offline parity; D-10 forbids url). Also write `data/golden-path/demo-profile.json` pinning the rehearsed persona the capture script reads: fields `profession`, `age`, `housing`, plus `cities: ["Austin, TX","Lisbon, Portugal"]` (the full `city.name` strings the capture script sends to the proxy and keys the snapshot by). Note in the file or SUMMARY that the persona is provisional until pinned against the demo script (D-06/D-07 open dependency).
  </action>
  <verify>
    <automated>node -e "const d=require('./data/golden-path/demo-results.json'); const cats=['jobs','housing_rent','housing_buy','dayinlife']; for(const c of cats){ if(!d[c]||!d[c]['Austin, TX']||!d[c]['Lisbon, Portugal']) process.exit(1);} if(!Array.isArray(d.jobs['Austin, TX'])||!Array.isArray(d.housing_buy['Austin, TX'])||typeof d.dayinlife['Austin, TX']!=='string') process.exit(1); const s=JSON.stringify(d); if(s.includes('\"url\"')||s.includes('\"image\"')) process.exit(1); const p=require('./data/golden-path/demo-profile.json'); if(!Array.isArray(p.cities)||!p.cities.includes('Austin, TX')||!p.cities.includes('Lisbon, Portugal')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - data/golden-path/demo-results.json has jobs/housing_rent/housing_buy/dayinlife each populated under the FULL-NAME keys "Austin, TX" AND "Lisbon, Portugal"
    - jobs[city], housing_rent[city], and housing_buy[city] are arrays of objects with a `source` string field; dayinlife[city] is a string
    - No `url` or `image` key appears anywhere in the JSON
    - data/golden-path/demo-profile.json exists with profession/age/housing and `cities: ["Austin, TX","Lisbon, Portugal"]` (full city.name strings)
  </acceptance_criteria>
  <done>Valid-shaped offline fallback fixture exists keyed by the full city.name for both demo cities; demo persona pinned (provisional).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the three Wave 0 test files (RED — define module contracts)</name>
  <read_first>
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-VALIDATION.md (Wave 0 Requirements; Per-Task Verification Map — exact test responsibilities)
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md (Pattern 4 validateItems guards; Pattern 1 proxy fromCache; Pattern 3 client fallback; Pitfall 7 dayinlife)
    - data/golden-path/demo-results.json (the fixture these tests assert against — created in Task 2; keyed by "Austin, TX"/"Lisbon, Portugal")
    - shared/types.ts (LiveCategory union the tests cover)
  </read_first>
  <behavior>
    tests/live-validation.test.ts — imports `validateItems` from `api/live-core.ts`:
      - validateItems('jobs', [valid JobListing]) returns the array
      - validateItems('jobs', [{ missing source }]) throws
      - validateItems('housing_rent', [valid HousingListing]) returns the array
      - validateItems('dayinlife', 'a narrative string') returns the string
      - validateItems('dayinlife', []) throws (Pitfall 7 — array is wrong shape)
      - validateItems('jobs', 'not an array') throws
    tests/live-proxy.test.ts — imports the default handler from `api/live.ts` with `@anthropic-ai/sdk` mocked so `messages.create` throws:
      - handler called with category=jobs, cityName="Austin, TX" → res.json receives `{ fromCache: true, items: <golden-path jobs["Austin, TX"]> }`, status 200 (LIVE-04 — never a 5xx with raw error). The cityName in the request body and the golden-path lookup key are the SAME full string "Austin, TX".
    tests/live-fallback.test.ts — imports `fetchCategoryLive` from `src/lib/fetchLive.js` with global `fetch` mocked to reject/throw:
      - resolves to the bundled golden-path items for that city+category (FOUND-04/LIVE-04), never undefined/blank/'coming_soon'. The mocked city object has `name: "Austin, TX"`, and the resolved items equal goldenPath[category]["Austin, TX"].
  </behavior>
  <action>
    Write the three vitest test files listed in 05-VALIDATION.md Wave 0 Requirements. Import targets that do not exist yet — `validateItems` from `api/live-core.ts` (Plan 02), the handler from `api/live.ts` (Plan 02), `fetchCategoryLive` from `src/lib/fetchLive.js` (Plan 03) — so these tests are RED now and lock the exported signatures. Use the FULL `city.name` strings ("Austin, TX" / "Lisbon, Portugal") everywhere a city is referenced — both in request bodies (`cityName: "Austin, TX"`) and in golden-path lookup assertions (`goldenPath[category]["Austin, TX"]`) — so the contract matches runtime. Mock `@anthropic-ai/sdk` and global `fetch` with vitest `vi.mock`/`vi.fn`; no real network calls. Use fixtures inline and/or imported from `data/golden-path/demo-results.json`. Do NOT author an automated live-Anthropic end-to-end test — the live happy path is manual-only (VALIDATION.md: all automated tests are pure-function/mocked); the proven-in-CI path is the FALLBACK path.
  </action>
  <verify>
    <automated>test -f tests/live-validation.test.ts && test -f tests/live-fallback.test.ts && test -f tests/live-proxy.test.ts && grep -q "live-core" tests/live-validation.test.ts && grep -q "fetchLive" tests/live-fallback.test.ts && grep -q "Austin, TX" tests/live-proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist under tests/
    - tests/live-validation.test.ts imports `validateItems` from a `live-core` path
    - tests/live-fallback.test.ts imports `fetchCategoryLive` from a `fetchLive` path
    - tests/live-proxy.test.ts imports the handler, mocks `@anthropic-ai/sdk`, and uses the full city key "Austin, TX" for both the request cityName and the golden-path lookup
    - `npm test` runs and these three suites are RED (import-unresolved / not-yet-implemented), not silently skipped — confirm in SUMMARY
  </acceptance_criteria>
  <done>Three RED Wave 0 tests exist, pinning the validateItems / handler / fetchCategoryLive contracts (with full-name city keys) for Plans 02-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → build | golden-path JSON is committed and bundled; it is trusted authored content, not runtime input |
| npm registry → repo | new dependency (@anthropic-ai/sdk) enters the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-SC | Tampering | npm install @anthropic-ai/sdk | accept | Package is Approved in 05-RESEARCH Package Legitimacy Audit via npm registry + official Anthropic docs (official @anthropics org). No `[ASSUMED]`/`[SUS]` packages → no blocking human-verify checkpoint required. |
| T-5-cache | Tampering | data/golden-path/demo-results.json | accept | Authored, committed content rendered as text downstream (no url/image, no HTML). Reviewed at commit time; not attacker-influenceable at runtime. |
</threat_model>

<verification>
- `npm test` executes; the three new suites are RED (targets not yet implemented).
- golden-path JSON parses, is valid-shaped for both cities under the full-name keys "Austin, TX"/"Lisbon, Portugal", contains no url/image keys.
- tsconfig has resolveJsonModule; package.json has the SDK.
</verification>

<success_criteria>
- SDK + resolveJsonModule in place (unblocks Plan 02 proxy compile).
- Valid-shaped offline fixture (keyed by full city.name) + pinned persona exist (unblocks Plans 03-04 and FOUND-04/SC4 render).
- Three RED Wave 0 tests lock the contracts for Plans 02-03.
</success_criteria>

<output>
Create `.planning/phases/05-proxy-live-ai-golden-path-cache/05-01-SUMMARY.md` when done. Note: whether the three suites are confirmed RED, and that "Austin, TX" is the assumed (provisional) #1 US match pending demo-persona pin.
</output>
</output>

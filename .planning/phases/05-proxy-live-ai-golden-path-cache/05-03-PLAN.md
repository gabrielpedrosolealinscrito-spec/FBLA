---
phase: 05-proxy-live-ai-golden-path-cache
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lib/fetchLive.js
  - src/screens/PotentialApp.jsx
autonomous: true
requirements: [FOUND-04, LIVE-01, LIVE-02, LIVE-03, LIVE-04]
user_setup: []

must_haves:
  truths:
    - "A single 'Pull live data' button on the city-detail panel fires all three shipped categories concurrently (D-04/D-05)"
    - "When the proxy is unreachable, each panel renders bundled golden-path content with no spinner-stuck/blank/'coming soon' state (FOUND-04/SC4)"
    - "Job and housing cards show the source name as styled text and contain no clickable <a> link (D-10)"
  artifacts:
    - path: "src/lib/fetchLive.js"
      provides: "Importable fetchCategoryLive(city, category, profession) with 20s timeout + golden-path fallback on any failure"
      exports: ["fetchCategoryLive"]
    - path: "src/screens/PotentialApp.jsx"
      provides: "Pull-live-data button, parallel fan-out, per-category skeletons, D-10 source-text cards"
      contains: "Pull live data"
  key_links:
    - from: "src/lib/fetchLive.js"
      to: "data/golden-path/demo-results.json"
      via: "import for client-bundled fallback (D-01)"
      pattern: "golden-path"
    - from: "src/screens/PotentialApp.jsx"
      to: "/api/live"
      via: "fetchCategoryLive POST"
      pattern: "fetchCategoryLive"
    - from: "src/screens/PotentialApp.jsx"
      to: "src/lib/fetchLive.js"
      via: "import { fetchCategoryLive }"
      pattern: "fetchLive"
---

<objective>
Build the client half of the live-AI vertical slice: an importable fetch+fallback function and the PotentialApp rewire that replaces the per-section expand-fetch stub with a single "Pull live data" button that fans out to all three shipped categories concurrently, shows per-category skeletons, and falls back to bundled golden-path content on any failure. Also fix the two D-10 contradictions flagged in RESEARCH: remove the clickable anchor and render the `source` name as text.

Purpose: This is the client half of the spine and the on-stage "wow moment" trigger (D-04). It satisfies FOUND-04/SC4 (instant offline render) and the see-not-click rule (D-10). It makes tests/live-fallback.test.ts GREEN.
Output: src/lib/fetchLive.js, modified src/screens/PotentialApp.jsx.
</objective>

## Phase Goal

> **MVP user-story discrepancy (surfaced, not fabricated):** MVP_MODE is on, which calls for a `**As a** … **I want to** … **so that** …` user story sourced from the ROADMAP `**Goal:**` line. The Phase 5 ROADMAP goal — "the live AI data layer works via a backend proxy that never exposes the API key, and the bundled golden-path cache has been tested as the actual offline fallback" — is **infrastructural** and is not in user-story form. Per the MVP rules the planner must NOT invent a story for it. Recommendation: if a strict user-story header is required, run `/gsd mvp-phase 5` to author one. The closest user-facing framing (informational only, not a substitute): *As a Plus-tier user, I want to pull real current jobs/housing/day-in-the-life for a city and still see content instantly when the network drops, so the result feels live and never breaks on stage.* The plans below remain vertical slices (proxy→validate→render→fallback), satisfying the spirit of MVP_MODE.


<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-01-SUMMARY.md
@src/screens/PotentialApp.jsx

<interfaces>
<!-- Integration target = src/screens/PotentialApp.jsx (the component App.jsx actually mounts; holds the AI stub). NOTE: src/screens/results/CityDetail.jsx exists on reconcile/v1 (Phase 3/4) but is NOT wired into App.jsx — it is out of scope for Phase 5. Live-AI target confirmed PotentialApp.jsx by user 2026-06-03, superseding the CityDetail.jsx reference in 05-CONTEXT.md canonical_refs. PotentialApp.jsx is byte-identical to the integrate/quiz-engine copy these line anchors were verified against. -->
- State: cityAIData / setCityAIData (line 44), aiLoading / setAiLoading (line 45), expandedSection (47).
- fetchCityAI stub (lines 72-77) — sets cityAIData[key]="coming_soon". REPLACE with real fetch via fetchCategoryLive.
- Section (164-178): the header button onClick toggles expand AND calls fetchCityAI(c, id) (line 168). REMOVE the fetch-on-expand; keep the expand toggle.
- AIList (180-201): renders loading spinner / "Tap to load real data" (191) / "error" / "coming_soon" (193-197) / string / array. Remove the dead "coming_soon" and "Tap to load" branches (button model replaces tap-to-fetch); keep loading/string/array/error.
- ItemCard (203-224): renders optional image (205-209) and a url anchor "View listing" (213-221). REMOVE both the image block and the url anchor (Pitfall 4 + D-10).
- Job card renderer (316-323) and housing card renderer (327-337): currently pass url/image and render NO source. ADD source as styled text; stop passing url/image.
- dayinlife Section (311-313): renders string via AIList — keep.
- The 3 deferred Sections nightlife/outdoors/food (340-371) — see Task 2 step 7.

<!-- CITY-KEY CONTRACT (load-bearing): the golden-path cache is keyed by the FULL runtime city.name string ("Austin, TX" / "Lisbon, Portugal"), confirmed in shared/data/cities.ts. fetchCategoryLive must look up goldenPath[category][city.name] using the full city.name verbatim (no token stripping) — this is the key that matches the JSON, the request cityName, and the proxy fallback. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/fetchLive.js — importable fetch + golden-path fallback</name>
  <read_first>
    - tests/live-fallback.test.ts (from Plan 01 — RED contract: mocked fetch throws then returns golden-path items; import path + assertions; city object has name "Austin, TX")
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md (Pattern 3 + "Client-side fetch with parallel fan-out"; Timeout Budget — 20s live, instant on network error)
    - shared/data/cities.ts (CONFIRM city.name = "Austin, TX"/"Lisbon, Portugal" — the lookup key)
    - data/golden-path/demo-results.json (the client-bundled fallback source — D-01; keyed by full city.name)
  </read_first>
  <behavior>
    - fetchCategoryLive(city, category, profession): POSTs /api/live with a 20s AbortController timeout; on res.ok resolves to data.items
    - on non-200, network error, AbortError, timeout, or thrown JSON parse → resolves to goldenPath[category][city.name] (FOUND-04/LIVE-04)
    - city.name is the FULL "City, ST"/"City, Country" string ("Austin, TX"), and that exact string is BOTH the request cityName and the golden-path lookup key (they match the JSON keys with no stripping)
    - never resolves to undefined/blank/"coming_soon"; if golden-path also lacks the city, resolves to a defined empty array (not a crash)
    - tests/live-fallback.test.ts: with global fetch mocked to reject, city={name:"Austin, TX"} → resolves to goldenPath[category]["Austin, TX"]
  </behavior>
  <action>
    Create src/lib/fetchLive.js exporting fetchCategoryLive(city, category, profession). Import the golden-path JSON from ../../data/golden-path/demo-results.json (Vite resolves JSON natively, D-01 single source). Implement a 20s AbortController timeout (NOT 7s — web_search at max_uses:3 takes 8-20s; 7s always aborts the live path, Pitfall 2). POST to /api/live with body category, cityName=city.name, profession. On res.ok return data.items. Wrap everything in try/catch and clear the timeout in finally; on ANY failure return goldenPath[category]?.[city.name] ?? []. Use `city.name` VERBATIM as the lookup key — it is the full "Austin, TX"/"Lisbon, Portugal" string and matches the JSON top-level city keys exactly; do NOT strip the state/country suffix. Keep this a pure async function with no React so live-fallback.test.ts can import it and mock fetch.
  </action>
  <verify>
    <automated>npm test -- tests/live-fallback.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/fetchLive.js exports fetchCategoryLive
    - npm test -- tests/live-fallback.test.ts exits 0 (was RED in Plan 01)
    - The golden-path lookup key is the full `city.name` (`goldenPath[category]?.[city.name]`) — resolves to the stub array for "Austin, TX" (no suffix stripping)
    - The AbortController timeout is 20000ms (grep shows 20_000 or 20000, not 7000)
    - No React/JSX in fetchLive.js (pure async)
  </acceptance_criteria>
  <done>Client fetch+fallback module green against live-fallback.test.ts; 20s live budget; full-name key lookup.</done>
</task>

<task type="auto">
  <name>Task 2: PotentialApp.jsx — Pull-live-data button, parallel fan-out, skeletons, D-10 cards</name>
  <read_first>
    - src/screens/PotentialApp.jsx (the integration points in the interfaces block — lines 44-47, 72-77, 164-201, 203-224, 305-372)
    - src/lib/fetchLive.js (Task 1 — the function this wires in)
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md (D-04/D-05 trigger rewire; Anti-Patterns; Pitfall 3 url anchor; Pitfall 4 images; D-09 category scope)
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-CONTEXT.md (D-04, D-05, D-08, D-09, D-10)
  </read_first>
  <action>
    Rewire the city-detail live layer.
    (1) Replace the fetchCityAI stub (72-77) by importing fetchCategoryLive from src/lib/fetchLive.js.
    (2) Add a single "Pull live data" button on the city-detail panel near the "Live data" header (~305-309). Its onClick fires all THREE shipped categories concurrently (D-04/D-05): jobs, then housing_rent or housing_buy per profile.housing, then dayinlife. For each: set aiLoading[key]=true, call fetchCategoryLive(selectedCity, category, profile.profession), on resolve set cityAIData[key]=items, finally clear loading — independently, so one slow/failed category never blocks another. (Pass the full selectedCity object so fetchCategoryLive keys on selectedCity.name = the full "City, ST" string.)
    (3) Remove the fetch-on-expand from the Section header onClick (line 168) — keep the expand toggle only; sections show a per-category skeleton via the existing aiLoading[key] spinner (D-05).
    (4) In AIList remove the dead "coming_soon" (193-197) and "Tap to load real data" (191) branches; keep loading-spinner/string/array.
    (5) In ItemCard REMOVE the image block (205-209) and the url anchor (213-221) entirely (Pitfall 4 + D-10).
    (6) Add source as styled text to the job card (316-323) and housing card (327-337) renderers — D-10's second half; stop passing url/image to ItemCard.
    (7) Per D-09 REMOVE the three deferred Sections (nightlife/outdoors/food, 340-371) so they do not show a permanently-stuck load state under the new button model (the button fires only the 3 shipped categories).
    Do NOT add any "cached"/"as of" badge — D-08 keeps the fallback visually invisible.
  </action>
  <verify>
    <automated>grep -q "Pull live data" src/screens/PotentialApp.jsx && grep -q "fetchCategoryLive" src/screens/PotentialApp.jsx && ! grep -q "View listing" src/screens/PotentialApp.jsx && ! grep -q "coming_soon" src/screens/PotentialApp.jsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - "Pull live data" button present; onClick fires jobs + housing_* + dayinlife concurrently (D-04/D-05)
    - Section header onClick no longer calls a fetch (grep line 168 region: no fetchCityAI/fetchCategoryLive in Section toggle)
    - No "View listing" anchor and no "coming_soon"/"Tap to load" branch remain in the file
    - Job and housing card renderers render a `source` field as text; ItemCard no longer accepts/renders url or image
    - nightlife/outdoors/food Sections removed
    - `npm run build` exits 0 (Vite bundles the golden-path JSON; no build error)
  </acceptance_criteria>
  <done>Button-triggered parallel fan-out wired; D-10 source-text cards; deferred sections + dead branches removed; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/live | client sends category/cityName/profession; same-origin proxy, no key client-side |
| LLM/cache data → DOM | listing fields rendered into the DOM as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-key | Information Disclosure | client bundle | mitigate | Client only calls the same-origin /api/live; no @anthropic-ai/sdk, no ANTHROPIC_API_KEY, no VITE_* key in src/. Verified by grep of built dist/ in Plan 04. |
| T-5-xss | Tampering (XSS) | ItemCard / AIList renderers | mitigate | All listing fields (title, company, source, price, narrative) rendered as React text children, never dangerouslySetInnerHTML; url/image removed entirely (D-10/Pitfall 4). |
| T-5-blank | Denial of Service (UX) | live panels offline | mitigate | fetchCategoryLive always resolves to golden-path or [] on failure; AIList "coming_soon"/"tap to load" dead-ends removed → no blank/stuck panel (FOUND-04/SC4). |
</threat_model>

<verification>
- npm test green (live-fallback suite).
- npm run build exits 0.
- grep confirms button present, anchor + coming_soon removed, source rendered.
- Manual (Plan 04 checkpoint): offline render is instant and badge-free (D-08).
</verification>

<success_criteria>
- One button fans out to 3 categories with per-category skeletons (D-04/D-05).
- Offline = instant golden-path render via full-name key, no blank/spinner-stuck state (FOUND-04/SC4/LIVE-04).
- Cards show source text, zero clickable links (D-10).
</success_criteria>

<output>
Create `.planning/phases/05-proxy-live-ai-golden-path-cache/05-03-SUMMARY.md` when done.
</output>
</output>

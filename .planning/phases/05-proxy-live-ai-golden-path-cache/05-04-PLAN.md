---
phase: 05-proxy-live-ai-golden-path-cache
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - scripts/capture-golden-path.ts
  - scripts/tsconfig.json
  - .env.example
autonomous: false
requirements: [FOUND-03, FOUND-04, LIVE-01, LIVE-02, LIVE-03, LIVE-04]
user_setup:
  - service: anthropic
    why: "Real capture run + on-stage live wow moment require a working key with web_search enabled"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → API Keys (set in .env for local capture)"
    dashboard_config:
      - task: "Enable web search for the organization"
        location: "Anthropic Console org settings (Pitfall 6 — web_search silently no-ops if disabled)"

must_haves:
  truths:
    - "A capture script can call the real local proxy once per category×city and write byte-identical-shape output to demo-results.json keyed by the full city.name (D-07)"
    - "On a working hotspot the 'Pull live data' button streams real listings into each panel (SC1)"
    - "DevTools shows zero direct calls to api.anthropic.com and the built dist/ contains no API key (FOUND-03/SC2)"
    - "Killing the hotspot makes panels render golden-path content instantly with no spinner/blank (FOUND-04/SC4)"
  artifacts:
    - path: "scripts/capture-golden-path.ts"
      provides: "Re-runnable real-proxy snapshot generator for the golden-path cache (D-07)"
      contains: "fromCache"
  key_links:
    - from: "scripts/capture-golden-path.ts"
      to: "http://localhost:3000/api/live"
      via: "fetch per category×city, guard on fromCache:true"
      pattern: "api/live"
    - from: "scripts/capture-golden-path.ts"
      to: "data/golden-path/demo-results.json"
      via: "writeFileSync of merged result"
      pattern: "demo-results"
---

<objective>
Ship the golden-path capture script and run the end-of-phase verification that proves all five ROADMAP Success Criteria on real hardware: build the re-runnable capture script (D-07) that snapshots real proxy output into demo-results.json, document the env requirement, then human-verify the live wow moment, the no-key/no-direct-Anthropic guarantee, and the offline instant-render fallback.

Purpose: Closes the phase. The automated tests prove the fallback path; these checkpoints prove the live path and the FOUND-03/SC4 behaviors that are manual-only by nature (real network, real DevTools, real hotspot kill). The real capture run is gated on a pinned demo persona + working key, so the script is built autonomously but its execution is a human-action step.
Output: scripts/capture-golden-path.ts, updated .env.example note, verification sign-off.
</objective>

## Phase Goal

> **MVP user-story discrepancy (surfaced, not fabricated):** MVP_MODE is on, which calls for a `**As a** … **I want to** … **so that** …` user story sourced from the ROADMAP `**Goal:**` line. The Phase 5 ROADMAP goal — "the live AI data layer works via a backend proxy that never exposes the API key, and the bundled golden-path cache has been tested as the actual offline fallback" — is **infrastructural** and is not in user-story form. Per the MVP rules the planner must NOT invent a story for it. Recommendation: if a strict user-story header is required, run `/gsd mvp-phase 5` to author one. The closest user-facing framing (informational only, not a substitute): *As a Plus-tier user, I want to pull real current jobs/housing/day-in-the-life for a city and still see content instantly when the network drops, so the result feels live and never breaks on stage.* The plans below remain vertical slices (proxy→validate→render→fallback), satisfying the spirit of MVP_MODE.


<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-VALIDATION.md
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-02-SUMMARY.md
@.planning/phases/05-proxy-live-ai-golden-path-cache/05-03-SUMMARY.md
@data/golden-path/demo-profile.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/capture-golden-path.ts — re-runnable real-proxy snapshot (D-07)</name>
  <read_first>
    - .planning/phases/05-proxy-live-ai-golden-path-cache/05-RESEARCH.md (Capture script section — outline, critical fromCache guard, pre-requisites; NOTE the outline's `CITIES = ['Austin','Lisbon']` is illustrative and uses bare tokens — the real script reads full city.name strings from demo-profile.json instead)
    - data/golden-path/demo-profile.json (Plan 01 — the pinned persona + `cities: ["Austin, TX","Lisbon, Portugal"]` the script reads)
    - data/golden-path/demo-results.json (the file the script overwrites; keyed by full city.name)
  </read_first>
  <action>
    Create scripts/capture-golden-path.ts (run via npx tsx). Read the persona + cities from data/golden-path/demo-profile.json (do NOT hardcode the cities — read `demo-profile.json.cities`, which are the FULL city.name strings "Austin, TX"/"Lisbon, Portugal"; re-pinning the persona is then a data edit, not a code edit). For each category in [jobs, housing per persona.housing, dayinlife] × each city, POST to http://localhost:3000/api/live (the local vercel-dev proxy) with `cityName` = the full city.name string and the persona profession. Key the merged output object by that SAME full city.name string, so the snapshot's top-level city keys are byte-identical to the stub's ("Austin, TX"/"Lisbon, Portugal") and to what the runtime lookups expect — preserving the byte-identical-shape guarantee (a re-capture overwrites values, never changes the key shape). CRITICAL GUARD: if any response has fromCache:true, throw with a message that the proxy fell back (means key missing or web_search disabled — Pitfall 6) and do NOT write the file (otherwise the snapshot is stale placeholder data). Only when ALL responses are fromCache:false, write the merged { category: { "City, ST": items } } object to data/golden-path/demo-results.json with 2-space indent. The script does not need a key itself — the running proxy holds it. NOTE: the root tsconfig.json only includes shared/ and api/, so scripts/ is NOT type-checked by `npx tsc --noEmit`. Add `scripts/tsconfig.json` that `extends` the root config and sets `"include": ["./*.ts"]`, `"types": ["node"]`, and `"lib": ["ES2022"]` so the capture script (which uses the Node `fetch` global + `fs`) type-checks in isolation WITHOUT adding scripts/ to the root include (which would otherwise make Plan 02's `npx tsc --noEmit` start failing once this file exists).
  </action>
  <verify>
    <automated>npx tsc -p scripts/tsconfig.json --noEmit && npx tsc --noEmit && grep -q "fromCache" scripts/capture-golden-path.ts && grep -q "demo-profile" scripts/capture-golden-path.ts && ! grep -qE "\\['Austin', ?'Lisbon'\\]|\\[\"Austin\", ?\"Lisbon\"\\]" scripts/capture-golden-path.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/capture-golden-path.ts exists and reads persona/cities from demo-profile.json (no hardcoded city list; cities come from `demo-profile.json.cities` = full "City, ST" strings)
    - The script POSTs `cityName` = the full city.name and keys the output object by that same full string (snapshot keys are byte-identical to the stub: "Austin, TX"/"Lisbon, Portugal")
    - No bare-token `['Austin','Lisbon']` literal remains in the script
    - The script throws and does NOT write when any response is fromCache:true
    - `npx tsc -p scripts/tsconfig.json --noEmit` passes (the capture script actually type-checks) AND `npx tsc --noEmit` (root) still passes (Plan 02 api/ unaffected)
    - .env.example documents ANTHROPIC_API_KEY + the "enable web search in Console" prerequisite (note added if not already present)
  </acceptance_criteria>
  <done>Re-runnable capture script built; refuses to snapshot fallback data; persona-driven; full-name city keys preserved byte-identical.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Pin demo persona + run the real capture (gated on key + persona)</name>
  <what-built>The capture script (Task 1), the proxy (Plan 02), and the client (Plan 03) are all in place. The golden-path cache currently holds the valid-shaped STUB from Plan 01, not real captured data.</what-built>
  <how-to-verify>
    Human-only because it needs a real ANTHROPIC_API_KEY, web_search enabled in the Console, and a pinned rehearsed persona (D-06/D-07 open dependency — cannot be done by the agent):
    1. Confirm/finalize data/golden-path/demo-profile.json against the rehearsed demo script (profession, age, housing, and `cities` = the #1 US match city + Lisbon, written as full city.name strings e.g. "Austin, TX"/"Lisbon, Portugal"). Update if the assumed "Austin, TX" is not the real #1 match.
    2. Set ANTHROPIC_API_KEY in .env and confirm web_search is enabled in the Anthropic Console.
    3. Run `npm run dev:full` (vercel dev) in one terminal.
    4. Run `npx tsx scripts/capture-golden-path.ts` in another.
    5. Confirm it wrote demo-results.json with real listings (no fromCache throw) and that the file has content for both cities (under their full-name keys) across all 3 categories.
  </how-to-verify>
  <acceptance_criteria>
    - data/golden-path/demo-results.json populated with real captured data for the pinned persona × 2 cities, keyed by full city.name (SC3)
    - The capture script completed without a fromCache:true throw (confirms web_search fired — usage.server_tool_use.web_search_requests > 0)
  </acceptance_criteria>
  <resume-signal>Type "captured" once demo-results.json holds real data, or "stub" to ship the phase on the valid stub and defer the real capture to pitch-prep (note which in the SUMMARY).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase verification — live wow, no-key, offline fallback (SC1-SC5)</name>
  <what-built>The full live-AI slice: proxy (Plan 02), button + parallel fan-out + D-10 cards (Plan 03), golden-path cache (Plan 01/Task 2). Automated suite (validation/proxy/fallback) is green.</what-built>
  <how-to-verify>
    Run each ROADMAP Success Criterion check (manual by nature — real network, DevTools, hotspot kill):
    1. SC1/LIVE-01..03: `npm run dev:full` on a working hotspot; open a city detail; click "Pull live data" — real job listings, housing listings, and a day-in-the-life narrative stream into their panels (8-20s is expected for web_search).
    2. SC2/FOUND-03: open DevTools Network tab during the click — confirm ZERO requests to api.anthropic.com (all hit /api/live). Then run `npm run build` and `grep -r "sk-ant" dist/` — confirm it returns nothing.
    3. SC4/FOUND-04: with vercel dev running, disable Wi-Fi, click "Pull live data" — panels render golden-path content INSTANTLY, no spinner, no blank state. Confirm no "cached"/"as of" badge appears (D-08).
    4. SC5/LIVE-04: confirm that on the offline/failure path nothing crashes and every panel shows content.
    5. Run `npm test` — full suite green.
  </how-to-verify>
  <acceptance_criteria>
    - SC1: real listings + narrative render live (or, if no key available, explicitly noted as deferred to pitch-prep with the fallback path verified instead)
    - SC2: DevTools shows zero api.anthropic.com calls; `grep -r "sk-ant" dist/` returns nothing
    - SC4: hotspot-kill renders golden-path instantly, no spinner/blank, no cache badge
    - SC5: no crash on failure path
    - `npm test` exits 0
  </acceptance_criteria>
  <resume-signal>Type "verified" with the SC1-SC5 results, or list any failing criterion to trigger gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture script → local proxy | dev-time call holding the real key via the running proxy |
| client bundle → judges' eyes (on stage) | built dist/ must contain no key and render no clickable links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-key | Information Disclosure | built dist/ bundle | mitigate | Task 3 verifies `grep -r "sk-ant" dist/` returns nothing and DevTools shows zero api.anthropic.com calls (FOUND-03/SC2). |
| T-5-stale | Tampering | capture output | mitigate | Capture script throws on fromCache:true → never snapshots fallback/stale placeholder data into the golden path (D-07). |
| T-5-blank | Denial of Service (UX) | on-stage offline render | mitigate | Task 3 SC4 explicitly hotspot-kills and confirms instant golden-path render, no blank/spinner (FOUND-04). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (capture script types).
- Capture script refuses to write on fromCache:true and keys output by the full city.name (no bare-token city list).
- SC1-SC5 human-verified per Task 3; `npm test` green; `grep -r "sk-ant" dist/` empty.
</verification>

<success_criteria>
- Golden-path cache holds real (or explicitly-deferred-stub) data for the demo persona × 2 cities, keyed by full city.name (SC3).
- Live wow moment fires on a working hotspot (SC1).
- No key in bundle, no direct Anthropic calls (FOUND-03/SC2).
- Hotspot-kill = instant offline render, badge-free (FOUND-04/SC4).
- Failure path never crashes (LIVE-04/SC5).
</success_criteria>

<output>
Create `.planning/phases/05-proxy-live-ai-golden-path-cache/05-04-SUMMARY.md` when done. Record SC1-SC5 results and whether the cache holds real captured data or a deferred stub.
</output>
</output>

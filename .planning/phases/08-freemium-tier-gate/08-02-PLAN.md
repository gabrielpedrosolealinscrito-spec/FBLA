---
phase: 08-freemium-tier-gate
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - src/components/LockGate.jsx
  - src/screens/PotentialApp.jsx
  - src/screens/ResultsMap.jsx
autonomous: true
requirements: [TIER-01, TIER-02]
must_haves:
  truths:
    - "A free user sees the #1 city name + match % unblurred (the teaser) (D-01)"
    - "A free user sees the #1 city's financials and live-AI blurred behind a padlock + 'what you unlock' CTA (D-03)"
    - "A free user sees the why-block and visa section as a frosted skeleton behind a padlock (no real content to blur) (D-03)"
    - "A free user sees the ranked list cut to the top 1 city, with cities #2+ as a blurred stack + 'N more cities matched — unlock your full ranking' (D-02)"
    - "Switching to basic/plus/premium reveals the corresponding sections (blur dissolves) (D-09)"
  artifacts:
    - path: "src/components/LockGate.jsx"
      provides: "Dual-mode blur/padlock gate + FrostedSkeleton, gold-literal themed"
      contains: "filter"
      min_lines: 60
    - path: "src/screens/PotentialApp.jsx"
      provides: "tier + modalOpen state; LockGate wrapping inline financials/live-AI/why/visa + roadmap CTA; tier + onUnlock props to ResultsMap"
      contains: "LockGate"
    - path: "src/screens/ResultsMap.jsx"
      provides: "rank-gate cutoff: top-N normal + blurred-stack-with-count, onUnlock opens modal"
      contains: "rankShowUpTo"
  key_links:
    - from: "src/components/LockGate.jsx"
      to: "shared/types canAccess"
      via: "import { canAccess } from '../../shared/types'"
      pattern: "canAccess"
    - from: "src/screens/ResultsMap.jsx"
      to: "shared/types TIER_FEATURES"
      via: "TIER_FEATURES.rankShowUpTo[tier]"
      pattern: "rankShowUpTo"
    - from: "src/screens/PotentialApp.jsx"
      to: "src/components/LockGate.jsx"
      via: "<LockGate requiredTier=... onUnlock=...>"
      pattern: "<LockGate"
    - from: "src/screens/ResultsMap.jsx blurred stack"
      to: "PotentialApp setModalOpen (PricingModal)"
      via: "onUnlock={() => setModalOpen(true)} passed to ResultsMap; blurred-stack count CTA calls onUnlock"
      pattern: "onUnlock"
---

<objective>
Deliver the free-teaser vertical slice end-to-end (SC1): build LockGate (dual-mode blur-real / frosted-skeleton), lift tier state into PotentialApp, and gate the real inline city-detail content + the ResultsMap ranked list so a free user sees exactly one unblurred payoff (#1 city name + match %) and everything deeper is visibly locked. After this plan, a real user can experience the entire free-tier funnel.

Purpose: This is the conversion hook — the thing judges see first. It must look like real depth exists behind the gate.
Output: LockGate.jsx (+FrostedSkeleton), gated PotentialApp city-detail, rank-gated ResultsMap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-freemium-tier-gate/08-PATTERNS.md
@.planning/phases/08-freemium-tier-gate/08-RESEARCH.md
@.planning/phases/08-freemium-tier-gate/08-UI-SPEC.md
@.planning/phases/08-freemium-tier-gate/08-01-SUMMARY.md

<interfaces>
<!-- Contract from Wave 0 (shared/types.ts) — import, do not redefine -->
```typescript
export const canAccess: (active: Tier, required: Tier) => boolean;
export const TIER_FEATURES: { rankShowUpTo: { free:1; basic:3; plus:number; premium:number } };
```
Import from src/ with the extensionless convention (Vite resolves): `import { canAccess } from '../../shared/types'`.

<!-- HOST FILE REALITY (verified) — PotentialApp.jsx -->
- Top-level state block: lines 27–50 (step, profile, results, selectedCity, sortBy, showRoadmap, ...). Add `tier`/`modalOpen` here; zero React Context in codebase — prop-drill.
- City detail branch: `if (step === 2 && selectedCity)` at :183. Inside it:
  - Financials inline block ≈ :269–325 (real content → blur-real LockGate, requiredTier="basic")
  - Live-AI inline block ≈ :327–375 (real content → blur-real LockGate, requiredTier="plus")
  - "View relocation roadmap" CTA ≈ :255–267 / showRoadmap (gate at the CTA, requiredTier="plus")
  - NO "why"/match-reasoning block and NO visa section exist → add as LockGate with children={null} → FrostedSkeleton (why requiredTier="basic", visa requiredTier="premium"). DECIDED: skeleton, do NOT author real why content.
- Early returns per step at :133/:138/:153/:167/:183; ends `return null` at :381.
- ResultsMap is invoked at :169 with signature `ResultsMap({ results, profile, sortBy, setSortBy, onSelect, onEdit })` (defined ResultsMap.jsx:78). Add `tier` AND `onUnlock` to both the call and the signature.

<!-- ResultsMap list render (verified ResultsMap.jsx:80-140) -->
- `sorted` computed :80-87; rendered `.map` at :133-140 producing `.row` cards.
- Rank-gate here: slice `sorted` to N = TIER_FEATURES.rankShowUpTo[tier]; render top-N as `.row` normally, then ONE blurred stack element for the remainder with count copy. The blurred-stack count CTA calls the passed `onUnlock` to open the PricingModal (DECIDED: onUnlock, not onSelect — this is an upsell, not a navigation).

<!-- THEME-SCOPE HAZARD (load-bearing) -->
LockGate mounts inside PotentialApp's GREEN scope (--accent:#6EE7B7). It MUST hardcode gold literals, NOT inherit var(--accent). Gold tokens:
--bg:#070a11  --card:rgba(255,255,255,.04)  --border:rgba(243,237,225,.12)
--accent:#e2b56b  --accent-dim:rgba(226,181,107,.13)  --ink:#f3ede1
Padlock stroke: rgba(226,181,107,0.9). Overlay bg: rgba(8,9,12,0.35).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build LockGate + FrostedSkeleton (dual-mode, gold-literal)</name>
  <files>src/components/LockGate.jsx</files>
  <read_first>
    - tests/lock-gate.test.tsx (the RED tests this task must turn GREEN — match their expectations exactly)
    - .planning/phases/08-freemium-tier-gate/08-PATTERNS.md (LockGate section: filter:blur vs backdrop-filter, scoped-CSS idiom, inline padlock SVG, prefers-reduced-motion guard, FrostedSkeleton widths)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (Pattern 2 LockGate code + FrostedSkeleton Code Example)
    - .planning/phases/08-freemium-tier-gate/08-UI-SPEC.md (Blur-Dissolve Unlock Animation; LockGate Click Behavior; Empty State FrostedSkeleton specs; Color reserved-for list)
    - shared/types.ts (import canAccess)
    - src/screens/ResultsMap.jsx (blur/scoped-<style>/inline-SVG idioms to mirror)
  </read_first>
  <behavior>
    - LockGate unlocked: canAccess true → renders children (and FrostedSkeleton is NOT shown).
    - LockGate locked: canAccess false → renders children inside a wrapper with `filter: blur(...)` AND a padlock <svg> overlay AND the lockedLabel CTA text.
    - LockGate skeleton: locked AND children == null → renders FrostedSkeleton (skeleton lines) inside the blurred wrapper, no real children.
    - Clicking the overlay calls onUnlock.
    - prefers-reduced-motion: when matchMedia reduce matches, unlock snaps (no transition).
  </behavior>
  <action>
    Create `LockGate({ tier, requiredTier, lockedLabel, onUnlock, children })`. Compute `locked = !canAccess(tier, requiredTier)`. Layer 1: content wrapper with `filter: blur(8px→0px)` transition `0.42s ease`, `userSelect:none`, `pointerEvents:none`, `will-change:filter` only during the ~450ms transition; render `children` when present, else `<FrostedSkeleton />`. Layer 2: absolutely-positioned overlay (`inset:0`, bg `rgba(8,9,12,0.35)`, opacity 1→0 transition `0.38s ease`) with an inline padlock SVG (stroke `rgba(226,181,107,0.9)`) + the `lockedLabel` CTA in Manrope; `onClick={onUnlock}`. Honor prefers-reduced-motion via `window.matchMedia("(prefers-reduced-motion: reduce)").matches` (snap, skip transition). Define `FrostedSkeleton` in the same file: 4 lines at static widths [85,70,90,60]%, height 14, radius 6, line bg `rgba(243,237,225,0.06)`, card bg `rgba(255,255,255,.04)`, border `1px solid rgba(243,237,225,.12)`. HARDCODE all gold/ivory literals — never `var(--accent)` (green host scope). Animate ONLY filter + opacity (no height/transform/margin).
  </action>
  <verify>
    <automated>npm test -- -t "LockGate"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- -t "LockGate unlocked"`, `-t "LockGate locked"`, `-t "LockGate skeleton"` all pass.
    - Source assertion (theme gate): `grep -c "#e2b56b\|226,181,107" src/components/LockGate.jsx` >= 1 AND `grep -c "var(--accent)" src/components/LockGate.jsx` === 0.
    - Source assertion: `grep -c "filter" src/components/LockGate.jsx` >= 1 AND `grep -c "backdrop-filter" src/components/LockGate.jsx` === 0 (locked content uses filter:blur, not backdrop-filter).
    - Source assertion: `grep -c "prefers-reduced-motion" src/components/LockGate.jsx` >= 1.
    - Behavior: unlocking a previously-locked gate dissolves the blur (no layout shift — only filter/opacity animate).
  </acceptance_criteria>
  <done>LockGate renders children when unlocked, blur+padlock+CTA when locked, FrostedSkeleton when locked with null children; reduced-motion honored; gold literals only; lock-gate tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tier state + LockGate into PotentialApp city detail</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (whole file; state block :27-50, city-detail branch :183-379, financials :269-325, live-AI :327-375, roadmap CTA :255-267, ResultsMap call :169)
    - src/components/LockGate.jsx (the component just built — props contract)
    - .planning/phases/08-freemium-tier-gate/08-PATTERNS.md ("PotentialApp.jsx — extend" section: lift state, gate inline sections, frosted-skeleton for why+visa, pass tier to ResultsMap)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (TIER_FEATURES Visibility Matrix — which requiredTier each section needs)
    - .planning/phases/08-freemium-tier-gate/08-UI-SPEC.md (Copywriting Contract → Lock CTA pattern: exact "Unlock {section label}" strings)
  </read_first>
  <action>
    Add `const [tier, setTier] = useState("free")` and `const [modalOpen, setModalOpen] = useState(false)` to the :27-50 state block. In the `step===2 && selectedCity` branch: wrap the financials block (:269-325) in `<LockGate tier={tier} requiredTier="basic" lockedLabel="Unlock Financial Snapshot" onUnlock={() => setModalOpen(true)}>` (blur-real). Wrap the live-AI block (:327-375) in `<LockGate ... requiredTier="plus" lockedLabel="Unlock Live AI Data">` (blur-real). Add a why/match-reasoning gate and a visa gate as `<LockGate ... children={null}>` (FrostedSkeleton): why → requiredTier="basic" lockedLabel="Unlock why this city" ; visa → requiredTier="premium" lockedLabel="Unlock Visa Concierge". Gate the "View relocation roadmap" CTA (:255-267) so a non-plus user clicking it opens the modal instead of navigating — wrap or guard with canAccess(tier,"plus"); lockedLabel "Unlock Relocation Roadmap". Pass BOTH `tier={tier}` AND `onUnlock={() => setModalOpen(true)}` to the `<ResultsMap>` call at :169 (the blurred-stack count CTA in ResultsMap uses this onUnlock to open the pricing modal — DECIDED: onUnlock, not onSelect). Use the exact CTA strings from the UI-SPEC Copywriting Contract. Leave PricingModal/DemoTierSwitcher mounting for later waves — for now `onUnlock` may set modalOpen (modal renders no-op until Wave 3); the blur/padlock/skeleton must render correctly at every tier NOW.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "<LockGate" src/screens/PotentialApp.jsx` >= 4 (financials, live-AI, why, visa; roadmap may be a canAccess guard).
    - Source assertion: `grep -c "useState(\"free\")\|useState('free')" src/screens/PotentialApp.jsx` >= 1 (tier state lifted).
    - Source assertion: ResultsMap call passes tier — `grep -c "tier={tier}" src/screens/PotentialApp.jsx` >= 1.
    - Source assertion: ResultsMap call passes onUnlock — `grep -c "onUnlock={() => setModalOpen(true)}" src/screens/PotentialApp.jsx` >= 1 (the blurred-stack → PricingModal key link).
    - Source assertion: the four lock CTA strings present — grep for "Unlock Financial Snapshot", "Unlock Live AI Data", "Unlock Relocation Roadmap", "Unlock Visa Concierge".
    - Behavior: at tier="free", financials + live-AI render blurred-with-padlock; why + visa render frosted-skeleton-with-padlock; the why/visa unlock reveals a FrostedSkeleton (NOT rich content — this is the decided graceful-degradation state, not a bug).
    - `npm test` full suite: no NEW failures introduced (component tests for switcher/badge remain RED awaiting Wave 2).
  </acceptance_criteria>
  <done>PotentialApp owns tier state and gates all inline city-detail sections per the visibility matrix; free tier shows the correct locked/skeleton presentation; ResultsMap receives tier + onUnlock.</done>
</task>

<task type="auto">
  <name>Task 3: Rank-gate the ResultsMap ranked list (top-N + blurred stack)</name>
  <files>src/screens/ResultsMap.jsx</files>
  <read_first>
    - src/screens/ResultsMap.jsx (whole file; signature :78, `sorted` :80-87, list `.map` :133-140, `.row`/`.sc` CSS for the blurred-stack styling)
    - .planning/phases/08-freemium-tier-gate/08-PATTERNS.md (rank-gate is owned by ResultsMap, NOT LockGate; D-02 blurred-stack-with-count)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (Architectural Responsibility Map — "Rank-gating owned by ResultsMap"; Visibility Matrix free=1/basic=3/all)
    - .planning/phases/08-freemium-tier-gate/08-UI-SPEC.md (Copywriting Contract → blurred-list copy: "N more cities matched — unlock your full ranking")
    - shared/types.ts (TIER_FEATURES.rankShowUpTo)
  </read_first>
  <action>
    Add `tier` (default "free") AND `onUnlock` to the ResultsMap signature at :78, and import `{ TIER_FEATURES }` from '../../shared/types'. Compute `const showN = TIER_FEATURES.rankShowUpTo[tier]`. In the list render (:133-140), render the first `showN` of `sorted` as the existing `.row` cards unchanged. For the remainder (`sorted.length - showN > 0` and showN !== Infinity), render ONE blurred-stack element: a `filter: blur(...)` wrapper showing a couple of stacked dummy/teaser `.row` shapes (or the real remaining rows blurred) with a centered count CTA "{remaining} more cities matched — unlock your full ranking" whose onClick calls the passed `onUnlock` to open the PricingModal (DECIDED: onUnlock, NOT onSelect — clicking the locked stack is an upsell, not a city navigation). HARDCODE gold literals for the count CTA accent (ResultsMap is gold-scoped so var(--accent) is gold here, but keep consistent with UI-SPEC gold). When showN is Infinity (plus/premium), render the full list as today (no stack). Do not move rank logic into LockGate.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "rankShowUpTo" src/screens/ResultsMap.jsx` >= 1 AND signature includes `tier` (`grep -c "function ResultsMap" src/screens/ResultsMap.jsx` === 1 and the param list contains tier).
    - Source assertion: the signature/render uses onUnlock for the stack CTA — `grep -c "onUnlock" src/screens/ResultsMap.jsx` >= 1 (the count CTA calls onUnlock, not onSelect).
    - Source assertion: the blurred-stack copy is present — `grep -c "more cities matched" src/screens/ResultsMap.jsx` >= 1.
    - Behavior: at tier="free" the list shows exactly the top 1 `.row` then the blurred stack; at "basic" shows top 3 then stack; at "plus"/"premium" shows all rows with no stack.
    - `npm test` passes for all currently-green tests; no regression in existing ResultsMap behavior.
  </acceptance_criteria>
  <done>ResultsMap cuts the ranked list at TIER_FEATURES.rankShowUpTo[tier] and teases the remainder as a blurred stack whose count CTA calls onUnlock to open the modal; rank logic stays in ResultsMap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Presentation-layer gating only. LockGate hides/blurs UI; it protects no server resource. Tier state is in-memory React state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | LockGate blur bypass via DevTools | accept | Un-blurring locked UI in DevTools reveals already-client-rendered content; no server resource unlocks. Acceptable for v1 demo (RESEARCH Security Domain). |
| T-08-06 | Tampering | FrostedSkeleton / blurred-stack content | accept | All teaser/skeleton content is static, non-user-input. No injection surface. |

No HIGH-severity threats. Zero new packages.
</threat_model>

<verification>
- `npm test -- -t "LockGate"` green (3 component tests now pass).
- Full `npm test` green for all logic + LockGate; switcher/badge tests remain RED (Wave 2).
- Source greps in acceptance criteria pass (gold-literal gate on LockGate, lock CTA strings, rank-gate copy, onUnlock wiring).
</verification>

<success_criteria>
SC1 met end-to-end: a free user sees the #1 city teaser unblurred, all deeper sections visibly locked (blur-real where content exists, frosted-skeleton where it doesn't), and the ranked list cut to top 1 with a blurred upgrade stack. Switching tiers (via direct setTier) reveals the correct sections.
</success_criteria>

<output>
Create `.planning/phases/08-freemium-tier-gate/08-02-SUMMARY.md` when done.
</output>

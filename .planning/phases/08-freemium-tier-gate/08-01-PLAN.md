---
phase: 08-freemium-tier-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/types.ts
  - shared/tierGate.test.ts
  - tests/lock-gate.test.tsx
  - tests/demo-switcher.test.tsx
  - tests/runs-badge.test.tsx
  - pitch/business-model.md
  - pitch/qa-bank.md
autonomous: true
requirements: [TIER-01, TIER-02, TIER-03]
must_haves:
  truths:
    - "canAccess(active, required) returns the correct boolean for all 16 tier pairs"
    - "TIER_FEATURES.rankShowUpTo gates free=1, basic=3, plus/premium=all"
    - "TIER_RUNS_MAP returns the correct badge label per tier (null for free)"
    - "Pitch docs describe Basic as top-3-cities and include the 30-day money-back guarantee"
  artifacts:
    - path: "shared/types.ts"
      provides: "canAccess + TIER_FEATURES + TIER_RUNS_MAP typed contract"
      contains: "export const canAccess"
    - path: "shared/tierGate.test.ts"
      provides: "16 tier-pair canAccess assertions + 4 rank-gate assertions (GREEN this wave)"
      contains: "canAccess"
    - path: "tests/lock-gate.test.tsx"
      provides: "RED component stubs for LockGate children/padlock/skeleton"
    - path: "tests/demo-switcher.test.tsx"
      provides: "RED component stubs for DemoTierSwitcher visibility"
    - path: "tests/runs-badge.test.tsx"
      provides: "RED component stubs for RunsBadge per-tier output"
  key_links:
    - from: "shared/tierGate.test.ts"
      to: "shared/types.ts"
      via: "import { canAccess, TIER_FEATURES } from './types'"
      pattern: "from ['\"]\\./types['\"]"
---

<objective>
Lay the tier-gate contract and the failing-test scaffold for the whole phase. This is the MVP "failing test first" task: the pure-logic tests (canAccess, rank-gate) go GREEN here against the real contract; the four component test files are written RED (they import components that do not exist yet) so Waves 1–3 turn them green one slice at a time. Also fix the two pitch docs that contradict the demo copy.

Purpose: Single source of truth for tier access, plus a complete Nyquist test net before any UI is built.
Output: Extended `shared/types.ts` contract, four test files, two corrected pitch docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-freemium-tier-gate/08-RESEARCH.md
@.planning/phases/08-freemium-tier-gate/08-PATTERNS.md
@.planning/phases/08-freemium-tier-gate/08-VALIDATION.md

<interfaces>
<!-- Existing contract the executor extends. shared/types.ts ENDS at line 204 with the Tier union. -->
From shared/types.ts:204 (last line of file):
```typescript
export type Tier = "free" | "basic" | "plus" | "premium";
```

Test analogs (read before writing tests):
- shared/engine logic tests: co-located `.test.ts`, `globals:true` (no vitest import needed), import unit-under-test directly.
- tests/live-fallback.test.ts: explicit `import { describe, it, expect } from 'vitest'` is also accepted.
- Component test setup is ready: jsdom env, `@testing-library/react` installed, `matchMedia` mocked at src/test-setup.js:13-30.

VITEST FLAG NOTE (verified in this repo): the filter flag is `-t "<name>"` (testNamePattern). VALIDATION.md/RESEARCH.md show `--grep` — that flag does NOT exist in Vitest 4 and errors. Use `npm test -- -t "..."`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared/types.ts with the tier-gate contract</name>
  <files>shared/types.ts</files>
  <read_first>
    - shared/types.ts (read the whole file; the `Tier` union is the current last line, :204)
    - .planning/phases/08-freemium-tier-gate/08-PATTERNS.md (section "shared/types.ts — extend": exact constants to add + import-convention note)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (TIER_FEATURES Visibility Matrix + Pitfall 5 TIER_RUNS_MAP)
  </read_first>
  <behavior>
    - canAccess("free","free") === true; canAccess("free","basic") === false
    - canAccess("premium","premium") === true; canAccess("premium","free") === true
    - canAccess("basic","plus") === false; canAccess("plus","basic") === true
    - TIER_FEATURES.rankShowUpTo.free === 1, .basic === 3, .plus === Infinity, .premium === Infinity
    - TIER_RUNS_MAP.free === null; .basic === "Basic · 1 of 1 run"; .plus === "Plus · 2 of 3 runs left"; .premium === "Premium · unlimited"
  </behavior>
  <action>
    Append after the existing `Tier` union (do not modify it). Add: a `TIER_ORDER: Record<Tier, number>` ({ free:0, basic:1, plus:2, premium:3 }); `export const canAccess = (active: Tier, required: Tier): boolean` comparing TIER_ORDER values with `>=`; `export const TIER_FEATURES` (typed `as const`) with a `rankShowUpTo` record per D-01/D-12 (free:1, basic:3, plus:Infinity, premium:Infinity); and `export const TIER_RUNS_MAP: Record<Tier, string | null>` with the exact label strings from the UI-SPEC RunsBadge Labels table (free → null). Keep these as pure data + one pure function — no React, no DOM, no side effects (mirror the shared/engine pure-function idiom).
  </action>
  <verify>
    <automated>npm test -- -t "canAccess"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const canAccess" shared/types.ts` returns a match.
    - `grep -n "rankShowUpTo" shared/types.ts` returns a match; `grep -c "Infinity" shared/types.ts` >= 2.
    - The Tier union at :204 is unchanged (`grep -c 'export type Tier' shared/types.ts` === 1).
    - `npm test -- -t "canAccess"` and `npm test -- -t "rankGate"` both pass (after Task 2 lands the assertions).
  </acceptance_criteria>
  <done>canAccess, TIER_FEATURES.rankShowUpTo, and TIER_RUNS_MAP are exported from shared/types.ts as a pure typed contract; existing Tier union untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the Wave-0 test scaffold (logic GREEN, components RED)</name>
  <files>shared/tierGate.test.ts, tests/lock-gate.test.tsx, tests/demo-switcher.test.tsx, tests/runs-badge.test.tsx</files>
  <read_first>
    - shared/types.ts (the contract Task 1 just added — import from it)
    - tests/live-fallback.test.ts (TS test idiom: explicit vitest import, describe/it/expect)
    - .planning/phases/08-freemium-tier-gate/08-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the test-name strings the -t patterns must match)
    - src/test-setup.js (confirm matchMedia mock is present so LockGate reduced-motion path is testable)
  </read_first>
  <behavior>
    - shared/tierGate.test.ts: a describe "canAccess" asserting all 16 active×required pairs; a describe "rankGate" asserting TIER_FEATURES.rankShowUpTo per tier (free=1, basic=3, plus/premium=Infinity). These import the real contract and pass NOW.
    - tests/lock-gate.test.tsx: describes/its named "LockGate unlocked" (renders children), "LockGate locked" (renders padlock svg + blurred wrapper), "LockGate skeleton" (locked + children null → FrostedSkeleton). Import `LockGate` from '../src/components/LockGate.jsx' — RED until Wave 1.
    - tests/demo-switcher.test.tsx: an it named "DemoTierSwitcher" asserting it renders nothing when visible=false and renders four buttons when visible=true; import from '../src/components/DemoTierSwitcher.jsx' — RED until Wave 2.
    - tests/runs-badge.test.tsx: an it named "RunsBadge" asserting label per tier (null/none for free, exact strings otherwise); import from '../src/components/RunsBadge.jsx' — RED until Wave 2.
  </behavior>
  <action>
    Create the four files. Name the describe/it blocks so the VALIDATION.md `-t` patterns match: "canAccess", "rankGate", "LockGate unlocked", "LockGate locked", "LockGate skeleton", "DemoTierSwitcher", "RunsBadge". Logic test imports `{ canAccess, TIER_FEATURES }` from '../shared/types' is wrong path for shared-co-located — use './types' since the test sits in shared/. Component tests use `render` + `screen` from '@testing-library/react'. The three component test files import components that do not exist yet and MUST be RED this wave (import error / module-not-found is the expected RED signal); do NOT stub the components to make them pass. tierGate.test.ts MUST be GREEN.
  </action>
  <verify>
    <automated>npm test -- -t "rankGate"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- -t "canAccess"` passes (16 assertions) and `npm test -- -t "rankGate"` passes (4 assertions).
    - Running the full suite (`npm test`) shows the three component test files FAILING with module-not-found for LockGate/DemoTierSwitcher/RunsBadge (expected RED — these are the gates Waves 1–2 close).
    - All four files exist: `ls shared/tierGate.test.ts tests/lock-gate.test.tsx tests/demo-switcher.test.tsx tests/runs-badge.test.tsx` succeeds.
    - Each component test file contains the exact `-t` name strings from VALIDATION.md (grep each: "LockGate unlocked", "LockGate locked", "LockGate skeleton", "DemoTierSwitcher", "RunsBadge").
  </acceptance_criteria>
  <done>Logic tests green against the real contract; three component test files exist and fail RED awaiting their slice. Nyquist net is laid for the phase.</done>
</task>

<task type="auto">
  <name>Task 3: Sync pitch docs to the demo (money-back guarantee + Basic = top 3 cities)</name>
  <files>pitch/business-model.md, pitch/qa-bank.md</files>
  <read_first>
    - pitch/business-model.md (find the "no money-back guarantee" line ~:48 and any "single most optimal city" / "#1 city" Basic description ~:42)
    - pitch/qa-bank.md (find the stale Basic copy "#1 city / single-city snapshot" / "full financial snapshot for #1 city")
    - .planning/phases/08-freemium-tier-gate/08-CONTEXT.md (OQ-1 + OQ-2 resolutions: ADD the 30-day money-back guarantee; Basic = top 3 cities fully)
  </read_first>
  <action>
    In business-model.md: change the "no money-back guarantee" decision line to state Potential offers a 30-day money-back guarantee (per OQ-1 / D-10), and update any Basic-tier description from "single most optimal city / #1 city" to "top 3 cities fully (name + why + core financials)" per D-12. In qa-bank.md: update the Basic-tier Q&A answer from "#1 city / single-city snapshot / full financial snapshot for #1 city" to "top 3 cities fully" so the verbal answer matches the demo; if a refund question exists or the money-back framing appears, align it to "30-day money-back guarantee." Do NOT touch ROADMAP.md or REQUIREMENTS.md — TIER-02 and SC2 are already synced. Keep edits surgical (no doc restructuring).
  </action>
  <verify>
    <automated>grep -ric "money-back" pitch/business-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no money-back" pitch/business-model.md` === 0 (the negation line is gone).
    - `grep -ic "30-day money-back" pitch/business-model.md` >= 1.
    - `grep -ic "top 3 cities" pitch/qa-bank.md` >= 1 AND `grep -ic "single-city\|#1 city" pitch/qa-bank.md` for the Basic description returns 0 (no stale Basic copy remains).
    - Behavior assertion: the Basic-tier description in both docs now reads "top 3 cities," matching ROADMAP SC2 and the PricingModal copy that Wave 3 will ship.
  </acceptance_criteria>
  <done>business-model.md and qa-bank.md describe Basic as top-3-cities and include the 30-day money-back guarantee; deck/demo/Q&A are internally consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 8 is presentation-layer only. No auth, no sessions, no server resources, no user data, no network calls. Tier state is in-memory React state that resets to "free" on reload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | Tier state (DevTools bypass) | accept | A user setting tier via DevTools only un-blurs already-rendered UI. No server resource is gated; real access control is v2-deferred (RESEARCH Security Domain). Acceptable for a demo. |
| T-08-02 | Information Disclosure | Presenter-mode gesture | accept | Security-by-obscurity over a demo control panel, NOT a security boundary. Discovering the gesture reveals tier controls, not secrets (D-05). |
| T-08-05 | Input Validation | setTier values | mitigate | setTier only ever receives values from the four-button DemoTierSwitcher / PricingModal CTAs — all members of the `Tier` union. No free-text input reaches state. (Wired in Waves 2–3.) |

No HIGH-severity threats. Zero new packages → no supply-chain (T-08-SC) row.
</threat_model>

<verification>
- `npm test -- -t "canAccess"` and `-t "rankGate"` pass.
- Full `npm test` shows the three component files RED (expected pre-Wave-1/2).
- Pitch docs grep clean per acceptance criteria.
</verification>

<success_criteria>
The tier-gate contract is the single source of truth, the full Nyquist test net exists (logic green, component red), and the two pitch docs match the demo copy.
</success_criteria>

<output>
Create `.planning/phases/08-freemium-tier-gate/08-01-SUMMARY.md` when done.
</output>

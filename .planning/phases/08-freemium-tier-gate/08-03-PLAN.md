---
phase: 08-freemium-tier-gate
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - src/components/DemoTierSwitcher.jsx
  - src/components/RunsBadge.jsx
  - src/screens/PotentialApp.jsx
autonomous: true
requirements: [TIER-02]
must_haves:
  truths:
    - "The presenter reveals a hidden floating Free|Basic|Plus|Premium pill via a corner triple-tap (D-04)"
    - "Tapping a pill button re-renders the current screen at that tier (sections unlock/lock accordingly) (D-04)"
    - "The pill is hidden by default so judges see a clean consumer app (D-05)"
    - "A runs badge in the city-detail header shows the correct run count per tier (none for free) (D-06)"
    - "The presenter can cycle all four tiers in under 60s (manual-verified)"
  artifacts:
    - path: "src/components/DemoTierSwitcher.jsx"
      provides: "Floating segmented pill, presenterMode-gated, gold-literal"
      contains: "visible"
    - path: "src/components/RunsBadge.jsx"
      provides: "Header runs badge reading TIER_RUNS_MAP, gold-literal, null for free"
      contains: "TIER_RUNS_MAP"
    - path: "src/screens/PotentialApp.jsx"
      provides: "presenterMode state + corner-triple-tap listener at root + switcher/badge mounted"
      contains: "presenterMode"
  key_links:
    - from: "src/components/DemoTierSwitcher.jsx"
      to: "PotentialApp setTier/presenterMode"
      via: "props tier/onTier/visible"
      pattern: "visible"
    - from: "src/components/RunsBadge.jsx"
      to: "shared/types TIER_RUNS_MAP"
      via: "TIER_RUNS_MAP[tier]"
      pattern: "TIER_RUNS_MAP"
    - from: "src/screens/PotentialApp.jsx"
      to: "window gesture listener"
      via: "useEffect([]) addEventListener click+touchstart"
      pattern: "addEventListener"
---

<objective>
Deliver the tier-switching vertical slice end-to-end (SC4): build the hidden DemoTierSwitcher and the RunsBadge, lift presenterMode state, and attach the corner-triple-tap gesture at PotentialApp root so it survives step changes. After this plan, the presenter can summon the pill on stage and flip all four tiers, watching the Wave-1 gates lock/unlock live — and the runs badge makes the business model visible.

Purpose: The on-stage control surface + the "see the business model live" pitch device.
Output: DemoTierSwitcher.jsx, RunsBadge.jsx, presenterMode + gesture wiring in PotentialApp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-freemium-tier-gate/08-PATTERNS.md
@.planning/phases/08-freemium-tier-gate/08-RESEARCH.md
@.planning/phases/08-freemium-tier-gate/08-UI-SPEC.md
@.planning/phases/08-freemium-tier-gate/08-02-SUMMARY.md

<interfaces>
<!-- Contract from Wave 0 -->
```typescript
export const TIER_RUNS_MAP: Record<Tier, string | null>;
// free → null, basic → "Basic · 1 of 1 run", plus → "Plus · 2 of 3 runs left", premium → "Premium · unlimited"
```
Import from src/: `import { TIER_RUNS_MAP } from '../../shared/types'`.

<!-- HOST FILE REALITY (verified) -->
- PotentialApp state block :27-50 (tier/modalOpen added in Wave 1). Add `presenterMode` here.
- Mount effect precedent: useEffect at :53. Add the gesture listener in a useEffect([],[]) here so it persists across all step early-returns (:133/:138/:153/:167/:183, ends :381). Landing.jsx's keydown listener unmounts with Landing — do NOT copy that scoping; the presenter gesture must live at PotentialApp root.
- City-detail header is in the step===2 && selectedCity branch (≈ :237-241) — mount RunsBadge there.
- Overlays (DemoTierSwitcher) must render on top of every step → render outside/after the `if(step===N)` switches. PotentialApp currently returns per-step then `return null`; wrap the screen switch in a fragment OR render the switcher before each early return. Simplest: convert to a single return with the switcher mounted alongside the active screen.

<!-- Analog idioms (verified) -->
- Segmented pill: ResultsMap.jsx `.pill` / `.pill.on` (CSS :46-48, render :117-119) — exact idiom.
- Mono accent chip (RunsBadge): ResultsMap.jsx `.row .sc` :71 (mono, 11px).
- Corner triple-tap: RESEARCH Pattern 4 — 80×80px bottom-right hotspot, 3 taps within 600ms toggle presenterMode; works for click AND touchstart (D-13).

<!-- THEME-SCOPE HAZARD -->
Both components mount inside PotentialApp's GREEN scope. HARDCODE gold literals:
active pill: color #e2b56b, bg rgba(226,181,107,.13), outline 1.5px solid #e2b56b.
RunsBadge: color #e2b56b, bg rgba(226,181,107,.13), JetBrains Mono 11px. NEVER var(--accent).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build DemoTierSwitcher (hidden floating pill, gold-literal)</name>
  <files>src/components/DemoTierSwitcher.jsx</files>
  <read_first>
    - tests/demo-switcher.test.tsx (RED test to turn GREEN — visibility + 4 buttons)
    - src/screens/ResultsMap.jsx (.pill / .pill.on idiom :46-48, :117-119)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (DemoTierSwitcher Code Example)
    - .planning/phases/08-freemium-tier-gate/08-UI-SPEC.md (DemoTierSwitcher Visibility; responsive label abbreviation Fr/Ba/+/★ <400px; weight-600 active label; spacing 8px padding / 4px gap)
  </read_first>
  <behavior>
    - DemoTierSwitcher visible=false → renders nothing (returns null).
    - DemoTierSwitcher visible=true → renders four buttons (Free, Basic, Plus, Premium).
    - Clicking a button calls onTier with that tier key.
    - The active tier button is visually distinguished (gold outline + weight 600).
  </behavior>
  <action>
    Create `DemoTierSwitcher({ tier, onTier, visible })`. `if (!visible) return null`. Render a fixed floating pill (bottom-center, zIndex high) over the four tier keys mapping to capitalized labels; active button gets gold literals (color #e2b56b, bg rgba(226,181,107,.13), outline 1.5px solid #e2b56b, fontWeight 600), inactive gets dim ivory. Use 8px uniform padding inside the pill and 4px gap between buttons (UI-SPEC spacing). Add a scoped `<style>` `@media(max-width:400px)` abbreviating labels to Fr|Ba|+|★. HARDCODE gold — never var(--accent).
  </action>
  <verify>
    <automated>npm test -- -t "DemoTierSwitcher"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- -t "DemoTierSwitcher"` passes.
    - Source assertion (theme gate): `grep -c "#e2b56b\|226,181,107" src/components/DemoTierSwitcher.jsx` >= 1 AND `grep -c "var(--accent)" src/components/DemoTierSwitcher.jsx` === 0.
    - Source assertion: four tier keys present — `grep -c "free\|basic\|plus\|premium" src/components/DemoTierSwitcher.jsx` >= 1 and a 4-element tier array exists.
    - Source assertion: `grep -c "return null" src/components/DemoTierSwitcher.jsx` >= 1 (hidden when not visible).
  </acceptance_criteria>
  <done>DemoTierSwitcher renders nothing when hidden, a 4-button gold pill when visible, calls onTier on tap; demo-switcher test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build RunsBadge (header chip, gold-literal)</name>
  <files>src/components/RunsBadge.jsx</files>
  <read_first>
    - tests/runs-badge.test.tsx (RED test to turn GREEN — label per tier)
    - src/screens/ResultsMap.jsx (.row .sc mono chip :71 — the idiom to scale up)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (Runs Badge Code Example + Pitfall 5)
    - .planning/phases/08-freemium-tier-gate/08-UI-SPEC.md (RunsBadge Labels table; Caption/Micro 11px JetBrains Mono)
    - shared/types.ts (TIER_RUNS_MAP)
  </read_first>
  <behavior>
    - RunsBadge tier="free" → renders nothing (null label).
    - RunsBadge tier="basic" → "Basic · 1 of 1 run".
    - RunsBadge tier="plus" → "Plus · 2 of 3 runs left".
    - RunsBadge tier="premium" → "Premium · unlimited".
  </behavior>
  <action>
    Create `RunsBadge({ tier })`. Read `const label = TIER_RUNS_MAP[tier]`; `if (!label) return null`. Render a single mono chip: JetBrains Mono 11px, color #e2b56b, bg rgba(226,181,107,.13), padding 3px 10px, radius 6, letterSpacing .03em. HARDCODE gold — never var(--accent) (this is the highest-risk component for the green host scope per PATTERNS). Source the labels from TIER_RUNS_MAP, do not re-hardcode the strings inside RunsBadge.
  </action>
  <verify>
    <automated>npm test -- -t "RunsBadge"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- -t "RunsBadge"` passes (all four tier cases).
    - Source assertion: `grep -c "TIER_RUNS_MAP" src/components/RunsBadge.jsx` >= 1 (reads the contract, not re-hardcoded strings).
    - Source assertion (theme gate): `grep -c "#e2b56b\|226,181,107" src/components/RunsBadge.jsx` >= 1 AND `grep -c "var(--accent)" src/components/RunsBadge.jsx` === 0.
    - Source assertion: `grep -c "return null" src/components/RunsBadge.jsx` >= 1 (no badge for free).
  </acceptance_criteria>
  <done>RunsBadge reads TIER_RUNS_MAP and renders the correct gold mono chip per tier (nothing for free); runs-badge test GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire presenterMode + corner gesture + mount switcher & badge</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (whole file; state :27-50, mount effect :53, step early-returns + final return null :381, city-detail header ≈ :237-241)
    - src/components/DemoTierSwitcher.jsx + src/components/RunsBadge.jsx (props contracts just built)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (Pattern 4 gesture code; Pitfall 2 gesture-must-survive-step-changes; State Architecture mount-overlays-outside-step-switch)
    - .planning/phases/08-freemium-tier-gate/08-PATTERNS.md (Presenter gesture listener at root; mount overlays outside step switches)
    - .planning/phases/08-freemium-tier-gate/08-UI-SPEC.md (DemoTierSwitcher Visibility — 80×80px hotspot, 3 taps / 600ms)
  </read_first>
  <action>
    Add `const [presenterMode, setPresenterMode] = useState(false)` to the :27-50 block. In a `useEffect(() => {...}, [])` (mount-only), attach a window `click` and `touchstart` listener implementing the corner triple-tap: read clientX/clientY (fallback to touches[0]), test the bottom-right 80×80px hotspot (`x > innerWidth-80 && y > innerHeight-80`), count taps, reset after 600ms, on the 3rd tap toggle setPresenterMode; return a cleanup that removes BOTH listeners. Mount `<DemoTierSwitcher tier={tier} onTier={setTier} visible={presenterMode} />` so it renders on top of every step — render it alongside the active screen (wrap the per-step returns in a fragment that always also renders the switcher, or restructure to a single return). Mount `<RunsBadge tier={tier} />` in the city-detail header (≈ :237-241). Document the gesture (corner triple-tap, bottom-right, 3 taps within 600ms) in the SUMMARY so the presenter can rehearse it.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "presenterMode" src/screens/PotentialApp.jsx` >= 2 (state + passed as visible).
    - Source assertion: gesture listener at root — `grep -c "addEventListener" src/screens/PotentialApp.jsx` >= 2 (click + touchstart) inside a `useEffect`, with matching `removeEventListener` cleanup (`grep -c "removeEventListener" src/screens/PotentialApp.jsx` >= 2).
    - Source assertion: `grep -c "DemoTierSwitcher" src/screens/PotentialApp.jsx` >= 1 AND `grep -c "RunsBadge" src/screens/PotentialApp.jsx` >= 1.
    - Behavior: the switcher is hidden by default; corner triple-tap toggles it; selecting a tier re-renders the city detail with the Wave-1 gates locking/unlocking; RunsBadge shows the correct chip (nothing for free).
    - `npm test` full suite GREEN (all logic + all four component tests now pass).
  </acceptance_criteria>
  <done>PotentialApp owns presenterMode, the root gesture listener survives step changes, and the switcher + runs badge are mounted; the presenter can cycle all four tiers live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Presenter mode is a demo UX device, not a security boundary. Tier state is in-memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | Presenter-mode corner gesture | accept | Security-by-obscurity over a demo control panel (D-05). Discovery reveals tier controls, not secrets. Documented as NOT a security boundary (RESEARCH Security Domain). |
| T-08-05 | Input Validation | setTier from switcher buttons | mitigate | onTier only ever fires with one of the four `Tier` union keys hardcoded in the switcher array; no free-text input reaches setTier. |

No HIGH-severity threats. Zero new packages.
</threat_model>

<verification>
- `npm test` full suite GREEN — all four component test files now pass.
- Source greps in acceptance criteria pass (gold-literal gates, gesture listener + cleanup, mounts).
- Manual (per VALIDATION.md): corner triple-tap reveals the pill; Free→Basic→Plus→Premium cycle < 60s on the demo device.
</verification>

<success_criteria>
SC4 met end-to-end: a hidden floating pill is summoned by the corner gesture, flips all four tiers live (locking/unlocking the Wave-1 gates), the runs badge surfaces the business model, and judges see a clean app until the presenter summons controls.
</success_criteria>

<output>
Create `.planning/phases/08-freemium-tier-gate/08-03-SUMMARY.md` when done.
</output>

---
phase: 08-freemium-tier-gate
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - src/components/PricingModal.jsx
  - src/screens/PotentialApp.jsx
autonomous: false
requirements: [TIER-03]
must_haves:
  truths:
    - "Clicking any locked section's padlock opens a full 4-tier pricing modal (D-07)"
    - "The modal shows Free/Basic/Plus/Premium side by side with Plus badged 'most popular' and primary CTA (D-08)"
    - "The modal includes the 30-day money-back guarantee + credits-never-expire + no-subscription copy (D-10)"
    - "The modal includes 3 testimonial cards (D-11)"
    - "Selecting a tier CTA sets the tier and closes the modal (sections unlock)"
    - "The modal locks body scroll while open and is responsive (Plus card first on mobile) (D-13)"
  artifacts:
    - path: "src/components/PricingModal.jsx"
      provides: "4-tier pricing overlay + testimonials, final UI-SPEC copy, gold-literal"
      contains: "most popular"
      min_lines: 80
    - path: "src/screens/PotentialApp.jsx"
      provides: "PricingModal mounted + wired to modalOpen/setTier"
      contains: "PricingModal"
  key_links:
    - from: "src/screens/PotentialApp.jsx"
      to: "src/components/PricingModal.jsx"
      via: "<PricingModal open={modalOpen} onClose=... onTier={setTier} currentTier={tier} />"
      pattern: "<PricingModal"
    - from: "src/components/LockGate onUnlock (Wave 1)"
      to: "PotentialApp setModalOpen(true)"
      via: "onUnlock callback opens modal"
      pattern: "setModalOpen"
---

<objective>
Deliver the upsell vertical slice end-to-end (SC3): build the 4-tier PricingModal and wire it so any locked padlock opens it. After this plan, the full funnel closes the loop — a free user hits a lock, opens the pricing modal, sees Plus as the hero, picks a tier, and watches the gates dissolve. This is the conversion beat.

Purpose: The single deliberate conversion moment + the business-model trust copy.
Output: PricingModal.jsx, mounted and wired in PotentialApp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-freemium-tier-gate/08-PATTERNS.md
@.planning/phases/08-freemium-tier-gate/08-RESEARCH.md
@.planning/phases/08-freemium-tier-gate/08-UI-SPEC.md
@.planning/phases/08-freemium-tier-gate/08-03-SUMMARY.md

<interfaces>
<!-- HOST FILE REALITY -->
- PotentialApp already has `tier`/`setTier` (Wave 1) and `modalOpen`/`setModalOpen` (Wave 1). LockGate onUnlock already calls setModalOpen(true), and the ResultsMap blurred-stack receives onUnlock={() => setModalOpen(true)} (Wave 1, 08-02). This plan mounts the modal and makes it real.
- Mount PricingModal alongside DemoTierSwitcher (Wave 2) — outside the per-step switches so it overlays every screen.

<!-- Analog idioms (verified) -->
- Overlay panel: Landing.jsx `.panel` :84-85 (opacity + translateY transition on `.open`).
- Body scroll-lock: REUSE existing class `lp-locked` (Landing.jsx :15 defines `body.lp-locked{overflow:hidden;height:100vh}`; add on open, remove on close + cleanup). Do NOT invent a new class.
- Responsive grid: scoped `<style>` `@media(max-width:520px)` (ResultsMap.jsx :74 idiom). Plus card gets `order:-1` on mobile.
- backdrop-filter on the modal CARD only; full-screen dim layer uses plain rgba (GPU-safe).

<!-- COPY IS FINAL — source from UI-SPEC, NOT the RESEARCH PENDING slot -->
RESEARCH Pattern 6 still ships a "PENDING OQ-1 / Option B (no money-back)" placeholder comment. IGNORE it. OQ-1 is RESOLVED: include the guarantee. Use the UI-SPEC Copywriting Contract verbatim:
- Header: "Unlock Your Full Potential"
- Subhead: "Credits never expire · No subscription · No commitment"
- Trust footer: "30-day money-back guarantee · Credits never expire · No subscription"
- Dismiss button label: "Dismiss"
- Tier feature bullets + prices + CTAs: UI-SPEC "Feature Copy by Tier" table (Basic = "Top 3 cities fully revealed", etc.)
- 3 testimonials: UI-SPEC "Testimonial Copy" (Alex M. / Priya K. / Jordan T., all ★★★★★)

<!-- THEME-SCOPE HAZARD -->
PricingModal mounts inside PotentialApp's GREEN scope. HARDCODE gold literals throughout:
Plus price/CTA/badge/checkmarks #e2b56b; Plus card border 1.5px solid rgba(226,181,107,.5), bg rgba(226,181,107,.06). NEVER var(--accent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PricingModal (4 tiers + testimonials, final copy, gold-literal)</name>
  <files>src/components/PricingModal.jsx</files>
  <read_first>
    - .planning/phases/08-freemium-tier-gate/08-UI-SPEC.md (Copywriting Contract — ALL final copy: header/subhead/footer/CTAs/feature bullets/testimonials; Color reserved-for + Plus-distinction; Interaction Contract entry/exit + body-lock; Responsive Breakpoint)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (Pattern 5 Landing .panel idiom + Pattern 6 PricingModal structure — STRUCTURE only; copy comes from UI-SPEC, ignore the OQ-1 PENDING comment)
    - .planning/phases/08-freemium-tier-gate/08-PATTERNS.md (PricingModal section: reuse lp-locked, backdrop-filter on card only, scoped media query, hardcode gold)
    - src/screens/Landing.jsx (.panel transition + lp-locked add/remove)
  </read_first>
  <behavior>
    - PricingModal open=false → renders nothing (returns null).
    - PricingModal open=true → renders four tier columns (Free/Basic/Plus/Premium) with prices, feature bullets, CTAs.
    - Plus column carries a "MOST POPULAR" badge and the primary (gold) CTA.
    - Trust footer contains "30-day money-back guarantee".
    - Three testimonial cards render with 5 stars each.
    - Clicking a tier CTA calls onTier(tierKey) then onClose; clicking the dim layer or "Dismiss" calls onClose; the Free column shows "Current plan" (non-clickable).
    - On open, body gets class `lp-locked`; on close/unmount it is removed.
  </behavior>
  <action>
    Create `PricingModal({ open, onClose, onTier, currentTier })`. `if (!open) return null`. useEffect on `open`: add/remove `lp-locked` on document.body (reuse the existing class), cleanup removes it. Render a fixed full-screen dim layer (plain rgba, onClick=onClose) containing a modal card (stopPropagation; backdrop-filter on the CARD only) with: header + subhead (UI-SPEC strings); a `.pm-grid` 4-column tier grid from a TIERS_CONFIG array (UI-SPEC Feature Copy by Tier — Basic = "Top 3 cities fully revealed"); Plus gets className "pm-plus-card", the "MOST POPULAR" badge (D-08), gold price/CTA, thicker accent border; each tier CTA `onClick={() => { onTier(key); onClose(); }}` except Free which shows a non-clickable "Current plan"; checkmarks "✓" in gold; trust footer (UI-SPEC exact: "30-day money-back guarantee · Credits never expire · No subscription"); a `.pm-testimonials` grid of 3 cards (UI-SPEC Testimonial Copy, ★★★★★); a "Dismiss" close button. Add a scoped `<style>` with `.pm-grid`/`.pm-testimonials` and `@media(max-width:520px)` collapsing to single column + `.pm-plus-card{order:-1}`. Entry/exit transition on opacity+transform per UI-SPEC Interaction Contract. HARDCODE gold literals throughout — never var(--accent). Do NOT include any "no money-back" copy or the RESEARCH PENDING placeholder.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (copy, final): `grep -ic "30-day money-back guarantee" src/components/PricingModal.jsx` >= 1 AND `grep -ic "most popular" src/components/PricingModal.jsx` >= 1 AND `grep -ic "Top 3 cities fully revealed" src/components/PricingModal.jsx` >= 1.
    - Source assertion: NO stale/pending copy — `grep -ic "no money-back\|PENDING\|OQ-1" src/components/PricingModal.jsx` === 0.
    - Source assertion (body-lock reuse): `grep -c "lp-locked" src/components/PricingModal.jsx` >= 1 (no new class invented).
    - Source assertion (theme gate): `grep -c "#e2b56b\|226,181,107" src/components/PricingModal.jsx` >= 1 AND `grep -c "var(--accent)" src/components/PricingModal.jsx` === 0.
    - Source assertion: three testimonials — grep "Alex M.", "Priya K.", "Jordan T." each return a match.
    - Source assertion (GPU-safe overlay): backdrop-filter appears only on the card, not the dim layer — manual review of the two style blocks; `grep -c "backdrop" src/components/PricingModal.jsx` should reflect card-only usage.
    - `npm test` full suite remains GREEN (no regression).
  </acceptance_criteria>
  <done>PricingModal renders the 4-tier grid with Plus as hero, final UI-SPEC copy including the money-back guarantee, 3 testimonials, reused body-lock, responsive layout, gold literals only.</done>
</task>

<task type="auto">
  <name>Task 2: Mount + wire PricingModal in PotentialApp</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (whole file; modalOpen/setModalOpen + tier/setTier already present from Wave 1; switcher mount point from Wave 2)
    - src/components/PricingModal.jsx (props contract just built)
    - .planning/phases/08-freemium-tier-gate/08-RESEARCH.md (State Architecture — mount overlays outside the step switch)
  </read_first>
  <action>
    Mount `<PricingModal open={modalOpen} onClose={() => setModalOpen(false)} onTier={setTier} currentTier={tier} />` alongside the DemoTierSwitcher (Wave 2), outside the per-step switches so it overlays every screen. Confirm the Wave-1 LockGate `onUnlock` handlers already call `setModalOpen(true)`, and that the ResultsMap blurred-stack already receives `onUnlock={() => setModalOpen(true)}` (wired in 08-02 Task 2 — the blurred-stack count CTA calls that onUnlock, NOT onSelect); if any locked section (financials/live-AI/why/visa/roadmap CTA/ResultsMap blurred-stack) is not yet wired to open the modal, wire it now. Verify selecting a tier from the modal updates `tier` and dissolves the relevant gates.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "<PricingModal" src/screens/PotentialApp.jsx` >= 1 with `open={modalOpen}` and `onTier={setTier}`.
    - Source assertion: every locked entry point opens the modal — `grep -c "setModalOpen(true)" src/screens/PotentialApp.jsx` >= 1 and the ResultsMap blurred-stack onUnlock path resolves to opening the modal (the `onUnlock={() => setModalOpen(true)}` passed to ResultsMap in 08-02 Task 2).
    - Behavior: at tier="free", clicking any padlock (financials, live-AI, why, visa, roadmap CTA, or the blurred-city-stack count) opens the PricingModal; clicking "Get Plus" sets tier="plus" and the live-AI + roadmap gates dissolve; "Dismiss"/dim-click closes it.
    - `npm test` full suite GREEN.
  </acceptance_criteria>
  <done>PricingModal is mounted over every screen and opened by every locked padlock (including the ResultsMap blurred-stack via its onUnlock); selecting a tier sets state and unlocks the matching sections. The full freemium funnel is closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the full freemium funnel on the demo device</name>
  <what-built>
    The complete freemium funnel is now wired: free teaser (blur-real + frosted-skeleton locks), hidden DemoTierSwitcher via corner triple-tap, RunsBadge, and the 4-tier PricingModal opened by every locked padlock. The behaviors below are visual / gesture / timing / cross-device and cannot be verified in jsdom — they are the manual checks listed in 08-VALIDATION.md, including phase success criterion SC4.
  </what-built>
  <action>No code in this task — verification only. The implementation landed in Tasks 1–2; here the presenter manually exercises the behaviors below on the actual demo device and approves or reports issues.</action>
  <how-to-verify>
    Run the app on the actual presenting device (`npm run dev`). Then:
    1. SC4 (key): Reveal the switcher (bottom-right corner triple-tap, 3 taps within 600ms) and cycle Free → Basic → Plus → Premium. Time it — the full cycle, with every tier's gates visibly locking/unlocking, must complete in under 60 seconds.
    2. TIER-02 rank cutoff (required — most visible TIER-02 behavior): Switch to each tier and confirm the ranked results list shows: Free → 1 city, Basic → 3 cities, Plus/Premium → all cities visible (no blurred stack at Plus/Premium). The remainder at Free/Basic must render as the blurred stack with the "{N} more cities matched — unlock your full ranking" count, and clicking that stack must open the PricingModal (not navigate to a city).
    3. D-05: Confirm the DemoTierSwitcher is hidden on load (judges see a clean app) and that the corner triple-tap reliably reveals/hides it.
    4. TIER-01: At each tier, confirm the correct sections are blurred-with-padlock vs. unlocked, and that the why/visa sections show a frosted skeleton (this is the intended graceful-degradation state, NOT a bug).
    5. D-09: Switch a locked→unlocked tier and watch the blur-dissolve — padlock fades, blur resolves to focus, no layout shift, smooth on battery.
    6. D-13: Open the PricingModal at desktop and mobile widths — background scroll is locked, all 4 tiers legible, Plus card appears first on mobile.
  </how-to-verify>
  <read_first>
    - .planning/phases/08-freemium-tier-gate/08-VALIDATION.md (Manual-Only Verifications table — the rows this checkpoint covers)
  </read_first>
  <acceptance_criteria>
    - SC4: timed Free→Premium cycle on the demo device is under 60 seconds (stopwatch).
    - TIER-02: ranked-list card count is correct per tier — Free → 1 city, Basic → 3 cities, Plus/Premium → all; the blurred-stack count CTA opens the PricingModal.
    - The switcher is hidden by default and revealed only by the corner triple-tap gesture.
    - Each tier shows the correct locked/unlocked/frosted-skeleton presentation per the visibility matrix.
    - Blur-dissolve is smooth with no layout shift on battery.
    - PricingModal body-lock + responsive layout (Plus first on mobile) verified at both widths.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any tier/gesture/visual/timing issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | The pricing modal performs no payment, no network call, no persistence. Selecting a tier sets in-memory React state only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | Testimonial / pricing copy | accept | All copy is static hardcoded strings, not user input — no XSS/injection surface (RESEARCH Security Domain). |
| T-08-05 | Input Validation | onTier from modal CTAs | mitigate | onTier fires only with the four `Tier` union keys defined in TIERS_CONFIG; no free-text reaches setTier. |

No payment processing exists (v2-deferred), so no PCI / billing attack surface. No HIGH-severity threats. Zero new packages.
</threat_model>

<verification>
- `npm test` full suite GREEN (all logic + component tests).
- Source greps in acceptance criteria pass (final copy present, no PENDING/no-money-back copy, lp-locked reused, gold-literal gate).
- Manual (per VALIDATION.md): modal body-lock + responsive layout (Plus first on mobile) at desktop and mobile widths; clicking a padlock opens the modal; selecting Plus dissolves the gates; ranked-list card count correct per tier (Free 1 / Basic 3 / Plus·Premium all).
</verification>

<success_criteria>
SC3 met end-to-end: every locked section opens a 4-tier pricing modal with Plus badged "most popular" as the primary CTA, the money-back/credits-never-expire/no-subscription trust copy, and 3 testimonials; selecting a tier closes the loop and unlocks content. The complete freemium funnel (teaser → lock → modal → unlock) is demonstrable.
</success_criteria>

<output>
Create `.planning/phases/08-freemium-tier-gate/08-04-SUMMARY.md` when done.
</output>

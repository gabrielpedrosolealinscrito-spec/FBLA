---
phase: 03-matching-us-financial-spine
plan: 03
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - shared/engine/financial.ts
  - shared/engine/financial.test.ts
autonomous: true
requirements: [FIN-01]
user_setup: []

must_haves:
  truths:
    - "computeFederalTax applies the real progressive TY2026 single-filer brackets + standard deduction (D-07)"
    - "computeUSTax adds flat state % and real FICA 7.65% on top of federal, with cost-indexed expenses (D-08)"
    - "computeUSExpenses produces a cost-indexed ExpenseBreakdown that never yields NaN"
    - "The US financial model is registered in a pluggable registry keyed by financialModelId"
  artifacts:
    - path: "shared/engine/financial.ts"
      provides: "computeFederalTax, computeUSTax, computeSalary, computeUSExpenses, FINANCIAL_MODELS registry"
      exports: ["computeFederalTax", "computeUSTax", "FINANCIAL_MODELS"]
    - path: "shared/engine/financial.test.ts"
      provides: "GREEN FIN-01 unit tests (bracket math, edges, state delta, NaN guard)"
  key_links:
    - from: "shared/engine/financial.ts"
      to: "shared/data/constants.js"
      via: "import BASE_SALARIES"
      pattern: "BASE_SALARIES"
---

<objective>
Implement the US financial model as pure TypeScript: progressive TY2026 federal brackets + standard deduction (D-07, replacing the prototype's flat 22%), flat state % + real FICA 7.65% (D-08), and the cost-indexed expense model ported from the prototype. Expose it through a `FinancialModel` interface and a `FINANCIAL_MODELS` registry keyed by `financialModelId` so Phase 4 can append country models without touching the US spine (FIN-02 forward-compat). This turns the RED `financial.test.ts` from Plan 01 GREEN.

Purpose: FIN-01 — every city's projection (salary, take-home after taxes, itemized expenses, monthly savings) must be judge-defensible. A judge asking "how did you get take-home?" gets real bracket math, not a flat rate.

Output: `shared/engine/financial.ts` (implementation) with `shared/engine/financial.test.ts` GREEN.

TDD: financial.test.ts already exists (RED) from Plan 01. Follow RED → GREEN → REFACTOR. Do NOT weaken a test to make it pass.
</objective>

<phase_goal>
**As a** person deciding where to move, **I want to** see a real income-adjusted financial breakdown per city (salary, take-home after taxes, itemized expenses, monthly savings), **so that** I can trust what my money would actually look like there.

(Derived capability framing per MVP rules — ROADMAP goal not in canonical user-story form.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-matching-us-financial-spine/03-RESEARCH.md
@.planning/phases/03-matching-us-financial-spine/03-PATTERNS.md

<interfaces>
From shared/types.ts (consume these — do not modify here):
```typescript
export interface ExpenseBreakdown { rent; food; transport; utilities; insurance; personal; childcare; pets; debtPay; total: number; }
export interface Profile { profession; income; debt; housing: "rent"|"buy"; hasPartner; partnerIncome; hasDependents; numDependents; hasPets; hasRemote; /* ... */ }
export interface City { costIndex; medianRent; medianHome; stateTax; /* ... */ }
```
From shared/engine/financial.ts (the interface to implement — RESEARCH D-08):
```typescript
export interface FinancialModel { id: string; computeTax(grossIncome: number, stateRate: number): number; computeExpenses(profile: Profile, city: City): ExpenseBreakdown; }
export const FINANCIAL_MODELS: Record<string, FinancialModel>;  // { "us": US_FINANCIAL_MODEL }
```
</interfaces>
</context>

<feature>
  <name>US financial model (federal brackets + state + FICA + cost-indexed expenses)</name>
  <files>shared/engine/financial.ts, shared/engine/financial.test.ts</files>
  <behavior>
    computeFederalTax(grossIncome): subtract $16,100 standard deduction, then apply TY2026 single brackets cumulatively.
    - computeFederalTax(110000) === 15370 ± 1 (taxable 93,900)
    - computeFederalTax(0) === 0
    - computeFederalTax(16100) === 0 (exactly at deduction)
    - monotonic non-decreasing in income
    computeUSTax(gross, stateRate): federal + gross*(stateRate/100) + gross*0.0765
    - computeUSTax(75000, 9.85) > computeUSTax(75000, 0)
    computeSalary(profile, city): BASE_SALARIES[profession] ?? 55000, × costIndex/100, rounded
    computeUSExpenses(profile, city): cost-indexed breakdown; total = sum of parts
    - city.costIndex <= 0 must be guarded so NO field is NaN (V5 input-validation threat)
  </behavior>
  <implementation>
    Read RESEARCH ## D-08 for the exact bracket array, function bodies, and expense formula. The TY2026 single brackets are: 10% to $12,400; 12% to $50,400; 22% to $105,700; 24% to $201,775; 32% to $256,225; 35% to $640,600; 37% above. Standard deduction $16,100. computeFederalTax iterates the bracket array accumulating `min(taxable,limit)-prev) * rate`. computeUSTax = federal + state(flat %) + FICA flat 0.0765 (add a comment: SS wage cap deferred per D-08; single-filer brackets applied to household income — note this simplification per RESEARCH Dual-Income section). computeSalary imports BASE_SALARIES from '../data/constants.js'. computeUSExpenses ports the prototype arithmetic verbatim (PATTERNS.md financial.ts analog) with `m = city.costIndex / 100`; add a guard `const idx = city.costIndex > 0 ? city.costIndex : 1; const m = idx/100;` so a zero/negative costIndex cannot produce NaN (T-3-04). Register `US_FINANCIAL_MODEL: FinancialModel = { id:"us", computeTax: computeUSTax, computeExpenses: computeUSExpenses }` and `FINANCIAL_MODELS = { "us": US_FINANCIAL_MODEL }` (Phase 4 appends here). Cite the IRS/Tax Foundation source URL in a comment above the bracket array. Do NOT change the expected values in financial.test.ts — if math disagrees, fix the implementation.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement financial.ts to turn financial.test.ts GREEN</name>
  <files>shared/engine/financial.ts, shared/engine/financial.test.ts</files>
  <read_first>
    - shared/engine/financial.test.ts (the RED tests from Plan 01 — the exact assertions to satisfy; do NOT edit to weaken)
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## D-07 brackets + reference calc; ## D-08 module interface, federal tax function, expense model)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### shared/engine/financial.ts — closure→pure-params transformation; the take-home line to REPLACE; expense port-verbatim block)
    - shared/data/constants.js (BASE_SALARIES — imported by computeSalary)
    - shared/types.ts (Profile, City, ExpenseBreakdown — the consumed shapes)
  </read_first>
  <action>RED → GREEN → REFACTOR. (1) Run `npx vitest run shared/engine/financial.test.ts` to confirm RED. (2) Implement `shared/engine/financial.ts` per the feature block + RESEARCH D-08: `STANDARD_DEDUCTION_SINGLE_2026 = 16100`, `FEDERAL_BRACKETS_2026` array (limits 12400/50400/105700/201775/256225/640600/Infinity at 0.10/0.12/0.22/0.24/0.32/0.35/0.37), `computeFederalTax`, `computeUSTax` (federal + flat state% + flat FICA 0.0765), `computeSalary` (BASE_SALARIES × costIndex/100), `computeUSExpenses` (cost-indexed, with costIndex<=0 NaN guard), the `FinancialModel` interface, `US_FINANCIAL_MODEL`, and `FINANCIAL_MODELS` registry. Add source-citation comments above the bracket array and the FICA/single-filer simplification comments. (3) Run the test again — GREEN. Refactor only if it stays GREEN.</action>
  <verify>
    <automated>npx vitest run shared/engine/financial.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine/financial.test.ts` exits 0 (all GREEN)
    - `shared/engine/financial.ts` exports `computeFederalTax`, `computeUSTax`, `computeSalary`, `computeUSExpenses`, `FINANCIAL_MODELS`
    - `FINANCIAL_MODELS["us"]` resolves and `.computeTax`/`.computeExpenses` are functions
    - No NaN for `computeUSExpenses(profile, {...city, costIndex: 0})` (guard present)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - financial.test.ts assertions unchanged from Plan 01 (no weakened expectations)
  </acceptance_criteria>
  <done>The US financial model computes real progressive TY2026 tax + FICA + state + cost-indexed expenses; all FIN-01 unit tests pass; registry is pluggable for Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile/City values → financial math | Quiz-derived numeric inputs (income, costIndex, stateTax) enter pure tax/expense functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Denial of service (NaN propagation) | computeUSExpenses with costIndex<=0 | mitigate | Guard `idx = costIndex>0 ? costIndex : 1` before dividing; unit test asserts no NaN |
| T-3-05 | Tampering (input validation, V5) | grossIncome / stateRate negative or malformed | mitigate | `Math.max(0, ...)` on taxable income; stateRate applied as flat % (no exponent path) |
| T-3-SC | Tampering | npm installs | n/a | No installs in this plan |
</threat_model>

<verification>
- `npx vitest run shared/engine/financial.test.ts` GREEN.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- Bracket math matches the IRS-cited reference (computeFederalTax(110000)=15370±1).
</verification>

<success_criteria>
- Real progressive TY2026 federal tax replaces flat 22%.
- State % + FICA 7.65% + cost-indexed expenses all computed.
- Pluggable FINANCIAL_MODELS registry exists for Phase 4.
- financial.test.ts is GREEN with unchanged expectations.
</success_criteria>

<output>
Create `.planning/phases/03-matching-us-financial-spine/03-03-SUMMARY.md` when done.
</output>

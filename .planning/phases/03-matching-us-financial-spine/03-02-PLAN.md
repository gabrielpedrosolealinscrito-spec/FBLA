---
phase: 03-matching-us-financial-spine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - shared/types.ts
  - shared/engine/scoring-weights.ts
  - shared/data/cities.ts
autonomous: true
requirements: [MATCH-01, FIN-01]
user_setup: []

must_haves:
  truths:
    - "The City contract carries every field the engine and dealbreaker checks need (stateTax, summerHighF, winterLowF, nearMountains, nearCoast, hasIntlAirport, pop, climate) — city data moves into shared/data/cities.ts with contract fields (D-09) and the enriched dealbreaker-fact fields (D-11)"
    - "Profile carries an optional weights field so the engine can consume Phase 2 output later while compiling now"
    - "22 typed US cities exist as a single importable dataset with cited, US-avg=100-scaled costIndex values — ~20-25 curated US cities (D-10) with real cited figures (D-12)"
    - "Every scoring magic number lives in one tunable config object (D-03)"
  artifacts:
    - path: "shared/types.ts"
      provides: "Extended City interface + optional Profile.weights"
      contains: "summerHighF"
    - path: "shared/engine/scoring-weights.ts"
      provides: "SCORING_WEIGHTS tunable config (global, dealbreaker, lifestyle, normalization)"
      contains: "SCORING_WEIGHTS"
    - path: "shared/data/cities.ts"
      provides: "CITIES_DATA: City[] — 22 curated US cities typed to the contract"
      contains: "CITIES_DATA"
  key_links:
    - from: "shared/data/cities.ts"
      to: "shared/types.ts"
      via: "import type { City }"
      pattern: "import type \\{ City \\}"
---

<objective>
Land the CONTRACT-FIRST foundation: extend `shared/types.ts` with the City fields the engine and dealbreaker checks depend on, add the optional `Profile.weights` field, create the single tunable `SCORING_WEIGHTS` config (D-03), and build the 22-city typed dataset (`shared/data/cities.ts`, D-09/D-10/D-11/D-12). Everything downstream (financial.ts, scoring.ts, dealbreakers.ts, index.ts) compiles against these shapes, so this MUST precede engine work (STRUCTURE.md contract-first rule).

Purpose: Without these fields the engine cannot evaluate dealbreakers against real city facts (D-11) or compute state tax (D-08), and without one config tuning is a logic hunt (D-03). The dataset is the source-of-truth list the engine ranks.

Output: extended `shared/types.ts`, new `shared/engine/scoring-weights.ts`, new `shared/data/cities.ts`.

MVP / division-of-labor note: planned under MVP_MODE=true, but the phase's division_of_labor (backend logic vs. friend-authored UI) governs layering. This plan is pure backend contract+data; it has no UI slice by design.
</objective>

<phase_goal>
**As a** person deciding where to move, **I want to** receive a ranked list of US cities scored against my profile with a real income-adjusted financial breakdown, **so that** I can see which cities fit me and what my money would look like there.

(Derived capability framing — ROADMAP Phase 3 Goal is not in canonical user-story form; surfaced per MVP rules, not invented.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-matching-us-financial-spine/03-RESEARCH.md
@.planning/phases/03-matching-us-financial-spine/03-PATTERNS.md

<interfaces>
From shared/types.ts (existing City — DO NOT remove any field; extend in place):
```typescript
export interface City {
  name: string; country: string; emoji: string; lat: number; lng: number;
  costIndex: number; medianRent: number; medianHome: number; avgTemp: number;
  vibe: string[]; walkScore: number; transitScore: number; safetyIndex: number;
  jobGrowth: number; topIndustries: string[]; financialModelId: string;
}
```
From shared/types.ts (existing Profile — add ONE optional field):
```typescript
export interface Profile { /* ...existing... */ importanceRank: string[]; moveTimeline: string; }
```
From shared/data/constants.js (consumed by the dataset's dealbreaker mapping):
```javascript
export const DEAL_BREAKERS = [ "No extreme cold","No extreme heat","Must have public transit",
  "Must be walkable","No state income tax","Must be near mountains","Must be near ocean/coast",
  "Low crime only","Need international airport","Must have strong job market in my field" ];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make tsconfig compatible with .ts engine modules importing constants.js and with excluded test files</name>
  <files>tsconfig.json</files>
  <read_first>
    - tsconfig.json (current: strict, no allowJs, include shared/**/*.ts + api/**/*.ts — which matches the .test.ts files written in Plan 01 AND would typecheck constants.js imports)
    - shared/data/constants.js (the typeless .js the engine imports BASE_SALARIES/DEAL_BREAKERS/PROFESSION_CATEGORIES from)
  </read_first>
  <action>Edit `tsconfig.json` to unblock the engine's `tsc --noEmit` gates used by every downstream plan. Add `"allowJs": true` to compilerOptions — VERIFIED REQUIRED: a `.ts` file inside the `include` importing `./data/constants.js` throws TS7016 ("implicitly has an 'any' type") under strict without it, and financial.ts (BASE_SALARIES) + dealbreakers.ts (DEAL_BREAKERS, PROFESSION_CATEGORIES) both import constants.js. Add `"exclude": ["**/*.test.ts", "node_modules"]` — VERIFIED REQUIRED: Plan 01 writes `shared/engine/*.test.ts` that import not-yet-existing modules; with tests in the `include`, `tsc` throws TS2307/TS2304 (cannot find module / cannot find name 'test'/'expect'), which would make the per-plan `tsc` gates in Plans 02–06 fail for reasons unrelated to the production code. Vitest still runs the excluded tests via esbuild (the intended RED is the runtime "cannot find module", not a tsc error). Keep all existing compilerOptions (target ES2022, module ESNext, moduleResolution bundler, strict, skipLibCheck, noEmit) and the existing include. Do NOT set checkJs (we allow JS, we don't typecheck it).</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && node -e "const t=require('./tsconfig.json'); if(!t.compilerOptions.allowJs){console.error('allowJs missing');process.exit(1)} if(!(t.exclude||[]).some(e=>/\*\.test\.ts/.test(e))){console.error('test exclude missing');process.exit(1)} console.log('tsconfig-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.json` compilerOptions contains `"allowJs": true`
    - `tsconfig.json` `exclude` contains `"**/*.test.ts"`
    - Existing compilerOptions (strict, noEmit, moduleResolution bundler) unchanged
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (current repo state, before engine modules exist — production files only)
  </acceptance_criteria>
  <done>tsconfig allows .ts→constants.js imports and excludes test files, so every downstream `tsc --noEmit` gate measures production code, not unresolved test imports.</done>
</task>


<task type="auto">
  <name>Task 2: Extend the City and Profile contract in shared/types.ts</name>
  <files>shared/types.ts</files>
  <read_first>
    - shared/types.ts (lines 12–61 — the Profile and City interfaces to extend in place; do not touch MatchResult/ExpenseBreakdown which are already correct)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### shared/types.ts — exact list of fields to ADD with their inline comments)
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## Critical Contract Gap table; ## Engine Input Contract for the weights decision)
  </read_first>
  <action>Add these eight fields to the `City` interface (after `financialModelId`, do not remove existing fields): `stateTax: number` (flat state income tax %, 0 = none), `summerHighF: number` (NOAA avg daily high, hottest month °F), `winterLowF: number` (NOAA avg daily low, coldest month °F), `nearMountains: boolean` (within ~60-min drive of a major range), `nearCoast: boolean` (within ~60-min drive of ocean/coastal bay), `hasIntlAirport: boolean` (direct international routes), `pop: string` (display metro population label), `climate: string` (display description). Add ONE optional field to `Profile`: `weights?: { cost: number; career: number; lifestyle: number; safety: number }`. It is OPTIONAL because Phase 2 has NOT yet executed (verified: types.ts currently has only `importanceRank`, the prototype derives weights from rank). Optional keeps strict-mode compilation green now AND lets the engine consume Phase 2's output later via `profile.weights ?? deriveFromRank(...)`. Add a one-line comment above it: `// Phase 2 emits this; if absent the engine derives it from importanceRank`. Keep this a small, announced commit per STRUCTURE.md.</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -q "summerHighF" shared/types.ts && grep -q "weights?" shared/types.ts && echo "contract-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `shared/types.ts` City interface contains: stateTax, summerHighF, winterLowF, nearMountains, nearCoast, hasIntlAirport, pop, climate
    - `shared/types.ts` Profile interface contains `weights?:` (optional) with the four numeric subfields
    - No existing City/Profile/MatchResult field removed
    - `npx tsc --noEmit -p tsconfig.json` exits 0 under strict mode
  </acceptance_criteria>
  <done>The contract carries all dealbreaker + financial fields and the optional weights field; strict-mode typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create the tunable scoring-weights config</name>
  <files>shared/engine/scoring-weights.ts</files>
  <read_first>
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## D-03/D-04: Scoring Engine Architecture — the full SCORING_WEIGHTS object shape with global/dealbreaker/lifestyle/normalization sections)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### scoring-weights.ts — every inline coefficient from getMatchScore that must get a named slot: 0.2 cost, 2 career jobGrowth, 0.08 safety, 25/30 penalties)
    - shared/data/constants.js (header-comment + named-export module convention to mirror)
  </read_first>
  <action>Create `shared/engine/scoring-weights.ts` exporting a single `export const SCORING_WEIGHTS = { ... } as const` object. Mirror the constants.js header-comment block (file name, purpose, who reads it). Structure it by concern exactly per RESEARCH: `global: { cost, career, lifestyle, safety }` (factor dominance multipliers, start cost/career/lifestyle 1.0, safety 0.8), `dealbreaker: { penalty: 30 }` (points deducted per triggered dealbreaker), `lifestyle: { tagVibeBonus, walkBonus, startupBonus }`, and `normalization: { costMaxContribution: 20, careerMaxContribution: 20, lifestyleMaxContribution: 20, safetyMaxContribution: 12 }`. Also export the recalibrated dealbreaker thresholds as named constants so dealbreakers.ts imports them instead of hardcoding: `heatThresholdF: 95`, `coldThresholdF: 25`, `transitMin: 40`, `walkMin: 50`, `safetyMin: 55`, `jobGrowthMin: 2.0`. Every value here is a tuning knob (D-03) — no scoring number may be inlined elsewhere. Add `export const BASE_SCORE = 50;`.</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && node -e "import('./shared/engine/scoring-weights.ts').catch(()=>{}); process.exit(0)" ; grep -q "SCORING_WEIGHTS" shared/engine/scoring-weights.ts && grep -q "penalty" shared/engine/scoring-weights.ts && grep -q "BASE_SCORE" shared/engine/scoring-weights.ts && echo "weights-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `shared/engine/scoring-weights.ts` exports `SCORING_WEIGHTS` `as const` with global/dealbreaker/lifestyle/normalization sections
    - Exports recalibrated thresholds incl. `heatThresholdF` (95) and `coldThresholdF` (25)
    - Exports `BASE_SCORE = 50`
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>One config file holds every scoring coefficient, penalty, and threshold; tuning is a one-constant edit.</done>
</task>

<task type="auto">
  <name>Task 4: Build the 22-city typed US dataset</name>
  <files>shared/data/cities.ts</files>
  <read_first>
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## D-10/D-11/D-12: Curated 22-City US Dataset — the full 22-row table with EXACT costIndex/medianRent/stateTax/walkScore/transitScore/safetyIndex/jobGrowth/summerHighF/winterLowF/nearMountains/nearCoast/hasIntlAirport values; the Sources key; the costIndex rescaling note; the Notes on specific entries; Pitfall 4 on Minneapolis state rate)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### shared/data/cities.ts — target module shape, header comment, import type from '../types.js', drop the prototype-only `color` field)
    - src/screens/PotentialApp.jsx (lines 11–24 CITIES_DATA — source for medianHome, topIndustries, vibe, lat/lng/emoji for the original 12 cities)
    - shared/types.ts (the extended City interface from Task 1 — the dataset must satisfy it exactly)
  </read_first>
  <action>Create `shared/data/cities.ts` exporting `export const CITIES_DATA: City[]` with `import type { City } from '../types.js'`. Populate all 22 cities from the RESEARCH 22-city table (Austin, Nashville, Miami, Denver, Pittsburgh, Raleigh, Portland, Boise, Salt Lake City, Chicago, San Diego, Seattle, Minneapolis, Phoenix, Atlanta, Charlotte, Tampa, Columbus, Indianapolis, San Antonio, Dallas, Brooklyn/NYC). Use the EXACT table values for costIndex, medianRent, stateTax, walkScore, transitScore, safetyIndex, jobGrowth, summerHighF, winterLowF, nearMountains, nearCoast, hasIntlAirport. Set `country: "US"` and `financialModelId: "us"` on every city (D-09). Carry `medianHome`, `topIndustries`, `vibe`, `lat`, `lng`, `emoji`, `avgTemp` from the prototype for the original 12; assign reasonable values (planner discretion per CONTEXT) for the 10 new cities — `topIndustries` and `vibe` must be populated for all 22 (engine reads them). Drop the prototype-only `color` field (not in contract). Resolve the documented data calls: for Minneapolis use stateTax `7.85` (effective rate for the demo salary band per Pitfall 4 / Open Question 3, NOT the 9.85 top marginal); add a `// ` comment at that entry explaining the choice. Add a header comment block stating: costIndex is US-national-average=100 scale (Numbeo NYC=100 × 1.431, Austin=103 anchor), and the per-field source key (Zumper May 2026, Numbeo 2026, Walk Score, CurrentResults/NOAA, BLS, Tax Foundation). For [ASSUMED] entries (Boise hasIntlAirport=false, Ohio ~3.99% rate), add inline `// [ASSUMED — verify]` comments so a judge probe has a flagged answer.</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && node -e "const s=require('fs').readFileSync('shared/data/cities.ts','utf8'); const n=(s.match(/name:/g)||[]).length; if(n<22){console.error('only',n,'cities');process.exit(1)} if(!/summerHighF/.test(s)||!/financialModelId/.test(s)){console.error('missing required field');process.exit(1)} if(/color:/.test(s)){console.error('prototype-only color field present');process.exit(1)} console.log('cities-ok',n)"</automated>
  </verify>
  <acceptance_criteria>
    - `shared/data/cities.ts` exports `CITIES_DATA: City[]` with exactly 22 city objects (>=22 `name:` occurrences)
    - Every city has `country: "US"`, `financialModelId: "us"`, `summerHighF`, `winterLowF`, `nearMountains`, `nearCoast`, `hasIntlAirport`, `topIndustries`, `vibe`
    - No `color:` field present (dropped)
    - Minneapolis stateTax is 7.85 with an explanatory comment
    - Header comment documents the US-avg=100 costIndex scale and the source key
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (dataset satisfies the City contract)
  </acceptance_criteria>
  <done>22 typed, cited US cities import cleanly against the extended City contract and carry every dealbreaker fact field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — static typed data + config, fully offline, author-controlled) | No untrusted input crosses any boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Tampering | City dataset values | accept | Values are static, cited, author-authored; offline single-user demo — no runtime mutation path |
| T-3-03 | Information disclosure | costIndex scale mismatch (Numbeo raw vs US-avg) | mitigate | Header comment fixes the US-avg=100 scale (Pitfall 3); engine math depends on it being correct |
| T-3-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (handled in 03-01) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` exits 0 — contract + config + dataset all typecheck under strict mode.
- 22 cities present; no prototype-only `color` field; all required dealbreaker fields present.
- SCORING_WEIGHTS holds all coefficients/thresholds; BASE_SCORE exported.
</verification>

<success_criteria>
- City contract extended with all 8 fields; Profile carries optional weights.
- One tunable config holds every scoring number.
- 22 typed, cited US cities import cleanly against the contract.
</success_criteria>

<output>
Create `.planning/phases/03-matching-us-financial-spine/03-02-SUMMARY.md` when done.
</output>

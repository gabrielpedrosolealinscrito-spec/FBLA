---
phase: 03-matching-us-financial-spine
plan: 04
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - shared/engine/scoring.ts
  - shared/engine/scoring.test.ts
autonomous: true
requirements: [MATCH-03]
user_setup: []

must_haves:
  truths:
    - "Each city is scored by the two-layer formula: global factor weight × personal weight × normalized factor score (D-04)"
    - "Personal weights come from Profile.weights when present, else are derived from importanceRank (rankToWeight fallback)"
    - "Per-factor contributions are collected as the ACTUAL additive terms so they sum to the raw score (no post-hoc explanation) — signed contribution bars (D-05)"
    - "No dealbreaker penalty is applied here (raw/un-penalized scoring only)"
  artifacts:
    - path: "shared/engine/scoring.ts"
      provides: "scoreCity(profile, city) → { rawScore, scoreFactors } using SCORING_WEIGHTS"
      exports: ["scoreCity"]
    - path: "shared/engine/scoring.test.ts"
      provides: "GREEN MATCH-03 test asserting contribution-sum invariant"
  key_links:
    - from: "shared/engine/scoring.ts"
      to: "shared/engine/scoring-weights.ts"
      via: "import SCORING_WEIGHTS, BASE_SCORE"
      pattern: "SCORING_WEIGHTS"
---

<objective>
Implement the config-driven scoring module: per-factor scores (cost, career, lifestyle, safety) combined via the two-layer D-04 formula (global factor weight × per-person weight × normalized factor score), collecting each factor's signed point contribution as the literal additive term that builds the score. This is the engine half of MATCH-03 "why a city scored as it did" — the contribution array IS the score arithmetic, not a separate visualization. Turns the RED `scoring.test.ts` from Plan 01 GREEN.

Critical design constraint (RESEARCH Pitfall 1): contributions must be collected INSIDE the same accumulation that produces rawScore, so `BASE_SCORE + sum(contributions) === rawScore`. Do not compute scoreFactors separately. Dealbreaker penalties are NOT applied here — that is a separate pass (Plan 05) required by the D-02 two-pass comparison.

Output: `shared/engine/scoring.ts` with `shared/engine/scoring.test.ts` GREEN.

TDD: scoring.test.ts already exists (RED). RED → GREEN → REFACTOR; do not weaken the test.
</objective>

<phase_goal>
**As a** person deciding where to move, **I want to** expand any city match and see which profile factors drove the score, **so that** the ranking is transparent and I can trust it.

(Derived capability framing per MVP rules — ROADMAP goal not in canonical user-story form.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-matching-us-financial-spine/03-RESEARCH.md
@.planning/phases/03-matching-us-financial-spine/03-PATTERNS.md

<interfaces>
From shared/engine/scoring-weights.ts (Plan 02 — consume, do not redefine):
```typescript
export const SCORING_WEIGHTS = { global:{cost,career,lifestyle,safety}, dealbreaker:{penalty}, lifestyle:{...}, normalization:{costMaxContribution,careerMaxContribution,lifestyleMaxContribution,safetyMaxContribution} } as const;
export const BASE_SCORE = 50;
```
From shared/types.ts:
```typescript
export interface Profile { weights?: {cost;career;lifestyle;safety:number}; importanceRank: string[]; lifestyleTags: string[]; hasRemote: boolean; /* ... */ }
export interface City { costIndex; jobGrowth; safetyIndex; walkScore: number; vibe: string[]; /* ... */ }
```
scoreCity output shape (this plan defines it):
```typescript
export interface CityScore { rawScore: number; scoreFactors: { factor: string; contribution: number }[]; }
export function scoreCity(profile: Profile, city: City): CityScore;
```
</interfaces>
</context>

<feature>
  <name>Two-layer config-driven city scoring with honest contribution collection</name>
  <files>shared/engine/scoring.ts, shared/engine/scoring.test.ts</files>
  <behavior>
    rankToWeight(profile): returns {cost,career,lifestyle,safety} = profile.weights if present, else derived from importanceRank (rank 0→4, 1→3, 2→2, 3→1, missing→1).
    scoreCity(profile, city):
    - cost factor: lower costIndex = higher normalized score (RESEARCH costFactorScore), × global.cost × personal.cost × costMaxContribution
    - career factor: jobGrowth normalized + remote bonus, × global.career × personal.career × careerMaxContribution
    - lifestyle factor: tag/vibe matches per RESEARCH, × global.lifestyle × personal.lifestyle × lifestyleMaxContribution
    - safety factor: safetyIndex/100 × global.safety × personal.safety × safetyMaxContribution
    - pushes one { factor, contribution } per factor (rounded), and rawScore = BASE_SCORE + sum(contributions)
    INVARIANT (MATCH-03): Math.abs(rawScore - (BASE_SCORE + sum(scoreFactors.contribution))) < 0.01
    NO dealbreaker penalty applied here.
  </behavior>
  <implementation>
    Read RESEARCH ## D-03/D-04 (Score Formula + Per-Factor Normalization with costFactorScore/careerFactorScore/safetyFactorScore bodies) and PATTERNS.md ### scoring.ts (full getMatchScore body to port + the contribution-collection pattern). Import SCORING_WEIGHTS and BASE_SCORE from './scoring-weights.js'; import LIFESTYLE_TAGS from '../data/constants.js' if needed for tag matching. Implement `rankToWeight` honoring `profile.weights ?? deriveFromImportanceRank` (Phase 2 not yet merged — verified — so the fallback path is the live one; comment it). Collect contributions in lockstep with each `score +=` so they are the same arithmetic (Pitfall 1 / Anti-pattern "post-hoc explanation"). Clamp normalized factor scores to [0,1] before scaling. Do NOT inline any coefficient — every number comes from SCORING_WEIGHTS. Do NOT apply dealbreaker penalties (Plan 05 owns that). Do NOT edit scoring.test.ts to weaken the invariant.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement scoring.ts to turn scoring.test.ts GREEN</name>
  <files>shared/engine/scoring.ts, shared/engine/scoring.test.ts</files>
  <read_first>
    - shared/engine/scoring.test.ts (RED test from Plan 01 — the contribution-sum invariant to satisfy; do NOT weaken)
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## D-03/D-04 Score Formula, Per-Factor Normalization functions, Anti-Patterns "post-hoc explanation"; Pitfall 1 contribution-sum)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### shared/engine/scoring.ts — full prototype getMatchScore body + closure→pure-params transformation + contribution-collection pattern; Transformation Rules table)
    - shared/engine/scoring-weights.ts (Plan 02 — SCORING_WEIGHTS, BASE_SCORE)
    - shared/types.ts (Profile.weights optional + importanceRank; City factor fields)
  </read_first>
  <action>RED → GREEN → REFACTOR. (1) Run `npx vitest run shared/engine/scoring.test.ts` — confirm RED. (2) Implement `shared/engine/scoring.ts`: export `interface CityScore { rawScore: number; scoreFactors: {factor;contribution:number}[] }` and `export function scoreCity(profile: Profile, city: City): CityScore`. Implement `rankToWeight(profile)` using `profile.weights ?? { cost: w("cost"), career: w("career"), lifestyle: w("lifestyle"), safety: w("safety") }` where `w(cat)` maps importanceRank index 0→4,1→3,2→2,3→1 (fallback path — Phase 2 not merged). For each factor compute the normalized score (cost: clamp((140-costIndex)/80,0,1); career: clamp(jobGrowth/5 + (hasRemote?0.3:0),0,1); safety: safetyIndex/100; lifestyle: per RESEARCH tag matches), then `contribution = round(global[f] × personal[f] × normalized × maxContribution[f])`, push `{factor, contribution}`, and accumulate. Set `rawScore = BASE_SCORE + sum(contributions)`. No penalties. All coefficients from SCORING_WEIGHTS. (3) Run test — GREEN. Refactor only while GREEN.</action>
  <verify>
    <automated>npx vitest run shared/engine/scoring.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine/scoring.test.ts` exits 0 (GREEN)
    - `shared/engine/scoring.ts` exports `scoreCity`
    - The contribution-sum invariant holds: BASE_SCORE + sum(scoreFactors.contribution) equals rawScore within 0.01 (asserted by the test)
    - `scoring.ts` contains NO bare numeric scoring coefficient inline (all via SCORING_WEIGHTS) — `grep -v '^\s*//' shared/engine/scoring.ts | grep -cE '\* 0\.[0-9]|\* [0-9]+ \*' ` returns 0
    - `rankToWeight` honors `profile.weights ??` fallback to importanceRank
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>scoreCity produces honest signed contributions (the literal score arithmetic) via the two-layer config-driven formula; MATCH-03 invariant test is GREEN; no penalties applied here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile.weights / importanceRank → scoring | Quiz-derived weights enter the scoring multiplier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Tampering (input validation, V5) | Profile.weights with values > 4 or negative | mitigate | Clamp personal weights to [0,4] when reading; normalized factor scores clamped to [0,1] before scaling |
| T-3-07 | Repudiation | scoreFactors drifting from matchScore (Pitfall 1) | mitigate | Contributions collected as the literal additive terms; invariant unit-tested |
| T-3-SC | Tampering | npm installs | n/a | No installs in this plan |
</threat_model>

<verification>
- `npx vitest run shared/engine/scoring.test.ts` GREEN.
- No inline scoring coefficients (grep gate, comment-filtered).
- `npx tsc --noEmit -p tsconfig.json` exits 0.
</verification>

<success_criteria>
- Two-layer (global × personal) config-driven scoring implemented.
- Contributions sum to rawScore (honest MATCH-03 explanation).
- Weights consumed from Profile.weights with importanceRank fallback.
- No dealbreaker penalty applied (left to Plan 05).
</success_criteria>

<output>
Create `.planning/phases/03-matching-us-financial-spine/03-04-SUMMARY.md` when done.
</output>

---
phase: 03-matching-us-financial-spine
plan: 05
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - shared/engine/dealbreakers.ts
  - shared/engine/dealbreakers.test.ts
autonomous: true
requirements: [MATCH-01]
user_setup: []

must_haves:
  truths:
    - "Each of the 10 dealbreakers is tested against the correct concrete city field (heat/cold use summerHighF/winterLowF, NOT avgTemp)"
    - "A triggered dealbreaker produces a heavy penalty as a signed negative contribution — never a hard delete (D-01)"
    - "checkReconfirm compares raw vs penalized #1 and emits a fact-citing signal when a dealbreaker demoted the would-be top match (D-02)"
  artifacts:
    - path: "shared/engine/dealbreakers.ts"
      provides: "getTriggeredDealbreakers, applyPenalties, checkReconfirm, ReconfirmSignal"
      exports: ["getTriggeredDealbreakers", "checkReconfirm"]
    - path: "shared/engine/dealbreakers.test.ts"
      provides: "GREEN tests for field mapping, penalty-not-delete, re-confirm present/absent"
  key_links:
    - from: "shared/engine/dealbreakers.ts"
      to: "shared/engine/scoring-weights.ts"
      via: "import penalty + thresholds"
      pattern: "heatThresholdF|penalty"
---

<objective>
Implement the dealbreaker layer: map each of the 10 `DEAL_BREAKERS` to the concrete city field it tests (D-11), apply a heavy score penalty (D-01 — penalty, NOT delete) as a signed negative contribution, and implement the D-02 re-confirm logic (`checkReconfirm`) that compares the raw (un-penalized) #1 to the penalized #1 and emits a fact-citing signal when a dealbreaker demoted the would-be top match. Turns the RED `dealbreakers.test.ts` from Plan 01 GREEN.

Critical (RESEARCH Pitfall 2): the re-confirm comparison is ENGINE logic, not UI — `checkReconfirm` is a pure function over the two rankings. The UI (Plan 07) only reads its output. Heat/cold checks use `summerHighF`/`winterLowF` (NOT `avgTemp` — the deprecated prototype behavior).

Output: `shared/engine/dealbreakers.ts` with `shared/engine/dealbreakers.test.ts` GREEN.

TDD: dealbreakers.test.ts already exists (RED). RED → GREEN → REFACTOR; do not weaken the test.
</objective>

<phase_goal>
**As a** person deciding where to move, **I want to** have my hard dealbreakers heavily demote (not silently erase) mismatched cities, and be asked to re-confirm when a dealbreaker costs me my would-be #1, **so that** I am never stranded with an empty list and the tool reasons with me about my constraints.

(Derived capability framing per MVP rules — ROADMAP goal not in canonical user-story form.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-matching-us-financial-spine/03-RESEARCH.md
@.planning/phases/03-matching-us-financial-spine/03-PATTERNS.md

<interfaces>
From shared/engine/scoring-weights.ts (Plan 02):
```typescript
export const SCORING_WEIGHTS = { dealbreaker: { penalty: 30 }, ... };
// recalibrated thresholds: heatThresholdF 95, coldThresholdF 25, transitMin 40, walkMin 50, safetyMin 55, jobGrowthMin 2.0
```
From shared/data/constants.js:
```javascript
export const DEAL_BREAKERS = ["No extreme cold","No extreme heat","Must have public transit","Must be walkable","No state income tax","Must be near mountains","Must be near ocean/coast","Low crime only","Need international airport","Must have strong job market in my field"];
```
This plan defines (RESEARCH D-02):
```typescript
export interface TriggeredDealbreaker { label: string; factLabel: string; }   // factLabel = real city fact, e.g. "summer highs above 95°F"
export interface ReconfirmSignal { city: City; dealbreaker: string; factLabel: string; }
export function getTriggeredDealbreakers(city: City, dealBreakers: string[], profile: Profile): TriggeredDealbreaker[];
export function checkReconfirm(penalizedRanking: MatchResult[], rawRanking: MatchResult[], profile: Profile): ReconfirmSignal | null;
```
</interfaces>
</context>

<feature>
  <name>Dealbreaker penalty (not delete) + D-02 two-pass re-confirm signal</name>
  <files>shared/engine/dealbreakers.ts, shared/engine/dealbreakers.test.ts</files>
  <behavior>
    getTriggeredDealbreakers(city, dealBreakers, profile): for each dealbreaker the user holds, test the mapped field:
    - "No extreme cold" → winterLowF < coldThresholdF (25)
    - "No extreme heat" → summerHighF > heatThresholdF (95)
    - "Must have public transit" → transitScore < 40
    - "Must be walkable" → walkScore < 50
    - "No state income tax" → stateTax > 0
    - "Must be near mountains" → !nearMountains
    - "Must be near ocean/coast" → !nearCoast
    - "Low crime only" → safetyIndex < 55
    - "Need international airport" → !hasIntlAirport
    - "Must have strong job market in my field" → jobGrowth < 2.0 OR profession's industry not in topIndustries
    Returns the triggered ones with a human factLabel (e.g. Phoenix → "summer highs above 95°F").
    - Phoenix (summerHighF 107) triggers "No extreme heat"; Minneapolis (winterLowF 9) triggers "No extreme cold"
    applyPenalties(result, profile): subtracts penalty per triggered dealbreaker, appends each as a negative scoreFactor; NEVER removes the result (D-01).
    checkReconfirm(penalized, raw, profile): if penalized[0].city.name === raw[0].city.name → null; else find the dealbreaker that demoted raw[0] and return its signal; null if none.
    - Returns a signal when a dealbreaker demotes raw #1; returns null when penalized #1 === raw #1.
  </behavior>
  <implementation>
    Read RESEARCH ## D-10/D-11/D-12 (Dealbreaker Field Mapping table — exact field+threshold+source per dealbreaker) and ## D-02 (ReconfirmSignal + checkReconfirm body) and PATTERNS.md ### dealbreakers.ts (prototype penalty block + the three required changes: avgTemp→summerHighF/winterLowF, fill missing 4 dealbreakers, penalties as separate returned value). Import penalty + thresholds from './scoring-weights.js' (no inline magic numbers). For the job-market dealbreaker, derive the profession's industry/category from PROFESSION_CATEGORIES in constants.js and check membership in city.topIndustries (OR jobGrowth < jobGrowthMin). Build factLabel from the actual city number ("summer highs above 95°F", "no state income tax: 9.85% state rate", etc.) so the D-02 copy cites a real fact. applyPenalties appends a `{factor: "<dealbreaker label>", contribution: -penalty}` per trigger (D-05 shows it as a real negative bar) and must NOT filter the result out. checkReconfirm is a PURE function (Pitfall 2 — not UI). Do NOT edit dealbreakers.test.ts to weaken assertions.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement dealbreakers.ts to turn dealbreakers.test.ts GREEN</name>
  <files>shared/engine/dealbreakers.ts, shared/engine/dealbreakers.test.ts</files>
  <read_first>
    - shared/engine/dealbreakers.test.ts (RED tests from Plan 01 — field-mapping, penalty-not-delete, re-confirm present/absent; do NOT weaken)
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## D-10/D-11/D-12 Dealbreaker Field Mapping table; ## D-02 Re-Confirm Logic; Pitfall 2 engine-not-UI; Pitfall 5 near-mountains/coast definitions)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### shared/engine/dealbreakers.ts — prototype penalty block + the three required changes; Transformation Rules)
    - shared/engine/scoring-weights.ts (Plan 02 — penalty + recalibrated thresholds)
    - shared/data/constants.js (DEAL_BREAKERS, PROFESSION_CATEGORIES for the job-market check)
    - shared/types.ts (City dealbreaker fields, Profile.dealBreakers, MatchResult for ranking signatures)
  </read_first>
  <action>RED → GREEN → REFACTOR. (1) Run `npx vitest run shared/engine/dealbreakers.test.ts` — confirm RED. (2) Implement `shared/engine/dealbreakers.ts`: export `TriggeredDealbreaker`, `ReconfirmSignal` interfaces; `getTriggeredDealbreakers(city, dealBreakers, profile)` mapping all 10 dealbreakers to fields per the RESEARCH table (heat→summerHighF>95, cold→winterLowF<25, transit→transitScore<40, walk→walkScore<50, no-state-tax→stateTax>0, mountains→!nearMountains, coast→!nearCoast, low-crime→safetyIndex<55, airport→!hasIntlAirport, job-market→jobGrowth<2.0 OR industry not in topIndustries), each returning a real factLabel; `applyPenalties(result, profile)` subtracting `SCORING_WEIGHTS.dealbreaker.penalty` per trigger and appending a negative scoreFactor, NEVER deleting; `checkReconfirm(penalizedRanking, rawRanking, profile)` per RESEARCH D-02 (null when tops match, else the demoting dealbreaker's signal). Use thresholds from scoring-weights.ts, not inline. (3) Run test — GREEN. Refactor only while GREEN.</action>
  <verify>
    <automated>npx vitest run shared/engine/dealbreakers.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine/dealbreakers.test.ts` exits 0 (GREEN)
    - `shared/engine/dealbreakers.ts` exports `getTriggeredDealbreakers` and `checkReconfirm`
    - dealbreakers.ts references `summerHighF` and `winterLowF` and does NOT use `avgTemp` for heat/cold: `grep -c avgTemp shared/engine/dealbreakers.ts` returns 0
    - applyPenalties never calls `.filter(` to remove a result (D-01 penalty-not-delete): `grep -c '\.filter(' shared/engine/dealbreakers.ts` returns 0
    - thresholds/penalty imported from scoring-weights (no inline 95/25/30)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>All 10 dealbreakers map to real city facts using the correct fields; penalties demote but never delete; checkReconfirm is a pure engine function that fires the D-02 signal correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile.dealBreakers + City facts → penalty/re-confirm logic | Quiz-selected dealbreaker strings drive field checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Tampering (input validation, V5) | Unknown dealbreaker string in Profile.dealBreakers | mitigate | Switch/map only matches known DEAL_BREAKERS; unknown strings are ignored (no crash, no penalty) |
| T-3-09 | Repudiation | Re-confirm computed UI-side and drifting (Pitfall 2) | mitigate | checkReconfirm is a pure engine function; UI only reads the signal |
| T-3-10 | Denial of service | Empty ranking after penalties (D-01 violation) | mitigate | applyPenalties never filters; ranking always full — unit test asserts non-empty |
| T-3-SC | Tampering | npm installs | n/a | No installs in this plan |
</threat_model>

<verification>
- `npx vitest run shared/engine/dealbreakers.test.ts` GREEN.
- No `avgTemp` in dealbreakers.ts; no `.filter(` removal; thresholds from config.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
</verification>

<success_criteria>
- 10 dealbreakers mapped to correct concrete city fields (heat/cold via summerHighF/winterLowF).
- Penalty-not-delete enforced (D-01); list never empties.
- D-02 re-confirm is a pure engine function citing a real city fact.
</success_criteria>

<output>
Create `.planning/phases/03-matching-us-financial-spine/03-05-SUMMARY.md` when done.
</output>

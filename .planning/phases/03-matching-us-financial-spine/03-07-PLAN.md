---
phase: 03-matching-us-financial-spine
plan: 07
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - src/screens/results/ResultsView.jsx
  - src/screens/results/CityDetail.jsx
  - src/screens/results/ReconfirmOverlay.jsx
  - src/screens/results/ResultsView.test.jsx
  - src/screens/PotentialApp.jsx
autonomous: true
requirements: [MATCH-04, MATCH-03, FIN-01]
user_setup: []

must_haves:
  truths:
    - "After submitting the quiz, the user sees a ranked list of matched US cities scored against their profile"
    - "The user can sort the ranked list by match score, savings, salary, and cost (MATCH-04)"
    - "The user can expand any city to see signed contribution bars (MATCH-03) and the full financial breakdown (FIN-01) — disclosure on expand (D-06)"
    - "When the engine emits a reconfirmSignal, a fact-citing re-confirm overlay lets the user keep or lift the dealbreaker (D-02)"
    - "The whole flow runs offline (no network/api calls)"
  artifacts:
    - path: "src/screens/results/ResultsView.jsx"
      provides: "Ranked list + sort pills consuming MatchResult[]"
      min_lines: 40
    - path: "src/screens/results/CityDetail.jsx"
      provides: "Expand-to-reveal contribution bars + financial breakdown"
      min_lines: 40
    - path: "src/screens/results/ReconfirmOverlay.jsx"
      provides: "D-02 re-confirm overlay reading reconfirmSignal"
    - path: "src/screens/results/ResultsView.test.jsx"
      provides: "GREEN MATCH-04 sort-order test"
  key_links:
    - from: "src/screens/PotentialApp.jsx"
      to: "shared/engine/index.ts"
      via: "rankCities(profile)"
      pattern: "rankCities"
    - from: "src/screens/results/ResultsView.jsx"
      to: "MatchResult[]"
      via: "props"
      pattern: "sortBy"
---

<objective>
Build the results surface that consumes the engine's `MatchResult[]` (and `reconfirmSignal`): a ranked list with sort (MATCH-04), an expand-to-reveal city detail showing signed contribution bars (MATCH-03/D-05/D-06) and the full income-adjusted financial breakdown (FIN-01), and the D-02 re-confirm overlay. Wire `rankCities(profile)` into `PotentialApp.jsx` where the prototype currently scores inline, replacing the deprecated inline scoring/financial logic with the engine call. The full flow (quiz → results → city detail) runs offline.

Specified at STRUCTURAL altitude only (per the phase division_of_labor): component responsibilities, data flow from `MatchResult[]`, state to extend (`sortBy`, `selectedCity`, `expandedSection`), the four sort keys, the contribution-bar data shape, expand-to-reveal mechanics, and the re-confirm interaction logic. A separate collaborator authors the final UI-SPEC, images, exact copy, and visual polish in a later pass — DO NOT over-specify visuals or final wording here. Reuse the existing dark-theme inline-style tokens and the prototype's component patterns (sort pills, Section collapsible, stacked-bar list, financial tile grid) per PATTERNS.md.

Output: `ResultsView.jsx`, `CityDetail.jsx`, `ReconfirmOverlay.jsx`, `ResultsView.test.jsx`, and the `PotentialApp.jsx` wiring.

MVP / division note: planned under MVP_MODE=true; the division_of_labor (backend logic done by Gabriel; this frontend pickable by the friend) governs. This plan is the frontend slice that makes the engine demoable end-to-end.
</objective>

<phase_goal>
**As a** person who just finished the quiz, **I want to** see my ranked US city matches, sort them, expand any city to see why it scored that way and what my money looks like there, and answer a re-confirm prompt when a dealbreaker cost me my top match, **so that** I get the full offline demoable result.

(Derived capability framing per MVP rules — ROADMAP goal not in canonical user-story form.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-matching-us-financial-spine/03-RESEARCH.md
@.planning/phases/03-matching-us-financial-spine/03-PATTERNS.md

<interfaces>
Engine boundary consumed (Plan 06):
```
rankCities(profile: Profile) => { results: MatchResult[]; reconfirmSignal?: { city: City; dealbreaker: string; factLabel: string } }
```
MatchResult shape (render targets):
```
MatchResult { city: City; matchScore: number; scoreFactors: {factor;contribution:number}[]; estSalary; monthlyTakeHome; expenses: ExpenseBreakdown; monthlySavings: number; }
```
Reusable prototype patterns (PATTERNS.md Shared Patterns — copy these):
- css design-token object (PotentialApp.jsx 153-160), pill() factory (163-167), heading/mono (161-162), fmt/fmtFull (26-27)
- sort impl + sort-pill group (449-455, 472-475), city card (477-500)
- Section collapsible (518-532), stacked-bar + row list (638-653), financial tile grid (601-619)
Transformation rules (PATTERNS.md): use `r.estSalary` (not city.salary); `a.city.costIndex` (MatchResult wraps city); contribution bars use var(--pos)/var(--neg) (dealbreaker = real negative bar, not asterisk).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ResultsView.jsx (ranked list + MATCH-04 sort) and its sort test</name>
  <files>src/screens/results/ResultsView.jsx, src/screens/results/ResultsView.test.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (lines 153-167 tokens+pill, 26-27 formatters, 448-505 results section, 449-455 sort impl, 472-475 sort pills, 477-500 city card — the patterns to copy/port)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### ResultsView.jsx + Shared Patterns + Transformation Rules table)
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## MATCH-04 Sort/Filter Pattern — four sort keys against MatchResult)
    - shared/types.ts (MatchResult field names — estSalary, monthlySavings, city.costIndex)
  </read_first>
  <action>Create `src/screens/results/ResultsView.jsx`: a component taking props `{ results, sortBy, setSortBy, onSelectCity }`. Copy the css token object, `pill()`, `heading`/`mono`, and `fmt`/`fmtFull` as module-level helpers (PATTERNS Shared Patterns). Render the four sort pills `[["match","Best match"],["savings","Most savings"],["salary","Top salary"],["cost","Lowest cost"]]` calling `setSortBy`. Export a pure `sortResults(results, sortBy)` helper computing the four keys against the typed MatchResult: match→`b.matchScore-a.matchScore`, savings→`b.monthlySavings-a.monthlySavings`, salary→`b.estSalary-a.estSalary`, cost→`a.city.costIndex-b.city.costIndex`. Render each result as a card (emoji, `r.city.name`, `r.matchScore`%, `r.estSalary`/yr, signed `r.monthlySavings`/mo with var(--pos)/var(--neg)) calling `onSelectCity(r)` on click. Keep structural — no final copy/imagery. Also create `src/screens/results/ResultsView.test.jsx`: import `sortResults`, build a 3-MatchResult fixture, and assert `sortResults(fixture,"savings")[0]` is the one with the highest monthlySavings (MATCH-04). Assert via the helper, not DOM, to avoid render flakiness.</action>
  <verify>
    <automated>npx vitest run src/screens/results/ResultsView.test.jsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/results/ResultsView.jsx` exists, exports the component and a `sortResults` helper
    - The four sort keys use `estSalary` and `city.costIndex` (typed MatchResult field names), not `salary`/`costIndex`
    - `ResultsView.test.jsx` asserts sort-by-savings puts highest monthlySavings first and is GREEN
    - `npx vitest run src/screens/results/ResultsView.test.jsx` exits 0
    - No network/fetch call introduced (offline): `grep -c "fetch(" src/screens/results/ResultsView.jsx` returns 0
  </acceptance_criteria>
  <done>The ranked list renders MatchResult[] and sorts by all four MATCH-04 keys; the sort test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Build CityDetail.jsx (contribution bars + financials) and ReconfirmOverlay.jsx</name>
  <files>src/screens/results/CityDetail.jsx, src/screens/results/ReconfirmOverlay.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (lines 518-532 Section collapsible, 601-619 financial tile grid, 622-657 expense stacked-bar + row list — patterns to copy)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### CityDetail.jsx — Section pattern, stacked-bar adaptation for scoreFactors, financial grid, D-02 overlay token guidance)
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## D-05/D-06 contribution bars + disclosure-on-expand; ## D-02 re-confirm copy template)
    - shared/types.ts (MatchResult.scoreFactors, ExpenseBreakdown)
  </read_first>
  <action>Create `src/screens/results/CityDetail.jsx` taking `{ result, expandedSection, setExpandedSection }`. Reuse the `Section` collapsible pattern for two expandable sections (D-06 disclosure-on-expand): (a) "Why this score" rendering `result.scoreFactors` as signed contribution bars — adapt the stacked-bar + row list, mapping each factor to a bar using `var(--pos)` for positive contribution and `var(--neg)` for negative; the dealbreaker penalty entry (negative contribution) shows as a real red bar with its signed number, not an asterisk (D-05). (b) "Financials" rendering the FIN-01 breakdown: the 4-tile financial grid (estSalary, monthlyTakeHome, monthlySavings, rent/home) plus the itemized `result.expenses` rows (rent/food/transport/utilities/insurance/personal/childcare/pets/debtPay/total). Use `mono` for numbers, `fmtFull` for currency. Create `src/screens/results/ReconfirmOverlay.jsx` taking `{ signal, onKeep, onLift }`: a dark-card overlay (var(--accent2) amber warning accent per PATTERNS) showing the D-02 copy "Based on your answers, {signal.city.name} would be your top match. But {signal.city.name}'s {signal.factLabel}. Is that still a dealbreaker for you?" with two actions — "Yes, still a dealbreaker" (onKeep) and "No, it's fine" (onLift). Structural only — the friend authors final copy/styling later; the prop contract and interaction logic are what matter here.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['CityDetail.jsx','ReconfirmOverlay.jsx'].forEach(f=>{const s=fs.readFileSync('src/screens/results/'+f,'utf8'); if(s.length<400){console.error(f,'too small');process.exit(1)}}); const cd=fs.readFileSync('src/screens/results/CityDetail.jsx','utf8'); if(!/scoreFactors/.test(cd)||!/expenses/.test(cd)){console.error('CityDetail missing scoreFactors/expenses');process.exit(1)} const ro=fs.readFileSync('src/screens/results/ReconfirmOverlay.jsx','utf8'); if(!/factLabel/.test(ro)){console.error('ReconfirmOverlay missing factLabel');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/results/CityDetail.jsx` renders `result.scoreFactors` (signed bars, var(--pos)/var(--neg)) and the full `result.expenses` breakdown + financial tile grid
    - `src/screens/results/ReconfirmOverlay.jsx` reads `signal.city.name` + `signal.factLabel` and exposes `onKeep`/`onLift`
    - Both components reuse existing dark-theme tokens (no new stylesheet)
    - The node verify command prints `ok` and exits 0
  </acceptance_criteria>
  <done>City detail discloses contribution bars (incl. real negative dealbreaker bars) and the full financial breakdown on expand; the re-confirm overlay reads the engine signal and offers keep/lift.</done>
</task>

<task type="auto">
  <name>Task 3: Wire rankCities into PotentialApp.jsx and remove deprecated inline logic</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (lines 76-142 deprecated inline getSalary/getTakeHome/getExpenses/getSavings/getMatchScore; lines 232-239 nextProfile inline scoring; lines 11-24 inline CITIES_DATA; state decls sortBy/selectedCity/expandedSection ~54-55; results section 448-730)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### index.ts nextProfile analog; Transformation Rules — deprecated behaviors to remove; Anti-Patterns to Avoid)
    - shared/engine/index.ts (rankCities + RankingOutput — the boundary to call)
    - shared/data/cities.ts (CITIES_DATA now lives here — remove the inline copy)
  </read_first>
  <action>Edit `src/screens/PotentialApp.jsx` to consume the engine instead of inline logic. At quiz completion (the `nextProfile` else-branch, ~lines 232-239), call `const { results, reconfirmSignal } = rankCities(profile)` (import from `'../../shared/engine/index.ts'`) and store both in state (`setResults(results)`, `setReconfirmSignal(reconfirmSignal)`). Replace the inline results/detail JSX with `<ResultsView results sortBy setSortBy onSelectCity={setSelectedCity} />`, render `<CityDetail result={selectedCity} ... />` when a city is selected, and render `<ReconfirmOverlay signal={reconfirmSignal} onKeep={...} onLift={...} />` when `reconfirmSignal` is present. onLift should re-run `rankCities` with that dealbreaker removed from a working copy of the profile (so the demoted city can reclaim rank, D-02); onKeep dismisses the overlay. DELETE the deprecated inline functions (getSalary/getTakeHome/getExpenses/getSavings/getMatchScore, lines ~76-142) and the inline `CITIES_DATA` array (lines ~11-24) — these are superseded by the engine + `shared/data/cities.ts` (PATTERNS Deprecated list). Keep the existing AI-fetch stub stubbed (offline; Phase 5 owns live data). Introduce NO network/fetch call.</action>
  <verify>
    <automated>npx vite build 2>&1 | tail -5 && grep -q "rankCities" src/screens/PotentialApp.jsx && [ "$(grep -c 'const getMatchScore' src/screens/PotentialApp.jsx)" = "0" ] && [ "$(grep -c '= totalSal \* 0.22' src/screens/PotentialApp.jsx)" = "0" ] && echo "wired-and-cleaned"</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/PotentialApp.jsx` imports and calls `rankCities(profile)` and stores `results` + `reconfirmSignal`
    - Deprecated inline `getMatchScore` removed: `grep -c 'const getMatchScore' src/screens/PotentialApp.jsx` returns 0
    - Deprecated flat-22% tax line removed: `grep -c '= totalSal \* 0.22' src/screens/PotentialApp.jsx` returns 0
    - Inline `CITIES_DATA` array removed from PotentialApp.jsx (now imported from shared/data/cities.ts or via the engine)
    - `npx vite build` succeeds (no broken imports)
    - No new fetch/network call: `grep -c "api.anthropic\|fetch(" src/screens/PotentialApp.jsx` shows the AI stub unchanged (offline preserved)
  </acceptance_criteria>
  <done>The quiz feeds rankCities; results, city detail, and the re-confirm overlay render from the engine output; deprecated inline scoring/financial/data logic is gone; the app builds and runs offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine output (MatchResult[]) → React render | Trusted in-process data; no external/network input crosses into the UI this phase |
| (no api.anthropic.com / network boundary — AI stub stays stubbed, Phase 5 owns it) | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-14 | Tampering (input validation) | scoreFactors/expenses possibly empty or negative in render | mitigate | Components guard for empty arrays and render signed numbers safely (negative savings/contribution shown, not crashed) |
| T-3-15 | Information disclosure | Accidental network call leaking offline guarantee | mitigate | No fetch introduced; AI stub left stubbed; grep gate asserts no new fetch in ResultsView; offline build verified |
| T-3-SC | Tampering | npm installs | n/a | No installs in this plan (testing-library already installed in 03-01) |
</threat_model>

<verification>
- `npx vitest run src/screens/results/ResultsView.test.jsx` GREEN (MATCH-04 sort).
- `npx vite build` succeeds with the engine wired in and inline logic removed.
- No new network/fetch call introduced (offline preserved).
- `npx vitest run` (full suite) GREEN as the phase gate.
</verification>

<success_criteria>
- Ranked list renders engine MatchResult[] with all four MATCH-04 sorts (sort-only; column filtering is deferred this phase per RESEARCH §MATCH-04 — all cities are US, no country filter needed until Phase 4).
- City detail discloses contribution bars (MATCH-03) and full financials (FIN-01) on expand.
- D-02 re-confirm overlay reads the engine signal and supports keep/lift.
- Deprecated inline scoring/financial/data logic removed; flow runs offline.
</success_criteria>

<output>
Create `.planning/phases/03-matching-us-financial-spine/03-07-SUMMARY.md` when done.
</output>

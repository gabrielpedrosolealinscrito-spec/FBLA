---
phase: 03-matching-us-financial-spine
plan: 06
type: tdd
wave: 3
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - shared/engine/index.ts
  - shared/engine/index.test.ts
autonomous: true
requirements: [MATCH-01]
user_setup: []

must_haves:
  truths:
    - "rankCities(profile) returns a non-empty, 0–99-clamped, match-sorted MatchResult[] for any valid profile (never stranded — D-01)"
    - "Each MatchResult carries real financials (estSalary, monthlyTakeHome, expenses, monthlySavings) from the financial model selected by financialModelId"
    - "The output includes a reconfirmSignal when (and only when) a dealbreaker demoted the would-be #1 (D-02)"
    - "Malformed profile inputs (negative/zero costIndex, out-of-range weights) cannot crash or NaN the engine"
  artifacts:
    - path: "shared/engine/index.ts"
      provides: "rankCities(profile) → RankingOutput { results, reconfirmSignal? } — the engine→UI boundary"
      exports: ["rankCities"]
    - path: "shared/engine/index.test.ts"
      provides: "GREEN MATCH-01 integration tests (never-empty, clamp, sort, Austin reference financials, re-confirm)"
  key_links:
    - from: "shared/engine/index.ts"
      to: "shared/data/cities.ts"
      via: "import CITIES_DATA"
      pattern: "CITIES_DATA"
    - from: "shared/engine/index.ts"
      to: "shared/engine/financial.ts"
      via: "FINANCIAL_MODELS[city.financialModelId]"
      pattern: "FINANCIAL_MODELS"
---

<objective>
Implement the engine orchestrator `rankCities(profile)` that ties scoring, dealbreakers, and the financial model together over the 22-city dataset and returns the `RankingOutput { results: MatchResult[], reconfirmSignal? }` — the single crisp engine→UI boundary the frontend (Plan 07) consumes. This is the MATCH-01 integration point and the MVP "happy-path" verification target (no UI exists yet to e2e-test, so this integration test stands in). Turns the RED `index.test.ts` from Plan 01 GREEN.

It runs the two-pass D-02 flow: build raw (un-penalized) MatchResults via scoring + financial, sort to get the raw ranking; apply dealbreaker penalties, sort to get the penalized ranking; call `checkReconfirm` to compare the two #1s; return penalized results + optional signal. Per-result financials come from `FINANCIAL_MODELS[city.financialModelId]` (defaults to the US model) so Phase 4 plugs in country models without changing this file.

Output: `shared/engine/index.ts` with `shared/engine/index.test.ts` GREEN; the full engine test suite GREEN.

TDD: index.test.ts already exists (RED). RED → GREEN → REFACTOR; do not weaken the test.
</objective>

<phase_goal>
**As a** person deciding where to move, **I want to** submit my profile and get back a ranked, never-empty list of US cities each with real financials (and a re-confirm prompt when a dealbreaker cost me my top match), **so that** I have a trustworthy, complete result to explore.

(Derived capability framing per MVP rules — ROADMAP goal not in canonical user-story form.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-matching-us-financial-spine/03-RESEARCH.md
@.planning/phases/03-matching-us-financial-spine/03-PATTERNS.md

<interfaces>
Consumed (Plans 02–05):
```typescript
// shared/data/cities.ts
export const CITIES_DATA: City[];
// shared/engine/financial.ts
export const FINANCIAL_MODELS: Record<string, FinancialModel>;
export function computeSalary(profile: Profile, city: City): number;
// shared/engine/scoring.ts
export function scoreCity(profile: Profile, city: City): { rawScore: number; scoreFactors: {factor;contribution:number}[] };
// shared/engine/dealbreakers.ts
export function applyPenalties(result: MatchResult, profile: Profile): MatchResult;
export function checkReconfirm(penalized: MatchResult[], raw: MatchResult[], profile: Profile): ReconfirmSignal | null;
```
Defined here (RESEARCH Engine Orchestrator Interface):
```typescript
export interface RankingOutput { results: MatchResult[]; reconfirmSignal?: ReconfirmSignal; }
export function rankCities(profile: Profile): RankingOutput;
```
From shared/types.ts: MatchResult { city, matchScore, scoreFactors, estSalary, monthlyTakeHome, expenses, monthlySavings }.
</interfaces>
</context>

<feature>
  <name>Two-pass engine orchestrator (rankCities) — the engine→UI boundary</name>
  <files>shared/engine/index.ts, shared/engine/index.test.ts</files>
  <behavior>
    rankCities(profile):
    - sanitize profile at entry: clamp weights to [0,4]; guard costIndex<=0 via the financial model's guard (V5).
    - for each city: model = FINANCIAL_MODELS[city.financialModelId] ?? US model; estSalary = computeSalary (or profile.income if hasRemote); tax via model.computeTax(gross, city.stateTax); monthlyTakeHome = (gross - tax)/12; expenses = model.computeExpenses; monthlySavings = monthlyTakeHome - expenses.total; matchScore (raw) = clamp(round(scoreCity.rawScore),0,99); scoreFactors from scoreCity.
    - rawRanking = results sorted by matchScore desc.
    - penalizedResults = results.map(applyPenalties) with matchScore re-clamped 0–99.
    - penalizedRanking = penalized sorted by matchScore desc.
    - reconfirmSignal = checkReconfirm(penalizedRanking, rawRanking, profile).
    - return { results: penalizedRanking, reconfirmSignal }.
    Invariants (MATCH-01): results.length === CITIES_DATA US count (never empty, never filtered); every matchScore ∈ [0,99] integer; sorted desc.
    Reference (RESEARCH D-07): Software Engineer profile → Austin estSalary ≈ 113300, monthlyTakeHome ≈ 7378.
    opennessToAbroad === 0 keeps US-only (all current cities are US, so no-op now but coded for Phase 4).
  </behavior>
  <implementation>
    Read RESEARCH ## Engine Orchestrator Interface (rankCities body) and ## D-07 reference calc; PATTERNS.md ### index.ts (nextProfile analog — reference only, the two-pass + signal output is novel). Import CITIES_DATA, FINANCIAL_MODELS/computeSalary, scoreCity, applyPenalties/checkReconfirm. Sanitize inputs at entry (clamp weights [0,4]; the financial model already guards costIndex). Re-clamp matchScore to 0–99 after penalties. NEVER `.filter()` cities out of the result (D-01). Select the financial model by `city.financialModelId` with `?? FINANCIAL_MODELS.us` fallback. Keep checkReconfirm as the imported pure function (do not re-implement). Do NOT edit index.test.ts to weaken assertions.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement index.ts orchestrator to turn index.test.ts GREEN and the full engine suite GREEN</name>
  <files>shared/engine/index.ts, shared/engine/index.test.ts</files>
  <read_first>
    - shared/engine/index.test.ts (RED integration tests from Plan 01 — never-empty, clamp, sort, Austin reference financials, re-confirm present/absent; do NOT weaken)
    - .planning/phases/03-matching-us-financial-spine/03-RESEARCH.md (## Engine Orchestrator Interface rankCities body; ## D-07 Reference Tax Calculation; ## Security Domain NaN/clamp guards)
    - .planning/phases/03-matching-us-financial-spine/03-PATTERNS.md (### shared/engine/index.ts — nextProfile analog + the engine-version differences list)
    - shared/engine/financial.ts, shared/engine/scoring.ts, shared/engine/dealbreakers.ts (the implemented modules to orchestrate)
    - shared/data/cities.ts (CITIES_DATA), shared/types.ts (MatchResult, RankingOutput shape)
  </read_first>
  <action>RED → GREEN → REFACTOR. (1) Run `npx vitest run shared/engine/index.test.ts` — confirm RED. (2) Implement `shared/engine/index.ts`: export `interface RankingOutput { results: MatchResult[]; reconfirmSignal?: ReconfirmSignal }` and `export function rankCities(profile: Profile): RankingOutput`. Sanitize profile at entry (clamp weights to [0,4]). Map CITIES_DATA → raw MatchResult: pick `model = FINANCIAL_MODELS[city.financialModelId] ?? FINANCIAL_MODELS.us`; `gross = (profile.hasRemote ? profile.income : computeSalary(profile, city)) + (profile.hasPartner ? profile.partnerIncome : 0)`; `tax = model.computeTax(gross, city.stateTax)`; `monthlyTakeHome = Math.round((gross - tax)/12)`; `expenses = model.computeExpenses(profile, city)`; `monthlySavings = monthlyTakeHome - expenses.total`; `{rawScore, scoreFactors} = scoreCity(profile, city)`; `matchScore = clamp(round(rawScore),0,99)`; `estSalary = gross`. Build rawRanking (sorted desc). Map through `applyPenalties`, re-clamp matchScore 0–99, build penalizedRanking (sorted desc). `reconfirmSignal = checkReconfirm(penalizedRanking, rawRanking, profile)`. Return `{ results: penalizedRanking, reconfirmSignal }`. NEVER filter cities out. (3) Run `npx vitest run shared/engine` — entire engine suite GREEN. Refactor only while GREEN.</action>
  <verify>
    <automated>npx vitest run shared/engine --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/engine/index.test.ts` exits 0 (GREEN)
    - `npx vitest run shared/engine` exits 0 — the ENTIRE engine suite (financial + scoring + dealbreakers + index) is GREEN
    - `shared/engine/index.ts` exports `rankCities` returning `{ results, reconfirmSignal? }`
    - `rankCities(validProfile).results.length` >= 1 and never filters cities (no `.filter(` that removes results): the never-empty test passes
    - every `matchScore` is an integer in [0,99] (clamp test passes); results sorted desc
    - Austin reference: Software Engineer profile yields estSalary ≈ 113300 and monthlyTakeHome ≈ 7378 (±2)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>rankCities is the crisp engine→UI boundary: never-empty, clamped, sorted MatchResult[] with real financials and a correct D-02 re-confirm signal; the full engine test suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile (quiz output) → rankCities entry | The full untrusted profile object enters the engine here — the one validation choke point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-11 | Tampering (input validation, V5) | Malformed Profile (weights > 4, negative income) | mitigate | Sanitize at rankCities entry: clamp weights [0,4]; financial model guards costIndex; matchScore re-clamped 0–99 |
| T-3-12 | Denial of service | Empty result set (D-01 violation strands the user) | mitigate | No `.filter()` removal anywhere in the pipeline; never-empty unit test enforces it |
| T-3-13 | Denial of service (NaN) | NaN matchScore/savings crashing the UI | mitigate | round+clamp matchScore; financial NaN guard upstream (Plan 03); integration test asserts finite numbers |
| T-3-SC | Tampering | npm installs | n/a | No installs in this plan |
</threat_model>

<verification>
- `npx vitest run shared/engine` GREEN (entire engine suite).
- Never-empty + clamp + sort + Austin reference financials all pass.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
</verification>

<success_criteria>
- rankCities returns a never-empty, clamped, sorted MatchResult[] with real financials.
- Two-pass D-02 re-confirm signal correct.
- Pluggable financial model selection by financialModelId.
- Full engine test suite GREEN — the MATCH-01 integration / MVP happy-path target.
</success_criteria>

<output>
Create `.planning/phases/03-matching-us-financial-spine/03-06-SUMMARY.md` when done.
</output>

---
phase: 02-quiz-profile-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - shared/types.ts
  - index.css
  - shared/quiz-engine/resolver.test.ts
  - shared/quiz-engine/synthesizer.test.ts
  - shared/quiz-engine/tension.test.ts
  - src/screens/quiz/QuizShell.test.jsx
  - src/test-setup.js
autonomous: false
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, QUIZ-04, QUIZ-05]
user_setup: []

must_haves:
  truths:
    - "framer-motion and pixelarticons are installed and importable"
    - "Profile type carries the new Phase-2 dimension fields without breaking existing engine test fixtures"
    - "RED test stubs exist for resolver, synthesizer, tension, and QuizShell and run (failing) under vitest"
    - "Implements decision D-05 (expands shared/types.ts Profile with the new dimension fields as optional, reconciling prototype state with the TS contract)"
  artifacts:
    - path: "shared/types.ts"
      provides: "Extended Profile contract with optional Phase-2 dimension fields"
      contains: "motivationToMove"
    - path: "shared/quiz-engine/synthesizer.test.ts"
      provides: "RED test stub for synthesizeProfile — raw-weight + immigration auto-derive assertions"
      contains: "synthesizeProfile"
    - path: "shared/quiz-engine/resolver.test.ts"
      provides: "RED test stub for getVisibleQuestions / clearHiddenAnswers / tension injection"
      contains: "getVisibleQuestions"
    - path: "shared/quiz-engine/tension.test.ts"
      provides: "RED test stub for detectTension"
      contains: "detectTension"
    - path: "src/screens/quiz/QuizShell.test.jsx"
      provides: "RED component test stub for QuizShell render + navigation"
      contains: "QuizShell"
  key_links:
    - from: "shared/types.ts"
      to: "shared/engine/scoring.test.ts"
      via: "existing typed Profile fixtures must still compile (new fields optional)"
      pattern: "motivationToMove\\?:"
---

<objective>
Wave 0 scaffolding for the quiz rebuild: install the two missing runtime/UI packages (framer-motion, pixelarticons) behind legitimacy checkpoints, extend the `Profile` contract with the new Phase-2 dimension fields as OPTIONAL (zero churn to existing engine fixtures), add the slider-thumb CSS the UI-SPEC requires, and write the RED test stubs named in 02-VALIDATION.md so Nyquist Dimension 8 passes before any implementation lands.

Purpose: Everything downstream (Plans 02–04) depends on the extended contract + the failing tests that define the target behavior. Getting the contract optional-not-required here prevents a fixture-ripple that would break the already-shipped Phase 3 engine tests.

Output: Updated package.json/lockfile, extended `shared/types.ts`, slider CSS in `index.css`, and four colocated `.test` stub files (RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quiz-profile-capture/02-CONTEXT.md
@.planning/phases/02-quiz-profile-capture/02-RESEARCH.md
@.planning/phases/02-quiz-profile-capture/02-PATTERNS.md
@.planning/phases/02-quiz-profile-capture/02-VALIDATION.md

<interfaces>
<!-- Current Profile contract (shared/types.ts lines 12–42) — extend in place. -->
<!-- weights? is ALREADY optional (line 39). Mirror that pattern for all new fields. -->

Current Profile fields (do NOT remove any):
  profession, hasRemote, income, savings, debt, housing, hasPartner,
  partnerIncome, hasDependents, numDependents, hasPets, age, education,
  currentCity, citizenship, immigrationStatus, opennessToAbroad,
  lifestyleTags, dealBreakers, importanceRank,
  weights?: { cost; career; lifestyle; safety },
  moveTimeline

<!-- STALE-DOC WARNING: 02-RESEARCH.md §Validation Architecture and 02-VALIDATION.md -->
<!-- say "vitest not yet installed / Wave 0 installs". That is STALE. Verified against -->
<!-- package.json: vitest@4.1.8, @testing-library/react@16.3.2, @testing-library/jest-dom@6.9.1, -->
<!-- jsdom@29.1.1 are ALL already installed. vite.config.js already has the test block. -->
<!-- src/test-setup.js already exists (imports @testing-library/jest-dom). -->
<!-- Do NOT reinstall the test runner. Do NOT recreate src/test-setup.js. -->

<!-- Existing test conventions (from scoring.test.ts / ResultsView.test.jsx): -->
<!--   - Engine tests import { describe, it, expect } from 'vitest' and fns from './x.js' -->
<!--   - Component tests import render from '@testing-library/react'; fireEvent covers nav -->
<!--     (NO @testing-library/user-event needed — do not install it) -->
<!--   - Existing testProfile fixture in scoring.test.ts has NONE of the new fields, -->
<!--     which is exactly why new Profile fields MUST be optional. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy gate — verify framer-motion and pixelarticons before install</name>
  <action>Blocking human-verify checkpoint. Do NOT run any npm install until this is approved. Both packages are tagged [ASSUMED] (02-RESEARCH Package Legitimacy Audit could not run slopcheck; pixelarticons is mandated by 02-UI-SPEC but was not in the audit table). This checkpoint is non-auto-approvable (security_enforcement on).</action>
  <what-built>Nothing yet — this checkpoint precedes the npm install. Both packages are tagged [ASSUMED] (02-RESEARCH Package Legitimacy Audit could not run slopcheck; pixelarticons is mandated by 02-UI-SPEC but was not in the audit table at all).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/framer-motion — confirm it is the Motion library (repo github.com/motiondivision/motion), recent publish, millions of weekly downloads.
    2. Open https://www.npmjs.com/package/pixelarticons — confirm MIT license, 24x24 pixel icon set with React components, legitimate maintainer.
    3. Confirm neither is a typosquat (exact spelling: `framer-motion`, `pixelarticons`).
  </how-to-verify>
  <verify><human-check>Both packages verified legitimate on npmjs.com; no typosquat.</human-check></verify>
  <done>User has confirmed both packages are legitimate and authorized the install.</done>
  <resume-signal>Type "approved" to authorize the install, or name the package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install framer-motion + pixelarticons</name>
  <files>package.json</files>
  <read_first>
    - package.json (confirm framer-motion + pixelarticons absent; vitest/@testing-library/* already present)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Standard Stack + Package Legitimacy Audit)
  </read_first>
  <action>After the Task 1 checkpoint is approved, run `npm install framer-motion pixelarticons` (runtime deps — both are imported by quiz components). Do NOT install vitest, @testing-library/*, or jsdom (already in package.json). Do NOT install @testing-library/user-event (QuizShell tests use fireEvent from @testing-library/react). Commit package.json + package-lock.json.</action>
  <verify>
    <automated>node -e "require.resolve('framer-motion'); require.resolve('pixelarticons'); console.log('ok')"</automated>
  </verify>
  <done>Both packages resolve via node require.resolve; package.json dependencies list framer-motion and pixelarticons.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Profile contract (optional fields) + slider-thumb CSS</name>
  <files>shared/types.ts, index.css</files>
  <read_first>
    - shared/types.ts (the Profile interface lines 12–42 — extend in place; weights? at line 39 is the optional-pattern analog)
    - shared/engine/scoring.test.ts (lines 16–42 testProfile fixture — must keep compiling untouched)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Pattern 4: shared/types.ts Extension)
    - .planning/phases/02-quiz-profile-capture/02-PATTERNS.md (Contract Extension Constraint — strict:true fixture ripple)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (§6 Slider — 28px thumb CSS)
    - index.css (the file being modified)
  </read_first>
  <action>Add the following NEW fields to the `Profile` interface in `shared/types.ts`, ALL OPTIONAL (suffix `?`) to mirror `weights?` and avoid breaking the existing typed fixtures in `shared/engine/*.test.ts` (tsconfig `strict: true`): `motivationToMove?: string`, `workStyle?: string`, `communityNeeds?: string[]`, `paceOfLife?: string`, `riskTolerance?: number`, and `tradeoffTolerance?: { dimension: string; preference: "a" | "b" | "balanced" }[]`. Keep `weights?` optional and unchanged. Do NOT remove or make-required any existing field. Add an entry to the module header comment block noting this Phase-2 D-05 contract extension. Separately, append to `index.css` the slider-thumb size override from 02-UI-SPEC §6 (`input[type="range"]::-webkit-slider-thumb { width:28px; height:28px } input[type="range"]::-moz-range-thumb { width:28px; height:28px }`).</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run shared/engine/ --reporter=verbose</automated>
  </verify>
  <done>tsc passes with no errors; all existing shared/engine tests still green (zero fixture churn); Profile has the 6 new optional fields; index.css contains the 28px thumb rules.</done>
</task>

<task type="auto">
  <name>Task 4: Write RED test stubs + jsdom matchMedia mock</name>
  <files>shared/quiz-engine/resolver.test.ts, shared/quiz-engine/synthesizer.test.ts, shared/quiz-engine/tension.test.ts, src/screens/quiz/QuizShell.test.jsx, src/test-setup.js</files>
  <read_first>
    - .planning/phases/02-quiz-profile-capture/02-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact files + behaviors)
    - shared/engine/scoring.test.ts (engine test style: import from './x.js', typed fixture, describe/it/expect from 'vitest')
    - src/screens/results/ResultsView.test.jsx (component test style — note it does NOT render the DOM, so it never needed matchMedia; QuizShell.test.jsx is the first render test)
    - .planning/phases/02-quiz-profile-capture/02-PATTERNS.md (SP-1 .js import extension; SP-6 colocated tests)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Motion Contract — QuizShell reads window.matchMedia('(prefers-reduced-motion: reduce)'), which is undefined in jsdom)
    - src/test-setup.js (already exists, imports jest-dom — APPEND the matchMedia mock; do NOT recreate the file)
  </read_first>
  <action>Create four colocated RED test stubs that import the not-yet-existing functions and assert the target behaviors from 02-VALIDATION.md, AND append a jsdom `window.matchMedia` mock to `src/test-setup.js` (jsdom does not implement matchMedia; QuizShell's reduced-motion check would throw at render in Plan 02 without it — define `window.matchMedia = (q) => ({ matches:false, media:q, addEventListener(){}, removeEventListener(){}, addListener(){}, removeListener(){}, onchange:null, dispatchEvent(){return false} })`). Use SP-1 `.js` import extensions in the TS stubs (`from './resolver.js'`, `from '../types.js'`). STAGGERED-GREEN DISCIPLINE (load-bearing — assertions must be reachable at the wave that owns them, see each plan's verify command): (a) `shared/quiz-engine/resolver.test.ts` — `resolver.js` is created in Plan 02, so this file's top-level import resolves from Plan 02 onward; write the showIf-hidden/shown + clearHiddenAnswers cases as real `it(...)` (green in Plan 02/03), but write the tension-injection-position case as `it.skip` (Plan 04 Task 1 un-skips it once detectTension + injection exist). HARD RULE — a forward-referencing test file MUST NOT top-level-import a module created in a later plan: write the skipped resolver case to assert ONLY against `getVisibleQuestions(conflictingAnswers, ALL_QUESTIONS)` output (both modules exist from Plan 02) — checking the injected question id appears at the expected index — with NO top-level `import` of `./tension.js` and no value-import of `TensionResult` anywhere in `resolver.test.ts`. (A `.skip` does not protect against a missing top-level import — the file fails to collect and takes the whole gate down.) (b) `shared/quiz-engine/synthesizer.test.ts` — assert RAW 1–4 `weights` (rank-0 → 4, NOT [0,1]), `immigrationStatus` auto-set to `"citizen"` for US, capture of `opennessToAbroad`/`moveTimeline`/`dealBreakers`; tradeoffTolerance assertion as `it.skip` until Plan 04 (assert only against `synthesizeProfile` output — no import of `./tension.js`). (c) `shared/quiz-engine/tension.test.ts` — assert `detectTension` non-null for nature+career and `null` otherwise. NOTE: this file has a top-level `import { detectTension } from './tension.js'`; `tension.js` does not exist until Plan 04, so this whole file IMPORT-ERRORS through Plans 02–03 (a `.skip` cannot save a missing top-level import). Therefore Plans 02/03 verify commands MUST exclude this file (they name specific test files, not the whole dir); only Plan 04's gate runs the full quiz-engine dir. (d) `src/screens/quiz/QuizShell.test.jsx` — render `<QuizShell onComplete={fn} />`, assert first question renders, answer+Continue advances, Back returns (use `render` + `fireEvent` from `@testing-library/react`). Do NOT create any source module in this plan.</action>
  <verify>
    <automated>npx vitest run shared/quiz-engine/synthesizer.test.ts --reporter=verbose; test $? -ne 0 && echo "RED-AS-EXPECTED"</automated>
  </verify>
  <done>Four stub files exist with SP-1 `.js` imports; src/test-setup.js has the matchMedia mock appended; the resolver tension-injection case and synthesizer tradeoffTolerance case are `it.skip` (un-skipped in Plan 04); synthesizer.test.ts is RED now (synthesizer.js absent) — confirming real assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Supply-chain: installing framer-motion + pixelarticons |
| (no client→server boundary this plan) | Contract + tests only; no runtime user input yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (framer-motion, pixelarticons) | mitigate | Task 1 blocking-human checkpoint verifies both [ASSUMED] packages on npmjs.com before install; non-auto-approvable (security_enforcement on) |
| T-02-01 | Tampering | shared/types.ts contract change | mitigate | New fields optional → no downstream type break; tsc --noEmit + engine test suite gate the change |
</threat_model>

<verification>
- `node -e require.resolve` confirms both packages installed.
- `npx tsc --noEmit -p tsconfig.json` passes (contract change is type-safe).
- `npx vitest run shared/engine/` stays green (no fixture ripple).
- `npx vitest run shared/quiz-engine/ src/screens/quiz/` is RED (stubs reference absent modules — the intended Wave 0 state).
</verification>

<success_criteria>
- framer-motion + pixelarticons installed and committed; no test-runner reinstall.
- Profile carries 6 new OPTIONAL fields; existing engine tests untouched and green.
- index.css has the 28px slider-thumb override.
- Four RED test stubs exist and fail for the right reason (missing source modules).
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-01-SUMMARY.md` when done.
</output>

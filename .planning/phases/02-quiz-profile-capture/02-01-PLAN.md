---
phase: 02-quiz-profile-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - shared/types.ts
  - shared/data/constants.js
  - shared/engine/derive-preferences.ts
  - shared/engine/derive-preferences.test.ts
  - shared/engine/conflict-detection.ts
  - shared/engine/conflict-detection.test.ts
  - shared/engine/quiz-branching.ts
  - shared/engine/quiz-branching.test.ts
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, QUIZ-04, QUIZ-05]
must_haves:
  truths:
    - "Known quiz answers run through derivePreferences produce normalized weights that sum to approximately 1.0"
    - "A motivation of 'save money' increases the cost weight; 'advance career' increases the career weight (transparent additive nudges)"
    - "opennessToAbroad === 0 sets excludeInternational = true on the emitted preference profile"
    - "Each conflict heuristic fires only on its trigger answer combination; only the highest-severity conflict surfaces"
    - "Branching predicates resolve correctly: non-US citizenship needs a status question; openness=0 collapses abroad framing; partner needs partner income"
    - "shared/types.ts (Profile + PreferenceProfile) and the engine modules type-check under the repo tsconfig"
  artifacts:
    - path: "shared/engine/derive-preferences.ts"
      provides: "Pure derivation: raw answers -> normalized PreferenceProfile weights + tradeoffs + hardFilters + excludeInternational"
      exports: ["derivePreferences"]
      min_lines: 40
    - path: "shared/engine/derive-preferences.test.ts"
      provides: "Unit tests proving weight normalization, motivation/pace nudges, openness=0 exclusion"
    - path: "shared/engine/conflict-detection.ts"
      provides: "Tension/tradeoff conflict heuristics (4 rules), highest-severity selection"
      exports: ["detectConflicts", "topConflict"]
    - path: "shared/engine/quiz-branching.ts"
      provides: "Pure branching predicates consumed by the quiz step machine"
      exports: ["needsStatusQuestion", "collapseAbroad", "needsPartnerIncome", "needsDependentCount", "needsPetType", "needsHubQuestions"]
    - path: "shared/types.ts"
      provides: "Extended Profile (new dimensions) + PreferenceProfile interface + City dealbreaker-attribute fields"
      contains: "interface PreferenceProfile"
    - path: "shared/data/constants.js"
      provides: "New option sets: MOTIVATIONS, WORK_STYLES, CITIZENSHIPS, MOVE_TIMELINES, IMMIGRATION_STATUSES, expanded DEAL_BREAKERS metadata"
  key_links:
    - from: "shared/engine/derive-preferences.ts"
      to: "shared/types.ts PreferenceProfile"
      via: "return type annotation"
      pattern: "PreferenceProfile"
    - from: "shared/engine/derive-preferences.test.ts"
      to: "shared/engine/derive-preferences.ts"
      via: "import + assertion"
      pattern: "derivePreferences"
---

## Phase Goal

**As a** prospective relocator, **I want to** complete a deeper, adaptive profile quiz that captures my career, finances, background, lifestyle, motivations, priorities, dealbreakers, openness to living abroad, and citizenship/immigration status, **so that** the app emits a structured, weighted preference profile that drives my matches, financials, roadmap, and visa concierge.


<objective>
Build the phase's REAL deliverable (D-04, D-05): the extended typed contract and the PURE, UNIT-TESTED preference-derivation logic. "The real logic lives in the profile, not the matcher." This slice stands alone — given quiz answers as plain objects, it emits a normalized `PreferenceProfile` that Phase 3 will score cities against, with the math re-derivable on stage in 60 seconds.

It also installs the test runner the whole phase depends on (no runner exists today) and resolves RESEARCH Open Question #1 by defining the `City` dealbreaker-attribute fields on the contract now (recommended), so Phase 3 can wire all 10 dealbreakers without inert no-ops (D-12).

Purpose: Make every downstream UI task verifiable (Nyquist — RESEARCH Validation Architecture) and give the quiz a single function to call on submit.
Output: vitest config, extended `shared/types.ts`, new option sets in `shared/data/constants.js`, and three pure tested engine modules (`derive-preferences`, `conflict-detection`, `quiz-branching`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-quiz-profile-capture/02-CONTEXT.md
@.planning/phases/02-quiz-profile-capture/02-RESEARCH.md
@shared/types.ts
@shared/data/constants.js
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test runner and extend the typed contract (Profile + PreferenceProfile + City dealbreaker fields)</name>
  <files>package.json, vitest.config.ts, shared/types.ts, shared/data/constants.js</files>
  <read_first>
    - shared/types.ts (the contract being extended — current Profile, City, the P2→P3 handshake)
    - shared/data/constants.js (PROFESSION_CATEGORIES, BASE_SALARIES, LIFESTYLE_TAGS, DEAL_BREAKERS — option sets to extend)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (section "The Derived Preference Profile (D-04)" for the exact PreferenceProfile shape; "QUIZ-03 Findings" for citizenship shortlist + status enum; "QUIZ-05 Findings" for timeline buckets; "Open Questions #1" for City fields)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (sections B/C for the exact option labels the UI will render — option VALUES stored here must match what the UI emits)
    - package.json (current scripts + deps; "type": "module" is set)
    - tsconfig.json (include globs cover shared/**/*.ts; strict true)
  </read_first>
  <action>
    Install vitest as a devDependency (npm install -D vitest) and add a "test" script ("vitest run") plus "test:watch" ("vitest") to package.json. Create vitest.config.ts at repo root with environment "node" and include "shared/**/*.test.ts" (the engine is pure — no jsdom needed). Per D-05/D-04 and RESEARCH "Derived Preference Profile": extend shared/types.ts Profile with the new raw-capture fields — motivations (string[]), workStyle ("remote"|"hybrid"|"onsite"|"freelance"|"jobhunting"), industryHubImportance? ("low"|"med"|"high"), commuteTolerance? (number), paceOfLife (number 0..100), communityScale (number 0..100), familyProximity (number 0..100), riskTolerance (number 0..100), and tiebreaker (the reconciliation result: { dimension: "A"|"B"; pair: [string,string] } | null). Add the derived preferences: number-keyed PreferenceProfile interface exactly as RESEARCH specifies — weights { cost, career, lifestyle, safety, climate, community, pace, international } all number 0..1 summing ≈1; tradeoffs Record<string,"a"|"b">; hardFilters string[]; excludeInternational boolean — and add `preferences: PreferenceProfile` to Profile. Reconcile prototype state extras per D-05 / code_context: keep customProfession folded into profession (single `profession` string, no separate field on the contract), drop `color`; keep pet detail (petType?: string). Resolve RESEARCH Open Question #1 (recommended path): add dealbreaker-attribute fields to the City interface so Phase 3 can wire all 10 dealbreakers (D-12) — hasInternationalAirport (boolean), nearCoast (boolean), nearMountains (boolean), stateIncomeTax (number; 0 = no state income tax), strongJobMarketFields (string[]). Add a comment on each new City field noting it is populated in Phase 3/4. In shared/data/constants.js add new option sets whose stored VALUES match the UI-SPEC labels/keys: MOTIVATIONS (the 8 from UI-SPEC section C sub-field 1), WORK_STYLES (5 from UI-SPEC section C), CITIZENSHIPS (the locked shortlist from UI-SPEC section B sub-field 2: "US Citizen" default + Canadian, British, Australian, EU Citizen, Indian, Mexican, Filipino, Brazilian, Nigerian, Other), IMMIGRATION_STATUSES (UI-SPEC non-US enum: US Citizen, Permanent Resident, Work Visa, Student Visa, Other/Not in US), MOVE_TIMELINES (UI-SPEC: Within 6 months, 6–12 months, 1–2 years, 2+ years, Just exploring), and a DEALBREAKER_META map keyed by the 10 DEAL_BREAKERS strings recording which City attribute each maps to (e.g. "Must be near ocean/coast" -> nearCoast) so D-12 "wire all 10" is traceable. Do NOT remove any of the existing 10 DEAL_BREAKERS. Confirm `npx tsc --noEmit` passes after editing.
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx tsc --noEmit && grep -q "interface PreferenceProfile" shared/types.ts && grep -q "excludeInternational" shared/types.ts && grep -q "hasInternationalAirport" shared/types.ts && node -e "import('./shared/data/constants.js').then(m=>{if(!m.MOTIVATIONS||!m.CITIZENSHIPS||!m.MOVE_TIMELINES)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - package.json has a "test" script running "vitest run"; vitest is in devDependencies.
    - vitest.config.ts exists at repo root and includes shared/**/*.test.ts.
    - shared/types.ts contains `interface PreferenceProfile` with weights (8 named keys), tradeoffs, hardFilters, and excludeInternational; Profile gains motivations, workStyle, paceOfLife, communityScale, familyProximity, riskTolerance, tiebreaker, and preferences.
    - Profile no longer references a `color` field; profession remains a single string (customProfession folded in).
    - City interface contains hasInternationalAirport, nearCoast, nearMountains, stateIncomeTax, strongJobMarketFields.
    - shared/data/constants.js exports MOTIVATIONS, WORK_STYLES, CITIZENSHIPS, IMMIGRATION_STATUSES, MOVE_TIMELINES, DEALBREAKER_META; the original 10 DEAL_BREAKERS array is unchanged.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Test runner installed; contract extended with PreferenceProfile + new Profile dimensions + City dealbreaker fields; new constant option sets present; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure tested derive-preferences function (D-04 — the real logic)</name>
  <files>shared/engine/derive-preferences.ts, shared/engine/derive-preferences.test.ts</files>
  <behavior>
    - Given importanceRank ["cost","career","lifestyle","safety"] and no nudges, base weights follow the rank-order split (~0.40/0.27/0.20/0.13) and weights.cost > weights.career > weights.lifestyle > weights.safety.
    - All eight weight keys are present and the sum of weights is within 0.001 of 1.0 (normalized) for any valid input.
    - motivations including "Lower cost of living" (the save-money motivation) raises weights.cost vs the no-nudge baseline; "Better career opportunities" raises weights.career.
    - High paceOfLife and high opennessToAbroad move the corresponding pace / international weights up relative to baseline (additive, capped nudges).
    - opennessToAbroad === 0 produces excludeInternational === true; opennessToAbroad > 0 produces false.
    - dealBreakers array is copied verbatim into preferences.hardFilters.
    - A tiebreaker resolution applies the documented weight delta to the two paired dimensions and the result still normalizes to ≈1.
  </behavior>
  <read_first>
    - shared/types.ts (Profile input fields + PreferenceProfile return type — created in Task 1)
    - shared/data/constants.js (MOTIVATIONS values to branch nudges on)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (section "The Derived Preference Profile" — the 4-step derivation method: base weights from importanceRank via rank-order 0.40/0.27/0.20/0.13, additive capped nudges from motivations/paceOfLife/communityScale/lifestyleTags/opennessToAbroad, tradeoff deltas, normalize to sum 1; "Don't Hand-Roll" — keep it linear/transparent, no black-box scorer)
  </read_first>
  <action>
    Write the test file FIRST (RED) encoding the behaviors above, then implement derivePreferences to pass (GREEN). Export `derivePreferences(profile: Pick<Profile, ...the raw fields>) : PreferenceProfile` from shared/engine/derive-preferences.ts as a pure function (no side effects, no I/O). Implement exactly the RESEARCH recipe: (1) base weights for the 4 ranked pillars (cost/career/lifestyle/safety) from importanceRank position using the 0.40/0.27/0.20/0.13 split, plus small base seeds for the derived-only keys (climate/community/pace/international); (2) additive capped nudges — map each motivation to a weight key (cost-of-living->cost, career->career, adventure/openness->international, quality-of-life/climate->lifestyle/climate, family->community), nudge pace from paceOfLife, community from communityScale + familyProximity, international from opennessToAbroad; cap each nudge so no single answer dominates; (3) apply tradeoff deltas from the tiebreaker (favored dimension gets +delta, other -delta); (4) normalize all eight weights to sum 1. Set excludeInternational = (opennessToAbroad === 0); copy dealBreakers into hardFilters; copy resolved conflicts into tradeoffs. Keep the math linear and commented so it is re-derivable on stage. Do NOT ask the user to tune weights and do NOT invent a non-transparent scorer (RESEARCH common pitfall: per-factor weighting overload).
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx vitest run shared/engine/derive-preferences.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - shared/engine/derive-preferences.test.ts exists and `npx vitest run shared/engine/derive-preferences.test.ts` exits 0.
    - A test asserts sum(weights) is within 0.001 of 1.0.
    - A test asserts the "save money"/"lower cost of living" motivation raises weights.cost above the no-nudge baseline.
    - A test asserts opennessToAbroad === 0 yields excludeInternational === true.
    - derivePreferences has no imports of fs/network and returns an object matching PreferenceProfile (type-checks under tsc).
  </acceptance_criteria>
  <done>derivePreferences is pure, returns a normalized PreferenceProfile, and all unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure tested conflict-detection and quiz-branching predicates (D-03, D-14)</name>
  <files>shared/engine/conflict-detection.ts, shared/engine/conflict-detection.test.ts, shared/engine/quiz-branching.ts, shared/engine/quiz-branching.test.ts</files>
  <behavior>
    - conflict-detection: each of the 4 RESEARCH heuristics fires ONLY on its trigger combo (budget-vs-vibrancy, abroad-vs-roots, money-vs-career, energy-vs-calm) and not on unrelated inputs.
    - When multiple conflicts are present, topConflict returns exactly one — the highest-severity — and each conflict carries the two dimension keys + display labels needed by the reconciliation panel.
    - When no conflict is present, detectConflicts returns [] and topConflict returns null (so the UI shows no reconciliation panel).
    - quiz-branching: needsStatusQuestion is true iff citizenship !== "US Citizen"; collapseAbroad is true iff opennessToAbroad === 0; needsPartnerIncome iff hasPartner; needsDependentCount iff hasDependents; needsPetType iff hasPets; needsHubQuestions iff workStyle is on-site/hybrid (not remote/freelance).
  </behavior>
  <read_first>
    - shared/types.ts (Profile fields these predicates read — created in Task 1)
    - shared/data/constants.js (MOTIVATIONS, CITIZENSHIPS values to match against)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (section "QUIZ-01 / SC6 / SC7" — the 4 conflict-detection heuristics with exact trigger conditions, the "show at most one, highest-severity" rule, and the concrete branch list: partner->income, dependents->count, pets->type, workStyle≠remote->hub/commute, citizenship≠US->status, openness=0->collapse)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (section E — reconciliation panel needs dimension A/B labels; section F — branching transition pattern)
  </read_first>
  <action>
    Write tests FIRST (RED) for both modules, then implement (GREEN). In conflict-detection.ts export `detectConflicts(profile)` returning an array of conflict objects { id, severity, pair: [keyA,keyB], labelA, labelB } and `topConflict(profile)` returning the single highest-severity conflict or null. Implement the 4 heuristics exactly per RESEARCH: (1) importanceRank[0]==="cost" AND lifestyle/motivation implies expensive (lifestyleTags includes nightlife/startup OR motivations includes a lifestyle-upgrade motivation) -> budget-vs-vibrancy; (2) opennessToAbroad >= 65 AND (motivations includes "Family reasons" OR high familyProximity OR a far-from-family dealbreaker) -> abroad-vs-roots; (3) motivations includes both a save-money and a career motivation while importanceRank can't put both in the top 2 -> money-vs-career; (4) paceOfLife high (fast) AND lifestyleTags weighted toward quiet/outdoors -> energy-vs-calm. Assign a deterministic severity ordering and have topConflict pick the max. In quiz-branching.ts export the six predicate functions named in must_haves, each a pure boolean over the relevant Profile fields. No React, no I/O — these are consumed by the src/ step machine in plans 02/03.
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx vitest run shared/engine/conflict-detection.test.ts shared/engine/quiz-branching.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist; `npx vitest run shared/engine/conflict-detection.test.ts shared/engine/quiz-branching.test.ts` exits 0.
    - A test proves each of the 4 heuristics fires on its trigger and stays silent on a non-trigger input.
    - A test proves topConflict returns exactly one conflict when two are present, and null when none.
    - A test proves needsStatusQuestion(citizenship="Canadian") === true and needsStatusQuestion(citizenship="US Citizen") === false.
    - A test proves collapseAbroad is true exactly when opennessToAbroad === 0.
  </acceptance_criteria>
  <done>conflict-detection (4 heuristics + highest-severity selection) and quiz-branching (6 predicates) are pure and fully unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quiz UI (src/) → shared engine | Quiz answers (in-browser, user-controlled) flow into the pure derivation/branching functions. No server, no persistence, no secrets in this phase. |
| npm install (vitest) | Third-party dev dependency added to the toolchain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | derivePreferences inputs | accept | Pure function over in-browser quiz state; output is advisory display data only, no security decision rides on it. No injection surface (no eval, no DOM strings). |
| T-02-02 | Information Disclosure | citizenship / immigrationStatus capture | mitigate | Store structured enum values only (never free-text status), client-side state only this phase — no transmission, no storage. RESEARCH pitfall "sensitive immigration questions" honored: minimal, neutral, US-fast-path. |
| T-02-SC | Tampering | npm install vitest | mitigate | vitest is a [VERIFIED] mainstream package (npmjs.com/package/vitest, tens of millions of weekly downloads, Vitest org). Single well-known dev dependency; no transitive install of unknown packages requested. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (contract type-checks).
- `npm test` (vitest run) passes all engine unit tests: derive-preferences, conflict-detection, quiz-branching.
- derivePreferences output matches PreferenceProfile (the P2→P3 handshake) — proven by the contract/type test compiling.
</verification>

<success_criteria>
- The phase's core deliverable (D-04) — a pure, transparent, tested preference-derivation function — exists and is green.
- The contract (D-05) is extended with PreferenceProfile + new dimensions; RESEARCH Open Question #1 resolved by defining City dealbreaker fields now.
- Test runner is installed so every later UI task is verifiable (Nyquist).
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-01-SUMMARY.md` when done.
</output>

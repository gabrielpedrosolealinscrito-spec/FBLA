---
phase: 02-quiz-profile-capture
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - shared/quiz-engine/questions.ts
  - shared/quiz-engine/resolver.ts
  - shared/quiz-engine/synthesizer.ts
  - src/screens/quiz/QuizShell.jsx
  - src/screens/quiz/QuestionCard.jsx
  - src/screens/quiz/ProgressBar.jsx
  - src/screens/quiz/inputs/SingleSelect.jsx
  - src/screens/quiz/inputs/MultiSelect.jsx
  - src/screens/quiz/inputs/SliderInput.jsx
  - src/screens/quiz/inputs/FreeText.jsx
  - src/screens/PotentialApp.jsx
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-04, QUIZ-05]

must_haves:
  truths:
    - "User can complete a linear multi-step quiz in the browser covering career, finances, background, lifestyle, priorities, dealbreakers, openness-to-abroad, citizenship, and move timeline"
    - "Completing the quiz synthesizes a Profile and produces a ranked results list via the existing rankCities handoff"
    - "Submitted Profile carries RAW 1–4 weights, dealBreakers, opennessToAbroad, and moveTimeline"
    - "Implements decisions D-01, D-02, D-04, D-08, D-09, D-10, D-12, D-16 (capture-layer rebuild via the ALL_QUESTIONS registry: adaptive dimensions, raw-weight preference synthesis, curated citizenship shortlist + immigrationStatus auto-derive for US citizens, locked 0–100 openness slider, all 10 DEAL_BREAKERS wired byte-exact, move-timeline field)"
  artifacts:
    - path: "shared/quiz-engine/questions.ts"
      provides: "ALL_QUESTIONS: QuestionDef[] + QuestionDef/Answers/QuestionType types"
      contains: "ALL_QUESTIONS"
    - path: "shared/quiz-engine/synthesizer.ts"
      provides: "synthesizeProfile(answers): Profile with raw 1–4 weights + immigration auto-derive"
      exports: ["synthesizeProfile"]
    - path: "shared/quiz-engine/resolver.ts"
      provides: "getVisibleQuestions + clearHiddenAnswers"
      exports: ["getVisibleQuestions", "clearHiddenAnswers"]
    - path: "src/screens/quiz/QuizShell.jsx"
      provides: "Quiz state machine + Framer Motion card transitions + onComplete callback"
      min_lines: 60
    - path: "src/screens/PotentialApp.jsx"
      provides: "Integration seam: step===1 renders QuizShell; onComplete runs rankCities handoff"
      contains: "QuizShell"
  key_links:
    - from: "src/screens/quiz/QuizShell.jsx"
      to: "shared/quiz-engine/synthesizer.ts"
      via: "onComplete(synthesizeProfile(answers))"
      pattern: "synthesizeProfile"
    - from: "src/screens/PotentialApp.jsx"
      to: "shared/engine/index.ts"
      via: "onComplete handler calls rankCities → setResults → goStep(2)"
      pattern: "rankCities"
    - from: "shared/quiz-engine/questions.ts"
      to: "shared/data/constants.js"
      via: "imports DEAL_BREAKERS / PROFESSION_CATEGORIES / LIFESTYLE_TAGS (byte-exact)"
      pattern: "DEAL_BREAKERS"
---

<objective>
First vertical slice: a real, navigable, multi-step quiz the user can complete end-to-end in the browser, producing a synthesized `Profile` that flows through the existing `rankCities` engine to the results screen. This is the linear-but-real backbone — branching, tension, and weight refinement layer on top in Plans 03–04.

Purpose: After this plan, a user can run the rebuilt quiz and see ranked matches — the MVP "can actually do something" bar. It replaces the prototype's inline 5-step quiz with the `shared/quiz-engine` + `src/screens/quiz` architecture and makes the resolver/synthesizer/QuizShell RED stubs from Plan 01 go GREEN.

Output: The quiz-engine pure modules (questions, resolver-basic, synthesizer), the quiz UI components, and the PotentialApp integration seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-quiz-profile-capture/02-CONTEXT.md
@.planning/phases/02-quiz-profile-capture/02-RESEARCH.md
@.planning/phases/02-quiz-profile-capture/02-PATTERNS.md
@.planning/phases/02-quiz-profile-capture/02-UI-SPEC.md
@.planning/phases/02-quiz-profile-capture/02-VALIDATION.md

<interfaces>
<!-- WEIGHT CONTRACT (load-bearing, from shared/engine/scoring.ts lines 34–61): -->
<!-- synthesizeProfile MUST emit Profile.weights as RAW 1–4 (rank0→4, rank1→3, rank2→2, rank3→1). -->
<!-- The engine's rankToWeight() normalizes to [0,1] and index.ts sanitizeProfile clamps to [0,4]. -->
<!-- If the synthesizer pre-normalizes, weights DOUBLE-SHRINK and matching breaks. DO NOT normalize. -->

<!-- IMMIGRATION AUTO-DERIVE (Pitfall 5 / D-09): always set, never undefined: -->
<!--   immigrationStatus: answers["citizenship"] === "US" ? "citizen" : String(answers["immigrationStatus"] ?? "") -->

<!-- INTEGRATION SEAM (src/screens/PotentialApp.jsx lines 150–172): the step===1 block currently -->
<!-- renders the inline 5-step quiz and on final step runs: -->
<!--   const { results: ranked, reconfirmSignal: signal } = rankCities(profile); -->
<!--   setResults(ranked); setReconfirmSignal(signal || null); setSelectedCity(null); -->
<!--   setSortBy("match"); setExpandedSection(null); goStep(2); -->
<!-- Replace the inline quiz JSX with <QuizShell onComplete={(profile)=>{...exact handoff...}} />. -->
<!-- Note: PotentialApp imports the engine as `from '../../shared/engine/index.ts'` (extension included) — preserve that import style. -->

<!-- .js IMPORT EXTENSIONS (SP-1) in shared/quiz-engine/*.ts: -->
<!--   import { ALL_QUESTIONS } from './questions.js'; import type { Profile } from '../types.js'; -->

<!-- DEAL_BREAKERS option strings (shared/data/constants.js) MUST be used BYTE-FOR-BYTE -->
<!-- (import the array; do not retype) — Phase 3 dealbreakers.ts matches them in a switch with -->
<!-- `default: break`; any string drift silently no-ops the filter. Exact strings: -->
<!--   "No extreme cold", "No extreme heat", "Must have public transit", "Must be walkable", -->
<!--   "No state income tax", "Must be near mountains", "Must be near ocean/coast", "Low crime only", -->
<!--   "Need international airport", "Must have strong job market in my field" -->

<!-- DESIGN TOKENS: 02-UI-SPEC §Color :root block is the source of truth for token VALUES -->
<!-- (border 0.06, border-active 0.14, accent-dim 0.10, --cta-shadow #3fbb8c, --pixel, --body). -->
<!-- Share tokens via one owner file + sibling import (SP-5: ResultsView.jsx→CityDetail.jsx pattern). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: questions.ts + resolver.ts (basic) + synthesizer.ts</name>
  <files>shared/quiz-engine/questions.ts, shared/quiz-engine/resolver.ts, shared/quiz-engine/synthesizer.ts</files>
  <read_first>
    - shared/quiz-engine/questions.test.ts is NOT present — read shared/quiz-engine/resolver.test.ts + synthesizer.test.ts (the RED stubs from Plan 01)
    - shared/data/cities.ts (analog: typed exported data array `export const CITIES_DATA: City[]`)
    - shared/engine/scoring.ts lines 34–61 (rankToWeight — the EXACT 4/3/2/1 raw-weight mapping to mirror)
    - shared/engine/index.ts lines 112–142 (pure orchestrator composition — analog for resolver)
    - shared/data/constants.js (DEAL_BREAKERS, PROFESSION_CATEGORIES, LIFESTYLE_TAGS — import, don't retype)
    - shared/types.ts (extended Profile from Plan 01)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Pattern 1 question graph; Pattern 3 synthesis; Pattern 2 resolver)
    - .planning/phases/02-quiz-profile-capture/02-PATTERNS.md (SP-1, SP-2, SP-3; synthesizer + resolver + questions assignments)
  </read_first>
  <behavior>
    - getVisibleQuestions(answers, ALL_QUESTIONS) returns all questions whose showIf is undefined or true, in array order.
    - clearHiddenAnswers(answers, ALL_QUESTIONS) drops any answer whose question is no longer visible.
    - synthesizeProfile({importanceRank:["cost",...]}) → weights.cost === 4 (RAW, not normalized).
    - synthesizeProfile({citizenship:"US"}) → immigrationStatus === "citizen".
    - synthesizeProfile({citizenship:"Brazil", immigrationStatus:"work_visa"}) → immigrationStatus === "work_visa".
    - synthesizeProfile captures opennessToAbroad (number), moveTimeline (string), dealBreakers (string[]).
    - Sparse answers never yield undefined on a required Profile field (defensive `?? ""`/`?? []`/`?? default`).
  </behavior>
  <action>Create the three pure TS modules with SP-2 boxed headers citing D-02/D-03/D-04 and the resolver trigger-before-injection invariant. `questions.ts`: define `QuestionType`, `QuestionDef` (with optional `showIf`, `groupHeader`, `autoAdvance`, `min/max/step`, `options`), `Answers` types and `export const ALL_QUESTIONS: QuestionDef[]` covering career (profession single-select sourced from PROFESSION_CATEGORIES, hasRemote/workStyle), finances (income/savings/debt sliders, housing rent/buy, hasPartner + partnerIncome conditional, hasDependents + numDependents conditional, hasPets — these are REQUIRED Profile fields the prototype captured and the financial model consumes via index.ts; the phase goal demands every field that drives financials, so capture them, do NOT silently default them), background (age, education, currentCity free-text, motivationToMove, communityNeeds, paceOfLife, riskTolerance), lifestyle (lifestyleTags multi-select from LIFESTYLE_TAGS), priorities (importanceRank), dealbreakers (multi-select from DEAL_BREAKERS imported byte-exact), and the Going Global group (opennessToAbroad slider 0–100 step 5, citizenship single-select, immigrationStatus single-select with `showIf:(a)=>a["citizenship"]!=="US"`, moveTimeline single-select). Citizenship shortlist = US (default) + ~10 destination-relevant entries + "Other" (Claude's discretion per CONTEXT). In THIS plan, `resolver.ts` exports `getVisibleQuestions(answers, all)` filtering on `showIf` ONLY (no tension call yet — leave a commented seam for Plan 04) and `clearHiddenAnswers(answers, all)`. `synthesizer.ts` exports `synthesizeProfile(answers): Profile` mirroring scoring.ts rankToWeight 4/3/2/1 RAW (do NOT normalize), always sets `immigrationStatus` per the D-09 ternary, populates all new optional dimension fields, and uses defensive `?? ` defaults so no required field is undefined. Emit `tradeoffTolerance: []` for now (Plan 04 fills it).</action>
  <verify>
    <automated>npx vitest run shared/quiz-engine/resolver.test.ts shared/quiz-engine/synthesizer.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/quiz-engine/synthesizer.test.ts` GREEN incl. assertion that rank-0 category weight === 4 (raw, not ≤1).
    - `npx vitest run shared/quiz-engine/resolver.test.ts` GREEN for showIf filtering + clearHiddenAnswers (tension-injection assertions may stay pending until Plan 04 — note which).
    - `grep -q 'DEAL_BREAKERS' shared/quiz-engine/questions.ts` and the dealbreaker options derive from the imported array (no retyped strings).
    - `npx tsc --noEmit -p tsconfig.json` passes.
  </acceptance_criteria>
  <done>Three pure modules exist; synthesizer + resolver(basic) tests green; weights are raw 1–4; dealbreaker strings sourced byte-exact from constants.js.</done>
</task>

<task type="auto">
  <name>Task 2: Quiz UI components (QuizShell, QuestionCard, ProgressBar, inputs/*)</name>
  <files>src/screens/quiz/QuizShell.jsx, src/screens/quiz/QuestionCard.jsx, src/screens/quiz/ProgressBar.jsx, src/screens/quiz/inputs/SingleSelect.jsx, src/screens/quiz/inputs/MultiSelect.jsx, src/screens/quiz/inputs/SliderInput.jsx, src/screens/quiz/inputs/FreeText.jsx</files>
  <read_first>
    - src/screens/quiz/QuizShell.test.jsx (RED stub from Plan 01 — the contract this must satisfy)
    - src/screens/PotentialApp.jsx lines 59–70 (upd/toggleArr helpers), 91–110 (pill/slider/input primitives), 150–186 (state machine + progress bar)
    - src/screens/results/ResultsView.jsx lines 8–31 (token object + style-helper export pattern, SP-5)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (Component Inventory §1–7, §11–13; Motion Contract; Color :root)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Code Examples: QuizShell navigation + slideVariants; Pitfall 2 dynamic progress; Pitfall 4 slider thumb)
    - .planning/phases/02-quiz-profile-capture/02-PATTERNS.md (QuizShell/QuestionCard/inputs assignments; SP-5)
  </read_first>
  <action>Build the quiz UI per 02-UI-SPEC. `QuizShell.jsx`: holds `answers` (Record), `currentIndex`, `direction` state; computes `visible = getVisibleQuestions(answers, ALL_QUESTIONS)` on every render; `handleAnswer` calls `clearHiddenAnswers({...answers,[id]:v}, ALL_QUESTIONS)`; `goNext` advances or, on last visible question, calls `onComplete(synthesizeProfile(answers))`; `goBack` decrements; `canProceed = !current.required || answers[current.id] != null`. Wrap the card in Framer Motion `AnimatePresence mode="wait"` with the slideVariants from 02-UI-SPEC Motion Contract, honoring `prefers-reduced-motion` (transition `{duration:0}`). Guard the matchMedia read so it cannot throw under jsdom or older browsers: `window.matchMedia?.('(prefers-reduced-motion: reduce)')?.matches ?? false` (Plan 01 also appends a matchMedia mock to src/test-setup.js — both must hold). Own the design tokens here (or in a sibling `ui-tokens` file) using the 02-UI-SPEC :root VALUES and export them for the other quiz files (SP-5). `ProgressBar.jsx`: segmented bar with one segment per `visible.length` (dynamic — Pitfall 2), mint=done/amber=current, `04/07` Pixelify counter, Back button (44px hit area, pixelarticons ChevronLeft, `aria-label="Go back"`). `QuestionCard.jsx`: switch on `question.type` → render the matching `inputs/*` component (analog: PotentialApp `profileStep===N` render-by-state); render kicker/prompt/subtext per §3. `inputs/SingleSelect.jsx` + `MultiSelect.jsx`: pill anatomy §4/§5 (icon chip, check mark, selected box-shadow `3px 3px 0`, `toggleArr` for multi with max + escalating-count subtext seam), pixelarticons icons. `SliderInput.jsx`: range input §6, live Pixelify value, min/max labels, opennessToAbroad value-0 inline note. `FreeText.jsx`: `inputStyle` + `maxLength={200}` (ASVS V5) + char countdown >150. Semantic `<button>` for all options/CTA; focus ring per Accessibility Contract. Do NOT call rankCities inside QuizShell — that stays in PotentialApp (Task 3).</action>
  <verify>
    <automated>npx vitest run src/screens/quiz/QuizShell.test.jsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - QuizShell.test.jsx GREEN: first question renders on mount; answering + Continue advances; Back returns to previous.
    - `grep -q 'maxLength={200}' src/screens/quiz/inputs/FreeText.jsx` (ASVS V5).
    - `grep -q 'AnimatePresence' src/screens/quiz/QuizShell.jsx` and `grep -q 'prefers-reduced-motion' src/screens/quiz/QuizShell.jsx`.
    - ProgressBar segment count derives from `visible.length`, not a static constant (Pitfall 2).
    - No `rankCities` import inside src/screens/quiz/ (engine call stays in PotentialApp).
  </acceptance_criteria>
  <done>Quiz UI renders and navigates under vitest; Framer Motion direction-aware transitions wired with reduced-motion fallback; free-text capped at 200 chars; tokens shared from one owner.</done>
</task>

<task type="auto">
  <name>Task 3: Integration seam — mount QuizShell in PotentialApp</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx lines 1–6 (imports), 150–186 (step===1 inline quiz block), 382–417 (step===2 results render — the destination of the handoff)
    - src/screens/quiz/QuizShell.jsx (the onComplete contract from Task 2)
    - .planning/phases/02-quiz-profile-capture/02-PATTERNS.md (PotentialApp.jsx integration-seam assignment — reuse existing rankCities handoff)
  </read_first>
  <action>Replace the inline 5-step quiz inside the `step === 1` block with `<QuizShell onComplete={(profile) => { const { results: ranked, reconfirmSignal: signal } = rankCities(profile); setResults(ranked); setReconfirmSignal(signal || null); setSelectedCity(null); setSortBy("match"); setExpandedSection(null); goStep(2); }} />` — i.e. run the EXACT existing handoff (PotentialApp lines 165–171), just sourced from the synthesized profile QuizShell passes up. Import QuizShell with the project's src import style. Remove the now-dead inline quiz JSX, `profileStep`/`goProfile`/per-step `canProceed`/`upd`/`toggleArr` only if they are no longer referenced elsewhere in the file (verify with grep before deleting; keep anything the results screens still use). Do NOT duplicate the rankCities call inside QuizShell. Preserve the existing `from '../../shared/engine/index.ts'` import.</action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (no unresolved imports / dead-reference errors).
    - `grep -q 'QuizShell' src/screens/PotentialApp.jsx` and the onComplete handler calls `rankCities` then `goStep(2)`.
    - No remaining reference to the deleted inline-quiz `profileStep` render blocks (grep returns only live uses or none).
    - Manual seam intact: starting the app (`npm run dev`) → quiz → completing it reaches the results list.
  </acceptance_criteria>
  <done>PotentialApp step===1 renders QuizShell; onComplete runs the verified rankCities→setResults→goStep(2) handoff; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard/pointer → quiz free-text inputs | Untrusted free-text (currentCity, custom fields) enters Profile state |
| Quiz state → rankCities (in-process) | Synthesized Profile crosses into the local engine; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering (XSS) | FreeText input rendered downstream | mitigate | React JSX auto-escapes; no `dangerouslySetInnerHTML` with user input anywhere in quiz/results path |
| T-02-03 | DoS (local) | Oversized free-text | mitigate | `maxLength={200}` on every FreeText input (ASVS V5) |
| T-02-04 | Information Disclosure | citizenship/immigrationStatus leaking off-device | accept | Phase 2 has zero network egress — quiz is client-only and rankCities is local; no analytics/fetch touches these fields this phase. Re-evaluate when Phase 5 proxy ships. |
</threat_model>

<verification>
- `npx vitest run shared/quiz-engine/resolver.test.ts shared/quiz-engine/synthesizer.test.ts src/screens/quiz/QuizShell.test.jsx --reporter=verbose` green (resolver-basic + synthesizer + QuizShell). Do NOT run the whole `shared/quiz-engine/` dir at this wave: `tension.test.ts` has a top-level import of the not-yet-created `tension.js` and import-errors until Plan 04 — the full-dir green is reserved for Plan 04's gate.
- `npx tsc --noEmit -p tsconfig.json` passes.
- `npm run build` clean.
- Manual (browser): complete the quiz → ranked results render.
</verification>

<success_criteria>
- A user can complete a linear multi-step quiz and reach ranked results (QUIZ-01).
- opennessToAbroad slider, dealbreaker selections, and move timeline are captured on the submitted Profile (QUIZ-02/04/05 — capture-side).
- Profile.weights are RAW 1–4; immigrationStatus is always populated.
- Plan-01 RED stubs for synthesizer + resolver-basic + QuizShell are now GREEN.
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-02-SUMMARY.md` when done.
</output>

---
phase: 02-quiz-profile-capture
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/screens/PotentialApp.jsx
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, QUIZ-05]
must_haves:
  truths:
    - "The quiz renders a multi-step flow (Career, Finances, Background, Going Global, Lifestyle) with the progress bar auto-scaling to the new step count"
    - "A user can set the openness-to-abroad slider (0–100); the value is stored on profile.opennessToAbroad and at 0 shows the 'International cities excluded' annotation"
    - "A user can pick citizenship from the locked shortlist (defaults to US Citizen); selecting US auto-sets immigrationStatus='citizen' with no follow-up; selecting non-US reveals the status enum"
    - "A user can pick a move timeline bucket stored on profile.moveTimeline"
    - "Conditional sub-fields branch correctly via the shared predicates: partner→income, dependents→count, pets→type, openness=0→collapse abroad framing"
    - "The quiz state is managed by a single useReducer holding the extended profile (no scattered useState for profile fields)"
  artifacts:
    - path: "src/screens/PotentialApp.jsx"
      provides: "Rebuilt quiz step machine (reducer) + steps 1–5 wiring the new contract fields with branching"
      contains: "useReducer"
  key_links:
    - from: "src/screens/PotentialApp.jsx"
      to: "shared/engine/quiz-branching.js"
      via: "import of branching predicates"
      pattern: "needsStatusQuestion|collapseAbroad|needsPartnerIncome"
    - from: "src/screens/PotentialApp.jsx"
      to: "shared/data/constants.js"
      via: "import of CITIZENSHIPS / MOVE_TIMELINES / IMMIGRATION_STATUSES"
      pattern: "CITIZENSHIPS|MOVE_TIMELINES"
---

## Phase Goal

**As a** prospective relocator, **I want to** complete a deeper, adaptive profile quiz that captures my career, finances, background, lifestyle, motivations, priorities, dealbreakers, openness to living abroad, and citizenship/immigration status, **so that** the app emits a structured, weighted preference profile that drives my matches, financials, roadmap, and visa concierge.


<objective>
Rebuild the capture-layer UI (D-01: rebuild, not bolt-on) for the first five steps, migrating quiz state to a single `useReducer` over the extended `Profile` (RESEARCH "Don't Hand-Roll" — one reducer for the now-large profile) and adding the "Going Global" demo-moment step (D-06, D-08, D-10, D-16). The immigration capture follows the US-first model (D-07): citizenship defaults to US and most users never see a status question. All visuals reuse the locked inline-style primitives per UI-SPEC (port-don't-redesign). Branching uses the pure predicates from Plan 01 (D-03) — no schema-driven engine.

This slice delivers QUIZ-02 (openness slider captured + passed), QUIZ-03 (citizenship declared + immigrationStatus auto/declared), QUIZ-05 (move timeline), and the structural multi-step flow toward QUIZ-01.

Purpose: A user can navigate Career → Finances → Background → Going Global → Lifestyle, with adaptive follow-ups, all writing to the typed profile.
Output: Rebuilt `src/screens/PotentialApp.jsx` quiz block (steps 1–5 + reducer + branching wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-quiz-profile-capture/02-CONTEXT.md
@.planning/phases/02-quiz-profile-capture/02-UI-SPEC.md
@.planning/phases/02-quiz-profile-capture/02-01-SUMMARY.md
@src/screens/PotentialApp.jsx
@shared/types.ts
@shared/data/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate quiz state to useReducer over the extended profile + rebuild steps 1–3 with branching</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (current useState profile, upd/toggleArr helpers, goProfile/goStep transitions, pill/inputStyle/btnPrimary/label/fadeIn/sectionGap primitives, the step===1 block and its 5 step bodies — this is the reference being rebuilt)
    - shared/types.ts (the extended Profile shape created in Plan 01 — every field the reducer state must hold)
    - shared/engine/quiz-branching.ts (needsPartnerIncome, needsDependentCount, needsPetType — the predicates that drive conditional sub-fields; import the compiled/source per repo module resolution)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (sections A "Quiz Step Architecture", F "Branching transition pattern", Primitive Inventory — exact primitive object shapes; Animation Contract)
    - .planning/phases/02-quiz-profile-capture/02-01-SUMMARY.md (what Plan 01 exported and how to import it from src/)
  </read_first>
  <action>
    Replace the prototype's `profile` useState with a single useReducer whose initial state is the full extended Profile (all Plan 01 fields) — defaults: profession "", hasRemote false, income 55000, savings 15000, debt 0, housing "rent", citizenship "US Citizen" (D-08 default), immigrationStatus "citizen" (D-09 auto for US), opennessToAbroad 50, moveTimeline "" , motivations [], lifestyleTags [], dealBreakers [], importanceRank ["cost","career","lifestyle","safety"], tiebreaker null, plus the new dimension fields. The reducer supports actions equivalent to the current upd(k,v) and toggleArr(key,val,max) helpers (keep helper call-sites working by re-implementing upd/toggleArr as dispatch wrappers). Keep the existing goProfile/goStep anim transition pattern (anim false → 60ms → true) verbatim — do NOT introduce CSS classes or Framer Motion (UI-SPEC Animation Contract). Set totalSteps to the new count and let the progress bar Array.from auto-scale (UI-SPEC: progress bar auto-scales). Rebuild step bodies for Career (step heading + profession pills + custom-profession input folded into the single `profession` field per D-05; remote toggle), Finances (income/savings/debt sliders, housing row, partner toggle → partner income gated by needsPartnerIncome, dependents toggle → count gated by needsDependentCount), and Background (age, education, currentCity, pets toggle → petType gated by needsPetType). Reuse pill()/inputStyle/btnPrimary/btnDisabled/label/sectionGap/fadeIn exactly as defined in the css/primitive block — do not alter their object values (D-01 port-don't-redesign lock). Drop the prototype's `color` field entirely (D-05 reconcile decision). Use conditional JSX rendering for all branched sub-fields (no display:none toggling) per UI-SPEC section F.
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx vite build 2>&1 | tail -5 && grep -q "useReducer" src/screens/PotentialApp.jsx && ! grep -q '"color":' src/screens/PotentialApp.jsx</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/PotentialApp.jsx uses useReducer for the profile (grep "useReducer" matches); no `color` profile field remains.
    - The reducer initial state includes citizenship ("US Citizen"), immigrationStatus ("citizen"), opennessToAbroad, moveTimeline, motivations, paceOfLife, communityScale, familyProximity, riskTolerance, tiebreaker.
    - Career/Finances/Background steps render and conditional sub-fields (partner income, dependent count, pet type) appear only when their predicate is true.
    - `npx vite build` completes without errors.
    - All reused primitives (pill, inputStyle, btnPrimary, label, fadeIn, sectionGap) retain their UI-SPEC object values verbatim.
  </acceptance_criteria>
  <done>Quiz state is reducer-driven over the extended profile; steps 1–3 rebuilt with branching sub-fields; build passes; no color field.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "Going Global" step (openness slider + citizenship + status + timeline) and the Lifestyle step (D-06, D-08, D-09, D-10, D-16)</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (the reducer + steps from Task 1; the existing lifestyle step grid pattern at step===3)
    - shared/data/constants.js (CITIZENSHIPS, IMMIGRATION_STATUSES, MOVE_TIMELINES, LIFESTYLE_TAGS — the exact stored values to render)
    - shared/engine/quiz-branching.ts (needsStatusQuestion, collapseAbroad — drive the conditional status enum and abroad-framing collapse)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (section B "Going Global Step" — the green left-border accent panel, slider living-label thresholds and the value-0 red annotation copy, citizenship shortlist pills + non-US status sub-row, timeline pills; Copywriting Contract table for exact strings; Color section for --accent left-border and the value-0 annotation colors)
  </read_first>
  <action>
    Add the "Going Global" step (D-06) as a new step in the sequence, positioned per UI-SPEC section A (after Background). Wrap its three sub-fields in the green group-accent panel (borderLeft "3px solid var(--accent)", paddingLeft 16, marginBottom 28). Sub-field 1 — openness slider (D-10): label "Open to living abroad", inline living-label readout using the exact threshold copy from UI-SPEC ("Not at all" / "A little curious" / "Definitely open" / "Ready to go"), range min 0 max 100 step 5 accentColor #6EE7B7 storing to opennessToAbroad; at value 0 render the red annotation pill with the UI-SPEC copy "International cities excluded from your results" (background rgba(248,113,113,0.08), color var(--neg)); when collapseAbroad(profile) is true, collapse the abroad-specific framing per UI-SPEC section B branch note but STILL keep citizenship captured (default US) so the contract stays populated. Sub-field 2 — citizenship (D-08): flexWrap pill row over CITIZENSHIPS with "US Citizen" active by default; selecting "US Citizen" sets immigrationStatus to "citizen" silently (no follow-up, D-09); when needsStatusQuestion(profile) is true, render the conditional status sub-row (label "Current immigration status (US)") over IMMIGRATION_STATUSES with the same fadeIn re-trigger, storing to immigrationStatus. Sub-field 3 — move timeline (D-16): flexWrap pills over MOVE_TIMELINES, single-select to moveTimeline; "Just exploring" is a valid no-penalty value. Step validation: always completable because citizenship defaults to US (UI-SPEC). Also rebuild the Lifestyle step using the existing 2-col LIFESTYLE_TAGS grid (≥1 required to proceed) reusing the shipped grid pattern (gridTemplateColumns "1fr 1fr", gap 10). Use the exact CTA copy from the Copywriting Contract ("Continue →" for non-final steps). Reuse all primitives verbatim; no new color tokens beyond those listed in UI-SPEC.
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx vite build 2>&1 | tail -5 && grep -q "Going Global" src/screens/PotentialApp.jsx && grep -q "International cities excluded from your results" src/screens/PotentialApp.jsx && grep -q "needsStatusQuestion" src/screens/PotentialApp.jsx</automated>
  </verify>
  <acceptance_criteria>
    - The "Going Global" step renders the green left-border accent panel containing the openness slider, citizenship shortlist (US Citizen default-selected), and move-timeline pills.
    - At opennessToAbroad === 0 the exact annotation "International cities excluded from your results" renders in --neg styling.
    - Selecting a non-US citizenship reveals the immigration-status enum (driven by needsStatusQuestion); selecting US shows no status question and immigrationStatus is "citizen".
    - moveTimeline is set from the MOVE_TIMELINES pills; "Just exploring" is selectable with no penalty.
    - Lifestyle step requires ≥1 tag to proceed.
    - `npx vite build` completes without errors.
  </acceptance_criteria>
  <done>Going Global (openness + citizenship + auto/declared status + timeline) and Lifestyle steps render with correct branching and locked visuals; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quiz UI (src/) → reducer state | User-controlled answers held in browser state; no network, no persistence this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | citizenship / immigrationStatus capture in UI | mitigate | Stored as structured enum values from CITIZENSHIPS/IMMIGRATION_STATUSES (never free-text); client-side only; no transmission. UI keeps the status question minimal and neutral (RESEARCH pitfall "sensitive immigration questions"). |
| T-02-03 | Tampering | slider / pill inputs | accept | Values constrained to slider range and enum option sets; output is advisory display data, no security decision depends on it. |
| T-02-SC | Tampering | npm/package installs | accept | This plan adds no new packages (vitest already installed in Plan 01); no install task here. |
</threat_model>

<verification>
- `npx vite build` passes after each task.
- Manual flow check (covered by the Plan 04 human-verify checkpoint): navigate steps 1–5, exercise branches.
- Branching driven exclusively by the Plan 01 tested predicates (no ad-hoc conditionals duplicating that logic).
</verification>

<success_criteria>
- QUIZ-02: openness slider captured to profile + (at 0) visible exclusion annotation.
- QUIZ-03: citizenship declared (default US) and immigrationStatus auto-set for US / declared for non-US.
- QUIZ-05: move timeline captured.
- Multi-step structural flow (steps 1–5) rebuilt on a single reducer with adaptive sub-fields.
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-02-SUMMARY.md` when done.
</output>

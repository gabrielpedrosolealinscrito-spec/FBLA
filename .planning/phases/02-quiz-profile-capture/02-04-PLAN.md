---
phase: 02-quiz-profile-capture
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - shared/engine/profile-completeness.ts
  - shared/engine/profile-completeness.test.ts
autonomous: false
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, QUIZ-04, QUIZ-05]
must_haves:
  truths:
    - "A profile-completeness assertion proves a submitted profile carries every required field and a valid PreferenceProfile"
    - "immigrationStatus is 'citizen' for US users; excludeInternational is true exactly when opennessToAbroad === 0"
    - "A human confirms the full quiz runs in the browser with no errors, the Going Global step shows the demo moment, branching works, the dealbreaker warning appears, and a conflict surfaces one reconciliation"
  artifacts:
    - path: "shared/engine/profile-completeness.ts"
      provides: "assertProfileComplete — validates a submitted Profile has all required fields + a normalized PreferenceProfile"
      exports: ["assertProfileComplete", "isProfileComplete"]
    - path: "shared/engine/profile-completeness.test.ts"
      provides: "Tests for completeness, US auto-status, openness=0 exclusion"
  key_links:
    - from: "shared/engine/profile-completeness.test.ts"
      to: "shared/engine/derive-preferences.ts"
      via: "builds a full profile, derives preferences, asserts completeness"
      pattern: "derivePreferences"
---

## Phase Goal

**As a** prospective relocator, **I want to** complete a deeper, adaptive profile quiz that captures my career, finances, background, lifestyle, motivations, priorities, dealbreakers, openness to living abroad, and citizenship/immigration status, **so that** the app emits a structured, weighted preference profile that drives my matches, financials, roadmap, and visa concierge.


<objective>
Close the phase with the two RESEARCH Validation-Architecture checks not yet covered: (1) a profile-completeness assertion (every required field present; `immigrationStatus` auto-`citizen` for US; `excludeInternational` set when openness=0; a valid normalized `PreferenceProfile`), and (2) a single visual human-verify checkpoint confirming the rebuilt quiz runs end-to-end in the browser with the demo moments intact (Going Global, branching, dealbreaker warning, tension reconciliation).

Purpose: Prove the quiz emits a complete, contract-correct preference profile (the P2→P3 handshake) and that the locked port-don't-redesign visuals + adaptive behavior actually render for a human.
Output: `shared/engine/profile-completeness.ts` + tests, and a verified checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-quiz-profile-capture/02-UI-SPEC.md
@.planning/phases/02-quiz-profile-capture/02-01-SUMMARY.md
@.planning/phases/02-quiz-profile-capture/02-02-SUMMARY.md
@.planning/phases/02-quiz-profile-capture/02-03-SUMMARY.md
@shared/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Profile-completeness assertion + tests (RESEARCH Validation Architecture)</name>
  <files>shared/engine/profile-completeness.ts, shared/engine/profile-completeness.test.ts</files>
  <behavior>
    - isProfileComplete returns false when any required field is missing (e.g. profession === "" or moveTimeline === "" or no lifestyleTags) and true for a fully answered profile.
    - A US-citizen profile has immigrationStatus === "citizen" (auto-derived); the assertion accepts it.
    - A profile with opennessToAbroad === 0 must have preferences.excludeInternational === true; the assertion fails if it is false.
    - A complete profile run through derivePreferences yields preferences whose weights sum to ~1 and contains all eight weight keys; the assertion validates that shape.
    - assertProfileComplete throws (or returns a structured error list) naming the missing/invalid fields rather than a generic failure.
  </behavior>
  <read_first>
    - shared/types.ts (Profile + PreferenceProfile — the required fields and the shape to validate)
    - shared/engine/derive-preferences.ts (to build a realistic full profile in the test and validate the emitted preferences)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Validation Architecture table — "Profile completeness" row: every required field present, immigrationStatus auto-citizen for US, excludeInternational set when openness=0)
  </read_first>
  <action>
    Write tests FIRST (RED) for the behaviors above, then implement (GREEN). Export isProfileComplete(profile): boolean and assertProfileComplete(profile): string[] (returns a list of problems; empty list = complete) from shared/engine/profile-completeness.ts as pure functions. Required fields to check: profession non-empty, a numeric income, housing set, citizenship set, immigrationStatus set (and === "citizen" when citizenship is the US value), moveTimeline non-empty, lifestyleTags length >= 1, importanceRank length 4, and a preferences object whose weights contain all eight keys summing to within 0.001 of 1.0 with excludeInternational === (opennessToAbroad === 0). Build the test fixtures by composing a full raw profile and calling derivePreferences to populate preferences, then asserting completeness; include a US-citizen case, a non-US case with an explicit status, and an openness=0 exclusion case. No I/O, pure functions only.
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx vitest run shared/engine/profile-completeness.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - shared/engine/profile-completeness.test.ts exists and `npx vitest run shared/engine/profile-completeness.test.ts` exits 0.
    - A test proves a US profile passes with immigrationStatus "citizen".
    - A test proves an openness=0 profile fails completeness if excludeInternational is not true, and passes when it is.
    - A test proves a profile missing profession or moveTimeline or lifestyleTags is reported incomplete with the offending field named.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Profile-completeness assertion is pure, tested, and validates the full emitted profile including the PreferenceProfile and US auto-status / openness=0 exclusion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the full quiz flow</name>
  <what-built>
    The rebuilt adaptive quiz: 7-step flow (Career, Finances, Background, Going Global, Lifestyle, Motivations & Work Style, Priorities & Dealbreakers) on a single reducer, with branching sub-fields, the Going Global demo moment (openness slider + citizenship + auto/declared immigration status + move timeline), hard dealbreakers with a capture-time warning, a conditional tension-reconciliation follow-up, and submit wiring that emits a derived PreferenceProfile. All engine logic (derive-preferences, conflict-detection, branching, profile-completeness) is unit-tested and green.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the printed local URL (default http://localhost:5173).
    2. Click "Start Exploring" to enter the quiz. Confirm the progress bar shows the new step count and no console errors appear.
    3. Career step: pick a profession (or type a custom one) and confirm Continue enables; toggle "remote".
    4. Finances step: toggle "partner" and confirm the partner-income slider appears; toggle "dependents" and confirm the count pills appear.
    5. Background step: toggle "pets" and confirm the pet-type pills appear.
    6. Going Global step: drag the openness slider to 0 and confirm the red "International cities excluded from your results" annotation appears; drag it up and confirm it disappears. Confirm "US Citizen" is pre-selected and NO immigration-status question shows. Select a non-US citizenship (e.g. "Canadian") and confirm the status enum appears. Pick a move timeline.
    7. Lifestyle step: confirm at least one tag is required to continue.
    8. Motivations step: confirm the motivation multi-select caps at 3; pick a work style of "On-site" and confirm the hub/commute follow-ups appear.
    9. Dealbreakers step: select a dealbreaker and confirm the amber warning banner ("Dealbreakers are hard filters.") appears; deselect all and confirm it disappears. Confirm all 10 dealbreakers are selectable.
    10. To exercise the reconciliation panel: rank "cost" #1 and select an expensive-lifestyle tag (e.g. Nightlife/Startup) so a conflict triggers — confirm ONE reconciliation panel appears with two options and a "Skip — I'm equally pulled both ways" link.
    11. Click "Show Me My Potential →" and confirm the app advances to results with no error.
    12. Confirm the visual identity is unchanged from the shipped dark theme (Instrument Serif headings, Manrope body, accent green #6EE7B7) — no redesign.
  </how-to-verify>
  <action>Human verification only — Claude runs `npm run dev`, then the user manually walks the full quiz flow in the browser per the steps above and confirms behavior + visual lock. No code is written in this task; it gates phase completion on a real end-to-end run.</action>
  <files>src/screens/PotentialApp.jsx (under verification — not modified)</files>
  <verify><human-check>User completes the numbered walkthrough above and types "approved" with no errors, no missing branch, correct copy, and no visual regression.</human-check></verify>
  <done>User has run the quiz end-to-end in the browser and approved: flow renders with the new step count, branching works, Going Global demo moment shows, dealbreaker warning toggles, one reconciliation surfaces, submit reaches results, and the dark-theme visual identity is unchanged.</done>
  <resume-signal>Type "approved" or describe any issues (errors, missing branch, wrong copy, visual regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quiz UI (src/) → shared engine | Submitted profile validated by the pure completeness assertion. No network/persistence this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | profile-completeness inputs | accept | Pure validation function over in-browser state; output is advisory (gates the demo, not security). |
| T-02-07 | Information Disclosure | immigration fields in the validated profile | mitigate | Completeness check reads structured enum values only; no logging of the values, no transmission. |
| T-02-SC | Tampering | npm/package installs | accept | No new packages added in this plan. |
</threat_model>

<verification>
- `npx vitest run` passes all four engine test suites (derive-preferences, conflict-detection, quiz-branching, profile-completeness).
- `npx tsc --noEmit` passes.
- Human checkpoint confirms the full adaptive flow, demo moments, and visual lock in the browser.
</verification>

<success_criteria>
- The emitted profile is provably complete and contract-correct (QUIZ-01..05 fields present; PreferenceProfile valid).
- A human has verified the quiz end-to-end with no errors and no visual regression.
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-04-SUMMARY.md` when done.
</output>

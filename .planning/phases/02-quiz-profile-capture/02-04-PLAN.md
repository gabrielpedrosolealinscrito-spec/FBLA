---
phase: 02-quiz-profile-capture
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - shared/quiz-engine/tension.ts
  - shared/quiz-engine/resolver.ts
  - shared/quiz-engine/synthesizer.ts
  - shared/quiz-engine/tension.test.ts
  - shared/quiz-engine/resolver.test.ts
  - shared/quiz-engine/synthesizer.test.ts
  - src/screens/quiz/QuestionCard.jsx
  - src/screens/quiz/QuizShell.jsx
autonomous: false
requirements: [QUIZ-01, QUIZ-04]

must_haves:
  truths:
    - "When a user's answers reveal a known conflicting priority, the quiz surfaces a reconciling follow-up question"
    - "The user's tiebreaker choice is stored as a tradeoffTolerance entry on the synthesized Profile"
    - "Selecting 3+ dealbreakers shows an escalating capture-time warning so the user knows hard filters remove cities"
    - "Implements decisions D-11, D-14, D-15 (dealbreakers framed as hard filters that eliminate cities; tension detection of conflicting priorities with a reconciling tiebreaker; tiebreaker stored as tradeoffTolerance so Phase 3 can rank balanced cities)"
  artifacts:
    - path: "shared/quiz-engine/tension.ts"
      provides: "detectTension(answers): TensionResult | null + TensionResult interface"
      exports: ["detectTension"]
    - path: "shared/quiz-engine/synthesizer.ts"
      provides: "tradeoffTolerance populated from tiebreaker answers"
      contains: "tradeoffTolerance"
    - path: "src/screens/quiz/QuestionCard.jsx"
      provides: "Tension callout + escalating dealbreaker warning render"
      contains: "tension"
  key_links:
    - from: "shared/quiz-engine/resolver.ts"
      to: "shared/quiz-engine/tension.ts"
      via: "getVisibleQuestions calls detectTension and splices the follow-up at afterId"
      pattern: "detectTension"
    - from: "shared/quiz-engine/synthesizer.ts"
      to: "shared/types.ts"
      via: "tiebreaker answer → Profile.tradeoffTolerance entry"
      pattern: "tradeoffTolerance"
---

<objective>
Final vertical slice and the adaptive showcase: the quiz notices conflicting priorities (e.g. loves-nature + career-first), surfaces ONE reconciling follow-up (D-14), and stores the resolution as a `tradeoffTolerance` tiebreaker on the Profile that Phase 3 will use to rank balancing cities (D-15). Pairs this with the escalating capture-time dealbreaker warning (D-11 guardrail / Pitfall 6) so the user understands hard filters remove cities.

Purpose: This is the "real logic" proof point — the moment that makes the quiz feel intelligent on stage and satisfies QUIZ-01/SC7 (conflicting priorities → reconciling follow-up → stored tiebreaker). It turns the resolver tension-injection + tension.ts RED stubs green and completes QUIZ-04's capture-time advisory warning.

Output: detectTension + tension injection in the resolver, tradeoffTolerance synthesis, the tension callout UI, and the escalating dealbreaker warning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-quiz-profile-capture/02-CONTEXT.md
@.planning/phases/02-quiz-profile-capture/02-RESEARCH.md
@.planning/phases/02-quiz-profile-capture/02-PATTERNS.md
@.planning/phases/02-quiz-profile-capture/02-UI-SPEC.md

<interfaces>
<!-- SIGNAL-OR-NULL shape (mirror shared/engine/dealbreakers.ts checkReconfirm lines 282–304 exactly): -->
<!--   export interface TensionResult { afterId: string; question: QuestionDef } -->
<!--   export function detectTension(answers: Answers): TensionResult | null -->
<!-- Return a typed signal when a known conflict fires, null otherwise. -->
<!-- Switch/guard discipline: unknown/absent answer keys → null, never a crash (cf. getTriggeredDealbreakers default: break). -->

<!-- RESOLVER INJECTION (02-RESEARCH Pattern 2): getVisibleQuestions filters showIf, then -->
<!--   const t = detectTension(answers); if (t) splice t.question in after the index of t.afterId. -->
<!-- The injected follow-up must sit at/after its trigger (importanceRank) — invariant holds. -->

<!-- PRIMARY tension pair (02-RESEARCH lines 357–385): lovesNature (lifestyleTags includes "outdoors"|"snow") -->
<!-- + careerFirst (importanceRank[0|1] === "career") → tiebreaker_nature_vs_career single-select -->
<!-- (nature / career / balanced). Planner adds ≥1 more genuine pair (Assumption A6), e.g. -->
<!-- cost-first + walkable/transit lifestyle. -->

<!-- SYNTHESIS: each fired tiebreaker answer → tradeoffTolerance.push({ dimension, preference }). -->
<!-- preference values per the type: "a"|"b"|"balanced" — map the question's option values onto these -->
<!-- (e.g. nature→"a", career→"b", balanced→"balanced") OR keep the type's union; be consistent with the -->
<!-- tradeoffTolerance type added in Plan 01. Do NOT pre-normalize weights here (raw 1–4 contract unchanged). -->

<!-- DEALBREAKER WARNING (02-UI-SPEC §10 / Pitfall 6): 0–2 hidden; 3 → amber tier-1 copy; 4+ → red tier-2 copy. -->
<!-- aria-live="polite". Inline below the dealbreaker pills, no modal. Copy is sketch-locked in the Copywriting Contract. -->

<!-- LIFESTYLE_TAGS ids (from constants.js) for the nature check: "outdoors", "snow". -->
<!-- .js import extensions in shared/quiz-engine/*.ts (SP-1). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detectTension + resolver injection + tradeoffTolerance synthesis</name>
  <files>shared/quiz-engine/tension.ts, shared/quiz-engine/resolver.ts, shared/quiz-engine/synthesizer.ts</files>
  <read_first>
    - shared/quiz-engine/tension.test.ts (the detectTension assertions to make green)
    - shared/quiz-engine/resolver.test.ts (the tension-injection assertion deferred from Plan 02/03)
    - shared/quiz-engine/synthesizer.test.ts (tradeoffTolerance assertion if present; otherwise add it)
    - shared/engine/dealbreakers.ts lines 282–304 (checkReconfirm — the signal-or-null analog) and lines 81–180 (switch + default:break discipline)
    - shared/quiz-engine/resolver.ts (getVisibleQuestions — add the detectTension call at the documented seam)
    - shared/quiz-engine/synthesizer.ts (synthesizeProfile — fill tradeoffTolerance)
    - shared/data/constants.js (LIFESTYLE_TAGS ids: outdoors, snow)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Pattern 2 injection; tension.ts example lines 357–385; Assumption A6)
    - .planning/phases/02-quiz-profile-capture/02-PATTERNS.md (tension.ts assignment SP-4)
  </read_first>
  <behavior>
    - detectTension({lifestyleTags:["outdoors"], importanceRank:["career",...]}) → non-null TensionResult with afterId==="importanceRank" and a single-select follow-up.
    - detectTension({lifestyleTags:["foodie"], importanceRank:["cost",...]}) → null (no conflict).
    - detectTension({}) → null (no crash on empty/sparse answers).
    - getVisibleQuestions injects the tension question immediately after the afterId question when tension fires.
    - synthesizeProfile with a tiebreaker answer → tradeoffTolerance contains one entry { dimension, preference }; without it → tradeoffTolerance === [].
    - At least one additional tension pair fires on its own conflict and not otherwise.
  </behavior>
  <action>Create `shared/quiz-engine/tension.ts` with the SP-2 header, `export interface TensionResult { afterId: string; question: QuestionDef }`, and `export function detectTension(answers: Answers): TensionResult | null` mirroring `checkReconfirm`'s signal-or-null shape. Implement the primary nature-vs-career pair (lifestyleTags includes "outdoors"|"snow" AND importanceRank[0|1]==="career") returning a `tiebreaker_nature_vs_career` single-select (afterId "importanceRank") with options nature/career/balanced and the §9 callout-adjacent copy. Add ≥1 more genuine pair (Assumption A6) — e.g. cost-first (importanceRank[0]==="cost") + a walkable/transit lifestyle tag → `tiebreaker_cost_vs_amenity`. Use guard discipline so unknown/absent keys → null, never throw. Wire `getVisibleQuestions` (resolver.ts) to call `detectTension` after the showIf filter and splice the follow-up after the index of `afterId` (Pattern 2). In `synthesizer.ts`, for each known tiebreaker answer present, push a `tradeoffTolerance` entry mapping the option value to the type's `preference` union; emit `[]` when none. Keep the raw 1–4 weight contract unchanged. CRITICAL: un-skip the two cases the Plan-01 stubs left as `it.skip` — the resolver tension-injection-position case in `resolver.test.ts` and the tradeoffTolerance case in `synthesizer.test.ts` — converting them to active `it(...)` so they gate this plan's behavior (also create/complete `tension.test.ts`'s assertions which now resolve because `tension.js` exists).</action>
  <verify>
    <automated>npx vitest run shared/quiz-engine/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run shared/quiz-engine/tension.test.ts` GREEN (fires on conflict, null otherwise, no crash on empty).
    - `npx vitest run shared/quiz-engine/resolver.test.ts` GREEN incl. tension-injection-position assertion.
    - `npx vitest run shared/quiz-engine/synthesizer.test.ts` GREEN incl. tradeoffTolerance population.
    - `npx tsc --noEmit -p tsconfig.json` passes.
    - All of `npx vitest run shared/quiz-engine/` green (full engine suite for this phase).
  </acceptance_criteria>
  <done>detectTension is signal-or-null with ≥2 pairs; resolver injects the follow-up at the correct position; synthesizer stores the tiebreaker as tradeoffTolerance; raw-weight contract intact.</done>
</task>

<task type="auto">
  <name>Task 2: Tension callout + escalating dealbreaker warning UI</name>
  <files>src/screens/quiz/QuestionCard.jsx, src/screens/quiz/QuizShell.jsx</files>
  <read_first>
    - src/screens/quiz/QuestionCard.jsx (current render; add callout + warning blocks)
    - src/screens/quiz/QuizShell.jsx (answers state — pass tension + dealbreaker-count context to the card)
    - src/screens/quiz/inputs/MultiSelect.jsx (dealbreaker multi-select — selection count source)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (§9 Tension callout; §10 escalating dealbreaker warning; Copywriting Contract — exact sketch-locked copy; Color reserved-for: indigo=tension, amber/red=warning tiers)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Pitfall 6 escalating warning)
  </read_first>
  <action>In `QuestionCard.jsx`: (a) when the resolver has injected a tension follow-up (the current question is a tiebreaker, OR detectTension(answers) is non-null on the triggering question), render the §9 indigo tension callout above/with the options — 18px pixelarticons AlertTriangle, indigo (`--accent3`) border + left stripe, bold lead + explanatory copy from the Copywriting Contract template ("[Priority A] + [Priority B] rarely overlap. Next we'll ask you to break the tie..."). Informational only — does not replace Continue. (b) On the dealbreakers question, render the escalating warning per §10: 0–2 hidden; exactly 3 → amber tier-1 copy "Heads up — dealbreakers remove cities entirely. 3 is a lot."; 4+ → red (`--neg`) tier-2 copy "4+ dealbreakers active. Some great cities may already be excluded." Inline, no modal, `aria-live="polite"`, `transition: all 0.3s` between tiers. Derive the count from the dealbreakers answer array length. Wire any needed context (current answers / tension result) from QuizShell into QuestionCard.</action>
  <verify>
    <automated>npx vitest run src/screens/quiz/QuizShell.test.jsx --reporter=verbose && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - QuizShell.test.jsx stays GREEN (no navigation regression).
    - `grep -qi 'dealbreakers remove cities' src/screens/quiz/QuestionCard.jsx` (tier-1 warning copy present, sketch-locked).
    - `grep -q 'aria-live' src/screens/quiz/QuestionCard.jsx` (warning announced).
    - Tension callout uses the indigo `--accent3` token (reserved-for: tension only).
    - `npm run build` clean.
    - Manual: selecting outdoors + career-first surfaces the tiebreaker step + callout; selecting 3 then 4 dealbreakers escalates amber→red.
  </acceptance_criteria>
  <done>Tension callout renders the adaptive moment; dealbreaker warning escalates 0–2→3→4+ with the sketch-locked copy and aria-live; build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — adaptive quiz end-to-end in browser</name>
  <action>Blocking human-verify checkpoint after all automated tests are green. Present the verification steps below; pause for the user to exercise the quiz in the browser and confirm the perceptual/visual behavior (per 02-VALIDATION Manual-Only Verifications). Do not mark the phase done until the user approves.</action>
  <what-built>The full rebuilt quiz: linear backbone (Plan 02), conditional citizenship/immigration branching + Going Global (Plan 03), and tension reconciliation + escalating dealbreaker warning (this plan). All automated tests are green; this checkpoint confirms the perceptual/visual behavior that unit tests cannot assert (per 02-VALIDATION Manual-Only Verifications).</what-built>
  <how-to-verify>
    1. Run `npm run dev`. Complete the quiz as a US citizen — confirm the immigration-status step does NOT appear and the Going Global group renders (amber header/sprite).
    2. Restart and complete as a non-US citizen (pick e.g. Brazil) — confirm the immigration-status step DOES appear and adds a progress segment.
    3. On the priorities step, choose a nature lifestyle tag (Hiking & Nature / Snow Sports) AND rank Career first — confirm the indigo tension callout appears and a reconciling tiebreaker step follows; pick an option and continue.
    4. On the dealbreakers step, select 3 → amber warning appears; select a 4th → it escalates to red.
    5. Finish the quiz — confirm it reaches the ranked results list (Phase 3 handoff intact).
    6. In devtools/console, inspect the submitted Profile and confirm `citizenship`, `immigrationStatus`, `opennessToAbroad`, `dealBreakers`, `moveTimeline`, raw `weights` (values 1–4), and `tradeoffTolerance` are all present (QUIZ-03 "visible in browser state").
  </how-to-verify>
  <verify><human-check>User confirms branching, Going Global, tension callout + tiebreaker, escalating dealbreaker warning, results handoff, and Profile fields visible in browser state.</human-check></verify>
  <done>User has exercised the quiz end-to-end and approved all six verification points.</done>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User answers → detectTension / tradeoffTolerance | Answer values drive injected follow-up + stored tiebreaker; no privilege change |
| Dealbreaker selections → in-memory Profile.dealBreakers | Hard-filter intent captured; enforcement is Phase 3 (local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | detectTension on sparse/unknown answers | mitigate | Signal-or-null with guard discipline (mirrors getTriggeredDealbreakers default:break) → unknown keys return null, never throw |
| T-02-08 | Information Disclosure | tradeoffTolerance / dealbreaker / immigration data | accept | No network egress in Phase 2; all values in in-memory React state feeding local rankCities. Re-evaluate at Phase 5 (proxy). |
</threat_model>

<verification>
- `npx vitest run shared/quiz-engine/ --reporter=verbose` fully green (resolver tension injection + tension + synthesizer tradeoffTolerance).
- `npx vitest run src/screens/quiz/ --reporter=verbose` green.
- `npx vitest run --reporter=verbose` (full suite) green before /gsd:verify-work.
- `npx tsc --noEmit -p tsconfig.json` passes; `npm run build` clean.
- Human checkpoint (Task 3) approved.
</verification>

<success_criteria>
- QUIZ-01/SC7: conflicting priorities surface a reconciling follow-up; the resolution is stored as tradeoffTolerance on the Profile.
- QUIZ-04: dealbreakers are captured with an escalating capture-time warning (the advisory guardrail).
- Every Plan-01 RED stub is now GREEN; full quiz-engine + component suites pass.
- The adaptive demo moment (tension) works in the browser.
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-04-SUMMARY.md` when done.
</output>

---
phase: 02-quiz-profile-capture
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - shared/quiz-engine/questions.ts
  - shared/quiz-engine/resolver.ts
  - src/screens/quiz/QuizShell.jsx
  - src/screens/quiz/QuestionCard.jsx
autonomous: true
requirements: [QUIZ-01, QUIZ-03]

must_haves:
  truths:
    - "The immigration-status question appears only for non-US citizens; US citizens never see it but still get immigrationStatus='citizen' on their Profile"
    - "The Going Global group (openness + citizenship + status + timeline) renders as a visibly distinct demo moment"
    - "Changing an earlier answer on Back that hides a later question clears that stale answer so the Profile never carries an answer the user reversed"
    - "The progress bar segment count adapts live as conditional questions appear/disappear"
    - "Implements decisions D-03 and D-07 (conditional/branching follow-ups via showIf so the quiz adapts to prior answers; US-citizen-primary framing where citizenship gates the immigration-status branch)"
  artifacts:
    - path: "shared/quiz-engine/resolver.ts"
      provides: "getVisibleQuestions with showIf branching exercised + clearHiddenAnswers correctness"
      contains: "clearHiddenAnswers"
    - path: "src/screens/quiz/QuestionCard.jsx"
      provides: "Going Global group header render (D-06 demo moment)"
      contains: "groupHeader"
  key_links:
    - from: "shared/quiz-engine/questions.ts"
      to: "shared/quiz-engine/resolver.ts"
      via: "immigrationStatus.showIf gates on citizenship !== 'US'"
      pattern: "citizenship"
    - from: "src/screens/quiz/QuizShell.jsx"
      to: "shared/quiz-engine/resolver.ts"
      via: "clearHiddenAnswers runs on every answer change (back-nav correctness)"
      pattern: "clearHiddenAnswers"
---

<objective>
Second vertical slice: make the quiz genuinely adaptive. The linear backbone from Plan 02 becomes a branching instrument — the immigration-status question conditionally appears for non-US citizens (D-09), the "Going Global" grouping becomes the on-stage international/visa demo moment (D-06), back-navigation correctly clears stale answers from now-hidden questions (Pitfall 3), and the progress bar adapts to the live visible-question count (Pitfall 2).

Purpose: After this plan a user gets a quiz that demonstrably changes shape based on their answers — the "real logic, not a linear demo" payoff (D-01/D-03) and the satisfaction of QUIZ-03's declare-citizenship-and-status requirement with the fast US-citizen path. This makes the resolver showIf/clearHiddenAnswers assertions go fully green.

Output: Branching wired in questions/resolver, the Going Global group header rendered, and back-nav stale-answer clearing exercised under test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-quiz-profile-capture/02-CONTEXT.md
@.planning/phases/02-quiz-profile-capture/02-RESEARCH.md
@.planning/phases/02-quiz-profile-capture/02-PATTERNS.md
@.planning/phases/02-quiz-profile-capture/02-UI-SPEC.md

<interfaces>
<!-- D-09 immigration branching: the immigrationStatus QuestionDef carries -->
<!--   showIf: (a) => a["citizenship"] !== "US" -->
<!-- US citizens never see it; synthesizer (Plan 02) auto-sets immigrationStatus="citizen". -->
<!-- SC3 is satisfied: every user DECLARES citizenship; status is auto-derived for US, declared for non-US. -->

<!-- RESOLVER INVARIANT (load-bearing): every conditional/injected question must appear AT OR AFTER -->
<!-- its trigger question in ALL_QUESTIONS (immigrationStatus after citizenship). currentIndex into the -->
<!-- recomputed visible[] stays aligned because questions never reveal earlier than their trigger. -->

<!-- Pitfall 3 (stale answers): when Back+change hides a previously-visible question, clearHiddenAnswers -->
<!-- must drop its answer. QuizShell already calls it on handleAnswer (Plan 02) — this plan verifies the -->
<!-- citizenship-flip case end-to-end (US→non-US→US must not strand an immigrationStatus answer). -->

<!-- D-06 Going Global group: groupHeader { label:"Going Global", subtext:"Tell us how far you'd go — literally." } -->
<!-- on the opennessToAbroad question; header renders ONCE above the first group question (02-UI-SPEC §8), -->
<!-- amber accent + sprite. Persists visually across openness/citizenship/status/timeline. -->

<!-- .js import extensions in shared/quiz-engine/*.ts (SP-1). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branching in questions.ts + resolver showIf/clearHiddenAnswers correctness</name>
  <files>shared/quiz-engine/questions.ts, shared/quiz-engine/resolver.ts</files>
  <read_first>
    - shared/quiz-engine/resolver.test.ts (the showIf + clearHiddenAnswers assertions to make green)
    - shared/quiz-engine/questions.ts (the ALL_QUESTIONS array from Plan 02 — add/confirm showIf + groupHeader)
    - shared/quiz-engine/resolver.ts (getVisibleQuestions + clearHiddenAnswers from Plan 02)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Pattern 1 load-bearing invariant lines 250; Pitfall 3 clearHiddenAnswers lines 545–551; D-09)
    - .planning/phases/02-quiz-profile-capture/02-PATTERNS.md (resolver assignment + invariant header note)
  </read_first>
  <behavior>
    - getVisibleQuestions with citizenship="US" → immigrationStatus question ABSENT.
    - getVisibleQuestions with citizenship="Brazil" → immigrationStatus question PRESENT, positioned immediately after citizenship.
    - clearHiddenAnswers({citizenship:"Brazil", immigrationStatus:"work_visa"}) then flip citizenship→"US" → result has NO immigrationStatus key.
    - Any additional showIf branch (e.g. a follow-up gated on a prior single-select) appears at/after its trigger.
  </behavior>
  <action>Ensure the `immigrationStatus` QuestionDef in `ALL_QUESTIONS` has `showIf:(a)=>a["citizenship"]!=="US"` and sits immediately after `citizenship` (trigger-before-injection invariant). Add the `groupHeader:{label:"Going Global", subtext:"Tell us how far you'd go — literally."}` to the FIRST Going Global question (`opennessToAbroad`). If CONTEXT/D-02 motivates one more genuine conditional follow-up that is content-cheap and demoable (e.g. a remote-specific follow-up gated on `workStyle==="remote"` or `hasRemote`), add it after its trigger — but keep it minimal; full schema-driven branching is explicitly deferred. Confirm `resolver.ts` `getVisibleQuestions` applies `showIf` and `clearHiddenAnswers` drops now-hidden answers; document the invariant in the SP-2 header. Do NOT call detectTension yet — that seam stays for Plan 04.</action>
  <verify>
    <automated>npx vitest run shared/quiz-engine/resolver.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - resolver.test.ts GREEN for: immigrationStatus hidden when citizenship==="US"; shown otherwise; clearHiddenAnswers strips immigrationStatus after a US flip.
    - `grep -q 'citizenship.*!==.*"US"\|!== "US"' shared/quiz-engine/questions.ts` (the showIf predicate present).
    - `grep -q 'groupHeader' shared/quiz-engine/questions.ts` on the opennessToAbroad question.
    - `npx tsc --noEmit -p tsconfig.json` passes.
  </acceptance_criteria>
  <done>Conditional branching wired and tested; clearHiddenAnswers correctness proven for the citizenship-flip case; Going Global group header present on the first group question.</done>
</task>

<task type="auto">
  <name>Task 2: Going Global group header + adaptive progress in the UI</name>
  <files>src/screens/quiz/QuestionCard.jsx, src/screens/quiz/QuizShell.jsx</files>
  <read_first>
    - src/screens/quiz/QuestionCard.jsx (renders kicker/prompt; add group-header render)
    - src/screens/quiz/QuizShell.jsx (visible-question recompute + progress wiring from Plan 02)
    - src/screens/quiz/ProgressBar.jsx (dynamic segment count from Plan 02)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (§8 Going Global Group Header; §2 Top Bar dynamic segments; Copywriting Contract kicker labels)
    - .planning/phases/02-quiz-profile-capture/02-RESEARCH.md (Pitfall 2 dynamic progress)
  </read_first>
  <action>In `QuestionCard.jsx`, when the current question has `groupHeader`, render the §8 group-header block ABOVE the question: separator line, amber (`--accent2`) "Going Global" pixel sprite (pixelarticons or the placeholder diamond sprite, `float` animation with `prefers-reduced-motion` off), uppercase Pixelify label (`GOING GLOBAL`, letter-spacing 0.14em), subtext. Render it only on the first question carrying the header (the resolver keeps openness first in the group). Set the per-question kicker from the question's group per the Copywriting Contract (CAREER / FINANCES / ABOUT YOU / LIFESTYLE / PRIORITIES / GOING GLOBAL). In `QuizShell.jsx`, confirm the progress bar receives `visible.length` recomputed each render so adding/removing the conditional immigrationStatus segment updates the count live (Pitfall 2) — adjust if it reads a stale total. Honor `aria-live="polite"` where the UI-SPEC specifies.</action>
  <verify>
    <automated>npx vitest run src/screens/quiz/QuizShell.test.jsx --reporter=verbose && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - QuizShell.test.jsx stays GREEN (no navigation regression).
    - `grep -q 'GOING GLOBAL\|Going Global' src/screens/quiz/QuestionCard.jsx` (group header rendered).
    - Progress segment count is driven by the live `visible.length` (no hard-coded total).
    - `npm run build` clean.
    - Manual: in `npm run dev`, picking a non-US citizenship adds a segment + the immigration-status step; reverting to US removes it.
  </acceptance_criteria>
  <done>Going Global demo moment renders; conditional immigration step appears/disappears with citizenship; progress adapts live; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User selection (citizenship) → conditional question graph | Answer value drives which questions render; no privilege change |
| citizenship/immigrationStatus → in-memory Profile | Sensitive immigration fields held in React state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | citizenship/immigrationStatus exposure | accept | No network egress in Phase 2; fields live in in-memory React state only and feed the local rankCities engine. No analytics/fetch carries them. Re-evaluate at Phase 5 (proxy). |
| T-02-06 | Tampering | unexpected citizenship value (e.g. "Other") driving showIf | mitigate | showIf is a pure boolean predicate over the answer; unknown values simply don't match `!== "US"` as intended (non-US → status shown). clearHiddenAnswers prevents stale-answer carryover. |
</threat_model>

<verification>
- `npx vitest run shared/quiz-engine/resolver.test.ts` green (showIf + clearHiddenAnswers).
- `npx vitest run src/screens/quiz/QuizShell.test.jsx` green (no nav regression).
- `npx tsc --noEmit -p tsconfig.json` passes; `npm run build` clean.
- Manual: non-US citizenship reveals the immigration step + an extra progress segment; US path skips it.
</verification>

<success_criteria>
- QUIZ-03: every user declares citizenship; non-US users declare status, US users auto-derive "citizen" — visible in Profile state.
- The quiz visibly adapts (branching) rather than running a fixed linear sequence (QUIZ-01 / SC6).
- Back-nav never strands a reversed answer on the Profile.
- Going Global is a distinct on-stage demo moment (D-06).
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-03-SUMMARY.md` when done.
</output>

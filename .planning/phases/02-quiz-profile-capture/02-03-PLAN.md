---
phase: 02-quiz-profile-capture
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/screens/PotentialApp.jsx
autonomous: true
requirements: [QUIZ-01, QUIZ-04]
must_haves:
  truths:
    - "A user can complete the Motivations & Work Style step (motivation multi-select max 3, work style, community/family, pace, risk)"
    - "Dealbreakers are presented as hard filters with a capture-time warning banner that appears when >=1 dealbreaker is selected and disappears when none are"
    - "All 10 DEAL_BREAKERS are selectable (the 4 previously inert ones included); each maps to a City attribute via DEALBREAKER_META"
    - "When conflicting priorities are detected, ONE reconciliation follow-up panel appears; the user's choice (or skip) is stored on profile.tiebreaker"
    - "On final submit the quiz calls derivePreferences and writes the normalized PreferenceProfile to profile.preferences before advancing to results"
    - "The submitted profile carries every required field; immigrationStatus is 'citizen' for US users and excludeInternational is true when openness=0"
  artifacts:
    - path: "src/screens/PotentialApp.jsx"
      provides: "Motivations step, hard-dealbreaker step + warning banner, tension reconciliation panel, submit wiring that emits preferences"
      contains: "derivePreferences"
  key_links:
    - from: "src/screens/PotentialApp.jsx"
      to: "shared/engine/derive-preferences.ts"
      via: "call on final submit"
      pattern: "derivePreferences\\("
    - from: "src/screens/PotentialApp.jsx"
      to: "shared/engine/conflict-detection.ts"
      via: "topConflict gate for reconciliation panel"
      pattern: "topConflict"
---

## Phase Goal

**As a** prospective relocator, **I want to** complete a deeper, adaptive profile quiz that captures my career, finances, background, lifestyle, motivations, priorities, dealbreakers, openness to living abroad, and citizenship/immigration status, **so that** the app emits a structured, weighted preference profile that drives my matches, financials, roadmap, and visa concierge.


<objective>
Complete the capture layer: add the Motivations & Work Style step (D-02 deeper dimensions), upgrade dealbreakers from soft penalties to HARD filters with the capture-time warning (D-11, D-12 — wire all 10), add the conditional Tension Reconciliation follow-up (D-03, D-14), and wire final submit to emit the derived `PreferenceProfile` (D-04) onto the profile. This is the slice that makes the quiz's output the structured preference profile Phase 3 consumes.

Delivers the remainder of QUIZ-01 (full multi-step flow complete + adaptive) and QUIZ-04 (hard dealbreakers).

Purpose: A user finishing the quiz produces a complete, weighted preference profile — the phase's whole reason for existing.
Output: Motivations step, hard-dealbreaker step + warning, reconciliation panel, and submit wiring in `src/screens/PotentialApp.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-quiz-profile-capture/02-CONTEXT.md
@.planning/phases/02-quiz-profile-capture/02-UI-SPEC.md
@.planning/phases/02-quiz-profile-capture/02-01-SUMMARY.md
@.planning/phases/02-quiz-profile-capture/02-02-SUMMARY.md
@src/screens/PotentialApp.jsx
@shared/types.ts
@shared/data/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Motivations and Work Style step + hard dealbreakers with capture-time warning (D-02, D-11, D-12)</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (the reducer + steps from Plan 02; existing dealbreaker pill grid at the priorities step; toggleArr helper)
    - shared/data/constants.js (MOTIVATIONS, WORK_STYLES, DEAL_BREAKERS, DEALBREAKER_META — values to render and the dealbreaker to City-attribute mapping)
    - shared/engine/quiz-branching.ts (needsHubQuestions — gates the industry-hub/commute follow-up when work style is on-site/hybrid)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (section C "Motivations & Work Style Step" — the 5 sub-fields with exact pill labels and the max-3 motivation cap; section D "Dealbreakers Upgraded Treatment" — the warning banner layout, styles, copy, and the --warn / --warn-dim tokens; Copywriting Contract for the exact warning heading + body strings)
  </read_first>
  <action>
    Add the "What drives you?" step (D-02) per UI-SPEC section C: motivation multi-select using toggleArr("motivations", val, 3) over MOTIVATIONS (cap 3); single-select work-style pills over WORK_STYLES; when needsHubQuestions(profile) is true, reveal the industryHubImportance (low/med/high) and commuteTolerance follow-ups (conditional JSX + fadeIn re-trigger); single-select community/family-proximity, pace-of-life, and risk-tolerance pill rows storing to familyProximity/communityScale, paceOfLife, riskTolerance respectively (map each 3-option pill choice to a representative 0..100 value, e.g. low/mid/high to 15/50/85). Step has no required field (UI-SPEC: CTA always enabled). Upgrade the dealbreaker step to hard filters (D-11/D-12): render all 10 DEAL_BREAKERS as selectable pills (remove any code path that made "Must be near mountains" / "Must be near ocean/coast" / "Need international airport" / "Must have strong job market in my field" inert — they are now first-class selections backed by DEALBREAKER_META). Below the grid, render the capture-time warning banner that appears only when dealBreakers.length >= 1 (and animates out at 0) with the exact UI-SPEC copy — heading "Dealbreakers are hard filters." and body "Cities that don't match will be removed from your results entirely — not just ranked lower. Add only true non-negotiables." — styled with --warn (#FBBF24) heading + a leading warning glyph and --warn-dim background, using the same fadeIn re-trigger when length crosses 0 to 1. The hard-filter-vs-advisory UX follows the competitor research directive (D-13), already discharged in RESEARCH §QUIZ-04 (Teleport restraint: a small binary filter set, not a weighting matrix). Keep the importanceRank reorder UI for the 4 pillars (RESEARCH Open Question #3: keep 4 visible). Do NOT introduce per-factor weight sliders (RESEARCH pitfall: weighting overload — weights are derived, not user-tuned). Reuse all primitives verbatim; introduce no color tokens beyond --warn / --warn-dim as listed in UI-SPEC.
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx vite build 2>&1 | tail -5 && grep -q "What drives you" src/screens/PotentialApp.jsx && grep -q "Dealbreakers are hard filters" src/screens/PotentialApp.jsx && grep -q "needsHubQuestions" src/screens/PotentialApp.jsx</automated>
  </verify>
  <acceptance_criteria>
    - The "What drives you?" step renders motivation multi-select (cap enforced at 3), work style, community/family, pace, and risk pill rows.
    - When work style is on-site or hybrid, the industry-hub / commute follow-ups appear; otherwise hidden (driven by needsHubQuestions).
    - All 10 DEAL_BREAKERS are selectable; none are inert no-ops.
    - The warning banner appears only when dealBreakers.length >= 1 with the exact UI-SPEC heading and body copy, in --warn / --warn-dim styling.
    - paceOfLife, communityScale, familyProximity, riskTolerance receive 0..100 values from the pill selections.
    - `npx vite build` completes without errors.
  </acceptance_criteria>
  <done>Motivations step and hard-dealbreaker step (all 10 wired + capture-time warning) render correctly; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tension reconciliation panel + final submit wiring that emits the derived PreferenceProfile (D-03, D-14, D-04)</name>
  <files>src/screens/PotentialApp.jsx</files>
  <read_first>
    - src/screens/PotentialApp.jsx (the reducer, the nextProfile submit path that currently builds results and calls goStep(2), the anim transition helpers)
    - shared/engine/conflict-detection.ts (topConflict — returns the single highest-severity conflict { id, pair, labelA, labelB } or null)
    - shared/engine/derive-preferences.ts (derivePreferences — the pure function to call on submit; its PreferenceProfile return shape)
    - shared/types.ts (Profile.preferences + Profile.tiebreaker shapes the submit must populate)
    - .planning/phases/02-quiz-profile-capture/02-UI-SPEC.md (section E "Conditional Tension Reconciliation Follow-Up" — trigger timing, the injected sub-step placement (no own progress segment), panel layout, heading at 24px, the two full-width reconciliation pills, the skip affordance copy "Skip — I'm equally pulled both ways", and that tiebreaker stores { dimension, pair }; Copywriting Contract for the heading/body/footnote templates)
    - .planning/phases/02-quiz-profile-capture/02-01-SUMMARY.md (how to import the engine functions from src/)
  </read_first>
  <action>
    Implement the conditional reconciliation follow-up (D-14) as an injected sub-step that does NOT consume its own progress-bar segment (UI-SPEC section E placement). After the user completes the dealbreaker/priorities step, evaluate topConflict(profile); if it returns a conflict, render the reconciliation panel (green left-border, --card background, 24px Instrument Serif sub-heading using the "You love {labelA} and {labelB}." template, body and footnote copy verbatim from the Copywriting Contract) with two full-width pill options (the standard pill primitive at flex 1, justifyContent center, padding 14) and a skip link "Skip — I'm equally pulled both ways". Selecting a pill stores profile.tiebreaker = { dimension: "A"|"B", pair: [conflict.pair[0], conflict.pair[1]] }; the skip link sets profile.tiebreaker = null. (Phase 3 consumes this tiebreaker to rank cities that balance the competing priorities per D-15 — that ranking is out of scope here; Phase 2 only captures the resolution.) If topConflict returns null, skip the panel entirely (no gap, no placeholder). After reconciliation (or immediately if no conflict), on final submit compute profile.preferences = derivePreferences(profile) and write it into the reducer state BEFORE advancing to results, so the emitted profile carries the normalized PreferenceProfile (D-04). Use the final-step CTA copy "Show Me My Potential →" from the Copywriting Contract. Ensure derivePreferences is called exactly once on submit and its result is stored on profile.preferences (the P2 to P3 handshake). Keep the existing goStep(2) results transition. Do not duplicate conflict logic inline — call topConflict; do not duplicate weight math inline — call derivePreferences.
  </action>
  <verify>
    <automated>cd "/Users/Luke2/Desktop/Competition app/FBLA" && npx vite build 2>&1 | tail -5 && grep -q "derivePreferences(" src/screens/PotentialApp.jsx && grep -q "topConflict" src/screens/PotentialApp.jsx && grep -q "I'm equally pulled both ways" src/screens/PotentialApp.jsx</automated>
  </verify>
  <acceptance_criteria>
    - The reconciliation panel renders only when topConflict(profile) is non-null, with the templated heading/body/footnote and two full-width option pills + the skip link.
    - Selecting an option sets profile.tiebreaker to { dimension, pair }; skip sets it to null.
    - Final submit calls derivePreferences(profile) exactly once and stores the result on profile.preferences before goStep(2).
    - The reconciliation sub-step does not add a progress-bar segment.
    - `npx vite build` completes without errors.
  </acceptance_criteria>
  <done>Tension reconciliation surfaces one conflict, stores the tiebreaker, and final submit emits the derived PreferenceProfile onto the profile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quiz UI (src/) → shared engine | User answers flow into the pure derivePreferences / topConflict functions on submit. No network or persistence this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | dealbreaker hard-filter selections | accept | Dealbreakers eliminate cities in Phase 3, but the never-empty floor + advisory override are Phase 3 guardrails (RESEARCH); Phase 2 only captures + warns. No security decision rides on these values. |
| T-02-05 | Denial of Service | derivePreferences on submit | accept | Pure synchronous linear function over a bounded fixed-size answer set; no unbounded loops or recursion; runs in microseconds. |
| T-02-SC | Tampering | npm/package installs | accept | This plan adds no new packages; no install task here. |
</threat_model>

<verification>
- `npx vite build` passes after each task.
- derivePreferences and topConflict are invoked from the UI (not re-implemented inline) — the tested Plan 01 logic is the single source of truth.
- Full submit produces profile.preferences matching PreferenceProfile (asserted by the Plan 04 completeness check).
</verification>

<success_criteria>
- QUIZ-01: full multi-step adaptive flow complete (Motivations step + reconciliation makes it non-linear).
- QUIZ-04: dealbreakers are hard filters with all 10 wired and a capture-time warning.
- D-04: quiz submit emits the derived PreferenceProfile onto the profile.
</success_criteria>

<output>
Create `.planning/phases/02-quiz-profile-capture/02-03-SUMMARY.md` when done.
</output>
